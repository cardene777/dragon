/**
 * 版に入る commit が変更履歴から辿れること (#1277)。
 *
 * `0.8.0` を切る時点で **55 commit のうち 43 件が変更履歴に載っていなかった**。
 * 既存の `changelog-covers-notation.test.ts` (#1206) は「記法に書ける項目」 と
 * 「cdl の依存の版」 が載っているかだけを見るため、commit の記載漏れは素通りする。
 *
 * ## 何を見るか
 *
 * 前の版から HEAD までの各 commit について、その commit が持つ番号 (`(#N)`) のどれかが
 * **変更履歴に載っている** か **「利用者から見えない」 と宣言されている** ことを見る。
 *
 * commit が持つ番号は Issue と PR の両方で、どちらで書いても辿れる = 変更履歴は Issue で
 * 書くことが多いのに、PR 番号だけを求めると書き方を縛ることになる。
 *
 * ## 宣言は両方向で見る
 *
 * 宣言に「実際には範囲に無い番号」 が残ると、消えた commit の宣言が残り続ける。
 * 同じ番号が別の変更で再び現れた時に黙って除外されるので、そちらも落とす。
 */
import { execFileSync } from "node:child_process";
import { readFileSync } from "node:fs";
import { fileURLToPath } from "node:url";
import { dirname, join } from "node:path";
import { describe, it, expect } from "vitest";

const ここ = dirname(fileURLToPath(import.meta.url));
const REPO = join(ここ, "..", "..", "..");
const CHANGELOG = readFileSync(join(REPO, "CHANGELOG.md"), "utf8");
const 版 = (
  JSON.parse(readFileSync(join(ここ, "..", "package.json"), "utf8")) as { version: string }
).version;

const git = (...args: string[]): string =>
  execFileSync("git", ["-C", REPO, ...args], { encoding: "utf8" }).trim();

/**
 * 前の版の位置。
 *
 * **いま切ろうとしている版の tag は使わない** (Round 1 の指摘)。 使うと `v0.8.0` を
 * 打った直後に `v0.8.0..HEAD` が空になり、検査が何も見なくなる。
 *
 * 1 つ前の版の tag があればそれを使う。 **無い時は版を最後に変えた commit へ落とす** =
 * この repo は tag を 1 つも持たない状態で 55 commit 積んだ実績があり、tag が無いと
 * 検査ごと止まる形にすると同じことが起きる。
 */
/** 版を最後に変えた commit。 tag が無い時の起点に使う */
function 版を上げたcommit(): string | undefined {
  return git("log", "--format=%H", `-S"version": "${版}"`, "--", "packages/dragon/package.json")
    .split("\n")
    .filter((l) => l !== "")
    .pop();
}

/**
 * いま切ろうとしている版に属する tag か。
 *
 * `v0.8.0` だけでなく `v0.8.0-rc1` のような前触れの tag も含む (Round 2 の指摘)。
 * 含めないと、前触れを前の版とみなして範囲がその後ろだけに縮む。
 */
function 今の版のtag(tag: string): boolean {
  return tag === `v${版}` || tag.startsWith(`v${版}-`);
}

function 前の版(): string {
  const 前 = git("tag", "--list", "v*", "--sort=-v:refname")
    .split("\n")
    .map((l) => l.trim())
    .filter((l) => l !== "" && !今の版のtag(l))[0];
  if (前) return 前;
  const 上げた = 版を上げたcommit();
  if (!上げた) throw new Error(`版 ${版} へ上げた commit が見つからない`);
  // **その commit 自身も範囲に含める** = 版を上げたcommit は次の版の一部で、
  // `..` は始点を含まないため 1 件取りこぼす
  return `${上げた}^`;
}

/**
 * 履歴が切り詰められていないか (Round 1 の指摘)。
 *
 * `--depth 1` の clone では版を変えた commit を辿れず、範囲が空になって検査が通ってしまう。
 * **通してはいけない** ので、その場で止めて理由を出す。
 */
function 履歴が揃っている(): void {
  const shallow = git("rev-parse", "--is-shallow-repository");
  if (shallow === "true") {
    throw new Error(
      "履歴が切り詰められている (shallow clone)。 版の範囲を導けないため検査できない。 " +
        "`git fetch --unshallow` で完全な履歴を取ってから実行する",
    );
  }
}

/** 変更履歴の指定した節。 `Unreleased` と版番号の両方を同じ規則で切り出す */
function 変更履歴の節(名前: string): string {
  const 始 = CHANGELOG.indexOf(`## [${名前}]`);
  if (始 < 0) throw new Error(`変更履歴に [${名前}] の節が無い`);
  const 次 = CHANGELOG.indexOf("\n## [", 始 + 1);
  return CHANGELOG.slice(始, 次 < 0 ? undefined : 次);
}

/**
 * commit を辿る対象の節。
 *
 * 現行版の tag を打った後の変更は `Unreleased` に入る。 現行版の節だけを見ると、tag 後の
 * 最初の commit が正しく `Unreleased` に載っていても「記載漏れ」になる (#1312 で実測)。
 * 一方、範囲の下端は前の版なので、現行版を作った commit も引き続き現行版の節から辿る。
 */
function 対象の節(): string {
  return `${変更履歴の節("Unreleased")}\n${変更履歴の節(版)}`;
}

/**
 * 利用者から見えない commit。 変更履歴に書かない代わりにここへ理由付きで宣言する。
 *
 * **書かない判断も残す**。 宣言が無いと「書き忘れ」 と「書かないと決めた」 が区別できない。
 *
 * **版を切る時に、前の版の範囲に落ちた番号をここから外す**。 範囲の外の番号が残っていると、
 * 「宣言が実物の範囲に残っている」 検査が落ちる = 宣言が実際に効いているかを見る検査で、
 * 使われない宣言が溜まると空文になるため (0.9.0 を切る時に 9 件を外した)。
 *
 * ## 番号は commit の本文まで見て探す (#1279)
 *
 * squash merge した commit の **件名** は「PR の題 + PR 番号」 に置き換わるため、
 * 件名だけを見ると branch 上の `(#1277)` が取り込み後に `(#1278)` になり、宣言がずれる
 * (実際に踏んだ)。
 *
 * **本文には元の commit の件名がそのまま残る** (実測 = 取り込んだ `#1278` の本文に
 * `(#1277)` が 4 回現れる)。 本文まで見れば、取り込みの前と後で同じ番号で辿れる。
 *
 * したがって宣言は **branch 上の commit が持つ番号** で書けばよく、取り込み後もそのまま
 * 効く。 未来の番号を通す抜け道は要らない。
 */
const 利用者から見えない: Record<string, string> = {
  // **branch 上の番号で書く** = 本文まで見るので取り込み後もそのまま効く。
  // 実際 `#1277` は取り込んだ commit の件名には無く、本文にだけ残っている。
  //
  // **版を切る時に、前の版の範囲に落ちた番号をここから外す**。 `0.11.0` を切った時点で
  // 範囲が `v0.10.0..HEAD` に縮み、それより前の 1 件 (#1321) が範囲の外に出たため外した
  // 0.13.0 を切った時点で範囲が `v0.12.0..HEAD` に縮み、それより前の 2 件 (#1361 / #1369) が
  // 範囲の外に出たため外した
  // 0.14.0 を切った時点で範囲が `v0.13.0..HEAD` に縮み、それより前の 3 件
  // (#1383 / #1384 / #1387) が範囲の外に出たため外した
  // 0.15.0 を切った時点で範囲が `v0.14.0..HEAD` に縮み、それより前の 1 件 (#1401) が
  // 範囲の外に出たため外した
  // 0.16.0 を切った時点で範囲が `v0.15.0..HEAD` に縮み、それより前の 1 件 (#1433 = 0.15.0 を
  // 切る commit) が範囲の外に出たため外した
  // 0.17.0 を切った時点で範囲が `v0.16.0..HEAD` に縮み、それより前の 1 件 (#1458 = 0.16.0 を
  // 切る commit) が範囲の外に出たため外した
  // 0.18.0 を切った時点で範囲が `v0.17.0..HEAD` に縮み、それより前の 7 件
  // (#1474 / #1476 / #1477 / #1479 / #1482 / #1486 / #1492) が範囲の外に出たため外した
  // 0.18.1 を切った時点で範囲が `v0.18.0..HEAD` に縮み、それより前の 1 件 (#1500 = 0.18.0 を
  // 切る commit) が範囲の外に出たため外した
  // 0.18.2 を切った時点で範囲が `v0.18.1..HEAD` に縮み、それより前の 1 件 (#1506 = 0.18.1 を
  // 切る commit) が範囲の外に出たため外した
  // 0.18.3 を切った時点で範囲が `v0.18.2..HEAD` に縮み、それより前の 1 件 (#1510 = 0.18.2 を
  // 切る commit) が範囲の外に出たため外した
  // 0.19.0 を切った時点で範囲が `v0.18.3..HEAD` に縮み、それより前の 1 件 (#1514 = 0.18.3 を
  // 切る commit) が範囲の外に出たため外した
  // 0.20.0 を切った時点で範囲が `v0.19.0..HEAD` に縮み、それより前の 12 件
  // (#1516 / #1518 / #1520 / #1522 / #1524 / #1526 / #1529 / #1555 / #1559 / #1561 /
  // #1563 / #1565) が範囲の外に出たため外した。 `v0.19.0` の tag は 0.19.0 を切った時に
  // 打たれておらず、範囲が `v0.18.3..HEAD` のまま縮んでいなかったので、この版で打ち直した
  // 0.21.0 を切った時点で範囲が `v0.20.0..HEAD` に縮み、 それより前の 1 件 (#1581 = 0.20.0 を
  // 切る commit) が範囲の外に出たため外した
  // 0.22.0 を切った時点で範囲が `v0.21.0..HEAD` に縮み、 それより前の 1 件 (#1585 = 0.21.0 を
  // 切る commit) が範囲の外に出たため外した
  // 0.23.0 を切った時点で範囲が `v0.22.0..HEAD` に縮み、 それより前の 1 件 (#1589 = 0.22.0 を
  // 切る commit) が範囲の外に出たため外した。 #1591 は範囲内に残る = その commit (#1592) は
  // `v0.22.0` より後にある
  // 0.24.0 を切った時点で範囲が `v0.23.0..HEAD` に縮み、 それより前の 3 件 (#1591 / #1606 /
  // #1611 = 0.23.0 を切る commit) が範囲の外に出たため外した
  "1618": "配信 script の直し。 公開する package の中身は動かない",
  "1621": "意匠帳だけの変更。 公開する package の中身は動かない",
  "1630": "版を切る commit そのもの。 変更履歴の更新が中身",
};

/**
 * `git log --format=%B%x00` の出力を commit ごとに割る (#1279)。
 *
 * **区切りは NUL** (Round 2 の指摘)。 記録区切り (U+001E) は commit の本文に書けてしまう
 * ため、その文字を含む 1 件が複数に割れて番号の対応が壊れる。 NUL は commit message に
 * 含められないので、どんな本文でも 1 件が 1 件のまま残る。
 */
function 本文で割る(raw: string): string[] {
  return raw
    .split("\u0000")
    .map((c) => c.trim())
    .filter((c) => c !== "");
}

/**
 * その commit を名乗る番号 (#1279)。
 *
 * **説明の本文からは拾わない** (Round 2 の指摘)。 本文には review の指摘や関連 Issue の
 * 番号も書くため、丸ごと拾うと「変更履歴に載っている別の番号がたまたま本文にある」 だけで
 * 辿れた扱いになる。
 *
 * 見るのは 2 種類の行だけ。
 *
 * | 行 | 何か |
 * |---|---|
 * | 1 行目 | その commit の件名 |
 * | closing keyword で始まる行 | commit が閉じる Issue (`Closes #1234` 等) |
 * | 列 0 の `* ` で始まる行 | squash merge が並べた、元の commit の件名 |
 *
 * squash した commit は件名が「PR の題 + PR 番号」 に置き換わるが、元の件名は列 0 の
 * `* ` 付きで本文に並ぶ。 この 2 種類を見れば、取り込みの前と後で同じ番号で辿れる。
 *
 * ## 中身では見分けない (Round 4 の指摘)
 *
 * 当初は元件名を `type(scope): ... (#N)` の形で判定していたが、**厳しすぎた** =
 * 実履歴の `(#N Round 1)` / `(#N 段 2)` のような注記付きを取りこぼす。 形で絞るのをやめた。
 *
 * 先頭に続く並びだけを取る案も採れない。 実測すると GitHub は元件名の間に **各 commit の
 * 本文を丸ごと挟む** ため、`* ` の行は連続しない (`#1278` の本文で 3 行目 / 26 行目 /
 * 54 行目)。
 *
 * ## 残る限界
 *
 * **列 0 に `* ` を書いた説明の箇条書きは、squash 由来と区別できない** (r4-f2)。
 * 構造上どちらも同じ形になる。 字下げした箇条書きは除くので、説明を字下げで書けば当たらない。
 *
 * 当たった場合の実害は「その番号でも辿れてしまう」 だけで、**辿れない commit を見逃す
 * 方向には倒れない** (拾う番号が増えるだけ)。 記載漏れを検出する目的からは緩む側だが、
 * 実履歴には該当が無い。
 */
function commitの番号(message: string): string[] {
  const { 件名, 元件名 } = 名乗る行の番号(message);
  return [...件名, ...元件名.flat()];
}

/**
 * 名乗る行の番号を、件名の分と元件名ごとの分に分けて返す (#1279)。
 *
 * 分けるのは **元件名を 1 行ずつ確かめる** ため。 まとめて「どれか 1 つが載っていれば
 * 辿れた」 にすると、説明として書いた箇条書きが載っている番号を含むだけで、**同じ commit の
 * 別の元件名が未記載でも通ってしまう** (Round 5 の指摘)。
 */
function 名乗る行の番号(message: string): { 件名: string[]; 元件名: string[][] } {
  const 行 = message.split("\n");
  const 番号 = (l: string): string[] => [...l.matchAll(/#(\d+)/g)].map((m) => m[1]!);
  // GitHub が Issue を閉じると解釈する keyword 行は commit 自身の識別子として扱う。
  // 行頭だけに限る = 説明の途中に `Closes #N` と書いた引用や補足からは拾わない。
  const 閉じる行 = 行.filter((l) => /^(?:close[sd]?|fix(?:e[sd])?|resolve[sd]?)\s+#\d+/i.test(l));
  // **列 0 の `* ` だけ**。 字下げは認めない = 説明の中で字下げした箇条書きを拾わない
  return {
    件名: [...番号(行[0] ?? ""), ...閉じる行.flatMap(番号)],
    元件名: 行
      .slice(1)
      .filter((l) => l.startsWith("* "))
      .map(番号),
  };
}

/**
 * その commit が辿れるか (#1279)。
 *
 * 件名の番号が載っていればそれでよい。 載っていなければ、**番号を名乗った元件名を
 * 1 行ずつ** 確かめる。
 *
 * どれか 1 行が載っていれば通す形にはしない (Round 5 の指摘)。 説明として列 0 に書いた
 * 箇条書きが載っている番号を含むだけで、**同じ commit の別の元件名が未記載でも通って
 * しまう** = 記載漏れを見逃す方向に倒れる。
 *
 * ## 番号を持たない行は数えない (Round 6 の指摘)
 *
 * 列 0 の `* ` には **番号を一つも持たない行が実在する** (実測 = 全履歴 82 件、
 * 例えば `* ⏪ revert(design): 本 PR と無関係の app.pen の取り込みを戻す`)。 番号を付ける
 * 前の書き方で積んだ branch を取り込むとこの形になる。
 *
 * この行を `every` の数に含めると、番号を何も名乗っていない行が「未記載の元件名」として
 * 数えられ、**実際には載っている commit を辿れないと判定する**。 名乗っていない以上
 * 確かめる対象が無いので数から外す。
 *
 * 全行が番号を持たない場合は、元件名からは何も辿れない = 件名で辿れなければ辿れないままにする。
 */
function 辿れるか(message: string, 載る: (n: string) => boolean): boolean {
  const { 件名, 元件名 } = 名乗る行の番号(message);
  if (件名.some(載る)) return true;
  // 番号を持たない行は何も名乗っていないので数えない (Round 6 の指摘)
  const 名乗った = 元件名.filter((ns) => ns.length > 0);
  return 名乗った.length > 0 && 名乗った.every((ns) => ns.some(載る));
}

describe("版に入る commit が変更履歴から辿れる (#1277)", () => {
  履歴が揃っている();
  const 範囲 = `${前の版()}..HEAD`;
  // **本文まで読む** (#1279)。 件名だけだと squash で番号が変わる。
  //
  // 区切りは **NUL** にする (Round 2 の指摘)。 記録区切り (U+001E) は commit の本文に
  // 書けてしまうため、その文字を含む 1 件が複数に割れて番号の対応が壊れる。
  // NUL は commit message に含められない
  const commit = 本文で割る(git("log", "--format=%B%x00", 範囲));

  it("commit を 1 件以上集められている", () => {
    // 集められていなければ、以下の検査は通って当然になる
    expect(commit.length, `${範囲} の commit が 1 件も無い (検査が空振りしている)`).toBeGreaterThan(
      0,
    );
  });

  it("全ての commit が変更履歴か宣言のどちらかから辿れる", () => {
    const 載っている = new Set([...対象の節().matchAll(/#(\d+)/g)].map((m) => m[1]!));
    /** その番号が変更履歴か宣言のどちらかにあるか */
    const 載る = (n: string): boolean => 載っている.has(n) || n in 利用者から見えない;
    const 辿れない = commit.filter((s) => !辿れるか(s, 載る));
    expect(
      辿れない.map((c) => c.split("\n")[0]),
      "変更履歴にも宣言にも無い commit がある",
    ).toEqual([]);
  });

  it("宣言が実物の範囲に残っている", () => {
    // 宣言が古くなったまま残らないようにする。 同じ番号が別の変更で再び現れた時に
    // 黙って除外されるのを防ぐ
    const 範囲の番号 = new Set(commit.flatMap((s) => commitの番号(s)));
    expect(
      範囲の番号.size,
      "commit から番号を 1 つも読めていない (検査が空振りしている)",
    ).toBeGreaterThan(0);
    // **抜け道は持たない** (#1279)。 本文まで見るので、branch 上の番号がそのまま
    // 取り込み後も残る = 宣言は常に範囲の中にあるはず
    const 範囲に無い = Object.keys(利用者から見えない).filter((n) => !範囲の番号.has(n));
    expect(範囲に無い, "宣言に、この版の範囲に無い番号がある").toEqual([]);
  });

  it("版を上げた commit が範囲に入っている", () => {
    // **範囲を狭める向きの誤りは、上の検査では捕まらない** (Round 1 の指摘)。
    // 覆う対象が減るだけなので「全て辿れる」 は通ってしまう。 下端を直接固定する。
    //
    // **tag を使っている時も見る** (Round 2 の指摘)。 前の版の tag から測っても、版を
    // 上げた commit はその後ろにあるので範囲に入るはず。 入らないなら起点が誤っている
    const 上げた = 版を上げたcommit();
    expect(上げた, "版を上げた commit が見つからない (検査が空振りしている)").toBeDefined();
    if (!上げた) return;
    const 範囲のsha = new Set(
      git("log", "--format=%H", 範囲)
        .split("\n")
        .filter((l) => l !== ""),
    );
    expect(
      範囲のsha.size,
      "範囲の commit を 1 件も読めていない (検査が空振りしている)",
    ).toBeGreaterThan(0);
    expect(範囲のsha.has(上げた), "版を上げた commit が範囲から漏れている").toBe(true);
  });

  describe("commit の割り方 (#1279)", () => {
    // **記録区切りでは割れない形を固定する**。 実履歴に U+001E を含む commit は無いので、
    // 割り方だけを切り出して確かめる
    it("NUL で 1 件ずつに割る", () => {
      const raw = "題 A\n\n本文 A\u0000題 B\n\n本文 B\u0000";
      expect(本文で割る(raw)).toEqual(["題 A\n\n本文 A", "題 B\n\n本文 B"]);
    });

    it("本文に記録区切り (U+001E) があっても 1 件のまま", () => {
      const raw = "題 A\n\n本文に \u001e が入る\u0000題 B\u0000";
      const 割った = 本文で割る(raw);
      expect(割った.length, "本文の U+001E で割れてしまっている").toBe(2);
      expect(割った[0]).toContain("\u001e");
    });

    it("末尾の空きは数えない", () => {
      expect(本文で割る("題 A\u0000")).toEqual(["題 A"]);
      expect(本文で割る("")).toEqual([]);
    });
  });

  describe("番号を拾う行 (#1279)", () => {
    // **説明の本文からは拾わない**。 本文には review の指摘や関連 Issue の番号も書くため、
    // 丸ごと拾うと「別の番号がたまたま本文にある」 だけで辿れた扱いになる
    it("件名から拾う", () => {
      expect(commitの番号("✨ feat(dsl): 何かする (#1234)")).toEqual(["1234"]);
    });

    it("squash が並べた元の件名から拾う", () => {
      const m = [
        "🔖 chore(release): 版を切る (#1278)",
        "",
        "* 🔖 chore(release): 版を切る (#1277)",
        "",
        "* test(catalog): 絵文字なしの元件名 (#1267)",
      ].join("\n");
      expect(commitの番号(m)).toEqual(["1278", "1277", "1267"]);
    });

    it("説明の本文にある番号は拾わない", () => {
      const m = [
        "🐛 fix(dsl): 何かを直す (#1234)",
        "",
        "既存の検査 (#9999) は別の観点を見る。 関連は #8888。",
      ].join("\n");
      expect(commitの番号(m), "説明の本文から拾ってしまっている").toEqual(["1234"]);
    });

    it("closing keyword の行から拾う", () => {
      const m = ["✨ feat(dsl): 何かを足す", "", "変更の説明。", "", "Closes #1234"].join("\n");
      expect(commitの番号(m)).toEqual(["1234"]);
      expect(辿れるか(m, (n) => n === "1234")).toBe(true);
    });

    it("元件名の間に本文が挟まっても拾う", () => {
      // 実測 = GitHub は元件名の間に各 commit の本文を丸ごと挟む。 連続しない
      const m = [
        "🔖 chore(release): 版を切る (#1278)",
        "",
        "* 🔖 chore(release): 版を切る (#1277)",
        "",
        "本文 A。",
        "",
        "* 🐛 fix(release): 範囲を直す (#1277)",
        "",
        "本文 B。",
      ].join("\n");
      expect(commitの番号(m)).toEqual(["1278", "1277", "1277"]);
    });

    it("説明の箇条書きが未記載の元件名を隠さない", () => {
      // **Round 5 の反例**。 説明として書いた箇条書きが載っている番号を含んでいても、
      // 同じ commit の別の元件名が未記載なら辿れないままにする
      const m = [
        "🔖 chore(release): 版を切る (#9001)",
        "",
        "* 🔖 chore(release): 版を切る (#9002)",
        "",
        "* 既に記載済の話 (#1206)",
      ].join("\n");
      const 載る = (n: string): boolean => n === "1206";
      expect(辿れるか(m, 載る), "説明の箇条書きが未記載の元件名を隠している").toBe(false);
    });

    it("全ての元件名が載っていれば辿れる", () => {
      const m = [
        "🔖 chore(release): 版を切る (#9001)",
        "",
        "* 🔖 chore(release): 版を切る (#9002)",
        "",
        "* 既に記載済の話 (#1206)",
      ].join("\n");
      const 載る = (n: string): boolean => n === "1206" || n === "9002";
      expect(辿れるか(m, 載る)).toBe(true);
    });

    it("件名が載っていれば元件名を問わない", () => {
      const m = ["🔖 版を切る (#9001)", "", "* 未記載の話 (#9002)"].join("\n");
      expect(辿れるか(m, (n) => n === "9001")).toBe(true);
    });

    it("番号を持たない元件名は辿れるかの判定から外す", () => {
      // **Round 6 の反例**。 番号を付ける前の書き方で積んだ branch を取り込むと、
      // 列 0 の `* ` に番号を一つも持たない行が並ぶ (実測 = 全履歴 82 件)。
      // 名乗っていない行を「未記載の元件名」 として数えると、載っている commit を
      // 辿れないと判定してしまう
      const m = [
        "✨ feat(catalog): 段を付ける (#9001)",
        "",
        "* ✨ feat(catalog): 段を付ける (#9002)",
        "",
        "* ⏪ revert(design): 無関係な取り込みを戻す",
      ].join("\n");
      const 載る = (n: string): boolean => n === "9002";
      expect(辿れるか(m, 載る), "番号を持たない元件名が辿れる commit を隠している").toBe(true);
    });

    it("番号を持たない元件名しか無ければ件名で辿るしかない", () => {
      // 元件名から辿れる番号が 1 つも無い = 元件名は判定材料にならない。
      // 件名が載っていなければ辿れないままにする
      const m = ["✨ feat(catalog): 段を付ける (#9001)", "", "* ⏪ revert(design): 戻す"].join(
        "\n",
      );
      expect(辿れるか(m, (n) => n === "9002")).toBe(false);
      expect(辿れるか(m, (n) => n === "9001")).toBe(true);
    });

    it("番号を持たない行が混ざっても未記載の元件名は隠れない", () => {
      // Round 5 の性質が Round 6 の修正で消えていないことを固定する
      const m = [
        "✨ feat(catalog): 段を付ける (#9001)",
        "",
        "* ✨ feat(catalog): 段を付ける (#9002)",
        "",
        "* ⏪ revert(design): 戻す",
        "",
        "* 既に記載済の話 (#1206)",
      ].join("\n");
      const 載る = (n: string): boolean => n === "1206";
      expect(辿れるか(m, 載る), "未記載の元件名 #9002 が隠れている").toBe(false);
    });

    it("字下げした箇条書きは拾わない", () => {
      const m = [
        "🐛 fix(dsl): 何かを直す (#1234)",
        "",
        "  * fix(test): 字下げした補足 (#8888)",
        "    * さらに字下げ (#7777)",
      ].join("\n");
      expect(commitの番号(m), "字下げした箇条書きから拾ってしまっている").toEqual(["1234"]);
    });

    it("番号の後に注記が付いた元件名も拾う", () => {
      // 実履歴に `(#N Round 1)` / `(#N 段 2)` の形がある。 形で絞ると取りこぼす
      const m = [
        "📝 docs(spec): 注記を直す (#1211)",
        "",
        "* 📝 docs(spec): animation の注記を直す (#1208 Round 4 MINOR)",
        "* ✨ feat(dsl): 値の形を書けるようにする (#1161 段 2)",
      ].join("\n");
      expect(commitの番号(m)).toEqual(["1211", "1208", "1161"]);
    });

    it("番号を持たない commit は空", () => {
      expect(commitの番号("題だけの commit")).toEqual([]);
    });
  });

  it("宣言に理由が書かれている", () => {
    const 理由なし = Object.entries(利用者から見えない).filter(([, v]) => v.trim() === "");
    expect(理由なし, "宣言に理由の無い項目がある").toEqual([]);
  });
});
