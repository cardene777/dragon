/**
 * 置けたかどうかの判定に使う幅 (#2034 で `compile.ts` から移した)。
 *
 * **葉に置く** = 配置を確かめる側 (`verifyPlacement` / `applyLayoutOffsets`) と
 * 部品の取り込み側 (`縦列に置いた部品を揃える`) の両方が読む。 どちらかに置くと、
 * もう片方から逆向きの取り込みが生まれて輪になる。
 */

/**
 * 書いた通りに置けたと見なす誤差。
 *
 * 補正が効いた図種では実測 0.0 で一致する。 効かない向き (順序図の縦) は数百ずれるので、
 * その間で切る。 丸めと配置計算の揺れを吸収する幅として 1 を取る。
 */
export const PLACEMENT_TOLERANCE = 1;
