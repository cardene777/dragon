/**
 * 複雑な版を作らないと決めた見本と、その理由 (#2197)。
 *
 * **`presets.cdl.ts` には置けない**。 あの file の export は全て図である前提で読まれており
 * (`preset-draw-line-1351.test.ts` / `preset-step-duration-1353.test.ts` が
 * `Object.values` を `CdlDiagram` として扱う)、図でない export を 1 つ足すと型が合わなくなる。
 */

/**
 * 複雑な版 (`pattern__<見本>__複雑`) を **作らないと決めた見本** と、その理由。
 *
 * 見本 19 種のうち 17 種には複雑な版がある (#2177 から #2191)。
 * 残る 2 種は測った上で見送ったが、以前はその記録が取り込み済 PR の本文にしか無く、
 * 「まだ手を付けていない」 のか「作れないと分かっている」 のかを読み手が区別できなかった。
 *
 * **件数ではなく名前で持つ**。 件数だけだと、1 種に複雑な版を足して別の 1 種を足し忘れても
 * 数が合ってしまう。 実物から導いた「複雑な版を持たない見本」 との突き合わせは
 * `complex-step-pacing.test.ts` が行う。
 *
 * 見送りを取り消す (複雑な版を足す) 時は、ここから行を消す。
 */
export const 複雑な版を作らない見本: Readonly<Record<string, string>> = {
  presetFlow:
    "1 本の縦列に段を積む図で、順番を飛ばす線も戻る線も間の箱を貫く。 " +
    "複雑な版に要る分岐と戻りを描くと重い指摘が 6 件出た (#2177 で実測)",
  presetStateMachine2:
    "横にしか伸びない。 状態を 1 つ足すごとに板が 582 広がり、縦は 334 のまま変わらない。 " +
    "今の 4 状態で既に 2423 幅で、拡大の器に収めると箱の題が 10.4px になる (下限 12px)。 " +
    "5 状態で 9.2px、6 状態で 7.6px、9 状態で 5.0px。 " +
    "状態の幅を 280 から 200 に絞れば 2103 まで縮むが、複雑な版は状態を増やす版なので足した分だけ戻る (#2197 で実測)",
};
