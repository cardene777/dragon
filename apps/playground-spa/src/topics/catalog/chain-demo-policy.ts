/**
 * 繋いだ動きの見本を作らないと決めた部品と、その理由 (#2212)。
 *
 * **`parts.cdl.ts` には置けない**。 あの file の書き出しは全て図である前提で読まれており
 * (`parts-count.test.ts` / `catalog-motion-coverage.test.ts` が `Object.values` を図として扱う)、
 * 図でない書き出しを 1 つ足すと型が合わなくなる (#2197 で実測)。
 */

/**
 * 繋いだ動きの見本 (`parts-motion.cdl.ts` の切替) を **持たないと決めた** 繋ぎ方の部品と、その理由。
 *
 * 繋ぎ方の部品は 26 枚あり、20 枚が繋いだ見本を持つ。 残る 6 枚がここに載る。
 *
 * **件数ではなく名前で持つ**。 検査は繋ぎ方の部品の一覧と、繋いだ見本に出る部品の一覧を
 * 実物から導いて突き合わせ、差がこの表のキーと一致することを見る。
 * 片方だけ直すと落ちるので、部品を足した時に「見本も作るか、理由を書くか」 を必ず選ばされる。
 *
 * 見送りを取り消す (繋いだ見本を足す) 時は、ここから行を消す。
 */
export const 繋いだ見本を持たない部品: Readonly<Record<string, string>> = {
  "parts-mini-network":
    "縦列を 3 本持つので、2 枚繋ぐと縦列が 5 本になる。 板が 2052 幅まで伸び、" +
    "一覧の器に収めると箱の題が 9.4px になる (下限 12px、#2212 で実測)",
  "parts-priority-queue":
    "縦列を 3 本持つ。 板が 2013 幅で箱の題が 9.6px になる (#2212 で実測)",
  "parts-queue-depth":
    "縦列を 3 本持つ。 板が 2018 幅で箱の題が 9.5px になる (#2212 で実測)",
  "parts-rate-limiter":
    "縦列を 3 本持つ。 板が 2013 幅で箱の題が 9.6px になる (#2212 で実測)",
  "parts-valve-flow":
    "縦列を 3 本持つ。 板が 2018 幅で箱の題が 9.5px になる (#2212 で実測)",
  "parts-funnel-3":
    "幅は収まる (1380 幅で指摘 0 件) が、箱の高さが他の部品の 150 と違うため縦の中心が 10 ずれ、" +
    "繋ぐ矢印が真横に引けない (#2159 の道筋の検査が落ちる)。 " +
    "高さを揃えると「段ごとに狭くなる」 という図の中身そのものが消える (#2212 で実測)",
};
