/**
 * 見本帳の図に、英語の仮置きの字が残っていないこと (#1778 / #1859)。
 *
 * 見本の図は `User` `handler(...)` `Sign up` のような英語の仮置きのまま描かれていた。
 * 詳細画面の説明文 (#1777) と組み立て関数が書き足す字 (cdl#835) を日本語にしても、
 * 図そのものの字が英語のままでは見本として読めない。
 *
 * ## 何を見るか
 *
 * **描いた結果の字**。 組み立てた図を `CdlDiagramView` で描き、 要素の中身として表に出る字を
 * 集める (属性の値は読む人に見えないので見ない)。 図の data の欄を拾う形にすると、
 * 描く側が足す字 (札の既定値など) と描かない欄の区別が付かない。
 *
 * 描いた図は最初の段しか説明を出さず、光る矢印の札も最初の段の分しか出さない。 そのため
 * 段の題と説明と札、矢印の札は data から全段ぶん足す。
 * **足す欄は `描かない欄の字()` が一覧で持つ** (#1870) = 足し忘れた欄を後から確かめられるようにする。
 * 札が抜けていた間、札だけを英語に戻す変異が判定を素通りしていた (段の札は #1870、矢印の札は #1888)。
 *
 * ## どこまで見るか (#1859)
 *
 * **見本帳の置き場にある `*.cdl.ts` を全部**。 #1778 では `presets.cdl.ts` だけを import して
 * おり、12 file 444 件のうち 21 件しか見ていなかった。 外した理由はどこにも書かれておらず、
 * 残り 423 件はどの検査からも読まれていなかった。
 *
 * file を手で並べる形は **書き手が思い付いた file が上限** になるので、置き場を走査して導く。
 *
 * 英語が残る file は `図の字の天井` に **いまの数** を置く。 外す / 入れるの 2 値にすると、
 * あと 3 語で 0 になる file も手付かずの file も同じ扱いになって、減った分が見えない。
 * 天井 0 の file は 1 語でも戻れば落ちる厳しい判定に回る。
 *
 * ## 残してよい英字
 *
 * 製品名 / 規格名 / 略語 (`CloudFront` `HTTPS` `API` など) は日本語に置き換える語を持たない。
 * 1 語ずつ理由を付けて `残してよい語` に載せる。
 *
 * **記法の綴りは一覧に載せない** (#1865)。 見本帳の図は記法そのものを見せる場所なので、
 * 箱や段の字に `fill` / `readout` / `dotted` がそのまま出る。 これは記法が配る名前から
 * 照合で引く (`記法が知る名前()` + `綴りの照合()`) = 画面の字 (#1825) と図の説明 (#1830) が
 * 既に引いている経路で、**図の中の字だけが引いていなかった**。
 *
 * ## 覆えていない所 (#1879)
 *
 * **記法が知る綴りを地の文として使った段の題は、この検査では拾えない**。 判定は「記法が
 * 知らない英語の語」 を数えるので (#1865)、`step 進行` / `state 表示` のような段の題は
 * 0 件のまま通る。 #1879 で `parts.cdl.ts` の 8 件を日本語にしたが、英語に戻す変異は
 * 1 件も落ちなかった。
 *
 * **一律に「段の題に英字を置かない」 とはできない**。 実測で `presets.cdl.ts` は 89 件、
 * `styles.cdl.ts` は 16 件の段の題と札が記法の綴りそのもの (`swimlane` / `dotted-flow`) で、
 * 見せることが目的。 識別子として見せているのか地の文に混ぜたのかは字の並びに現れない。
 *
 * 表と型の名前を見せる図 (ER 図とクラス図) は、 箱の題と行が SQL やコードに書く識別子そのもの。
 * `users` `bigint` `Money` を訳すと図の意味が変わるため、 箱の字は見ずに段の字だけを見る。
 */
import { describe, it, expect, vi } from "vitest";
import { renderToStaticMarkup } from "react-dom/server";
import { CdlDiagramView, diagram } from "@cardenelabs/cdl";
import type { CdlDiagram } from "@cardenelabs/cdl";
import { 記法が知る名前, 記法が配る図の型, 綴りの照合, 経路ごとの専有 } from "./notation-names";
import { 図に画面の言語を当てる } from "./diagram-lang";

/**
 * 記法が知っている綴り (#1865)。
 *
 * **見本帳の図は記法そのものを見せる場所**なので、箱や段の字に記法の綴りがそのまま出る
 * (`fill 進行` / `flow 通過` / `dotted`)。 訳した瞬間に図が別の意味になるので、
 * 「開くべき英語」 には数えない。
 *
 * 画面の字 (#1825 / #1827) と図の説明 (#1830) が同じ照合を引いており、
 * **図の中の字だけが引いていなかった**。 判定を 2 度書かないよう、同じ 2 つの関数に通す。
 */
const 記法の経路 = 記法が知る名前().経路;

/**
 * 記法が配る図の型の名前 (#1870)。
 *
 * 段の札は書き手が書くとは限らない = 見本の組み立て (`withSteps()`) が、記法の作った段の札を
 * `auto?.badge` で引き継ぐ。 その字は図の型そのもの (`journey` / `swimlane`) で、
 * 訳すと図の型が読めなくなる。
 *
 * **4 経路では引けない** = 型の一覧は集合 (`Set`) で配られており、経路の走査が中身を数えない
 * (#1842 で実測)。 経路を広げると種類の一覧 117 件まで一緒に入って照合が緩むので、
 * 型の一覧だけを別に引く。
 */
const 図の型 = 記法が配る図の型();

/**
 * 日本語に置き換える語を持たない英字。 語ごとに何の名前かを書く。
 *
 * **記法の綴りはここに置かない** (#1865) = 照合の経路が引く。 重ねて持つと、経路が
 * その語を落とした日に手書きの側が黙って覆い、気付けなくなる。 死蔵は検査が見る。
 */
const 残してよい語: Record<string, string> = {
  AWS: "クラウドの事業者名",
  CloudFront: "AWS の配信網の製品名",
  ALB: "AWS の負荷分散の製品名",
  RDS: "AWS のデータベースの製品名",
  ECS: "AWS のコンテナ実行の製品名",
  Redis: "一時保存の製品名",
  PostgreSQL: "データベースの製品名",
  PgBouncer: "PostgreSQL の接続を束ねる製品名",
  HTTPS: "通信の規格名",
  SQL: "問い合わせ言語の名前",
  TCP: "通信の規格名",
  VLAN: "ネットワークを論理的に分ける規格名",
  DMZ: "ネットワークの区画の略語",
  LAN: "ネットワークの区画の略語",
  API: "外部から呼ぶ口の略語",
  DB: "データベースの略語",
  POST: "HTTP の要求の種類",
  login: "`POST /login` の道筋の一部",
  SNS: "交流サイトの定着した略語。 日本語の文でもこの綴りで書く (#1861)",
  ms: "時間の単位の記号。 単位は綴りが決まっている (`ミリ秒` は読みであって単位ではない、#1861)",
  dragon: "この製品の名前 (#1861)",
  Transfer: "送金を知らせる Solidity の出来事の名前。 コードに書く識別子で、訳すと実物と対応しなくなる (#1876)",
  gwei: "イーサリアムの手数料の単位。 単位は綴りが決まっている (#1881)",
  Kafka: "分散メッセージ配送の製品名 (#1884)",
  Cloudflare: "配信網の事業者名 (#1884)",
  Express: "サーバ側の枠組みの製品名 (#1884)",
  Bull: "待ち行列の製品名 (#1884)",
  SQS: "AWS の待ち行列の製品名 (#1884)",
  HMAC: "鍵付きハッシュの規格名 (#1884)",
  REST: "通信の設計様式の名前 (#1884)",
  CDN: "配信網の略語 (#1884)",
  GB: "情報量の単位の記号 (#1884)",
  CPU: "演算装置の略語 (#1884)",
  users: "`POST /users` の道筋の一部 (#1884)",
  GET: "HTTP の要求の種類。 既にある `POST` と同じ (#1886)",
  SELECT: "表から読み出す SQL の命令。 命令は綴りが決まっている (#1886)",
  UPDATE: "表を書き換える SQL の命令。 命令は綴りが決まっている (#1886)",
  orders: "`POST /orders` と `UPDATE orders` の道筋と表の名前 (#1886)",
  OrderCreated: "注文ができたことを知らせる出来事の名前。 コードに書く識別子 (#1886)",
  BEGIN: "取引の開始を書く SQL の命令。 命令は綴りが決まっている (#1888)",
  COMMIT: "取引の確定を書く SQL の命令 (#1888)",
  ROLLBACK: "取引の巻き戻しを書く SQL の命令 (#1888)",
  tx: "取引の定着した略記。 SQL の文でこの綴りで書く (#1888)",
  zod: "入力の形を確かめる枠組みの製品名 (#1888)",
  yup: "入力の形を確かめる枠組みの製品名 (#1888)",
  TLS: "通信を暗号にする取り決めの名前。 `TLS 1.3` のように版と組で書く (#1888)",
  SET: "鍵と値の保管庫へ書き込む命令。 読み出しの `GET` と対で `GET/SET` と書く (#1888)",
  PUT: "HTTP の要求の種類。 既にある `GET` / `POST` と同じ (#1890)",
  INSERT: "表へ行を足す SQL の命令。 命令は綴りが決まっている (#1890)",
  WHERE: "行を絞る SQL の句。 句は綴りが決まっている (#1890)",
  ORDER: "並べ替えを書く SQL の句 `ORDER BY` の前半 (#1890)",
  BY: "並べ替えを書く SQL の句 `ORDER BY` の後半 (#1890)",
  DESC: "`ORDER BY` で大きい順に並べる指定 (#1890)",
  LIMIT: "取り出す行の数を決める SQL の句 (#1890)",
  JSON: "データを文字で書く書式の名前 (#1890)",
  CSV: "表を文字で書く書式の名前。 カンマで列を区切る (#1890)",
  JWT: "署名付きの鍵の書式の名前 (#1890)",
  transfer: "ERC-20 の送金の関数名。 規格で決まった識別子で、出来事の `Transfer` と対になる (#1890)",
  PDF: "文書を配る書式の名前 (#1892)",
  MB: "情報量の単位の記号。 既にある `GB` と同じ (#1892)",
  km: "距離の単位の記号。 単位は綴りが決まっている (#1892)",
  zsh: "命令を打つ画面で動く対話の仕組みの製品名 (#1892)",
  pnpm: "部品の取り込みを受け持つ道具の製品名。 命令に打ち込む綴りそのもの (#1892)",
  iPhone: "携帯電話の製品名 (#1892)",
  Starlink: "低軌道の衛星で通信を配る事業の名前 (#1892)",
  RPC: "遠くの処理を呼ぶ取り決めの略語。 既にある `API` と同じ (#1892)",
  Alchemy: "分散台帳への問い合わせ窓口を貸す事業者の名前 (#1892)",
  MetaMask: "閲覧ソフトで使う暗号資産の財布の製品名 (#1892)",
  ETH: "Ethereum の通貨の略号。 通貨の略号は綴りが決まっている (#1892)",
  NFT: "代わりの利かない証票の定着した略語 (#1892)",
  CryptoPunk: "NFT の作品群の名前 (#1892)",
  ERC: "Ethereum の規格の番号に付く略号 (`ERC-20` / `ERC-721`) (#1892)",
  UFJ: "銀行の名前 (`三菱 UFJ 信託`) の一部 (#1892)",
  Stripe: "決済を代行する事業者の名前 (#1892)",
  Coinbase: "暗号資産の取引所の名前 (#1892)",
  ATM: "現金自動預払機の定着した略語。 日本語の文でもこの綴りで書く (#1892)",
  Amazon: "通販の事業者の名前 (#1892)",
  Kong: "API の入口を受け持つ製品の名前 (#1892)",
  Bitcoin: "分散台帳とその通貨の名前 (#1892)",
  Ethereum: "分散台帳とその通貨の名前 (#1892)",
  Infura: "分散台帳への問い合わせ窓口を貸す事業者の名前。 既にある `Alchemy` と同じ (#1894)",
  Solidity: "分散台帳の契約を書く言語の名前 (#1894)",
  VISA: "カードの国際ブランドの名前 (#1894)",
  MasterCard: "カードの国際ブランドの名前。 `VISA` と同じ (#1894)",
  OpenSea: "NFT を売り買いする場の名前 (#1894)",
  Arbitrum: "Ethereum の上に重ねた分散台帳の名前 (#1894)",
  USDC: "米ドルに値を固定した通貨の略号。 通貨の略号は綴りが決まっている (#1894)",
  aUSDC: "Aave に `USDC` を預けると受け取る、利息の付く通貨の略号 (#1894)",
  Aave: "分散台帳の上で貸し借りを受け持つ仕組みの名前 (#1894)",
  BTC: "Bitcoin の通貨の略号。 既にある `ETH` と同じ (#1894)",
  KIWA: "見本で発行する通貨の略号 (`$KIWA`)。 通貨の略号は英大文字で書く (#1894)",
  iOS: "携帯端末の基本ソフトの名前 (#1894)",
  Android: "携帯端末の基本ソフトの名前。 `iOS` と同じ (#1894)",
  SwiftUI: "iOS の画面を組む枠組みの製品名 (#1894)",
  GraphQL: "API への問い合わせを書く言語の名前。 既にある `SQL` と同じ扱い (#1894)",
  Solana: "分散台帳の名前 (#1894)",
  GitHub: "コードを預ける事業の名前 (#1894)",
  TypeScript: "プログラムを書く言語の名前 (#1894)",
  OpenZeppelin: "分散台帳の契約の部品集の名前 (#1894)",
  PayPal: "決済を代行する事業者の名前。 既にある `Stripe` と同じ (#1894)",
  Fastly: "配信網の事業者名。 既にある `Cloudflare` と同じ (#1894)",
  GCP: "Google のクラウドの略称。 既にある `AWS` と同じ (#1894)",
  px: "画面の長さの単位の記号。 既にある `ms` と同じく単位は綴りが決まっている (#1900)",
  React: "画面を組む部品集の製品名。 既にある `SwiftUI` と同じ (#1902)",
  Svelte: "画面を組む部品集の製品名。 既にある `SwiftUI` と同じ (#1902)",
  Vue: "画面を組む部品集の製品名。 既にある `SwiftUI` と同じ (#1902)",
  Python: "プログラムを書く言語の名前。 既にある `TypeScript` と同じ (#1902)",
  Rust: "プログラムを書く言語の名前。 既にある `TypeScript` と同じ (#1902)",
  Go: "プログラムを書く言語の名前。 既にある `TypeScript` と同じ (#1902)",
  Deno: "プログラムを動かす実行環境の製品名 (#1902)",
  rpm: "回転数の単位の記号。 既にある `ms` / `px` と同じく単位は綴りが決まっている (#1902)",
  Vite: "画面を組み立てる道具の製品名。 既にある `Deno` と同じ (#1904)",
  Bun: "プログラムを動かす実行環境の製品名。 既にある `Deno` と同じ (#1904)",
  SOL: "Solana の通貨の略号。 既にある `ETH` / `BTC` と同じ (#1904)",
  DOGE: "Dogecoin の通貨の略号。 既にある `ETH` / `BTC` と同じ (#1904)",
  UTC: "時刻の基準の略語。 時差は `UTC+9` の形で書き、日本語の文でもこの綴りを使う (#1904)",
  ls: "ファイルの名前を並べる命令。 既にある `SELECT` と同じく命令は綴りが決まっている (#1906)",
  cd: "作業する場所を移る命令。 既にある `SELECT` と同じ (#1906)",
  git: "変更の履歴を扱う道具の製品名。 命令に打ち込む綴りそのもの。 既にある `pnpm` と同じ (#1906)",
  diff: "変更の差分を出す `git` の命令。 既にある `SELECT` と同じ (#1906)",
  wc: "行や字の数を数える命令。 既にある `SELECT` と同じ (#1906)",
  SMS: "携帯電話の短い文の送り方の略語。 日本語の画面でもこの綴りで書く (#1906)",
  NPS: "顧客が薦めたいと思う度合いを表す指標の略語。 日本語の画面でもこの綴りで書く (#1906)",
  png: "画像の書式の名前。 ファイル名の拡張子として綴りが決まっている。 既にある `PDF` と同じ (#1906)",
  DELETE: "HTTP の要求の種類。 既にある `GET` / `POST` と同じ。 描画側が `API` の入口の形に日本語でも略さずこの綴りで書く (#1910)",
  ts: "TypeScript のファイル名の拡張子。 既にある `png` と同じく綴りが決まっている。 描画側がコードの札に `共通.ts` と書く (#1910)",
};

/**
 * 記法が図の型ごとに段の札へ自動で付ける綴り (#1870)。
 *
 * 段の札は書き手が書くとは限らない。 見本の組み立て (`withSteps()`) が、記法の作った段の札を
 * `auto?.badge` で引き継ぐ。 その字は照合でも図の型の一覧でも引けない = **図の型の一覧とは
 * 別の綴りが札に出る** (一覧は `state` / `mind` だが、札は `fsm` / `mindmap` で出る)。
 *
 * **開くのではなく、綴りを型の一覧と揃えて外す** (#1872)。 図の型は記法に打ち込む字なので
 * 日本語にはしない。 描画側 (別 package) の札を `state` / `mind` に揃えれば、型の一覧で
 * 引けるようになってこの手書きの一覧は空になる。
 *
 * 一覧が死蔵にならないことは下の検査が見る (札に 1 度も現れない綴りが残ると、同じ字が
 * 仮置きとして戻った日に黙って通る)。
 */
const 記法が札に付ける綴り: Record<string, string> = {
  fsm: "状態の移り変わりの図の札。 型の一覧は `state` だが札は別の綴りで出る",
  mindmap: "考えの枝分かれの図の札。 型の一覧は `mind` だが札は別の綴りで出る",
  statemachine2: "2 つ目の状態の移り変わりの図の札。 型の一覧に対応する綴りが無い",
};

/**
 * 箱の字が識別子そのものの図。 段の題と説明だけを見る。
 * 鍵が実在することは下の検査が確かめる (消えた図の宣言を残さない)。
 */
const 識別子を見せる図: Record<string, string> = {
  presetEr: "表の名前と列の名前と型は SQL に書く識別子",
  presetErComplex: "表の名前と列の名前と型は SQL に書く識別子",
  presetClassDiagram: "クラスの名前と型はコードに書く識別子",
  presetClassComplex: "クラスの名前と型はコードに書く識別子",
  textDslEr: "表の名前と列の名前は SQL に書く識別子 (#1886)",
  textDslErMesh: "表の名前と列の名前と型は SQL に書く識別子 (#1886)",
  textDslClass: "クラスの名前と持ち物はコードに書く識別子 (#1886)",
};

const 描く時刻 = new Date("2026-01-01T00:00:00Z");

/**
 * 描いた字を図ごとに覚えておく (#1890)。
 *
 * 見本帳を走査する検査は、それぞれが 444 図を描き直していた。 混んだ機械では 1 本が
 * 5 秒の上限を超えた (実測 = 負荷平均 100 を超えた時に `経路の専有` が打ち切られ、
 * 単独で回すと 225ms だった)。 同じ図を同じ時刻で描くので、覚えた字と描き直した字は変わらない。
 *
 * 鍵は図の object そのもの。 植え込み対照は毎回新しい図を作るので、覚えた字を引かない。
 */
const 描いた字の控え = new WeakMap<CdlDiagram, string[]>();

function 描いた字(d: CdlDiagram): string[] {
  const 控え = 描いた字の控え.get(d);
  if (控え) return 控え;
  const 字 = 描き直した字(d);
  描いた字の控え.set(d, 字);
  return 字;
}

function 描き直した字(d: CdlDiagram): string[] {
  vi.useFakeTimers();
  vi.setSystemTime(描く時刻);
  let s: string;
  try {
    // 詳細画面と同じ指定で描く (`PresetDetailPage`)。 見出しと段ごとの差分の表は画面が出さない。
    // **図をそのまま渡す** (#1898)。 画面はどれも `layout()` を通さずに渡している。 `layout()` の
    // 戻り値は入力欄と読み出しを持ち越さないので、通すと描画側が操作を持たない図と見なし、
    // 画面の操作部 (入力欄の名札 / 選択肢 / 読み出しの名札と単位) を描かず、入力欄の初期値も
    // 差し込まない (実測 = `eventVariety` が `{lastEvent} · 累計 {received} 回` のまま残った)
    //
    // **画面の既定の言語 (日本語) を当ててから描く** (#1910)。 画面は図を描く直前に言語を当て、
    // 形の絵の中の字 (`PAY` / `hash` など) を日本語で描く。 当てずに描くと、画面に出ない英語を数える
    s = renderToStaticMarkup(<CdlDiagramView hideMiniPhaseIndicator hideHeader diagram={図に画面の言語を当てる(d, "ja")} />);
  } finally {
    vi.useRealTimers();
  }
  return s
    .replace(/<style[\s\S]*?<\/style>/g, "")
    .replace(/<script[\s\S]*?<\/script>/g, "")
    .split(/<[^>]*>/)
    .map((t) =>
      t
        .replace(/&lt;/g, "<")
        .replace(/&gt;/g, ">")
        .replace(/&quot;/g, '"')
        .replace(/&#x27;/g, "'")
        .replace(/&amp;/g, "&")
        .trim(),
    )
    .filter((t) => t !== "");
}

/**
 * 描いた図が出さない欄の字。 **data から全段ぶん足す** (#1778 / #1870 / #1888)。
 *
 * | 欄 | なぜ足すか |
 * |---|---|
 * | 段の `title` / `body` | 描いた図は最初の段しか説明を出さない |
 * | 段の `badge` | 描いた図に 1 度も現れない (実測 = `stateInactive` の描いた字は `["A","B","停止中"]` で、札の `矢印は停止中` が無い) |
 * | 矢印の `label` / `sub` | 描いた図は最初の段で光る矢印の札しか出さない (実測 = `patternLoop` の `品目ごと` は 2 段目から光るので、`each item` へ戻しても判定を素通りした) |
 *
 * **足す欄を一覧にする** = 足し忘れた欄を後から確かめられるようにするため。 #1870 まで
 * `badge` が抜けており、札だけを英語に戻す変異が図の判定を素通りしていた (記法と図の一致を
 * 見る別の検査だけが落ちた)。 矢印の札も #1888 まで同じ形で抜けていた。
 */
function 描かない欄の字(d: CdlDiagram): string[] {
  return [
    ...d.phases.flatMap((p) => [p.title, p.body, p.badge]),
    ...d.edges.flatMap((e) => [e.label, e.sub]),
  ].filter((t): t is string => !!t);
}

/** 字に現れる英字の語。 1 文字の英字は語と見なさない (`スイッチ A` の区別や `Q1` の四半期) */
function 語の並び(t: string): string[] {
  return [...t.matchAll(/[A-Za-z][A-Za-z0-9]*/g)]
    .map((m) => m[0])
    .filter((語) => /[A-Za-z].*[A-Za-z]/.test(語));
}

/**
 * その綴りを記法が知っているか (#1865)。
 *
 * **判定は `綴りの照合()` に任せる** = 経路の見方を 2 度書かない。 1 語ずつ通すので
 * 結果を覚えておく (図 444 件を走査するため、同じ語を何度も引く)。
 */
const 記法が知るか = ((): ((語: string) => boolean) => {
  const 覚え = new Map<string, boolean>();
  return (語) => {
    let v = 覚え.get(語);
    if (v === undefined) {
      v = 綴りの照合([{ 元: 語, 綴り: 語 }], 記法の経路).無い.length === 0;
      覚え.set(語, v);
    }
    return v;
  };
})();

/**
 * 横棒 (`-`) で繋いだ英字の塊 (#1867)。
 *
 * 記法は横棒を値の一部として持つ (`dotted-flow` / `merkle-tree`) 一方、`語の並び()` は
 * 横棒を区切りとして扱う。 その食い違いで記法が知る値が断片に割れるので、塊を先に見る。
 */
const 横棒繋ぎの形 = /[A-Za-z][A-Za-z0-9]*(?:-[A-Za-z0-9]+)+/g;

/**
 * 16 進の値 (#1874)。
 *
 * 語を拾う形は英字始まりの並びを取るので、`0x7b3f92c1` が `x7b3f92c1` に割れる。
 * **16 進は値であって英語ではない** = 日本語に開く先が無く、開くと図が別の値を指す。
 *
 * **`0x` の付かない形は外さない** = `af31c9d2` は普通の英字の並びと見分けが付かない。
 *
 * **大文字の接頭辞 (`0X`) も外さない**。 書き方の揺れとして `0[xX]` にする案を試したが、
 * 見本に `0X` の形が 0 件で、変異で外しても検査が 1 件も動かなかった (実測)。
 * 動かない分岐を置くと、壊れても気付けない守りが 1 つ増える。 `0X` で書かれた日は
 * その値が「開くべき英語」 に数えられて天井を超えるので、気付ける側に倒れる。
 */
const 十六進の形 = /0x[0-9a-fA-F]+/g;

/**
 * 色の値 (#1896)。
 *
 * `0x` の 16 進と同じく、`#8a5a2a` の `8` を飛ばして `a5a2a` が英語の語として数えられる。
 * 副題の差し込み `hex: {color}` は色を選ぶ部品の値をそのまま出すので、書き手の字を直しても
 * 消えない。 **色の値は開く先が無く、開くと図が別の色を指す**。
 *
 * `#` が値の接頭辞になるので、`0x` と同じく普通の英字の並びと見分けが付く。
 *
 * **6 桁に限る**。 3 桁 (`#fff`) と 8 桁 (`#rrggbbaa`) は見本に 1 件も無い。 足しても変異で
 * 動かない分岐になる (`0X` を足さなかった理由と同じ)。 後ろに 16 進の字が続く並び
 * (`#8a5a2a0`) は色の値ではないので外さない。
 */
const 色の値の形 = /#[0-9a-fA-F]{6}(?![0-9a-fA-F])/g;

/**
 * 語に割る前に字から外す並び (#1867 / #1874 / #1896)。
 *
 * どれも**語の形になってから判定しても手遅れ**なので、字の段階で外す。
 *
 * | 外す相手 | 語に割ると何が起きるか |
 * |---|---|
 * | 記法が知る横棒繋ぎ | `dotted-flow` が `dotted` と `flow` に割れ、照合を外れた断片が残る |
 * | 16 進の値 | `0x7b3f92c1` の `0` を飛ばして `x7b3f92c1` が英語の語として数えられる |
 * | 色の値 | `#8a5a2a` の `8` を飛ばして `a5a2a` が英語の語として数えられる |
 *
 * **横棒を語の一部にするだけでは足りない** = `Sign-up` のような記法と無関係な綴りまで
 * 1 語になり、断片ごとに数えていた従来の結果が変わる。 外すのは記法が知る塊だけ。
 *
 * **塊の一部が記法の値でも、全体を知らなければ外さない** = `user-alice` を外すと
 * `alice` まで消える。
 */
function 語になる前に外す(t: string): string {
  return t
    .replace(十六進の形, " ")
    .replace(色の値の形, " ")
    .replace(横棒繋ぎの形, (塊) => (記法が知るか(塊) ? " " : 塊));
}

/** 字に現れる、記法が知っている横棒繋ぎの塊。 検査が母集団を実物から導くのに使う (#1867) */
function 記法が知る横棒繋ぎ(字: readonly string[]): string[] {
  const 出た = new Set<string>();
  for (const t of 字) {
    for (const m of t.matchAll(横棒繋ぎの形)) if (記法が知るか(m[0])) 出た.add(m[0]);
  }
  return [...出た].sort();
}

/** 字に現れる 16 進の値。 検査が母集団を実物から導くのに使う (#1874) */
function 字に出る十六進(字: readonly string[]): string[] {
  const 出た = new Set<string>();
  for (const t of 字) for (const m of t.matchAll(十六進の形)) 出た.add(m[0]);
  return [...出た].sort();
}

/** 字に現れる色の値。 検査が母集団を実物から導くのに使う (#1896) */
function 字に出る色の値(字: readonly string[]): string[] {
  const 出た = new Set<string>();
  for (const t of 字) for (const m of t.matchAll(色の値の形)) 出た.add(m[0]);
  return [...出た].sort();
}

/**
 * その語を「開くべき英語」 から外すか。 **外す相手を 1 箇所に並べる** (#1870)。
 *
 * | 相手 | 何を外すか |
 * |---|---|
 * | `残してよい語` | 製品名 / 規格名 / 単位 (手書き、理由付き) |
 * | `記法が札に付ける綴り` | 記法が段の札へ自動で付ける字 (手書き、理由付き) |
 * | `許す語` | 識別子を見せる図で、図に描いた識別子 |
 * | `図の型` | 記法が配る図の型の名前 |
 * | `記法が知るか` | 記法が配る名前の 4 経路との照合 |
 */
function 開かない語(語: string, 許す語: ReadonlySet<string>): boolean {
  return (
    語 in 残してよい語 ||
    語 in 記法が札に付ける綴り ||
    許す語.has(語) ||
    図の型.has(語) ||
    記法が知るか(語)
  );
}

/** 字の並びから、 開かない語を除いた英語の語を拾う */
function 英語の語(字: readonly string[], 許す語: ReadonlySet<string> = new Set()): string[] {
  const 見つけた = new Set<string>();
  for (const t of 字) {
    for (const 語 of 語の並び(語になる前に外す(t))) {
      if (開かない語(語, 許す語)) continue;
      見つけた.add(`${語} (「${t}」)`);
    }
  }
  return [...見つけた];
}

/**
 * 識別子を見せる図は、 段の説明が図の中の識別子を呼ぶ (`manager_id は同じ表を指す`)。
 * **図に描いた識別子だけ** を許す = 図に無い英語の語は段の説明でも拾う。
 */
/**
 * 判定が読む字。 **空振りの下限もここから数える** (#1879)。
 *
 * 判定が読む集合と、空振りを見る集合がずれていると「1 つも拾えていない」 が実際には見ている
 * 図で立つ。 実測で `parts.cdl.ts` の 50 図が描いた字を 1 つも持たなかった = 絵だけを描く種別
 * (`dyn-arc` / `dyn-rect` など) は箱の題を `data-cdl-title` の控えにしか置かず、表に出る字が
 * 1 つも無い。 その 50 図も段の題は持っており、判定はそちらを読んでいた。
 */
function 判定が読む字(鍵: string, d: CdlDiagram): string[] {
  return 鍵 in 識別子を見せる図 ? 描かない欄の字(d) : [...描いた字(d), ...描かない欄の字(d)];
}

function 英語の残り(鍵: string, d: CdlDiagram): string[] {
  if (鍵 in 識別子を見せる図) {
    return 英語の語(判定が読む字(鍵, d), new Set(描いた字(d).flatMap(語の並び)));
  }
  return 英語の語(判定が読む字(鍵, d));
}

/**
 * 手書きの一覧のうち、記法の経路が既に覆っている語 (#1865)。
 *
 * 本番 (`残してよい語`) と植え込み対照の両方が通る。 探し方を 2 度書くと、
 * 片方だけ直して対照が本番と別のものを見る。
 */
function 経路と重なる語(一覧: readonly string[]): string[] {
  return 一覧.filter((語) => 記法が知るか(語));
}

/**
 * 見本帳の図から、判定が見る字を全部集める (#1867)。
 *
 * 識別子を見せる図は箱の字を見ない、という除外を **1 箇所に持つ**。
 * 走査を書き足すたびに同じ分岐を写すと、片方だけ直って母集団が食い違う。
 */
function 見本帳の字(): string[] {
  return 見本帳の図().件.flatMap((r) => 判定が読む字(r.鍵, r.図));
}

/** 見本帳の図が持つ段の札。 一覧の死蔵を見る検査が母集団を実物から導くのに使う (#1870) */
function 見本帳の札(): string[] {
  return 見本帳の図().件.flatMap((r) =>
    r.図.phases.map((p) => p.badge).filter((b): b is string => !!b),
  );
}

/**
 * 図の字に実際に出ている、記法が知る綴り (#1865)。
 *
 * 経路の専有を測る相手。 **記法の一覧をそのまま渡さない** = 記法は数千の綴りを配っており、
 * その全部を相手にすると「図の字でどの経路が効いているか」 が読めなくなる。
 */
function 図に出る記法の綴り(): string[] {
  const 集 = new Set<string>();
  for (const t of 見本帳の字()) for (const 語 of 語の並び(t)) if (記法が知るか(語)) 集.add(語);
  return [...集];
}

/**
 * 見本帳の図の置き場にある file を全部読む (#1859)。
 *
 * `eager` で読むのは、走査の途中で待たないため。 444 件を描いて 555ms なので重くない。
 */
const 見本帳 = import.meta.glob<Record<string, unknown>>("../topics/catalog/*.cdl.ts", {
  eager: true,
});

/** 図とみなす export = 箱と段の並びを持つ object */
function 図か(v: unknown): v is CdlDiagram {
  return (
    typeof v === "object" &&
    v !== null &&
    Array.isArray((v as CdlDiagram).nodes) &&
    Array.isArray((v as CdlDiagram).phases)
  );
}

/**
 * 見本帳の図を file ごとに導く (#1859)。
 *
 * `file数` を返すのは、置き場を読めていない時に「図が 0 件」 と区別するため。
 */
function 見本帳の図(): { 件: { file: string; 鍵: string; 図: CdlDiagram }[]; file数: number } {
  const 件: { file: string; 鍵: string; 図: CdlDiagram }[] = [];
  for (const [path, mod] of Object.entries(見本帳)) {
    const file = path.slice(path.lastIndexOf("/") + 1);
    for (const [鍵, v] of Object.entries(mod)) if (図か(v)) 件.push({ file, 鍵, 図: v });
  }
  return { 件, file数: Object.keys(見本帳).length };
}

/**
 * file ごとの、英語が残る図と語の天井 (#1859)。
 *
 * **減った時も落とす** = 下回ったまま通すと、一度直した分がまた増えても気付けない。
 * 落ちた時の文で下げる先を出す。
 *
 * `presets.cdl.ts` が 0 なのは #1778 で全部日本語に直したから。 **0 の file は
 * 1 語でも戻れば落ちる厳しい判定に回る** (下の `it.each`)。
 *
 * `charts.cdl.ts` の 3 種のように 0 に近い file がある。 外す / 入れるの 2 値にすると
 * こういう file が手付かずの file と同じ扱いになるので、数で持つ。
 *
 * **天井の上下は、数え方が変わっただけの回と実物を直した回が混ざる**。 混ぜて読まない。
 *
 * | 回 | 向き | 何をしたか |
 * |---|---|---|
 * | #1861 | 下がる | `charts.cdl.ts` の 3 語を実際に日本語へ開いた |
 * | #1865 | 下がる | 記法の綴り (`fill` / `readout`) を照合で落とした |
 * | #1867 | 下がる | 横棒繋ぎの値 (`dotted-flow`) が断片に割れるのを止めた |
 * | #1869 | 下がる | `styles.cdl.ts` の 6 語を実際に日本語へ開いた |
 * | #1870 | **上がる** | 段の札を母集団に足した (いままで 1 度も見ていなかった) |
 * | #1874 | 下がる | 16 進の値 (`0x7b3f92c1`) が語に割れるのを止めた |
 * | #1876 | 下がる | 分散台帳の見本の人名 2 語を日本語にし、Solidity の出来事の名前を一覧に載せた |
 * | #1879 | 下がる | `parts.cdl.ts` の段の題 26 件を日本語に開いた (80 図が厳しい判定へ) |
 * | #1881 | 下がる | `animation.cdl.ts` の 19 語を日本語に開き、単位の `gwei` を一覧に載せた |
 * | #1884 | 下がる | `primitives-extra.cdl.ts` の 52 語を日本語に開き、製品名と規格名と単位 11 語を一覧に載せた |
 * | #1886 | 下がる | `text-dsl.cdl.ts` の仮置きの役名を日本語に開き、表と型を見せる 3 図を識別子の側へ回した |
 * | #1888 | 下がる | `patterns.cdl.ts` の仮置きの役名と段の説明の地の文を日本語に開いた |
 * | #1888 | **上がる** | 矢印の札 (`label` / `sub`) を母集団に足した (最初の段で光らない矢印の札を見ていなかった)。 増えた `TLS` / `SET` は一覧に載せたので天井は動かない |
 * | #1890 | 下がる | `cookbook.cdl.ts` の仮置きの役名と矢印と段の字を日本語に開き、HTTP と SQL の綴りと書式の名前 11 語を一覧に載せた (`interactive` / `primitives` も下がった) |
 * | #1892 | 下がる | `primitives.cdl.ts` の部品と形の見本 59 図の書き手の字を日本語に開き、製品名と規格名と単位 22 語を一覧に載せた (`interactive` も下がった)。 残る 14 図は描画側が箱に書く字だけ (#1877) |
 * | #1894 | 下がる | `primitives.cdl.ts` の場面の見本 30 図の書き手の字を日本語に開き、製品名と言語名と通貨の略号 22 語を一覧に載せた (`interactive` も下がった)。 残る 33 図は描画側が箱に書く字だけ (#1877) |
 * | #1896 | 下がる | 色の値 (`#8a5a2a`) が語に割れるのを止めた (`interactive.cdl.ts` の断片 3 語 / 5 か所) |
 * | #1898 | **上がる** | 図を `layout()` に通さず画面と同じくそのまま描き、画面の操作部 (入力欄の名札 / 選択肢 / 読み出しの名札と単位) と入力欄の初期値の差し込みを母集団に足した (`interactive.cdl.ts` が 13 図 / 235 語 / 延べ 950 上がる)。 新たに見えた `parts` 15 図 / `animation` 1 図 / `ethereum` 1 図の字は日本語に開いたので、その 3 file は動かない |
 * | #1900 | 下がる | `interactive.cdl.ts` の 1〜45 図の書き手の字と選択肢と状態の値を日本語に開き、単位の `px` を一覧に載せた (33 図 / 193 語 / 延べ 440 下がる)。 1〜45 図に残る 12 図は描画側が書く字だけ (式の名前と中身 / 巻き取りの名前 / 升目の見出し / 真偽の字、#1877) |
 * | #1902 | 下がる | `interactive.cdl.ts` の 46〜68 図の書き手の字と配列と状態の値 (人名 / 工程名 / 会話の文) を日本語に開き、製品名 7 語と単位の `rpm` を一覧に載せた (19 図 / 160 語 / 延べ 415 下がる。 うち延べ 10 は一覧に載せた製品名を持つ 69〜96 図の分)。 46〜68 図に残る 4 図は描画側が書く字だけ (傾きの目盛 / 星の見出し / 値札の割引 / 信号の値、#1877) |
 * | #1904 | 下がる | `interactive.cdl.ts` の 69〜96 図の書き手の字と配列と状態の値 (人名 / 都市 / 曲名 / 知らせる文 / 検索結果) を日本語に開き、実在の曲名と場所の URL を架空の見本に置き換え、製品名 2 語と通貨の略号 2 語と時刻の基準の `UTC` を一覧に載せた (22 図 / 270 語 / 延べ 561 下がる)。 69〜96 図に残る 6 図は描画側が書く字と読む値だけ (進捗の値 / 式の名前と中身 / 確信度の帯 / 優先度の値 / 再生数の後ろの字 / 出席表の角の見出し、#1877) |
 * | #1906 | 下がる | `interactive.cdl.ts` の 97〜129 図の書き手の字と配列と状態の値 (かんばんの札 / 予定 / 人名 / 公開の手順 / ログの文 / 見本の流れ 3 図の箱と札と段の説明) を日本語に開き、端末の命令を英語の見出しを出さない形に組み直し、実在の銀行名と一覧に無い製品名を一般の呼び名に置き換え、命令 5 語と略語 2 語と画像の書式の `png` を一覧に載せた (13 図 / 202 語 / 延べ 337 下がる)。 97〜129 図に残る 18 図は描画側が書く字と読む値と、描画側が色を引く鍵を題に残した共有ボタンの図だけ (曜日の見出し / かんばんの列の見出し / 状態の略記 / 在席の値 / 票の後ろの字 / 前月の見出し / 参加者と発言者の見出し / 既読の段 / 強度の名前 / 取り込み済みの字 / ログの重さ / 買い物かごの見出し / 料金表の字 / 割引券の字 / 著者の見出し / 形が描く字 / 信号の色の名前、#1877) |
 * | #1910 | 下がる | 図を画面の既定の言語 (日本語) を当ててから描き、描画エンジン `0.58.0` が形の絵の中の固定の字を日本語で描くようにした。 日本語の辞書が英字のまま残す `DELETE` (HTTP の要求の種類) と `ts` (拡張子) を一覧に載せた。 `primitives.cdl.ts` の 33 図 / 34 語 / 延べ 84 と `ethereum.cdl.ts` の 2 図 / 5 語 / 延べ 5 が 0 になり、`interactive.cdl.ts` の決済の流れの形の字 5 語 / 延べ 5 が下がった。 残る `interactive.cdl.ts` の 40 図は読み出しの字 (#1877) |
 * | #1912 | 下がる | 描画エンジンを `0.59.0` に上げ、操作部の読み出しと再生欄の固定の字 (値の無い読み出し / かんばんの列の見出し / 再生欄の倍率と秒 / 曜日 / 買い物かごの見出し など) を日本語で描くようにした (`interactive.cdl.ts` が 22 図 / 41 語 / 延べ 417 下がる)。 残る 18 図は、描画側が書き手の値や識別子をそのまま描く字だけ (入力欄の選択肢 / 状態の値 / 式と巻き取りの名前と中身 / 状態の略記 / 共有ボタンの鍵、#1877) |
 *
 * **手書きの一覧に 1 語足すと、その語を持つ file すべてが下がる** (#1861 / #1876 で実測)。
 * 一覧は 12 file に効くので、足した回は全 file の数を測り直す。
 */
const 図の字の天井: Record<string, { 図: number; 語: number; 延べ: number }> = {
  "animation.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "charts.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "cookbook.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "ethereum.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "interactive.cdl.ts": { 図: 18, 語: 38, 延べ: 58 },
  "parts.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "patterns.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "presets.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "primitives-extra.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "primitives.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "styles.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
  "text-dsl.cdl.ts": { 図: 0, 語: 0, 延べ: 0 },
};

/** file ごとに、英語が残る図の数と語の種類と延べを数える */
function file別に数える(): Map<string, { 図: number; 語: Set<string>; 延べ: number }> {
  const 表 = new Map<string, { 図: number; 語: Set<string>; 延べ: number }>();
  for (const r of 見本帳の図().件) {
    const 行 = 表.get(r.file) ?? { 図: 0, 語: new Set<string>(), 延べ: 0 };
    const 残り = 英語の残り(r.鍵, r.図);
    if (残り.length > 0) {
      行.図 += 1;
      行.延べ += 残り.length;
      for (const w of 残り) 行.語.add(w.slice(0, w.indexOf(" (")));
    }
    表.set(r.file, 行);
  }
  return 表;
}

/**
 * 1 語でも英語が戻れば落ちる file の図。 **天井 0 の file から導く** =
 * file 名を 2 箇所に書くと片方だけ直って食い違う。
 */
const 見本: [string, CdlDiagram][] = 見本帳の図()
  .件.filter((r) => 図の字の天井[r.file]?.図 === 0)
  .map((r) => [r.鍵, r.図]);

/** 植え込み対照の土台。 見本帳の data に依らず、 日本語だけで組む */
function 土台(箱の題: string): CdlDiagram {
  return diagram("土台", { topic: "土台の図" })
    .lane("左", { width: 400, label: "受け付け" })
    .node("a", { lane: "左", stack: 0, kind: "function", title: 箱の題 })
    .phase("p1", { duration: 1000, title: "1. 受け付ける", body: "申請を受け付ける。" }, (p) =>
      p.activate("a"),
    )
    .build();
}

describe("英語が片付いた見本の図の字 (#1778)", () => {
  it("対象の見本を見つけている", () => {
    // 見本が 1 件も取れないと、 下の検査は何も見ずに通る
    expect(見本.length, "見本を 1 件も見つけていない (検査が空振りしている)").toBeGreaterThan(0);
    expect(見本.map(([k]) => k)).toEqual(expect.arrayContaining(Object.keys(識別子を見せる図)));
  });

  it.each(見本)("%s の図に英語の仮置きの字が残っていない", (鍵, d) => {
    // 下限は **判定が読む字から数える** (#1879)。 描いた字だけを数えると、絵だけを描く図が
    // 「1 つも拾えていない」 で落ちる (段の題は読んでいるので、判定は空振りしていない)
    const 字 = 判定が読む字(鍵, d);
    expect(字.length, `${鍵} の字を 1 つも拾えていない (検査が空振りしている)`).toBeGreaterThan(0);
    expect(英語の残り(鍵, d)).toEqual([]);
  });

  it("残してよい語は、 どれかの図で実際に使われている", () => {
    // 使われない語が残ると、 同じ語が仮置きとして戻った時に黙って通る。
    // **見本帳の図を全部見る** (#1859) = 一覧は 12 file すべてに効くので、
    // 1 file の中だけで死蔵を判定すると別の file で生きている語を死蔵と読む
    const 全部の字 = 見本帳の図()
      .件.flatMap((r) => [...描いた字(r.図), ...描かない欄の字(r.図)])
      .join("\n");
    const 使われない = Object.keys(残してよい語).filter(
      (語) => !new RegExp(`(^|[^A-Za-z])${語}([^A-Za-z]|$)`).test(全部の字),
    );
    expect(使われない).toEqual([]);
  });
});

describe("見本帳の図に残る英語の歯止め (#1859)", () => {
  it("いまの天井を超えていない、下回ってもいない", () => {
    const { 件, file数 } = 見本帳の図();
    const 表 = file別に数える();
    console.log(
      `[図の字] file=${file数} 図=${件.length} / ` +
        [...表.entries()]
          .sort()
          .map(([f, r]) => `${f} 図=${r.図} 語=${r.語.size} 延べ=${r.延べ}`)
          .join(" / "),
    );
    // 空振り防止 = 置き場を読めていないと 3 つの数がそろって 0 になり、天井の下回りで落ちる。
    // それでも下限を置くのは、**落ちた時に直す先が違う** ため
    // (天井の下回りは「直したので下げる」、下限は「置き場の走査が壊れた」)
    expect(file数, "見本帳の file を 1 つも読めていない (検査が空振りしている)").toBeGreaterThan(5);
    expect(件.length, "図を 1 件も見つけていない (検査が空振りしている)").toBeGreaterThan(100);
    for (const [f, r] of [...表.entries()].sort()) {
      const 上 = 図の字の天井[f];
      expect(上, `天井の表に無い file がある: ${f} (表に 1 行足す)`).toBeDefined();
      for (const [名, いま, 天井] of [
        ["図", r.図, 上!.図],
        ["語", r.語.size, 上!.語],
        ["延べ", r.延べ, 上!.延べ],
      ] as const) {
        expect(
          いま,
          `${f} の ${名} が天井を超えた。 直すか、直せない理由を書く (${天井} → ${いま})`,
        ).toBeLessThanOrEqual(天井);
        expect(
          いま,
          `${f} の英語が減ったので天井を下げる (${名}: ${天井} → ${いま})`,
        ).toBeGreaterThanOrEqual(天井);
      }
    }
  });

  it("天井の表と走査した file が一致している (母集団の完全性)", () => {
    // 表に 1 行足し忘れた file は、上の検査が拾うまで気付けない。 逆に、消えた file の
    // 行が残ると **その行は永久に何も守らない**。 両方向を見る
    const 走査 = [...file別に数える().keys()].sort();
    expect(走査.length, "file を 1 つも走査していない (検査が空振りしている)").toBeGreaterThan(5);
    expect(走査, "天井の表と走査した file が食い違っている").toEqual(
      Object.keys(図の字の天井).sort(),
    );
  });

  it("天井 0 の file が実在し、厳しい判定に回っている (収容対照)", () => {
    // 直し終えた file が候補から消える形だと、直した先を 1 件も見ていないことになる
    const 零 = Object.entries(図の字の天井)
      .filter(([, r]) => r.図 === 0)
      .map(([f]) => f);
    expect(零, "天井 0 の file が 1 つも無い (厳しい判定の相手が居ない)").not.toEqual([]);
    const 走査 = file別に数える();
    for (const f of 零) {
      expect(走査.has(f), `天井 0 の file を走査していない: ${f}`).toBe(true);
      expect(
        見本帳の図().件.filter((r) => r.file === f).length,
        `${f} の図を 1 件も見つけていない`,
      ).toBeGreaterThan(0);
    }
    // 厳しい判定の相手が、天井 0 の file の図と同じ数になっている
    expect(見本.length).toBe(
      見本帳の図().件.filter((r) => 零.includes(r.file)).length,
    );
  });
});

describe("16 進の値を英語の語として数えない (#1874)", () => {
  it("図に出る 16 進の値が、断片に割れて数えられていない", () => {
    // **値だけを含む字を作って通す** = 他の語の影響を受けない
    const 値たち = 字に出る十六進(見本帳の字());
    expect(値たち.length, "16 進の値が 1 件も無い (検査が空振りしている)").toBeGreaterThan(0);
    console.log(`[16 進] ${値たち.length} 種 = ${値たち.join(" ")}`);
    const 残る = 値たち.filter((v) => 英語の語([v]).length > 0);
    expect(残る, "16 進の値が断片に割れて数えられている").toEqual([]);
  });

  it("`0x` の付かない英字の並びは、従来どおり数える (植え込み対照)", () => {
    // 0 件を期待する上の検査が恒真でないことを、逆向きの入力で見る
    expect(記法が知るか("af31c9d2"), "af31c9d2 を記法が知っている (対照の前提が崩れた)").toBe(
      false,
    );
    expect(英語の語(["af31c9d2"])).toEqual(["af31c9d2 (「af31c9d2」)"]);
  });

  it("16 進の後ろに続く英語は、従来どおり数える", () => {
    // 値だけを外す。 同じ字の中の別の語まで消すと判定が緩む
    expect(英語の語(["0x1234 の Alice"])).toEqual(["Alice (「0x1234 の Alice」)"]);
  });
});

describe("色の値を英語の語として数えない (#1896)", () => {
  it("図に出る色の値が、断片に割れて数えられていない", () => {
    // **値だけを含む字を作って通す** = 同じ字の中の他の語 (`hex` / `stroke`) の影響を受けない
    const 値たち = 字に出る色の値(見本帳の字());
    expect(値たち.length, "色の値が 1 件も無い (検査が空振りしている)").toBeGreaterThan(0);
    console.log(`[色の値] ${値たち.length} 種 = ${値たち.join(" ")}`);
    const 残る = 値たち.filter((v) => 英語の語([v]).length > 0);
    expect(残る, "色の値が断片に割れて数えられている").toEqual([]);
  });

  it("`#` の付かない 6 桁の並びは、従来どおり数える (植え込み対照)", () => {
    // 0 件を期待する上の検査が恒真でないことを、逆向きの入力で見る
    expect(記法が知るか("a5a2a"), "a5a2a を記法が知っている (対照の前提が崩れた)").toBe(false);
    expect(英語の語(["8a5a2a"])).toEqual(["a5a2a (「8a5a2a」)"]);
  });

  it("7 桁以上の 16 進が続く並びは、色の値として外さない (植え込み対照)", () => {
    // 6 桁で切ると `#8a5a2a0` の頭 6 桁だけが外れ、残りが語に割れて意味の違う判定になる
    expect(英語の語(["#8a5a2a0"])).toEqual(["a5a2a0 (「#8a5a2a0」)"]);
  });

  it("色の値の後ろに続く英語は、従来どおり数える", () => {
    // 値だけを外す。 同じ字の中の別の語まで消すと判定が緩む
    expect(英語の語(["#2563eb の Alice"])).toEqual(["Alice (「#2563eb の Alice」)"]);
  });
});

/**
 * 描いた字に、差し込まれずに残った `{名前}`。 形は描画側が差し込めなかった名前を残す形と
 * 同じにする (`@cardenelabs/cdl` の `TEMPLATE_LEFTOVER_SRC`。 package が export しないので写す)。
 * 名前の後ろの取り出し (`.length` / `[0]`) も含めて 1 件と数える。
 */
const 差し込み残りの形 = /\{\w+(?:[.[][^}]*)?\}/g;
function 差し込まれていない名前(字: readonly string[]): string[] {
  return 字.flatMap((t) => t.match(差し込み残りの形) ?? []);
}

describe("画面と同じく図をそのまま描く (#1898)", () => {
  /**
   * 入力欄を 1 つ持つ図。 入力欄の名札は画面の操作部に出て、副題の `{choice}` には入力欄の
   * 初期値が差し込まれる。 `状態` を渡すと同じ名前の状態も持つ (画面では入力欄の値が勝つ)
   */
  function 入力欄の図(名札: string, 状態?: string): CdlDiagram {
    const b = diagram("入力欄の図", { topic: "入力欄の図" })
      .lane("左", { width: 400, label: "受け付け" })
      .input.dropdown("choice", { options: ["未選択", "受付済"], defaultValue: "受付済", label: 名札 });
    return (状態 === undefined ? b : b.state("choice", { initial: 状態 }))
      .node("a", { lane: "左", stack: 0, kind: "function", title: "受付", subtitle: "選んだ値は {choice}" })
      .phase("p1", { duration: 1000, title: "1. 受け付ける", body: "申請を受け付ける。" }, (p) =>
        p.activate("a"),
      )
      .build();
  }

  it("入力欄の名札にだけ英語を置いた図から拾う (植え込み対照)", () => {
    // `layout()` を通して描くと操作部が出ず、名札を 1 度も読まない
    expect(記法が知るか("Choice"), "Choice を記法が知っている (対照の前提が崩れた)").toBe(false);
    expect(英語の残り("入力欄の図", 入力欄の図("Choice"))).toEqual(["Choice (「Choice」)"]);
  });

  it("入力欄の名札が日本語なら拾わない (陰性対照)", () => {
    expect(英語の残り("入力欄の図", 入力欄の図("選んだ窓口"))).toEqual([]);
  });

  it("入力欄だけが持つ名前に、入力欄の初期値が差し込まれる", () => {
    const 字 = 描いた字(入力欄の図("選んだ窓口"));
    expect(字.join("\n")).toContain("選んだ値は 受付済");
    expect(差し込まれていない名前(字)).toEqual([]);
  });

  it("同じ名前の状態より、入力欄の初期値が勝つ", () => {
    const d = 入力欄の図("選んだ窓口", "未選択");
    // 対照の前提 = 状態の初期値と入力欄の初期値が違う値を持っている
    expect(d.states.find((s) => s.id === "choice")?.initial).toBe("未選択");
    const 字 = 描いた字(d).join("\n");
    expect(字).toContain("選んだ値は 受付済");
    expect(字).not.toContain("選んだ値は 未選択");
  });

  it("見本帳の図の描いた字に、差し込まれていない名前が残っていない", () => {
    const { 件 } = 見本帳の図();
    expect(件.length, "図を 1 件も見つけていない (検査が空振りしている)").toBeGreaterThan(100);
    // 入力欄を持つ図が居ないと、差し込みの相手が居ないまま 0 件になる
    const 入力欄を持つ = 件.filter((r) => ((r.図 as { inputs?: unknown[] }).inputs?.length ?? 0) > 0);
    expect(入力欄を持つ.length, "入力欄を持つ図が 1 つも無い (検査が空振りしている)").toBeGreaterThan(0);
    // 収容対照 = `layout()` を通して描いていた間、ここだけが `{lastEvent}` を残していた
    const 受け取り分け = 件.find((r) => r.鍵 === "eventVariety");
    expect(受け取り分け, "eventVariety を見つけていない").toBeDefined();
    expect(描いた字(受け取り分け!.図)).toContain("まだ無し · 累計 0 回");
    const 残り = 件.flatMap((r) => 差し込まれていない名前(描いた字(r.図)).map((n) => `${r.鍵}: ${n}`));
    expect(残り).toEqual([]);
  });

  it("差し込まれていない名前を探す検査が、植え込んだ 1 件を見つける (植え込み対照)", () => {
    // どの状態にも入力欄にも無い名前は、描画側が差し込めずに残す
    expect(差し込まれていない名前(描いた字(土台("{missing} の窓口")))).toEqual(["{missing}"]);
  });
});

describe("矢印の札も判定に入る (#1888)", () => {
  /** 2 段目から光る矢印を持つ図。 描いた図は最初の段で光る矢印の札しか出さない */
  function 後から光る矢印(札: string, 添え書き: string): CdlDiagram {
    return diagram("後から光る矢印", { topic: "後から光る矢印の図" })
      .lane("左", { x: 0, width: 280, label: "申請" })
      .lane("右", { x: 600, width: 280, label: "受付" })
      .node("a", { lane: "左", stack: 0, kind: "actor", title: "申請者" })
      .node("b", { lane: "右", stack: 0, kind: "function", title: "受付係" })
      .edge("a", "b", { id: "e", label: 札, sub: 添え書き, tone: "accent", style: "dotted-flow" })
      .phase("p1", { duration: 1000, title: "1. 待つ", body: "申請者が待つ。" }, (p) => p.activate("a"))
      .phase("p2", { duration: 1000, title: "2. 送る", body: "申請を送る。" }, (p) =>
        p.activate("a", "b", "e"),
      )
      .build();
  }

  it("2 段目から光る矢印の札は、描いた図に出ない (対照の前提)", () => {
    // 描いた図が札を出すようになると、下の植え込み対照は data を足さなくても通る
    const 字 = 描いた字(後から光る矢印("Draft", "Alice")).join("\n");
    expect(字.length, "描いた字を 1 つも拾えていない (検査が空振りしている)").toBeGreaterThan(0);
    expect(字, "描いた図が 2 段目の矢印の札を出している (対照の前提が崩れた)").not.toContain("Draft");
    expect(字, "描いた図が 2 段目の矢印の添え書きを出している (対照の前提が崩れた)").not.toContain(
      "Alice",
    );
  });

  it("矢印の札と添え書きにだけ英語を置いた図から拾う (植え込み対照)", () => {
    expect(英語の残り("後から光る矢印", 後から光る矢印("Draft", "Alice"))).toEqual([
      "Draft (「Draft」)",
      "Alice (「Alice」)",
    ]);
  });

  it("矢印の札と添え書きが日本語なら拾わない (陰性対照)", () => {
    expect(英語の残り("後から光る矢印", 後から光る矢印("送る", "下書き"))).toEqual([]);
  });
});

describe("描いた字の控え (#1890)", () => {
  it("覚えた字と、描き直した字が一致する", () => {
    const 件 = 見本帳の図().件.slice(0, 30);
    expect(件.length, "図を 1 件も見つけていない (検査が空振りしている)").toBeGreaterThan(0);
    for (const r of 件) expect(描いた字(r.図), `${r.鍵} の覚えた字が描き直した字と違う`).toEqual(描き直した字(r.図));
  });

  it("id が同じでも別の図は別に描く (植え込み対照)", () => {
    // 土台の図は id がどれも `土台`。 id を鍵にして覚えると、後の図に前の図の字が返る
    const 甲 = 土台("申請の受付");
    const 乙 = 土台("審査の窓口");
    expect(甲.id).toBe(乙.id);
    expect(描いた字(甲).join("\n")).toContain("申請の受付");
    expect(描いた字(乙).join("\n")).toContain("審査の窓口");
    expect(描いた字(乙).join("\n")).not.toContain("申請の受付");
  });
});

describe("段の札も判定に入る (#1870)", () => {
  it("札にだけ英語を置いた図から拾う (植え込み対照)", () => {
    // 段の題と説明は日本語のまま。 札だけが英語の図を作り、判定が札を見ていることを確かめる
    const d = 土台("申請の受付");
    const 植えた = { ...d, phases: d.phases.map((p) => ({ ...p, badge: "Draft" })) };
    expect(英語の残り("土台", 植えた)).toEqual(["Draft (「Draft」)"]);
  });

  it("記法の綴りを札に置いても拾わない (対象外の対照)", () => {
    const d = 土台("申請の受付");
    expect(図の型.has("sequence"), "sequence が図の型の一覧に無い (対照が古い)").toBe(true);
    const 植えた = { ...d, phases: d.phases.map((p) => ({ ...p, badge: "sequence" })) };
    expect(英語の残り("土台", 植えた)).toEqual([]);
  });

  it("記法が札に付ける綴りが、どれかの図の札に実在する (死蔵の検査)", () => {
    // 札に 1 度も出ない綴りを一覧が持つと、同じ字が仮置きとして戻った日に黙って通る
    const 札 = 見本帳の札();
    expect(札.length, "札を 1 件も拾えていない (検査が空振りしている)").toBeGreaterThan(100);
    const 全部 = 札.join("\n");
    const 死蔵 = Object.keys(記法が札に付ける綴り).filter(
      (語) => !new RegExp(`(^|[^A-Za-z])${語}([^A-Za-z]|$)`).test(全部),
    );
    expect(死蔵, `札に実在しない綴りを一覧が持っている: ${死蔵.join(", ")}`).toEqual([]);
    // 植え込み対照 = 0 件を期待するので、探し方が本当に見つけるかを別に見る
    const 見つからない = ["ZzNoSuchBadge"].filter(
      (語) => !new RegExp(`(^|[^A-Za-z])${語}([^A-Za-z]|$)`).test(全部),
    );
    expect(見つからない, "死蔵の探し方が何も見つけない (検査が恒真)").toEqual(["ZzNoSuchBadge"]);
  });
});

describe("横棒で繋いだ記法の値を割らない (#1867)", () => {
  it("記法が知る塊を、断片に割って数えない", () => {
    // **塊だけを含む字を作って通す** = 他の語の影響を受けない。 同じ綴りが単独で別の図に
    // 出ている時の判定 (従来どおり数える / 通す) は、この検査の対象外
    const 塊たち = 記法が知る横棒繋ぎ(見本帳の字());
    expect(塊たち.length, "横棒繋ぎの塊が 1 件も無い (検査が空振りしている)").toBeGreaterThan(0);
    // 集めた塊が本当に横棒を含むこと。 塊の形が横棒を落とすと、記法が知る普通の語が
    // 塊として集まり、下の判定が素通りする (天井の検査だけが捕まえる形になる)
    for (const 塊 of 塊たち) {
      expect(塊, "横棒を含まない綴りが塊として集まっている").toContain("-");
    }
    console.log(`[横棒繋ぎ] 記法が知る塊 ${塊たち.length} 種 = ${塊たち.join(" ")}`);
    const 残る = 塊たち.filter((塊) => 英語の語([塊]).length > 0);
    expect(残る, "記法が知る塊が断片に割れて数えられている").toEqual([]);
  });

  it("記法が知らない横棒繋ぎは、従来どおり断片ごとに数える (植え込み対照)", () => {
    // 0 件を期待する上の検査が恒真でないことを、逆向きの入力で見る
    // 塊も断片も記法が知らない形を選ぶ (片方が記法の値だと、断片ごとに数えたことを示せない)
    for (const 語 of ["Alice-Bob", "Alice", "Bob"]) {
      expect(記法が知るか(語), `${語} を記法が知っている (対照の前提が崩れた)`).toBe(false);
    }
    expect(英語の語(["Alice-Bob"])).toEqual(["Alice (「Alice-Bob」)", "Bob (「Alice-Bob」)"]);
  });

  it("塊の一部だけが記法の値の形は、塊として飛ばさない", () => {
    // `user-alice` の全体を記法は知らない。 `user` が記法の値でも、
    // 塊ごと飛ばすと `alice` まで消える
    expect(記法が知るか("user-alice"), "user-alice を記法が知っている (対照の前提が崩れた)").toBe(
      false,
    );
    expect(英語の語(["user-alice"]).map((s) => s.slice(0, s.indexOf(" (")))).toContain("alice");
  });
});

describe("記法の綴りを開くべき英語に数えない (#1865)", () => {
  it("残してよい語が、記法の経路と重なっていない (死蔵の検査)", () => {
    // 経路が覆う語を手書きでも持つと、**経路がその語を落とした日に手書きの側が黙って覆う**。
    // 覆われている間は誰も赤を踏まないので、照合が壊れたことに気付けない
    const 語数 = [...記法の経路.values()].reduce((n, s) => n + s.size, 0);
    expect(語数, "記法の経路が 1 語も持っていない (検査が空振りしている)").toBeGreaterThan(100);
    const 重なり = 経路と重なる語(Object.keys(残してよい語));
    expect(
      重なり,
      `記法の経路が覆う語を手書きでも持っている。 一覧から外す: ${重なり.join(", ")}`,
    ).toEqual([]);
    // 植え込み対照 = 0 件を期待する検査なので、探し方が本当に見つけるかを別に見る。
    // 同じ `経路と重なる語()` に通す = 探し方を 2 度書かない
    expect(経路と重なる語(["fill"]), "重なりの探し方が何も見つけない (検査が恒真)").toEqual([
      "fill",
    ]);
  });

  it("4 経路それぞれが、図の字で守りを足している (経路の専有)", () => {
    // 抜いて何も落ちない経路は、図の字に対して守りを 1 つも足していない。
    // 探し方は本番と同じ `経路ごとの専有()` に通す = 2 度書かない
    const 綴り = 図に出る記法の綴り().map((w) => ({ 元: w, 綴り: w }));
    expect(綴り.length, "図の字に記法の綴りが 1 語も無い (照合が効いていない)").toBeGreaterThan(50);
    const 専有 = 経路ごとの専有(綴り, 記法の経路);
    console.log(
      `[図の字/経路の専有] 相手=${綴り.length} / ` +
        [...専有].map(([名, ws]) => `${名}=${ws.length}`).join(" / "),
    );
    expect([...専有.keys()].sort(), "経路の一覧が食い違っている").toEqual(
      [...記法の経路.keys()].sort(),
    );
    // **この相手では `記法が配る一覧` が専有 0 件** (#1906)。 最後の 1 語は見本の名札の
    // `Day events` で、仮置きの英語を開いた時に図の字から消えた。 経路が守りを足していることは
    // 説明文を相手にした検査 (`screen-words.test.ts` の `reveal`) が見る。
    // 相手ごとに専有が変わるのは画面の字の検査の `cdl が配る名前` と同じ形。
    // 0 件を固定しておくのは、記法の一覧だけが知る綴りが図の字に戻った日に、ここを直させるため
    expect(専有.get("記法が配る一覧"), "この相手では記法が配る一覧の専有が無いはず").toEqual([]);
    for (const [名, ws] of 専有) {
      if (名 === "記法が配る一覧") continue;
      expect(ws.length, `${名} が図の字に対して守りを 1 つも足していない`).toBeGreaterThan(0);
    }
  });
});

describe("拾い方の対照", () => {
  it("日本語だけの図からは何も拾わない", () => {
    expect(英語の残り("土台", 土台("申請の受付"))).toEqual([]);
  });

  it("記法の綴りを図に置いても拾わない (対象外の対照)", () => {
    // **対照の語を守りの実装から導かない** (#1863)。 導くと、照合を壊した瞬間に対照も
    // 一緒に壊れて恒真になる。 語を literal で持ち、記法が本当に知っていることを先に見る
    for (const 語 of ["fill", "readout"]) {
      expect(記法が知るか(語), `${語} を記法が知らない (対照が古い)`).toBe(true);
    }
    expect(英語の残り("土台", 土台("fill の進み"))).toEqual([]);
    expect(英語の残り("土台", 土台("readout の値"))).toEqual([]);
  });

  it("箱の題に置いた英語の語を拾う", () => {
    expect(英語の残り("土台", 土台("Submit request"))).toEqual([
      "Submit (「Submit request」)",
      "request (「Submit request」)",
    ]);
  });

  it("段の説明に置いた英語の語を拾う", () => {
    const d = 土台("申請の受付");
    const 植えた = { ...d, phases: d.phases.map((p) => ({ ...p, body: "申請を review する。" })) };
    expect(英語の残り("土台", 植えた)).toEqual(["review (「申請を review する。」)"]);
  });

  it("残してよい語は拾わない", () => {
    expect(英語の残り("土台", 土台("API の受付"))).toEqual([]);
  });
});
