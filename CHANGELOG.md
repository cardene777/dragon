# Changelog

dragon DSL の主要変更履歴。
[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) format + [Semantic Versioning](https://semver.org/lang/ja/) 準拠。

## [Unreleased]

### Added

- **入口と出口を名前で選べる部品を 6 枚足した** (#2129)

  部品 80 枚のうち、矢印の繋ぎ先を名前で選べるのは要素を 2 つ以上持つ 18 枚だけだった。
  その 18 枚は「並べて見せる」 部品 (ゲージ 3 連 / 星 5 つ / 等化器 5 本) が大半で、
  入口と出口の区別を持つ部品は 1 枚も無かった。 繋ぎ方そのものを表す部品を 6 枚足した。

  | 部品 | 入口 | 出口 | 何を見せるか |
  |---|---|---|---|
  | 振り分け器 | `inP` | `outA` / `outB` | 1 本の流れを 2 つへ分け、分けた割合を両方の棒で見せる |
  | 合流点 | `inA` / `inB` | `outP` | 2 本の流れを 1 つへ集め、合計が出口の棒に出る |
  | 待ち行列の深さ | `qIn` | `qOut` | 入る量と出る量の差が中央の `qBody` に溜まる |
  | 弁の開き | `vIn` | `vOut` | 中央の `vGate` の開度で出口の量が決まる |
  | 三段の漏斗 | `fTop` | `fBot` | 段ごとに箱の幅と残る割合が減る |
  | 小さな網 | `nw1` | `nw4` | 起点から終点まで上下 2 つの経路が並ぶ |

  宿主の図からは、矢印に繋ぎ先の名前を書いて選ぶ。

  ```yaml
  actors:
    - 受注: { kind: card }
    - router: { kind: split-router, phase: false }

  flow:
    - 受注 -> router: "届く" { toPartNode: inP }
  ```

  **要素の名前は英字で書く**。 宿主の段が `{部品の名前}__{値の名前}` で中の値を動かすとき、
  値の名前は英字と `_` しか受け付けない (#2125)。 繋ぐ時と動かす時で書き方が割れないよう、
  要素の名前も同じ綴りにした。

- **カタログに、部品を繋いだまま動かす頁を足した** (#2125) (#2126)

  部品は 80 枚あってどれも自分の段で動き、部品どうしを矢印で繋ぐ仕組みも揃っていた。
  それでも「繋いだまま動く」 見本は 1 枚も無く、繋ぎ方を見せる 6 枚は段を 1 つも持たず、
  動く 80 枚は矢印を 1 本も持たなかった。 読む人は「置く」 と「動く」 を別々に見るだけで、
  繋いだ先へ値が渡っていく絵をどこでも見られなかった。

  部品の頁の後ろに「部品を繋いで動かす」 の行を足し、切替を 5 つ置いた。

  | 切替 | 見せること |
  |---|---|
  | 流れに沿って動かす | 受け入れ → 槽 → 送り出しの 3 部品を繋ぎ、段ごとに受け手の値を動かす |
  | 要素ごとに繋ぐ | 3 層の部品の上層と底層を名指しして繋ぎ、その層の値だけを動かす |
  | 分ける | 1 つの箱から 2 つの部品へ分け、1 つの段で 2 つの値を別々に動かす |
  | 集める | 2 つの部品から 1 つの箱へ集め、集める段で 2 本の矢印を同時に光らせる |
  | 部品の段を残す | 部品の段のままの部品と、宿主が動かす部品を並べて見比べる |

  宿主の段は `{部品の名前}__{値の名前}` を書いて部品の中の値を動かす。

  ```yaml
  actors:
    - tank: { kind: wave-gauge, phase: false }

  animation:
    - step: "溜まる" 1.2s
      tween:
        tank__lv: 0 -> 72
  ```

  **部品の名前は英字で書く**。 値の名前は英字と `_` しか受け付けないため、日本語の名前を
  付けた部品は繋ぐことはできても宿主の段から中の値を動かせない。 繋ぐだけの箱は日本語のままでよい。

  **分けると集めるは 1 枚にまとめない**。 4 つの箱で「1 → 2 → 1」 を 1 枚に書くと縦列が
  4 本になり、図が 2634 × 780 になって一覧の器では箱の題が 9.6px まで縮む (下限 12px)。
  3 本ずつの 2 枚に割ると、どちらも図形の検査の指摘が 0 件で収まる。

- **見本の段を使うかどうかを、JSON でも箱の欄として書けるようにした** (#2125) (#2126)

  記法は中括弧に書いた名前をすべて状態の上書きへ入れるため、`phase: false` がそのまま
  組み立てへ届いていた。 JSON は欄ごとに型を宣言する形なので、同じ名前を欄として持たず、
  書くと「知らない項目です (`shape` のことですか)」 で弾かれていた。

  ```json
  { "name": "tank", "kind": "wave-gauge", "phase": false }
  ```

  届く先は変えていない。 欄で受けたものを状態の上書きの `phase` へ入れ直すので、
  これまでどおり `"state": { "phase": false }` と書いた形とも同じ図になる。

  普通の箱に書くと誤りとして返す (`state` / `scale` と同じ向き)。 普通の箱は自分の段を
  持たないため、通すと「書いたのに図が変わらない」 だけになる。

- **位置を他の要素からの相対で書く形を、JSON でも書けるようにした** (#2039)

  記法には座標の代わりに「誰の」「どちら側に」「どれだけ離して」 を書く形がある。

  ```yaml
  actors:
    - 受付: { kind: card }
    - 設備の稼働:
        kind: state-indicator
        位置: 受付 の右 200
  ```

  これまで JSON では同じことを書けなかった。 `posRel` は読み取り側に 1 件も無く、
  受け付ける欄の表にも載っていなかったため、書いても「知らない項目です」 で弾かれていた。

  JSON では 1 本の文字列ではなく 3 つの欄に分ける。

  ```json
  {
    "name": "設備の稼働",
    "kind": "state-indicator",
    "posRel": { "anchor": "受付", "dir": "right", "gap": 200 }
  }
  ```

  | 欄 | 書くもの | 書かなかった時 |
  |---|---|---|
  | `anchor` | 基準にする相手の名前 | 誤りとして返る |
  | `dir` | `right` / `left` / `above` / `below` | 誤りとして返る |
  | `gap` | 相手の縁との間隔 | 160 で置く |

  文字列を組み立てて渡す形にしなかったのは、JSON を書くのが主に LLM だから。
  記法の並び順と区切りを覚えさせるより、欄の名前で読み取りの規則を示す方が短い。

  解けない書き方 (相手が居ない / 自分を基準にした / 基準が輪になっている) は、
  どちらの入口でも誤りとして返る。 判定は 1 箇所に置き、誤りの文だけを入口ごとに作る
  (記法は行番号を、JSON は欄の場所を添える)。

- **カタログに、位置を相対で書く見本を足した** (#2039)

  JSON で書けなかったことが理由で、相対で置く形はカタログに 1 枚も載っていなかった。
  カタログは見本ごとに本文と JSON を対で持ち、同じ図に解決されることを見る決まりのためで、
  対を作れない書き方は載せられない。

  | 頁 | 足した見本 | 形 |
  |---|---|---|
  | 書き方 | 位置を相対で書く (切替 5 つ) | 右 / 左 / 上 / 下 と、間隔を書かない形 |
  | 部品を箱に使う | 部品を基準にする | 部品の右に箱、その右に別の部品を置く |

  書き方の頁では向きを 4 つとも見せる。 1 つだけ載せると、記法が受ける残り 3 つが画面のどこにも出ない。

  部品の頁では、基準にされる部品と、基準にして置く部品の両方を 1 枚に入れる。
  前者は部品の箱の範囲が、後者は部品の大きさが要る = 別の宣言が読むため、
  片方だけだともう片方が読まれていなくても絵が変わらない。

  基準にする部品は位置を書かない。 基準にできるのは範囲が決まっている部品だけで、相対で書いた部品は
  組み立ての段階でまだ座標が決まっておらず、範囲を出す側が「後で埋める」 として飛ばす。
  位置を書かない部品は格子に置かれるので範囲が決まり、それを基準にできる。

  この見本を足してカタログは 512 枚、「部品を箱に使う」 の切替は 14 つになる。

### Fixed

- **段で光らせた暗い地の箱 (`service`) の字が面と同じ色になり、読めなかった不具合を直した** (#2137)

  `service` の箱は墨のベタに紙の色の字で描く。 段で光らせた箱は地を橙の面に替えるが、
  その規則が暗い地の箱の地も替え、字は紙の色のまま残していた。 **光った間だけ、題も説明も消えていた**。

  | 明暗 | 光った箱の面 | 題の字 (直す前) | 対比 (直す前) |
  |---|---|---|---|
  | 暗 | `rgb(61, 33, 22)` | `rgb(47, 41, 32)` | 1.02 |

  光る段を持つ `service` の箱は、カタログ 524 枚のうち 9 枚にあった (プリセットの流れ図 / 網の図 / 基盤の図 / 構成の図 / C4 の見本など)。

  光った暗い地の箱の字を、他の光った箱と同じ墨の字に塗り直した (題は `--d-text-primary`、説明は `--d-text-secondary`)。
  面は他の光った箱と同じ橙の面のままにし、光っていない暗い地の箱は変えていない。

- **C4 の図の見本で、箱が 1 列に縦積みになり段が分かれていなかった形を直した** (#2135)

  C4 の図 (`type: c4`) は、箱の説明の先頭に書いた目印で箱を 3 つの段に分ける。

  | 目印 | 段 |
  |---|---|
  | `L1` | 全体の見取り図 |
  | `L2` | 動かす単位 |
  | `L3` | 部品 |

  カタログの見本「C4 の系統図」 は目印を 1 つも書いておらず、目印の無い箱は全て `L1` に入るため、
  **4 つの箱が「全体の見取り図」 の 1 列に縦積みになっていた**。 段で粒度を分ける C4 の図の特徴が見本に出ていなかった。

  見本を「ネット注文の C4 系統図」 に書き直し、3 段に 2-3 箱ずつ置いた。 上の段の箱から下の段の箱へ
  「中を開く」 矢印を引き、段 (`animation`) は 全体 → 中を開く → 動かす単位の間 → 部品まで開く の順に進む。

  ```yaml
  actors:
    - 購入者: { kind: person, subtitle: "L1 画面から注文する人" }
    - 注文の仕組み: { kind: service, subtitle: "L1 注文を受けて在庫を引き当てる系統" }
    - 画面: { kind: service, subtitle: "L2 閲覧ソフトで動く" }
    - 注文の受け口: { kind: api, subtitle: "L2 要求を受けて処理する" }
    - 注文の記録: { kind: database, subtitle: "L2 注文と在庫を保つ" }
    - 認証部品: { kind: service, subtitle: "L3 鍵を確かめる" }
    - 在庫部品: { kind: service, subtitle: "L3 在庫を引き当てる" }
  ```

  目印は説明の先頭に書き、画面の説明には目印を除いた字が出る。

- **順序図の板が段を進めても最初の言づてで止まる不具合を直した** (#2133)

  順序図 (`type: sequence` / `type: solidity`) は 1 枚の板に言づてを並べ、段ごとに 1 通を強調する。
  どの言づてかを段の `focus:` から決める時、読めるのが矢印 (`A -> B`) だけだった。
  **箱の名前で書いた段は全て 0 番になり、板が 1 通目のまま動かなかった**。 同じ 2 者の矢印が
  続く図では、最初の段で最後の言づてまで飛んでいた。

  | カタログの板 30 件 | 直す前 | 直した後 |
  |---|---|---|
  | 全ての段が 1 通目のまま | 26 件 | 0 件 |
  | 最初の段で先へ飛ぶ | 3 件 | 0 件 |
  | 段ごとに進む | 1 件 | 30 件 |

  言づてを先頭から段の数に切り分け、各段がその区間で `focus:` に合う最後の言づてを強調する形にした。
  書き方ごとの合い方と切り分けの決まりは README の「順序図の段は `focus:` で強調する言づてを選ぶ」 にある。

  ```yaml
  animation:
    - step: "照合" 1.5s
      focus: [利用者, 認証窓口, DB]   # 直す前は 0 番、直した後は「照合」
    - step: "発行" 1.0s
      focus: [認証窓口, 利用者]       # 直す前は 0 番、直した後は「JWT を発行」
  ```

  README の「最小の例」 もこの書き方で、写した図が進まなかった。 直した後は 1 通ずつ進む。

  段の並びと言づての並びが食い違っていた見本 5 件も直した。

  | 見本 | 直す前に強調していた言づて | 直した形 |
  |---|---|---|
  | CSRF の鍵 | 「発行」 の段で `GET 入力欄` | 段を言づての順 (保管 → 発行 → 送信 → 照合 → 受理) に並べ替えた |
  | 読み込み時の一時置き場 | 「無い」 の段で `行` | 段を 3 つから 5 つに分けた |
  | 入力欄の送信 | 「埋める」 の段で `送信` | 「押す」 の段を足した |
  | OAuth の認可 | 「同意の確認」 の段で `許可` | 「許可」 の段を足した |
  | 投票の契約 (編集画面) | 最後の言づて `結果` を描かずに終わる | 「結果」 の段を足した |
- **契約の図の見本で、縦線が題と逆の順に並んでいた形を直した** (#2131)

  契約の図 (`type: solidity`) は、箱の種別で縦線を 人 → 契約 → 保存 → 出来事 の順に並べ替える。
  カタログの見本「ERC-20 の送金 (種別ごとに縦列が並ぶ)」 は種別を 1 つも書いておらず、
  全ての箱が `actor` として扱われて並べ替えが働かなかった。 見本はわざと逆順で書いてあるため、
  **図は出来事から始まり、題と説明が言う並びと逆になっていた**。

  | | 縦線の並び |
  |---|---|
  | 直す前 | Transfer / 残高表 / トークン契約 / 利用者 |
  | 直した後 | 利用者 / トークン契約 / 残高表 / Transfer |

  4 つの箱に種別 (`event` / `storage` / `contract` / `eoa`) を書いた。 書く順は逆順のまま残し、
  並べ替えが働いていることが見本そのもので分かる形を保った。

  ```yaml
  actors:
    - Transfer: { kind: event }
    - 残高表: { kind: storage }
    - トークン契約: { kind: contract }
    - 利用者: { kind: eoa }
  ```

  **種別を書かない箱は人 (`actor`) として左端に並ぶ**。 全ての箱で書かないと全てが同じ順位になり、
  書いた順のまま並ぶ。

- **編集画面の YAML 欄に出る注意が、どの行の話か分からない形を直した** (#2117)

  YAML 欄は本文を素の値に変えてから組み立てる。 素の値は書いた場所を持たないため、この経路の注意は全て行番号を
  持たず、画面は `L{行}` を添えられなかった。 同じ図を本文欄と YAML 欄で読ませると、本文欄は `L5` / `L7` / `L9` を
  指し、YAML 欄は 3 件とも行番号なしだった。

  YAML を読む時に、同じ本文を事象の並びとしてもう一度読んで「書いた場所 → 行」 の表を作り、組み立てに渡す。
  場所の鍵は JSON Pointer と同じ形 (`/actors/1`) で、表を作る側と引く側が同じ関数 (`書いた場所の鍵`) を呼ぶ。

  | 書いた場所 | 直す前 | 直した後 |
  |---|---|---|
  | `actors` / `flow` / `values` など 14 種の欄 | 全て `L0` (画面は行を出さない) | 書いた行 |
  | 表を渡さない呼出 (LLM が作った JSON を直に渡す経路) | `L0` | 変わらない (行が分からないことを 0 で表す) |

  図は変わらない (行は注意にだけ載る)。 別名の取り込み (`<<: *ref`) と、鍵が入れ子になった形の下は場所を作らず、
  その欄だけ行なしに戻る。

  行を持つようになったことで、同じ行に注意を 2 件並べない判定 (#2111) が矢印ごとに効くようになった。
  値の図 (`pie` 等) に多重度を書いた 2 本目の矢印の注意は、これまで 1 本目の注意と同じ行 (`L0`) とみなされて
  出ていなかった。

- **部品の名前や箱の種類を書き間違えても、編集画面に注意が出ない形を直した** (#2113)

  種類 (`kind`) に箱の種類に無い名前を書くと、記法はその名前を部品の名前とみなす。 部品の一覧 (`partsCatalog`) にも
  無ければ種類を書かなかった箱として描き、`console.warn` に英語の 1 文を出すだけで、知らせ (`onNotice`) は出していなかった。
  編集画面は知らせだけを画面に出すため、`evnet` のような 1 字の書き間違いでも、ただの箱が描かれて理由が見えなかった。

  | 組み立て方 | 直す前 | 直した後 |
  |---|---|---|
  | 部品を持つ一覧を渡し、名前が一覧に無い | `console.warn` だけ | 新しい種類 `part-not-found` を書いた行で知らせ、箱の種類と部品の名前から綴りの近い名前を案内する (`evnet` なら `event`)。 同じ文を `console.warn` にも出す |
  | 一覧を渡さない / 部品を 1 つも持たない一覧 | `console.warn` だけ | 変わらない (比べる部品の名前が無く、部品か書き間違いかを決められない)。 `console.warn` の文を日本語にした |

  編集画面は部品を読み込み終わる前に空の一覧を渡して組み立てるため、読み込み中に正しい部品の名前へ注意は出ない。
  図は変わらない (一覧に無い名前は、これまでどおり種類を書かなかった箱になる)。

  あわせて、編集画面の本文欄で知らせの行番号がずれる形を直した。 本文欄は部品の行を抜いた本文を組み立てるため、
  部品の行より後ろの行の知らせが 1 行ずつ手前を指していた (11 行目の知らせが `L10`)。 矢印の行と同じ表で元の本文の行へ戻す。
  JSON の知らない項目名で綴りの近い名前を探していた関数は、部品の名前と共有する file (`near-name.ts`) へ移した。

- **工程表 (`type: gantt`) で効かない書き方の注意が、編集画面に出ない形を直した** (#2111)

  工程表の組み立ては、効かない形を `console.warn` にだけ出し、知らせ (`onNotice`) を 1 件も出していなかった。
  編集画面は知らせだけを画面に出すため、書いている人には理由が見えなかった。
  値の図 (`pie` 等) と同じく、知らせと `console.warn` の両方へ同じ文を出し、知らせは書いた行を指す。

  | 書いた形 | 知らせの種類 | 指す行 |
  |---|---|---|
  | 時期を書かない項目 | `chart-value-unreadable` (値の図と同じ) | 最初の項目の行 |
  | 時期の無い項目へ引いた矢印 | `chart-edge-dropped` (値の図と同じ)。 文は矢印ごとに 1 件 | その矢印の行 |
  | 文字 / 色 / 線種 / 多重度を書いた矢印 | 新しい種類 `edge-option-not-honored`。 書いた飾りを 1 件に並べる | その矢印の行 |
  | 終わりが始まりより前 / 状態で決まる終わりが始まりより前の値を取る | 新しい種類 `gantt-end-before-start` | その項目の行 |

  `actors` に無い名前へ引いた矢印は、全ての図種に共通の知らせ (`flow-actor-missing`) が書いた行で伝えているため、
  工程表の組み立ては伝えなくなった (`console.warn` の「依存を結べない矢印があります」 も出ない)。

  カタログの「四半期の計画を書く例」 は矢印に描かれない文字 (`"前提"`) を書いており、今回の知らせで見本の検査が
  落ちた。 見本から文字を外した (工程表は矢印の文字を描かないため、組み立てた図は外す前と同じ)。

  あわせて、#2107 の多重度の知らせが同じ行に 2 件目を足す形を 3 つ直した。

  | 形 | 直す前の同じ行の知らせ | 直した後 |
  |---|---|---|
  | 木で自分を親にする矢印に多重度 | 木の知らせ + 多重度の知らせ | 木の知らせだけ |
  | 工程表 / 木で `actors` に無い名前へ引いた矢印に多重度 | 居ない名前の知らせ + 多重度の知らせ | 居ない名前の知らせだけ |
  | 順序図の板で `actors` に無い名前へ引いた言づてに飾り | 居ない名前の知らせ + 板の飾りの知らせ | 居ない名前の知らせだけ |

  多重度の知らせは、図種の組み立てが知らせた行を種類を問わず避け、`actors` に無い名前を指す矢印も避ける。
  行を持たない矢印 (JSON から読んだ文書) は全て 0 行になるため、図種の組み立てが 0 行を指して知らせると、
  その文書の多重度の知らせは出ない (矢印を見分けられず、同じ矢印に 2 件出さない側に倒す)。

- **矢印の多重度を、端の形にならない語や図種に書いても知らせが出ない形を直した** (#2107)

  矢印の多重度 (`cardinality`) で両端の形が決まるのは、ER 図に 6 語 (`1:1` / `1:N` / `N:1` / `N:M` / `0..1` / `1..*`) を
  書いた時だけ。 それ以外の形は図に何も足さないか名前に `(語)` と添えるだけで、知らせ (`onNotice`) も出ず、
  書いた人は端の形が付くと思い込んでいた (`N:M` の代わりに `N:N` と書く形で起きる)。

  | 書いた形 | 直す前 | 直した後 |
  |---|---|---|
  | ER 図に 6 語以外 (`N:N` / `2..5`) | 名前に `(N:N)` が付くだけ | 図は変えず、描かない端と端の形が決まる 6 語を、書いた行で知らせる (`cardinality-not-honored`) |
  | ER 図に 6 語以外 + `tailHead` と `head` を両方 | 名前に添え、端は書いた形 | 変わらない (描かない端が無いので知らせない) |
  | ER 図以外の図種 | 何も描かない | 多重度を描かないことを書いた行で知らせる。 クラス図は `sub` / `tailSub` に書く形を案内する |
  | 順序図の板 (`sequence` / `solidity`) | 何も描かない | 色味 / 添え字 / 寄せを知らせる既存の知らせに「多重度」 を足す |

  同じ行に知らせを 2 件並べない。 値の図 (`pie` 等) のように矢印を捨てたと既に知らせている行には足さない。
  値の図は捨てた本数を最初の矢印の行でまとめて伝えるため、2 本目以降の矢印に書いた多重度はその行で知らせる。

  あわせて、`compile.ts` に残っていた古い説明 (ER の多重度を名前へ `(1:N)` と足す) を今の挙動に直し、
  説明する関数が別のファイルに移った後も残っていたコメントを、その関数の隣へ移した。

- **ER 図の多重度が、書いた語と違う端の形や字で描かれる形を直した** (#2105)

  ER 図の関係には多重度の語 (`1:1` / `1:N` / `N:1` / `N:M` / `0..1` / `1..*`) を書け、
  語ごとに両端の形が決まる。 記法では、書き方と段の有無で結果が割れていた。

  | 書き方 | 直す前 | 直した後 |
  |---|---|---|
  | 欄 `cardinality: "1:1"` (段なし) | 名前が `注文する (1:1)`、名前の下に `1:N`、端は `1:N` の形 | 名前が `注文する`、名前の下に `1:1`、端は `1:1` の形 |
  | 欄か名前 (段あり) | 名前に `(1:1)` が付くだけで、端の形が付かない | 段なしと同じ |
  | 語を書かない (段なし) | 書いていない `1:N` が名前の下と端に出る | 何も出さない |
  | 端の形を明示 (段なし) | 明示した端の横に、書いていない `1:N` が名前の下に出る | 明示した端だけが出る |

  26 通りを測ると 18 通りで食い違っていた。 直した後の形は、組み立て API の `er().relation` に
  同じ語を渡した時と同じで、検査が全ての組で突き合わせる。

  語を書かない段なしの図から `1:N` が消えるのは挙動の変更になる。 組み立て API と段を持つ図は
  元から何も出しておらず、そちらに揃えた。 端の形が要る関係は、語か `tailHead` / `head` を書く。

  6 語以外 (`N:N` 等) を欄に書くと、名前に `(N:N)` と添えるだけで端の形は決まらない。

- **カタログのテキストDSLの ER 図に、多重度を全て並べる切替を足した** (#2105)

  元の見本は `1:N` の 1 語しか書いておらず、残る 5 語の端の形をカタログで見られなかった。
  通販の 7 つの表を 2 列に置き、6 本の関係に 6 語を 1 つずつ当てる。
  段は 1 本ずつ関係を光らせ、札に語を出す。

  描画側が語の一覧を公開している語 (多重度 `ER_CARDINALITY_HEAD` / 状態遷移の行頭の印
  `FSM_ACTION_MARK`) は、一覧から導いてカタログの記法が全て書いているかを数えるようにした。
  自由な文字列の欄に書く語なので、型定義を歩く網羅の検査では数えていなかった。

  この切替を足してカタログは 513 枚になる。

- **編集画面の部品の一覧で、押して使う button に掴む形の cursor が出る形を直した** (#2072)

  部品の一覧の button は、押すと本文の `actors:` に 1 行足す。
  それなのに指すと掴む形 (`grab`) の cursor が出て、押している間は `grabbing` になり、引きずって置く操作に見えていた。
  引きずって置く操作は #923 で外れており、cursor の規則だけが残っていた。

  ```
  直す前 ... 見本の button: pointer / 部品の button: grab (押している間 grabbing)
  直した後 ... 見本の button: pointer / 部品の button: pointer (押している間 pointer)
  ```

  舞台に描いた図の箱 ・ 線 ・ 区画に付いていた掴む形の規則も外した。
  図の要素を掴む操作は無く、掴む形の cursor は舞台の平行移動の側が持つ。
  この規則があると、箱の上から押して舞台を動かしている間だけ cursor が `grab` のまま残り、
  空いた所から押した時の `grabbing` と食い違っていた。
  舞台の平行移動と拡大縮小、`relations: hover` の図で箱を押した時の選択は変わらない。

- **段の型の説明が、隣の帯の型に付いていた形を直した** (#2066)

  編集器で型を見ると、帯の型 (`DslBand`) に段の説明が重なり、段の型 (`DslPhase`) には説明が出なかった。

  ```ts
  // 直す前 (配る型の宣言)
  /** ステップ (phase) */
  /** 動いている間の帯 (#1466)。 順序図で、面がいつ動いているかを段の番号で持つ */
  type DslBand = { ... };
  type DslPhase = { ... };
  ```

  帯の型を足した時に、段の型の説明とその型の間へ差し込んでいた。
  説明を段の型の直前へ戻した。 型の中身は変えていない。

  同じ形 (説明の塊の上に、空行を挟まずに別の説明の塊が積まれる) は、記法の package、
  編集画面のコード、検査のコードの 3 か所の合計で 37 か所あった。
  配る宣言では、種類の一覧 (`NODE_KIND_VALID`) にも説明が 2 つ重なっていたので 1 つにまとめた。
  他の箇所も説明を持ち主の宣言の直前へ戻した。
  持ち主が既に消えていた説明は消し、実装が変わって古くなっていた記述は今の実装に合わせて直した。
  積み重なった形が戻ったら単体検査が落ちる。

- **ひな形を移った直後、倍率の欄に前の図の百分率が残る形を直した** (#2044)

  詳細画面で次のひな形へ移ると、図は新しい倍率で描かれるのに、欄だけが前の図の値を出していた。

  ```
  直す前 ... flow: 図 200% / 欄 60% (1 フレーム後に 200%。 負荷が高いと 500ms 以上残る)
  直した後 ... flow: 図 200% / 欄 200% (同じフレームで替わる)
  ```

  欄が前の図の値を持っている間は、「上げる」 と「下げる」 も前の図の倍率を起点にする。
  60% の図の次に開いた 200% の図で「上げる」 を押すと、75% へ縮んでいた。

  engine は図が替わっても同じ svg の中身を書き換えるため、倍率の見張り (大きさの変化) は
  次のフレームでしか受け取れない。 図の鍵が替わった描画の中 (画面に出す前) で 1 度測り直すようにした。
  図がまだ無ければ測れていない表示 (`--%`) にして、前の図の値を別の図の欄に残さない。
  カタログの並べて見る側と拡大表示も同じ部品を使うので、同じく直る。

  この形は e2e 検査が全件を回した時にだけ時々落ちる形で見えていた。 検査は固定で 500ms 待ってから
  欄を読んでいたので、1 フレームの遅れはほぼ見逃し、負荷が高い時だけ食い違いを拾っていた。
  検査は「図が前の図から替わるまで待ち、替わった時点の図と欄を 1 度で読んで待たずに比べる」 形にした。
  直しを外した版では 4 回とも落ち、直した版では 16 本同時 × 16 回を 3 度回して 0 件だった。

- **格子に置いた部品を基準にすると、置いた箱の縦位置が揃わない形を直した** (#2041)

  `位置: 設備の稼働 の右 200` のように部品を基準にして箱を置くと、横の間隔は書いたとおりに空くのに
  縦だけが数百ずれていた。

  ```
  直す前 ... 設備の稼働 cy=1118 / 受付 cy=654   (464 離れる)
  直した後 ... 設備の稼働 cy=490 / 受付 cy=490  (0)
  ```

  原因は格子の起点にある。 位置を書かない部品は本体の図の下端から下に並ぶが、その「本体」 に
  **相対で置く箱も入っていた**。 箱の位置が決まるのは格子を決めた後なので、基準の範囲を出す時
  (箱は自動配置の位置) と、部品を取り込む時 (箱は相対で解いた位置) で起点が動いていた。

  格子の起点を数える時に、相対で書いた箱を外した。 座標で書いた箱は引き続き数える
  (こちらは格子より先に位置が決まるため動かない。 外すと座標で下に置いた箱に部品が重なる)。

  **知らせは 1 件も出ていなかった**。 効いたかを見る側も範囲を出す側と同じ値を期待値にするため、
  実物と期待値が同じ向きにずれて一致していた。 組み立ての検査 34,302 件と絵の検査 512 枚が
  全部通った状態で、絵だけが階段状に崩れていた。

  #2039 で足したカタログの見本はこのずれを避けるため基準に座標を書いていた。 座標を外して、
  位置を 1 つも書かない形に戻した。 図が 1649 × 684 から 1489 × 774 になり、一覧に並べた枠でも
  箱の題が下限に届く (11.7px から 12.9px)。

- **編集画面で、部品を基準にして位置を書いた箱があると図が出なくなる形を塞いだ** (#2039)

  編集画面は部品を本文から抜いて別に重ねることがある。
  抜いた後に残った箱がその部品を基準にしていると、基準の名前が本文から消えて
  「位置の基準が見つかりません」 で組み立てが止まる。

  部品へ矢印を引いた時は既に抜かない判定があったので、位置の基準にも同じ判定を足した。

## [0.28.0] - 2026-09-16

### Added

- **カタログに、2 つの部品から同じ箱へ矢印を集める見本を足した** (#2011)

  #2010 で「部品を 2 つ置いて矢印を引く」 見本を足した時、合流する形だけは足せなかった。
  縦列を 1 本飛ばす矢印が間の部品を貫いていた
  (部品は高さ 380-400 で、普通の箱 (68) より深い位置に置かれる)。

  描画エンジン `@cardenelabs/cdl` の依存を `^0.66.0` に上げて直ったので、見本を 1 枚足す。

  | 足した見本 | 形 |
  |---|---|
  | 成形機と塗装機の稼働を、どちらも記録へ集める | 箱 → `state-indicator` 2 つ → 箱 (2 本の矢印が同じ箱に入る) |

  成形機から記録へ引いた矢印を、上げる前と上げた後で測った
  (間の 塗装機 は上げる前が 左 1536 / 右 1896、 上げた後が 左 1636 / 右 1996)。

  ```
  上げる前 ... M 1138 506 L 1996 506 Q 2010 506, 2010 492 L 2010 150 Q 2010 136, 2024 136 L 2314 136
  上げた後 ... M 1138 506 L 1508 506 Q 1522 506, 1522 492 L 1522 150 Q 1522 136, 1536 136 L 2314 136
  ```

  上げる前は `y = 506` の横の走りが `x = 1138` から `1996` まで伸び、その間の 塗装機 を横切っていた。
  絵の検査の `edge-node-cross` が 2 件出る形。 上げた後は折れる位置が 2 つの部品の間まで退き、0 件になる。

  同じ並びと同じ矢印を普通の箱だけで書いた図は、上げる前も後も 0 件のまま。
  部品だけの特例ではなく、深い位置に置かれた箱に共通する形を直している。

  この見本を足してカタログは 506 枚、「部品を箱に使う」 の切替は 13 つになる。

- **カタログに、高さの違う部品どうしを繋ぐ見本を足した** (#2012)

  #2010 で「部品どうしを矢印で繋ぐ」 見本を足した時、高さの違う部品どうしの形だけは足せなかった。
  繋ぎ口の高さが 10 ずれ、その縦の走りが角の丸み (14) の 2 倍より短いため、
  角が行き過ぎて線が戻っていた (描画エンジンが丸みを走りの長さで頭打ちにしていなかった)。

  描画エンジン `@cardenelabs/cdl` の依存を `^0.65.0` に上げて直ったので、見本を 1 枚足す。

  | 足した見本 | 形 |
  |---|---|
  | 成形機の稼働から乾燥炉の温度へ、高さの違う部品どうしを矢印で繋ぐ | 箱 → `state-indicator` (高さ 380) → `thermometer` (高さ 400) |

  実測した道筋。 縦の走り (`1267 511` → `1267 511`) が繋ぎ口の向き (506 → 516) と逆を向かない。

  ```
  M 1068 506 L 1262 506 Q 1267 506, 1267 511 L 1267 511 Q 1267 516, 1272 516 L 1466 516
  ```

  上げた版の中身は角の丸みの頭打ち 1 件だけで、丸みが足りている対の道筋は 1 文字も変わらない。
  足した 1 枚を含むカタログ 505 枚 (辺 472 本のうち測れた 468 本 / 角 280 個) を角の行き過ぎの検査に通し、指摘 0 件を確かめた。

- **カタログに、部品を 2 つ置いて矢印を引く見本を足した** (#2010)

  部品に矢印を引く見本は 2 枚あったが、どちらも部品が 1 つで、形も同じ「箱 → 部品 → 箱」 だった。
  1 つの箱から 2 つの部品へ分ける形と、部品から部品へ繋ぐ形は 1 枚も無く、書けるのに書き方が見えなかった。

  | 足した見本 | 形 |
  |---|---|
  | 検査の結果を設備の稼働と炉の温度へ分けて送る | 箱 1 つ → 部品 2 つ (繋ぎ先が 2 つとも部品) |
  | 成形機の稼働から塗装機の稼働へ、部品どうしを矢印で繋ぐ | 箱 → 部品 → 部品 (1 本目の繋ぎ先が 2 本目の繋ぎ元) |

  分ける方は種類の違う部品 (`state-indicator` と `thermometer`) を並べ、大きさの違う部品にも同じ書き方で繋がることを見せる。

  2 つの部品から同じ箱へ矢印を集める形は足していない。
  矢印が間の縦列の部品を貫くため (#2011)。
  高さの違う部品どうしを横に繋ぐ形も、この時点では足していない。
  縦の走りが角の丸みより短く、線が行き過ぎて戻っていた (#2012 で描画エンジンが直り、同じ節の上で足した)。

- **記法の JSON の型定義に在る欄と値を全てカタログで見せるようにし、抜けたら検査で落ちるようにした** (#1969)

  #1966 の検査は描画側の図の欄を読むため、記法にしか無い欄は図に写ると別の形になって数え落としていた。
  記法の JSON の型定義 (`diagramJsonSchema`) を歩いて欄の道と列挙値を集め、カタログの JSON と突き合わせると、型定義の道 552 個のうち 64 個に見本が無かった。

  | 足した見本 (分類) | 見せる欄と値 |
  |---|---|
  | 縦列の縦の点線 (部品) | 縦列の `lifeline` を引かない / 引く |
  | 図全体の間隔と大きさ (部品) | 視点 (`viewport`) の `laneGap` / `nodeGap` / `labelMargin` / `gap` / `laneWidth` / `width` / `height` / `scale` |
  | 流れ図の並ぶ向き (部品) | `direction` を書かない / `vertical` / `horizontal` |
  | 状態の始まりと終わり (部品) | 箱の `initial` / `final` を書かない / 書く (終わりを 2 つ持つ) |
  | 節の色 6 種 (スタイル) | 箱の `color` の欄 |
  | 矢印の端の形 (スタイル) | 端の塗り `headFill` / `tailHeadFill` の `solid` / `hollow` |
  | 形の満ちる向き 4 種 (スタイル) | 形の外枠の色 (`shape.stroke`) |
  | 棒グラフ (図表) | 段の描く割合 (`draw: bar 0.4`) |
  | 時間の信号で図形 2 種を動かす (対話) | 時間の速さの選択肢 (`speeds`) と既定の番号 (`defaultSpeedIdx`) |
  | input 入力 4 種の合わせ技 (対話) | 日時の入力 (`datetime`) |
  | 矢印や縦列や図全体での受け取り (対話) | 出来事の `drag` / `drop` と、相手 `arrow` / `lane` / `diagram` |
  | 5 段階の CSV 処理の進捗 (動き) | 数え上げの時間 (`durationMs`) |

  見本の無い道は、裏取り付きの覆い方に載せる。

  | 覆い方 | 対象 | 裏取り |
  |---|---|---|
  | 色の別名 | `成功` / `neutral` などの別名 | 別名の指す色が同じ欄に書かれている |
  | 既定 | `reveal=phase` / `relations=off` | 書いた図と書かない図の描画が一致し、既定でない値を消すと描画が変わる |
  | 画面の切替 | `palette=celadon` | 画面の配色の切替の選択肢の数が値の数と一致する |
  | 直してから見本 | 位置の欄 (#1971)、組の枠 (#1972)、見本の状態と倍率 (#1973)、札の色の出どころ (#1974) | Issue 番号と理由を持ち、道がまだ書かれていない |

  表に載せた道が型定義から消えた時と、見本が書かれた時も落ちる。
  最上位の欄を書いた JSON を外すと、その欄が足りないと見つかること (植え込み対照) も確かめる。

  操作の見本に合わせて、カタログの受け取り手に動かす (押したまま 6px 動かした時に 1 回)・落とす・図に入る / 出るを足した。
  動かす操作は押した時点で図の移動へ渡さないので、箱を動かしても図ごと動かない。
  5 つの操作が個別に届くことは `catalog-event-targets.spec.ts` が実ブラウザで確かめる。

- **欄が取る値を全てカタログで見せるようにし、見本の無い値が出たら検査で落ちるようにした** (#1966)

  カタログの網羅の検査は「欄を 1 度書いたか」 までしか見ておらず、値の違いを数えていなかった。
  節の色は `accent` を 1 件書いただけで通り、残る 5 色はどこにも出ていなかった。
  engine の値の一覧と突き合わせると、次の 16 個に見本が無かった。

  | 欄 | 見本の無かった値 | 足した見本 (スタイル / 図表) |
  |---|---|---|
  | 節の色 (`tone`) | `teal` / `success` / `error` / `warning` / `info` | 節の色 6 種 |
  | 矢印の先端の形 (`head`) | `diamond` / `crow` | 矢印の端の形 (先端の矢じり / 先端の多重度) |
  | 矢印の根元の形 (`tailHead`) | `triangle` / `open` / `crow` / `many` / `zero-many` | 矢印の端の形 (根元の矢じり / 根元の多重度) |
  | 矢印の出る辺 (`side`) | `top` / `right` | 矢印の出る辺 (書かない / 上 / 右 / 下 / 左) |
  | 形の満ちる向き (`shape.orient`) | `left` | 形の満ちる向き 4 種 |
  | 体験の道筋の気持ち | `怒り` (`angry`) | 体験の道筋の「5つの気持ち」 |

  見本は値の一部だけでなく、欄が取る値を 1 枚に全て並べた (矢じりは 5 形と 4 形、出る辺は 4 辺、満ちる向きは 4 種)。

  矢印の出る辺は、1 枚に「辺を書かない組」 と「同じ置き方で辺を書いた組」 を上下に並べた。
  1 組だけだと、別のレーンで右へ出す形のように、書いた辺が推し量った辺と同じになって違いが見えない。
  右と左は同じレーンの中で行き先を横にずらす必要があり、図の検査の揃えの 2 軸は理由を書いて見逃す組に登録した。

  `catalog-value-coverage.test.ts` は、欄ごとの覆い方 (見本 / 既定 / 画面の切替) の表を `satisfies Record<値の型, 覆い方>` で閉じる。
  engine が値を足すと型検査が落ち、表から値を消しても型検査が落ちる。
  見本を持つ図を外すと足りない値として見つかること (植え込み対照)、「既定」 と書いた値は書いても書かなくても配置が一致すること、
  状態の初期値と段が置く値の両方を記法の値として解くことも確かめる。

- **ひな形の詳細画面 (`/preset/:id`) の図も、倍率のボタン・ホイール・つまみで拡大縮小し、ドラッグで動かせるようにした** (#1964)

  #1961 で拡大と移動を入れたのはカタログの分類画面だけで、ひな形の詳細画面の図は台の幅に縮めて描くだけだった。
  広い図 (`er-complex-demo` など) は字が読めない大きさのまま、拡げる手段が無かった。

  | 操作 | 詳細画面での動き |
  |---|---|
  | 図の上の倍率の欄 (下げる / 今の倍率 / 上げる / 幅に合わせる) | カタログの並べて見る側と同じ刻みで動く。 既定は幅に合わせ、描かれた倍率を数字で出す |
  | `⌘` / `Ctrl` + ホイール、つまみ | カーソルの下の点を残して拡大縮小 |
  | 修飾キー無しのホイール | 画面を送る (縦に読む画面なので奪わない) |
  | 倍率を指定した図のドラッグ | 横は台の中で巻き取り、縦は画面ごと送る |
  | 前へ / 次へ | 移った先のひな形は幅に合わせる状態から始まる |

  巻き取るのは台の内側で、台そのものではない。 台は段の札を置く基準なので、台が巻き取ると札も図と一緒に流れる。

  倍率の欄は 1 つの部品 (`DiagramZoomControls`) にし、カタログの 2 か所と詳細画面の計 3 か所で使う。
  3 か所に同じ形を書き写すと、#1961 で欄の文字を 2 か所直したのと同じく片方だけ直す形になるため。
  ボタンの名前は場所ごとに違える (並べて見る側と拡大表示は 1 つの画面に同時に出る)。

  検査は 3 つ足した。 `preset-panzoom.spec.ts` は実ブラウザで、ボタンで次の刻みの幅 (viewBox の幅 × 倍率、±2px) に
  なること、カーソルの下の点が 2px 以内に残ること、ドラッグと逆向きに巻き取ること、段の札が動かないこと、
  前へ / 次へで幅に合わせる状態へ戻ること、ひな形の全ての詳細画面で欄が数字の百分率になることを確かめる。
  `DiagramZoomControls.test.tsx` は部品単体で、場所ごとの名前が重ならないことと、押せる / 押せないの判断を確かめる。
  図に画面の言語を当てる入口の検査 (`diagram-lang.test.tsx`) には、詳細画面が言語を当てた図を変数で渡すことを宣言した。

- **カタログの図を、ホイールやつまみ操作で拡大縮小し、ドラッグで見たい所へ動かせるようにした** (#1961)

  カタログの分類画面の図は、＋ / − のボタンで倍率の刻みを 1 つずつ動かすことしかできず、
  図の上でトラックパッドをつまむとブラウザの画面全体が拡大していた。 拡げた図もスクロールで
  巻き取るしかなかった。 並べて見る側と拡大表示の両方で、次の操作を受けるようにした。

  | 操作 | 並べて見る側 | 拡大表示 |
  |---|---|---|
  | `⌘` / `Ctrl` + ホイール、つまみ | カーソルの下の点を残して拡大縮小 | カーソルの下の点を残して拡大縮小 |
  | 修飾キー無しのホイール | 一覧の画面を送る (今のまま) | 拡大縮小 |
  | 倍率を指定した図のドラッグ | 巻き取る (縦は画面ごと送る) | 巻き取る |

  操作は 1 つの部品 (`useDiagramPanZoom`) にまとめ、2 か所で使う。 倍率の状態は画面が持ち、
  ボタンとホイールが同じ状態を動かす。

  倍率の欄も直した。 器に収めている時に「幅に合わせる」 / 「収める」 と出しており、隣のボタンと同じ語が
  2 つ並んで今の倍率が読めなかった。 実際に描かれている倍率を数字で出す。 拡大表示では svg の箱が器
  いっぱいに広がる一方、図は縦横比を保って箱の中に小さく描かれるため、箱ではなく描かれた範囲で測る
  (`er-complex-demo` は箱で測ると 50%、実際は 22%)。 ＋ / − はその数字から次の刻みへ動き、
  ホイールで刻みの外の値 (46% など) にした後も次に大きい / 小さい刻みへ動く。 下にも上にも刻みが無い時は
  押せない (22% の図で「下げる」 を押すと 50% へ跳ねて大きくなっていた)。 ボタンの印は虫眼鏡にした。

  倍率の決まり (`diagram-zoom.ts`) には、刻みの外の値の扱い、ホイール 1 回の効き (1 目盛で約 1.22 倍、
  つまみの細かい量には比例)、カーソルの下を残す巻き取りの差を足した。 画面の検査
  (`catalog-panzoom.spec.ts`) は、描かれた図の外枠と巻き取りの位置を測って、カーソルの下の点が 2px 以内に
  残ること、ドラッグと逆向きに巻き取ること、ひな形 19 件の欄が並べて見る側と拡大表示の両方で数字の
  百分率になることを確かめる。 収めた図より大きい倍率を前提にしていた倍率の検査 2 本は、描かれた倍率から
  次の刻みを求める形に直した。

- **記法で選択肢と切り替えに見せる名前を書けるようにし、見本帳の状態の値を日本語で描くようにした** (#1920)

  `dropdown` / `radio` / `tabs` / `multi-select` の `options:` に、文字列のほかに `{ value, label }` の組を
  書ける。 名前を書いた選択肢は、操作部の選択肢と箱の字に差し込む値を名前で描く (`状態: {status}` が
  `状態: green` ではなく `状態: 成功` になる)。 状態に入る値は綴りのままなので、表示の出し分けと、値を綴りで
  読む読み出し (信号の色 / 優先度の札 / 知らせの種類) は変わらない。 `toggle` は `onLabel` / `offLabel` で
  真偽の名前を書ける。

  ```yaml
  inputs:
    status: { kind: dropdown, options: [{ value: red, label: "失敗" }, green], defaultValue: green }
    pressed: { kind: toggle, defaultValue: false, onLabel: "押した", offLabel: "押していない" }
  ```

  `JSON` も同じ形を受け、記法の定義 (`schemas/diagram.json`) と構文の手引きにも載せた。 組の `value` か
  `label` が無い / 空、知らない項目名は、`YAML` は行番号を、`JSON` は場所を付けて知らせる。 組の `value` に
  書いた外部参照は、文字列の選択肢と同じく出口で落とす。

  描画エンジン (`@cardenelabs/cdl`) を `^0.60.0` から `^0.61.0` へ上げた (`cdl#855` = 選択肢と切り替えを
  名前で描く)。 値を綴りで読む見本帳の 5 図 (`alertNotification` / `deploySpinner` / `issuePriorityBadge` /
  `buildStatusTrafficLight` / `clickToggle`) は、組み立て関数 / `YAML` / `JSON` の 3 つとも名前を持つ。
  `buildStatusTrafficLight` と `clickToggle` は名前を書かない変種 (`値のまま描く`) を切替で持ち、値が英語の
  まま出る形と見比べられる。

  英語が残る操作できる図は 7 図から 3 図に減った。 残るのは稼働の状態の略記 / 在席の値 / 共有ボタンの鍵で、
  描画エンジンの次の件で扱う。

- **記法で式に名札を書けるようにし、見本帳の式の名前を日本語で描くようにした** (#1916)

  `formulas:` の値に、式の文字列のほかに `{ expression, label }` の組を書ける。 名札を書いた式は、操作部が
  式の名前の代わりに名札を描き、中身に出る名前も名札を持つ入力欄と式だけ名札で描く (`total1` と
  `= baseFee + priority` が `ブロック N の手数料` と `= 基準手数料 (gwei) + 優先手数料` になる)。 文字列の形は
  変わらず書け、名札を持たない。

  ```yaml
  formulas:
    doubled: "input * 2"
    total: { expression: "doubled + 10", label: "合計" }
  ```

  `JSON` も同じ組を受け、記法の定義 (`schemas/diagram.json`) と構文の手引きにも組の形を載せた。 組に
  `expression` が無い / 空、知らない項目名、空の名札は、`YAML` は行番号を、`JSON` は場所を付けて知らせる。

  描画エンジン (`@cardenelabs/cdl`) を `^0.59.1` から `^0.60.0` へ上げた (`cdl#853` = 操作部の式と巻き取りを
  名札で描く)。 見本帳の式を持つ 11 図は、組み立て関数 / `YAML` / `JSON` の 3 つとも名札を持つ。
  `formulaTextBind` は名札を書かない変種 (`名前のまま描く`) を切替で持ち、式の名前が英語のまま出る形と
  見比べられる。

  英語が残る操作できる図は 18 図から 7 図に減った。 残るのは選択肢と状態の値 / 真偽 / 在席の値 / 状態の略記 /
  共有ボタンの鍵で、描画エンジンの次の件で扱う。

- **クラス図の記法に、出どころ側の多重度 `tailSub` を足した** (#1771)

  UML は関係の両端に多重度を書く (例 = 管理者 `1` が注文 `1..*` を持つ)。 記法は行き先の端の多重度 (`sub`) しか
  書けなかった。 矢印に `tailSub` を書くと、出どころの端の近くに多重度が出る (`head` / `tailHead` と同じ対の名前)。
  関係の語 (`relation`) と同じく、クラス図でだけ効く。

  ```
  - Admin -> Order: "持つ" { relation: aggregates, sub: "1..*", tailSub: "1" }
  ```

  描画エンジン (`@cardenelabs/cdl`) を `^0.51.0` から `^0.52.0` へ上げた (`cdl#825` で出どころの端の字 `tailLabel` が入った)。
  見本帳のクラス図 (`class-demo`) の 3 つの書き方に、出どころ側の多重度 `1` を書いた。

- **順番を持たない図を触れて読めるようにした** (#1757)

  クラス図と ER 図は箱と箱の関係が **同時に** 成り立っている。 線を 1 本ずつ引く動きは
  作り手が決めた読ませ方でしかなく、読み手の問い (この箱はどこと繋がっているか) には
  答えていなかった。 `class-complex-demo` は 8 段 ・ 900ms で 14 本を順に引くが、
  `Transaction` を読んでいる人に `Transaction` の 5 本を見せてはくれない。

  `@cardenelabs/cdl` を `^0.46.0` から `^0.47.0` へ上げ、記法に `relations:` を足した。

  | 書いた値 | 触れた時 |
  |---|---|
  | 書かない / `relations: off` | 何もしない (既定) |
  | `relations: hover` | 触れた箱と繋がる線と相手の箱だけが光り、繋がる線に粒が流れる |

  押すと留まり、もう一度押すか `Esc` で外れる (線を目で追う間に指が外れると読めないため)。
  仕組みは `cdl#806` が持つ。

  見本帳のクラス図と ER 図 6 枚には `relations: hover` と `reveal: all` を併せて入れた。

  | 図 | 段 |
  |---|---|
  | `er-demo` | 4 |
  | `er-complex-demo` | 9 |
  | `class-demo` | 6 |
  | `class-complex-demo` | 8 |
  | `スキーマ (DSL)` | 3 |
  | `多対多が 2 組 / 8 表 8 関係` | 4 |

  **段は消していない**。 段の説明文 (「白抜きの三角。 三角は親の側に付く。」 等) は記法の
  解説そのもので、触れる操作では運べない。 `reveal: all` を併せて入れると、段は線を
  **引く** のをやめて **光らせる** だけになる = 読み手が嫌っていた動きだけが消えて説明は残る。

  対象は図が持つ `type` (`class` / `er`) から導く。 図の id を手で並べると、クラス図や
  ER 図を足した時に一覧へ足し忘れても検査が緑のまま通る。

- **並べて見る側でも図の倍率を変えられるようになった** (#1749)

  並べて見る側の台は幅 874px (画面 1440 で実測) で高さの上限が無く、図は幅いっぱいに描かれる。
  **拡大表示より器が狭いので、小さくなる図はこちらの方が多い**。

  | どこ | 器 | 箱の題が 12px 未満 |
  |---|---|---|
  | 拡大表示 | 1150 × 630px | 20 枚 |
  | 並べて見る側 | 幅 874px (高さの上限なし) | 53 枚 |

  `swim-demo` は拡大表示で 13.7px あるが、並べて見る側では 10.4px しかない。

  逆向きの困りごともある。 幅に合わせるので細い図は引き伸ばされ、`scene-web-infra` は
  viewBox 510 幅が 874px に伸びて高さが 1988px になる。 39 枚が高さ 1200px を超え、
  最長の `parts-traffic-light-stack` は 2292px ある。

  拡大表示と同じ操作を見出しの「拡大」 の隣に置いた。 **どちらの向きにも効く** =
  広い図は拡げて読め、細い図は縮めて 1 画面に収められる。 はみ出す分は台の内側が巻き取る。

  既定の呼び名は場所で変えている。 拡大表示は縦横とも収めるので「収める」、
  並べて見る側は幅にだけ合わせるので「幅に合わせる」。
  同じ言葉にすると、細い図が 1.7 倍に伸びるのを「収める」 と呼ぶことになる。

  拡大表示の倍率とは別の状態で、互いに影響しない。 器が違うので同じ倍率でも見え方が揃わない。

- **拡大表示で図を実寸まで拡げて読めるようになった** (#1745)

  拡大表示は図を器に収まるよう縮めるため、**大きい図ほど小さく描かれる**。
  器は画面 1440 × 900 で 1150 × 630px しかなく、`er-complex-demo` は箱の題が 4.8px まで縮む =
  一覧の並びで見た時 (8.8px) より小さい。

  器を広げる余地は無かった。 拡大表示の枠の外は 見出し 124px + 余白 24px × 2 + 縁 1px × 2 = 174px で、
  CSS が引いている `180px` はその近似。 決め打ちをやめても器は 630 から 636 になるだけ (+0.95%)。

  そこで **器に収める前提を外せる** ようにした。 見出しに倍率の操作を置き、
  `50% / 75% / 100% / 150% / 200% / 300%` の 6 段を選べる。 既定は今までどおり「収める」。

  倍率を指定すると viewBox の 1 world を 1px として描き、器に収まらない分は本体が巻き取る。
  実寸は画面の実測でなく `layout()` が返す viewBox から取る。
  別の図を開くと「収める」 に戻る = 倍率を持ち越すと小さい図が画面からはみ出したまま開く。

  見本帳の図は 1 枚も変えていない。 判断の経緯は #1740 に残っている。

- **100 個の印も見本帳で 1 個ずつ埋まるようにした** (#1670)

  `@cardenelabs/cdl` を `^0.39.0` から `^0.40.0` へ上げ、段の `draw:` が受ける語に
  `waffle` を足した。 見本帳の「対応済みの問い合わせ」 もその語を書いている。

  印は左上から読む向きに 1 個ずつ埋まる。 左の一覧 (名前と印の数) は最初から出す =
  この図は「数えて確かめられる」 ことが存在理由で、一覧はその答え合わせの表になる。

  これで段の `draw:` が受ける語は 13 になり、記法で描ける図種のうち動きを持たないのは
  指標 (`stat`) と 四象限 (`quadrant`) の 2 つだけになった。

- **半円ゲージ / 放射 / 積層帯も見本帳で起点から伸びるようにした** (#1668)

  `@cardenelabs/cdl` を `^0.38.0` から `^0.39.0` へ上げ、段の `draw:` が受ける語に
  `gauge` / `radial` / `stacked` を足した。 見本帳の 3 つの図もその語を書いている。

  | 語 | 起点 | 動き |
  |---|---|---|
  | `gauge` | 9 時 | 弧が上を通って 3 時へ伸びる |
  | `radial` | 12 時 | 各輪が時計回りに開く |
  | `stacked` | 左端 | 帯が右へ伸びる |

  半円ゲージと放射の見本には「描画側が動きを持たないので `draw` を書かない」 という
  注記が残っていた。 描画側が持つようになった (cdl#715) ので外している。

  合わせて、図全体を 1 箱で描く種別の一覧 (`SINGLE_BOX_KINDS`) に図表の 5 種
  (半円ゲージ / 放射 / 積層帯 / 指標 / 割合の印) を足した。 抜けていた間は `draw:` を
  書いても指す先を引けず、**書けるのに動かない** 状態になっていた。 段の `focus:` で
  登場人物の名前を書いた時に何も光らないのも同じ穴で、こちらも直っている。

- **傾き図の見せ方に「順位」 を足した** (#1664)

  見本帳の切替が「今の値」 「増減」 「順位」 の 3 つになった。 順位を選ぶと縦の位置が
  値ではなく順位の段 (等間隔) になり、 線の交差が順位の入れ替わりと 1 対 1 で対応する。

  軸の脇の字は値のままなので、 量は字で読める。

  選択肢は engine の型から漏れない形にしてあり (`Record<ChartSlopeForm, string>`)、
  版を上げた時点で `Property 'rank' is missing` が出て漏れを止めた。 その仕掛けが
  実際に効いたのがこの取り込み。

### Changed

- **カタログのクラス図と ER 図で、簡単な版と複雑な版を 1 つの見本の中で切り替えられるようになった** (#1960)

  ひな形の一覧では、複雑な版が別の行 (「クラス図 (複雑)」 「ER図 (複雑)」) になっており、規模違いを
  見比べるには一覧で行を選び直す必要があった。 複雑な版を元の見本の中のパターン「複雑」
  (`pattern__presetClassDiagram__複雑` / `pattern__presetEr__複雑`) に移し、元の見本の呼び名を「簡単」 にした。
  押すと図とコードの欄が入れ替わる。

  | 見本 | 簡単 | 複雑 |
  |---|---|---|
  | クラス図 | `class-demo` (7 クラス × 6 関係) | `class-complex-demo` (12 クラス × 14 関係) |
  | ER図 | `er-demo` (3 表 × 3 関係) | `er-complex-demo` (12 表 × 14 関係) |

  ひな形の件数は 21 から 19 になり、ひな形の一覧 (`PRESETS`) と名前の表からも複雑な版の 2 件を外した。
  ひな形の詳細画面の `/preset/er-complex` と `/preset/class-complex` は開けなくなる (画面のどこからも
  リンクしていない)。 複雑な版を名指ししていた画面の検査 (倍率・線を 1 度だけ描く・行の範囲) は、
  元の見本を開いてからパターンを押す形に直した。 線を 1 度だけ描く検査は、パターンも開いて見る。

- **記法の検査の道具と実証用の台本が端末に出す見出しと案内の文を日本語に開いた** (#1946)

  記法の検査の道具 (`pnpm lint:notation`) と実証用の台本 (`pnpm lint:notation:proof`) は、#1940 で開いた
  指摘の文を英語の見出しに挟んで出していた (`diagram total:` / `issue total:` / `auto-fixable:` /
  `Usage:` / `(auto-fix 可)` / `before:` / `after:` など)。 指摘の文と同じく、識別子と書き手の値だけを
  `` ` `` で囲む日本語にした。 まとめは「検査した図 / 指摘 / 自動修正できる指摘」 の件数で出し、指摘の行は
  「⚠ 注意 `規則` (対象 `部品`、自動修正できる)」 の形になる。 重さの記号には `注意` / `参考` の見出しを添えた。

  指摘の行の形は 2 つの道具に同じ字で書かれていたので、1 か所 (`lint-output.mjs`) にまとめた。
  2 つの道具を素の `node` で起動し、出る経路をすべて踏んだ出力に画面の字と同じ判定を当てる検査
  (`lint-cli-words.test.ts`) を足した。 `` ` `` を外してから判定する部品は、指摘の文の検査と共有するため
  `screen-words.ts` に移した。

- **図の見本を分類ごとに並べた画面の呼び名を `見本帳` から `カタログ` に戻した** (#1938)

  #1854 で `カタログ` を `見本帳` に呼び直したが、画面の持ち主の指示で `カタログ` に戻した。
  画面の行き先 (`/catalog`) と英語の呼び名 (`Catalog`) と同じ語になる。 帯の行き先・一覧の見出しと道筋・
  分類と見本の詳細の道筋・呼びかけの釦 (`カタログで図を見る →` / `カタログを見る`) が `カタログ` になった。

  仕様書 (`screens.md`) と意匠 file (`app.pen`) の画面の枠 8 枠と字も同じ呼び名にした。 意匠 file は元の部品
  31 箇所と、部品の字を上書きしている 4 箇所だけを直した。 実装と検査と記法の package の注釈も含め、
  変更履歴の過去の記載を除いて `見本帳` の字は残っていない。

  呼び名の検査の対を `見本帳 → カタログ` の向きに入れ替えたので、画面・仕様書・意匠のどこかに `見本帳` が
  戻ると落ちる。 `カタログ` は理由を付けて残す語の一覧 (`残してよいカタカナ語`) に載せ、画面のカタカナ語の
  天井を実数 (含む字 204 / 語 74 / 延べ 237) に上げた。

- **描画エンジンを 0.63.0 に上げ、状態の図と発想の図の段の札を図の型と同じ綴りで出すようにした** (#1872)

  描画エンジン (`@cardenelabs/cdl`) を `^0.62.0` から `^0.63.0` へ上げた (`cdl#859`)。 記法で `type: state`
  と書いた図の札は `fsm` ではなく `state` と出る。 見本帳の状態の図 2 種と発想の図も札が `state` / `mind` に
  なり、3 つの書き方の札を揃えた。 発想の図 (`type: mind`) の根の `data-cdl-type` は `mindmap` から `mind` に
  変わる。

  英語の天井の検査は、札の綴りのずれを判定から外す手書きの一覧 (`fsm` / `mindmap` / `statemachine2`) を
  消した。 代わりに、見本帳の英小文字の札がすべて図の型の一覧か記法の名前で引けることを確かめる。

- **描画エンジンを 0.62.0 に上げ、見本帳の図に残る英語を 0 にした** (#1922)

  描画エンジン (`@cardenelabs/cdl`) を `^0.61.0` から `^0.62.0` へ上げた (`cdl#857` = 稼働の時系列と在席の
  状態を図の言語で描く)。 `serverUptimeStatus` の区間は `ACTI` ではなく `稼働` / `待機` / `異常`、
  `teamPresenceStatus` の状態は `offline` ではなく `オンライン` / `離席` / `オフライン` と描く。 区間の字が
  4 字で切られると書いていた段の説明は、3 つの書き方とも名前で出る形に書き直した。

  `socialShareButtons` の箱の題から、描画側が色を選ぶ鍵 (`X (tw)` の `(tw)` など) を外した。 箱の副題が語る
  順位 (`共有が最も多い先`) を行の値と照らす検査は題に書いた鍵で行を探していたので、この図の 4 つの箱は
  行の鍵との対応を検査の側に宣言した。 共有ボタンが
  LinkedIn を描く印の `in` は訳す字ではないので、英語の天井の検査で **字の塊全体が印の時だけ** 残してよいと
  宣言した。 語の一覧に載せると、どの図の文でも `in` が通るため。

  英語の天井は 12 ファイルすべてが 0 になった。

- **操作できる図の読み出しと再生欄に残っていた英語を日本語にした** (#1912)

  操作できる図は、書き手の字を日本語にしても、描画エンジンが読み出しと再生欄に書く英語が残っていた
  (値の無い読み出しの `empty`、かんばんの列の見出しの `Todo` / `In Progress` / `Done`、ファイル置き場の
  `⬆ Drop file here`、再生欄の `1x` と `0.0s / 2.0s` など)。

  描画エンジン (`@cardenelabs/cdl`) を `^0.58.0` から `^0.59.0` へ上げた (`cdl#849` = 図の言語で読み出しと
  再生欄の固定の字を引き替える)。 #1910 で図に画面の言語を当てているので、日本語の画面では `なし` /
  `未着手` / `1倍` と描き、英語の画面では変更前と同じ英語で描く。

  英語が残る操作できる図は 40 図から 18 図に減った。 残るのは、描画エンジンが書き手の値や識別子をそのまま
  描く字 (入力欄の選択肢、状態の値、式の名前と中身、状態の略記) で、描画エンジンの次の件で扱う。

- **見本帳の図を画面の言語で描き、形の絵の中の英語を日本語にした** (#1910)

  形の見本 14 種 (`shape-warehouse` / `shape-payment-provider` / `shape-blockchain-block` など) は、書き手の字を
  日本語にしても、描画エンジンが絵の中に書く英語が残っていた (`WAREHOUSE` / `PAY` / `hash` / `→ 12 chunks in 1.8s`)。

  描画エンジン (`@cardenelabs/cdl`) を `^0.57.1` から `^0.58.0` へ上げた (`cdl#847` = 図の言語で形の絵の中の
  固定の字を引き替える)。 見本帳の画面は、図を描く直前に画面の言語 (日本語 / 英語の切り替え) を図に当てる。
  日本語の画面では `倉庫` / `決済` / `ハッシュ` と描き、英語の画面では変更前と同じ英語で描く。

  見本の記法 (YAML / JSON と組み立て API) には言語を書いていない。 見本を写した利用者の図は、見本帳の画面の
  言語に縛られず、描画エンジンの既定 (英語) で描かれる。 図が言語を書いている時は、画面の言語で上書きしない。

- **C4 の図の枠の札を日本語にした** (#1886)

  `type: c4` の枠の札は描画側が段ごとに決めており、`System Context` / `Container` /
  `Component` の英語が出ていた。 枠の札は **読む人に見せる字** で、記法に打ち込む識別子では
  ない (打ち込むのは `L1` / `L2` / `L3` の目印の側)。 `全体の見取り図` / `動かす単位` /
  `部品` に開いた。 目印の綴りは変えていない。

- **図が切り替わる単位を画面と設計で 4 通りに呼んでいたのを「段」 に揃えた** (#1819)

  同じものを 段 / シーン / 局面 / フェーズ の 4 通りで呼んでいた。
  決定的なのは `docs/design/specs/screens.md` と `src/pages/HomePage.tsx` が
  **一字一句同じ 1 文を別の語で書いている** ことで、片方を直しても もう片方に伝わらない。

  | 直す前 | 件数 | 置き場 |
  |---|---|---|
  | 段 | 107 | 記法の説明 (`lib/syntax-forms.ts`) |
  | 局面 | 14 | 設計の仕様書 (`docs/design/specs/screens.md`) |
  | シーン | 6 | 画面 (`pages/HomePage.tsx`) と分類の説明 (`lib/catalog.ts`) |

  揃える先は作文せず、最も多く使われていて **札を描く file 自身が自分の説明で使っている**
  「段」 に寄せた。 `PhaseChrome.tsx` はコメントで 14 箇所を段と書きながら、画面には
  シーンと出していた。

  呼び名を揃える表 (`揃える呼び名`) は置き場ごとに持たず 1 つにする。 #1811 で作った表は
  画面の枠 (`components/`) 側だけが持っていたため、**表を持たない置き場に逃げた語を
  1 件も拾えなかった** (実測で `pages/` の 4 件が見えていなかった)。

  母集団も 画面 / 枠 / 材料 / 設計の仕様書 の 4 つに広げた。 実測 861 件 (画面 663 / 設計 198) で
  20 件の食い違いを検出し、直して 0 件にした。 決まりを書いた file 自身は母集団から外す
  (表の `直す:` の値を表が咎めるため)。

  札の字を掴んでいた実ブラウザの検査 3 file は `PhaseChrome.tsx` の `段の呼び名` を
  引く形に直した。 字で書いた検査は、呼び名を直した日にまとめて落ちる。

- **画面の字の英語を見る判定を 1 つにした** (#1817)

  判定が 2 つあり、**同じ字に別の答えを返していた**。 日本語の字の範囲、語の切り出し、
  file 名と斜線でつないだ名の扱い、残してよい語の一覧の 4 つがそれぞれ別に書かれていた。

  実測で走査 4181 件のうち 80 件が割れる。 `API` は画面で通り枠で咎められ、
  `posX` は枠で通り画面で咎められる = **同じ字が置いてある場所で扱いが変わる**。

  | 部品 | 直す前 (画面 / 枠) | 直した後 |
  |---|---|---|
  | 日本語の字 | `\p{Script=...}` / `[ぁ-んァ-ヶ一-龯]` | `\p{Script=...}` (範囲を手で書くと `ー` が落ちる) |
  | 語の切り出し | `[A-Za-z]+` / `[A-Za-z][A-Za-z0-9_-]+` | 繋ぎの横棒を語に含める (`alarm-clock` は記法の値で 1 語) |
  | file 名 / 斜線の名 | 外す / 外さない | 外す |
  | 残してよい語 | 20 語 / 20 語 (共有 5 語) | 1 つの一覧 35 語 |

  判定は `apps/playground-spa/src/lib/screen-words.ts` が 1 つだけ持ち、3 つの検査が引く。
  **拾い方 (どの字を集めるか) は寄せない** = 画面は差し込みを文の一部として読み、枠は
  見た目の札を外し、材料は引用符の中身だけを見るので、組み立ての形が違う。

  規格の名前は番号と組で 1 つの固有名詞なので、末尾の番号を落としてから一覧を引く
  (`ERC-20` / `EIP-1559` / `ERC-4337` が `ERC` / `EIP` の 2 語で足りる)。
  版の番号 (`v0.5`) は file 名と同じく語に割らない。

- **更新履歴の画面が見本を 12 件と書いて実物の 21 件とずれていたのを直した** (#1806)

  「最新リリース」 の札が `12` を字で持っており、実物 (`PRESETS`) は 21 件だった。
  説明文も 12 個の図の名前を手で並べていて、こちらも実物の一部でしかない。

  この画面は見出しが「最新リリース」、説明が「いまの最新は v0.5」 なので、
  読み手は「今これが手に入る」 と読む。 過去の版の記録ではないため、今の数を出す。

  **説明書と違って数は落とさない**。 動いている頁で実物がその場にあるので、
  `PRESETS.length` から出せば書き写しにならない (`rules/quality.md` の経路 1)。

  図の名前の一覧は落として、見本の一覧への行き先に替えた。
  説明文に 12 個の名前を並べるより、押せば全部見られる方が読み手の手数が少ない。

- **見本帳の分類の呼び名 7 つを日本語に開いた** (#1805)

  分類の呼び名 11 件のうち 9 件がカタカナ語だった。 呼び名は分類の頁で最も大きく出る字なので、
  ここが開けていないと下の説明文だけ日本語という不揃いになる。

  | 今まで | これから | 根拠 |
  |---|---|---|
  | プリセット | ひな形 | 説明「1 行で骨格ができる」 |
  | レシピ集 | 実用例 | 説明書の画面がこの分類へ送る札を既に「9 実用例」 と書いていた |
  | パターン | 組み方の型 | 説明「図の組立てで繰返し出る汎用構造の**型**」 |
  | アニメーション | 動く図 | 説明の冒頭「**動く図**の書き方」 |
  | パーツ | 部品 | 前置き「合成用の小さな**部品**」 |
  | スタイル | 線と色 | 前置き「**線と色**で意味を分ける」 |
  | インタラクティブ | 操作で動く | 前置き「user **操作で図が動く**」 |

  **言い換えを作文していない**。 7 件とも、同じ分類の説明文か別の画面が既にその日本語を
  使っていた。 呼び名だけがカタカナで取り残されていた形。

  据え置いたのは `テキスト記法` (#1788 から 7 層に渡って揃えた直後、`テキスト` は定着した語) と
  `イーサリアム` (製品の名前)。 理由は検査の中の表に書いてあり、次に分類を足す時に読める。

  `slug` は変えていない。 `/catalog/presets` は外から張られるので、変えると link が切れる。

- **紹介文が見本帳の分類を 9 つと書き、2 つ落としていたのを直した** (#1803)

  `README.md` が分類を 2 箇所で手で並べており、どちらも実物の 11 件から
  `イーサリアム` (`ethereum`) と `図表` (`charts`) が抜けていた。
  残った `テキスト DSL` は #1787 から続く言い換えの 7 層目。

  一覧は呼び名と識別子の 2 通りで書かれているので、検査も 2 通りとも
  `CATEGORIES` と突き合わせる。 片方だけ見ると、残りが手書きのまま通る。

  **探す手掛かりを実物から引く**。 一覧の頭が `CATEGORIES[0]` であることを使って括弧を探す。
  「カタログ (」 のような語を書くと、説明文を言い換えた日に探し方が何にも当たらなくなり、
  検査が黙って通る。

  図の数 `380+` も 5 箇所から落とした。 実物は 444 枚で、どれとも合っていない。

  #1801 は走査先を意匠帳に限っており、repo の入口である `README.md` が外に残っていた。
  走査先を説明書全体へ広げ、変更履歴だけを外す (その版で何が起きたかを書く場所なので、
  今を指す数として読むと過去の記述を書き換えることになる)。

- **意匠帳が実物からずれた数と古い分類名を書き写していたのを直した** (#1801)

  意匠帳 (`docs/design/`) が、実物から導ける数を書き写していた。
  見本帳の枚数は `385` と `280` の 2 通りが書かれていたが、実物は 444 枚で、
  どちらも実物と合っていない。 色変数の数と参照の箇所、分類の札の枚数も同じ形だった。

  **数を落として、実物を見る場所を書く**。 図の枚数は走査した検査の文面に出るので
  そこで読む。 色変数は `.pen` を JSON として読んで数える。 分類の枚数は `CATEGORIES` が決める。
  数を書き写すと、図を 1 枚足した日から古くなる。

  同じ file の分類の一覧に、古い呼び名 `テキスト DSL` も残っていた。
  #1787 から続く言い換えの 6 層目で、こちらは数と違って中身そのものが情報なので、
  `CATEGORIES[].label` と丸ごと突き合わせる検査を置いた。

- **説明書が見本帳の頁を古い呼び名で指していたのを直した** (#1799)

  分類の呼び名を `テキストDSL` から `テキスト記法` にした (#1787 / #1788) 時、
  見本の名前 (#1790) と図の題 (#1792) は揃えたが、説明書だけが古いまま残っていた。
  画面を開くとその頁の題は「テキスト記法」 で、説明書の指す先と名前が合わない。

  同じ言い換えがこれで 5 層目になる (画面の説明文 / 分類の呼び名 / 見本の名前 / 図の題 / 説明書)。
  どの層も「直した層から見える範囲」 では正しく見えるので、直した直後の目視では残りに気付けない。

  **検査は分類の一覧から導く**。 説明書の中の「見本帳の ◯◯ の頁」 という形を拾い、
  `◯◯` が `CATEGORIES[].label` のどれかであることを確かめる。 古い呼び名を手で並べると、
  次に呼び名を変えた時にまた並べ直すことになる。

- **走査の軸の名前を日本語にした** (#1796)

  #1795 で編集画面の案内を日本語にしたが、案内へ差し込まれる軸の名前だけが
  `structured-data-extraction` のような識別子のまま出ていた。
  読み手には「どの検査に引っかかったか」 が伝わらず、識別子を repo の中で探すしかない。

  67 件すべてに日本語の呼び名を付けた。 呼び名は engine が出す指摘文から導いている
  = 識別子の直訳ではなく「その軸が何を見ているか」 を書く
  (`structured-data-extraction` → `図の中身を機械が読み取れるだけの要素があるか`)。

  **呼び名は画面の側で持つ**。 engine の版を上げずに言葉を直せるのと、同じ軸でも
  出す場所によって言い方を変えられるため。 表が遅れる心配は検査が受け持つ =
  **engine が返しうる軸を実物から導き** (走査の結果の鍵を読む)、呼び名を持たない軸が
  1 つでもあれば落ちる。 逆に engine から消えた軸の呼び名が残っていても落ちる。

  案内文の組み立ても画面から `lib/axis-names` へ移した。 画面の中にあった間は、
  画面を丸ごと描かないと文を確かめられなかった。

- **編集画面の案内に残る英語の言い回しを日本語にした** (#1795)

  #1785 / #1787 で画面の説明文の英語を日本語にしたが、対象は `pages/` 配下だった。
  編集画面の枠 (`components/`) はその走査の外で、英語の言い回しが 17 件残っていた。

  `同一 node fan の out edge Y を DSL で揃える (layout engine の auto-align 待ち)` のような
  案内文は、図の走査で見つかった注意を直す時に画面へ出る。 `node` / `edge` / `fan` /
  `layout engine` はどれもこの repo の中の呼び名で、外の読み手には何を直せばよいか取れない。
  `TONE_COLORS` のような実装の中の定数名がそのまま出ているものもあった。

  **文言は 3 つの形で書かれている**。 二重引用符の中と、1 行に収まる差し込みつきの字
  (`` `${n} 件を記法に書き戻しました` ``) と、JSX の地の文と。 3 つとも見る検査を置いた
  = 1 つでも外すと、そこへ逃げられる。

  残す語は理由つきで一覧にした (`URL` は定着した略語、`SVG` は日本語の呼び名が無い、
  `labelOffsetX` は画面に出す記法と同じ綴りで書く必要がある、など)。
  **理由を書けない語は開ける語** で、一覧に足すこと自体が「直さない」 の言い換えになる。

  案内へ差し込まれる走査の軸の名前 (`structured-data-extraction`) は識別子のまま残っている。
  軸は 51 件あり、呼び名の表をどこに置くかの判断が要るため #1796 に切り出した。

- **図の題に残る英語の呼び名を日本語に直した** (#1792)

  #1787 で画面の説明文から、#1790 で見本の名前から `DSL` を外した。 図そのものに焼き付いた題だけが
  `認証フロー (DSL)` のまま 11 件残っていた。

  この題は 2 つの経路で画面に出る。 図の上の見出しとしてそのまま出るのと、題から導いた `id` が
  見本一覧の 2 行目に出るのと。 後者は小文字化して英数字と仮名漢字以外を `-` に潰した形なので、
  `認証フロ-dsl` や `四半期ロ-ドマップを-text-dsl-で書く例` という読めない字になっていた。

  11 件を `認証の流れ` / `同時に走らせる処理` のような、何の図かが読める語にした。
  `(DSL)` を外すだけだと `認証フロー` と `認証 FSM` のように同じ語で始まる題が並ぶため、
  中身を表す語に置き換えている。

  **題は記法の本文と組立て済みの `sourceJson__*` の 2 箇所に書かれている**。 片方だけ直すと
  記法の頁と図の見出しが食い違うので、432 対すべての対応を見る検査を置いた。

  題を変えると `id` も変わる。 縦に長い図を受け入れた一覧 (`responsive-accepted.ts`) が
  古い `id` を 6 件持っていたので追随させた。

- **見本の名前を分類の呼び名に揃えた** (#1790)

  分類の呼び名を `テキストDSL` から `テキスト記法` にした (#1787 / #1788) 一方で、その分類に並ぶ
  見本の名前は `テキストDSLのシーケンス` のまま 15 件残っていた。 分類の題が「テキスト記法」 で
  その下の見本が「テキストDSLの…」 では、同じものを 2 通りで呼んでいるように見える。

  15 件を `テキスト記法の…` にした。 英語名 (`Text DSL flow` 等) は原語のまま残している
  = 英語の読み手には `DSL` が通じるため。

  **呼び名は分類から導く**。 見本側に `テキスト記法` を literal で書くと、分類の呼び名を変えた時に
  また片方だけ古いまま残る (#1788 で直したのと同じ形)。 検査は `CATEGORIES[].label` を引いて
  突き合わせるので、分類の呼び名を変えると見本の名前の検査が落ちる。

  見本の名前 432 件を走査し、残る英単語 58 種類の内訳も確かめた。 いずれも固有名詞
  (`Ethereum` `Bitcoin` `GitHub`)、定着した略語 (`API` `JWT` `CRUD` `CSRF`)、
  記法の項目の名前 (`repeat` `derive` `waterfall` `renderOffset`) で、直す対象は `DSL` だけだった。

- **分類の日本語の呼び名の出どころを 1 つにした** (#1788)

  `CATEGORY_JA_LABEL` という同じ表が 2 つの画面 file に丸ごと置かれ、`CATEGORIES[].label` を
  上書きしていた (片方には `SSOT` と書いてあったが唯一の出どころではなかった)。 #1787 で
  `テキスト DSL` を `テキスト記法` に直した時、同じ 1 語を 2 箇所で直している。

  上書きの表は 9 件しか持たず、分類は 11 件ある。 後から足した 2 件 (イーサリアム・図表) は
  表から漏れていた。 足した分類が漏れても画面は落ちないため、気付く手がかりが無い。

  2 つの表を外し、呼び名は `CATEGORIES[].label` だけが持つ形にした。
  上書きが唯一効いていた `text-dsl` は、出どころ側を `テキスト記法` にして画面の字を保っている。
  分類名 (`eyebrow`) が呼び名と同じになるため `箇条書きで図を書く` に変えた (#1783 と同じ決まり)。

  検査は 3 本。 画面 file が分類の鍵を持たないこと、一覧の画面と分類の画面が描いた見出しの字が
  `CATEGORIES[].label` と丸ごと一致すること、分類名が呼び名の言い換えでないこと。
  **含むかどうかでは見ない** = 見本の名前が呼び名を字として含む (`テキストDSLのシーケンス`) ため、
  画面全体に対する部分一致だと呼び名がずれていても通る。

- **画面の説明文に裸で残っていた英単語を日本語にした** (#1787)

  分類名と札を日本語にした (#1785) ぶん、同じ画面の説明文に残る英語が目立っていた。
  参加のしかたの画面が最も濃く、`SSOT` `maintainer` `concern` `typecheck` のように、この repo の外の
  読み手には意味が取れない語が並んでいた (`dragon は MIT License の OSS プロジェクト。…`)。

  6 画面の 23 箇所を日本語にした (`動作証明のないコードは merge の対象外` → `動くことを示せていない
  コードは取り込まない`、`テキスト DSL` → `テキスト記法`、`Catalog で見本を見る` → `カタログで見本を見る`)。
  カタカナ語 (`カタログ` `エディタ` `プリセット`) は画面の呼び名として定着しているため今回の対象外。

  検査は `src/pages/*.tsx` の**日本語を含む字をすべて**拾い、残してよい語の一覧に無い英単語を探す。
  class の一覧を手で並べないのは、並べた時の数が上限になって後から足した文が無防備に増えるため。
  画面に出ない字 (コメントと見本のコード) は外し、外せていることを対照で確かめる。
  file 名 (`CONTRIBUTING.md`) と斜線でつないだ名前 (`/editor` `feature/<番号>-<短い説明>`) は
  打ち込む相手そのものなので語に割らずに通す。

  #1785 の `page-labels.test.ts` を `screen-text.test.ts` に改名し、分類名と説明文の 2 つの検査を
  1 file にまとめた。 残してよい語の一覧を 2 つに分けると片方だけ直して食い違うため。

- **見本帳以外の画面に残っていた英語の分類名と札を日本語にした** (#1785)

  概要・参加のしかた・公開の記録・ドキュメントの 4 画面に、`3 steps` `BUG REPORT` `reproduction`
  `1 PR = 1 concern` のような英語の分類名と札が 22 件残っていた。 どれも日本語の見出しのすぐ上か、
  日本語の説明文のすぐ下に置かれている。

  `見つけたら伝える` `再現手順` `使いどころ` `1 PR = 1 つの主題` のように日本語にした。
  言語を切り替える画面 (概要・参加のしかた) は札も切り替わるようにしている。
  分類名はすぐ下の見出しと違う内容にした (`バグ報告` の上は `見つけたら伝える`)。

  `src/pages/*.tsx` を読んで分類名と札を拾い、英語の語が残っていないことを検査する
  (`page-labels.test.ts`)。 拾う file は `readdirSync` で導く。 `SVG` `PR` `ER` `dragon` の 4 語は
  理由を書いて残し、挙げた語が実際に画面で使われていることも見る。

  **言語の切り替えを足した札を候補から外さない**。 `{isJa ? "…" : "…"}` の形も拾って日本語の側を
  見る = 字を直に書いた札しか見ない検査だと、直した札が候補ごと消えて素通りする。
  読めない式は未解決として数え、件数と中身を出す (いま 1 件、見本帳の分類名で別の検査が見ている)。

- **見本の名前の上に出る分類名を日本語にした** (#1783)

  21 件すべての分類名が `SWIMLANE / LAYOUT` `STATE / NESTED FSM` のような英語の大文字のままで、
  日本語の名前と日本語の説明文の間に英語の 1 行だけが挟まっていた。 `FSM` や `CONVERSION` が
  何を指すか知らないと、どの系統の図で何に使うものかを読み取れない。

  `<系統> / <用途>` の形の日本語に置き換えた (泳ぎ線の図は 流れの図 / 担当の切り分け、
  入れ子の状態図は ふるまいの図 / 入れ子、体験の図は 満足度の図 / 段階ごとの手ごたえ)。
  分類名は名前のすぐ上に出るため、名前の言い換え (`ステート図` に対する `状態の図`) にすると
  同じことを 2 度読ませる。 名前とも札とも違う内容を書いている。

  英字が残っていないこと、日本語を含むこと、` / ` で割った語が名前 (日本語・英語) とも札とも
  一致しないことを検査する (`presets.test.ts`)。 4 つの難をそれぞれ植えた対照つき。

- **入れ子の状態図の説明文が、縮まず 2 行で読めるようになった** (#1781)

  読み込み の箱の説明文 (`入る時: 印を出す / 出る時: 印を消す`) が幅に入らず、字が下限の 11 まで縮んで
  1 行で描かれていた。 同じ図の 待機 の説明文は 19 で描かれるため、同じ役割の字が 2 倍近く違って見えていた。

  描画エンジン (`@cardenelabs/cdl`) を `^0.56.0` から `^0.57.0` へ上げた (`cdl#843` = 読みやすさの下限を
  割る説明文を 2 行に折る)。 見本の字と箱の指定は変えていない。 手元で測ると 21 の 2 行になった。

- **見本帳の図に描かれる字を、英語の仮置きから日本語にした** (#1778)

  図の中の字が `User` `handler(...)` `Sign up` `Quick win` のような英語のままだった。 16 件の見本について、
  箱の題・縦列の見出し・矢印の説明・段の題と説明を、その図が示す場面の日本語に書き直した
  (例 = 泳ぎ線の図は クライアント / サービス / イベント の 3 列に 利用者 / 注文の処理 / 注文済み、
  分岐の図は 申請者 / 承認者 の 2 列に 申請を出す / 審査 / 承認 / 直して出し直す)。
  組み立て API と記法 (YAML / JSON) の 3 つの書き方すべてを同じ字に揃えている。

  中身の食い違いも直した。 漏斗の図は最後の段が「申込み」 なのに説明が別の語だったので 有料 に揃え、
  折れ線の図は 1月-4月 の点なのに説明が四半期ごとと書いていたので 月ごと にした。

  描いた結果から字を拾い、英語の語が残っていないことを検査する (`preset-diagram-words.test.tsx`)。
  製品名と規格名 (`CloudFront` `HTTPS` `PostgreSQL` 等) は語ごとに理由を書いて残し、
  表と型の名前を見せる図 (ER 図とクラス図) は箱の字が SQL とコードの識別子そのものなので段の字だけを見る。

  描画エンジン (`@cardenelabs/cdl`) を `^0.53.0` から `^0.56.0` へ上げた。 図に出る字のうち engine が
  足すものが 3 つ残っていたため (`cdl#835` = 形と機器の札や既定の説明文、 `cdl#839` = 分岐の札
  「はい」「いいえ」 を線の上に重ねる、 `cdl#841` = 気持ちの行の名前 大満足 / 満足 / ふつう / 不満 / 怒り)。

- **見本の詳細画面の説明文と札を、図が何を示すかを言う日本語にした** (#1777)

  説明文が作った側の覚え書きのまま出ていた (泳ぎ線の図なら「3 lane 自動配置。 laneId(label) で slug 参照、
  lane.x を auto-layout。」)。 21 件とも、図の中身を確かめた上で何をどう示す図かを書いた。 札も `auto layout`
  `saas` `nw` のような英語の小文字をやめ、`役割の分担` `階層構成` `接続` のような日本語にした。
  説明文に書いた数 (「7 クラス × 6 関係」 「3 レーン」) が図と合うことは検査で確かめる。

- **見本帳のクラス図と ER 図の関係名を、UML の用語と表どうしの関係を言い表す日本語に直した** (#1775)

  クラス図は関係の 6 種を `継ぐ` `満たす` `持つ` `抱える` `結ぶ` `使う` と呼んでいた。 UML の用語に揃え、
  継承 / 実装 / 集約 / コンポジション / 関連 / 依存 にした (記法の `relation` の値は変えていない)。
  段の説明も「命が同じ」「たどれるだけ」 のような言い回しをやめ、全体と部分の寿命や参照のしかたで書いた。

  ER 図は `抱える` `集める` `貼る` `束ねる` `属す` と英語の `places` を、線の向きと端の個数に合う言い方
  (`担当する` `メンバーを持つ` `案件に付く` `商品を含む` `属する` `注文する` 等) にした。 表の名前の `札` `組` も
  `タグ` `チーム` にした。 記法の手引き、JSON の型説明、見本帳の図の紹介文も同じ語に揃えた。

- **描画エンジンを `0.49.0` へ上げ、走査の error 一覧に直せなかった理由を並べる** (#1761 / #1762)

  描画エンジン (`@cardenelabs/cdl`) を `^0.48.0` から `^0.49.0` へ上げた。
  engine は `clearance` の違反に、なぜ直せなかったかを添える欄 (`reason`) を持つようになった (`cdl#810`)。

  見本帳の走査 (`visual-validate-sweep.test.ts`) が error を出す時の一覧 2 種 (抜粋と図ごとの一覧) に、
  違反が理由を持つ時だけ行の末尾へ足す。 理由を持たない違反の行は変わらない。

  ```
  error: clearance — edge-label:code-exchange ↔ edge-path:api-client gap=10.0px (need 14px) (diag=...) 逃げ場なし: 上=edge-label:consent-client (36→0) / 下=edge-label:token-issue (36→28) / ...
  ```

  見本帳 444 枚には今 `clearance` の error が無いので、図の見た目と走査の件数は変わらない。

- **一覧の台に描かれる高さも機械で数えるようにした** (#1753)

  `#1751` は一覧の枠 (幅 874px) で **文字が小さくなる側** を測った。 同じ枠には逆向きの
  性質がある = 台は幅に合わせて図を伸ばすので、細い図ほど大きく伸びて縦に長くなる。

  画面 1440 × 900 で実測した値。

  | 項目 | 実測 |
  |---|---|
  | 台の内側の幅 | 874px |
  | 台の上端の位置 | 562px |
  | 巻き上げずに見える高さ | 338px |
  | 台の高さの頭打ち | `max-height: none` |

  伸ばされる図は 444 枚中 238 枚。 最長は `parts-traffic-light-stack` で、
  viewBox 305 × 800 が 2.87 倍に伸びて画面で 874 × 2292px に描かれる (式と実測が一致)。

  **線は基準の画面の高さ 900px に引いた**。 `#1740` 以降の実測は全て 1440 × 900 で
  取っており、台の内側の幅 874px も同じ画面で測った値。 ここより長い図はどの位置まで
  巻き上げても 1 画面に入り切らない。 超えるのは 52 枚。

  | 線 | 超える枚数 |
  |---|---|
  | 630px (拡大表示の器の高さ) | 135 |
  | 900px (基準の画面の高さ) | 52 |
  | 1200px (`#1749` で数えた時の値) | 39 |

  「巻き上げずに見える高さ 338px」 には引かない = その線ではほぼ全枚数が引っかかる。
  一覧は元々巻き上げて見る面で、1 画面に収まることを求めていない。

  **伸ばすのをやめる道は採らなかった**。 台に伸びの上限を入れるか高さの頭打ちを入れれば
  長さは消えるが、238 枚の見え方が変わる。 幅に合わせて伸ばすのは `CdlDiagramView` の
  長年の挙動で、読み手には倍率の操作がある (`#1749`)。

  52 枚を名前で持ち、`#1747` / `#1751` と同じ両方向の突き合わせで見る。
  記法も図も画面も 1 文字も変わらない。


- **一覧に並べた枠でも図が読める大きさかを機械で数えるようにした** (#1751)

  描画エンジンを `^0.45.0` に上げた。 読みにくさを測る枠 (器) を呼び出し側から渡せる
  (`cdl#797`)。

  検査は拡大表示 (1150 × 630px) だけを見ていた。 見本帳は同じ図を一覧に並べた枠
  (幅 874px、縦は巻き取るので上限なし) でも出すので、そちらは 1 度も測っていなかった。

  | 器 | 下限 12px を割る枚数 | 内訳 |
  |---|---|---|
  | 拡大表示 1150 × 630px | 20 | 一覧でも割る 12 / 拡大だけ 8 |
  | 一覧の枠 幅 874px | 55 | 拡大でも割る 12 / 一覧だけ 43 |

  一覧だけで割る 43 枚は横に長い図で、拡大表示のほうが器が広いので割らない。
  拡大だけで割る 8 枚は縦に長い図で、一覧は縦を巻き取るので割らない。

  55 枚とも受け入れる (#1740 と同じ判断)。 一覧でも倍率を上げて読めるため (#1749)。
  受け入れた図を名前で持ち、実際に注意が出る図と両方向で突き合わせる = 新しい 1 枚が
  境を割ったら落ち、境の内側に入った図が一覧に残っていても落ちる。

  一覧は 2 つの器で 12 枚が重なるため、1 箇所 (`test/support/responsive-accepted.ts`) に
  置いて図ごとに器を書く。 群ごとにまとめられないのは、同じ群でも器によって割れ方が
  違うため (`縦にも横にも大きい図` の 5 枚は、拡大では 5 枚とも割るが一覧では 2 枚だけ)。

  `#1749` で画面から数えた時は 53 枚だった。 差の 2 枚 (`erc20-transfer-solidity` /
  `interactive-exemplar-login-flow`) はちょうど 12.0px で、画面の実測では丸めで境の内側に
  入っていた。 今回は engine が viewBox から導くので、境ちょうどの 2 枚も外側に数える。

  記法も図も画面も 1 文字も変わらない。

- **節と矢印の欄も数えるようにして、書けるのに 1 度も見せていない 5 つを埋めた** (#1707)

  #1706 で数えたのは中身の欄 (`*Data`) だけで、**節そのものと矢印の任意の欄は誰も
  数えていなかった**。 engine の型から 58 本 (節 36 / 矢印 22) を出し、`satisfies` で閉じた。

  欄ごとに「何が覆っているか」 を決め、覆うものが無い欄は **見本帳が 1 度は書いている**
  ことを求めるようにした。

  | 扱い | 本数 | 求めるもの |
  |---|---|---|
  | 中身 | 8 | #1706 の軸が数える |
  | 画面の切替 | 5 | 押せば両方の形を見られる |
  | 動き | 11 | 書いた図が 1 つ以上 |
  | 置き場所 | 13 | 書いても役割が増えないこと |
  | 記法 | 21 | 書いた図が 1 つ以上 + 消すと絵が変わること |

  記法から書けるのに見本帳が 1 度も書いていない欄が 5 つ出たので、見本に足した。

  | 足した欄 | どこに | 見えるもの |
  |---|---|---|
  | 遷移の条件 (`guard`) | テキストDSLのステート | `success` の下に「入力が正しい」 |
  | 後戻りの矢印 (`routing`) | 同上 | `retry` が箱の上を回って戻る |
  | 関係の多重度 (`cardinality`) | テキストDSLのER図 | 名前が `places (1:N)` になる |
  | 高さの追随 (`hBind`) | スライダーで矩形の幅と高さが変わる | つまみで高さが伸びる箱 |
  | 線の色の追随 (`strokeBind`) | edgeの太さや色にsignal連動 | 選んだ色が線に出る |

  最後の 2 つは **説明文が実装より先に進んでいた**。 見本の名前は既に「太さや色に
  signal連動」 と書いてあったのに、色を追随させる欄は 1 度も書かれていなかった。

  数えて分かったことが 2 つある。 工程表の横軸の目盛り数 (`ganttAxisMax`) は engine の型に
  在るが **記法に口が無く**、見本帳が書きようがない。 遷移の条件と関係の多重度は記法から
  書けるが、値は組み立ての時に説明文と名前へ写されるので **描画エンジンはその欄を読まない**。
  どちらも理由を表に書き、記法に口が開いたら / 絵に届き始めたら検査が落ちる。

- **中身を持つ図すべてで、足りない切替を描画エンジンから見つけて足した** (#1706)

  見本の抜けを数える検査は図表 (`chartData`) だけを見ていた。 中身を持つ節は他に 7 つ
  (工程表 / 発想の枝 / 漏斗 / 四象限 / 系統樹 / 体験の道筋 / 時系列のやり取り) あり、
  **誰も数えていなかった**。 家族の一覧も各家族の軸も型から出し、`satisfies` で閉じた。

  判定を 3 つに分けた。 engine を実際に描いて、役割が増える (新しい絵が出る) /
  絵だけ変わる (色や文字) / 絵も変わらない (engine が描いていない) を見分ける。
  3 つ目は理由付きで表に残し、engine が描き始めたら検査が落ちる。

  さらに **両側は 1 つの見本の切替で見せる** ことを求めるようにした。 別々の行に分かれて
  いると見比べられない。 これで 5 つの抜けが出たので、切替を足した。

  | 見本 | 足した切替 | 押すと変わるもの |
  |---|---|---|
  | 工程表 | `前後つき` / `帯だけ` | 前後の矢印 |
  | 枝分かれ図 | `説明つき` / `見出しだけ` | 根と枝の説明 |
  | 階層図 | `見出しだけ` / `説明つき` | 箱の中の説明 |
  | 体験の道筋 | `気持ちだけ` / `接点つき` | 接点の札 |
  | テキストDSLのシーケンス | `名前だけ` / `説明つき` | 面の説明 |

  検査を **最後の段で描く** ようにしたのが効いた。 段を指さずに描くと `draw:` で後から出す
  要素が 1 つも出ず、発想の枝は根だけ、漏斗は段の名前だけになる。 その状態では書いた欄が
  効いているか測れない。

  記法で書けない項目を並べた手書きの表も直した。 発想の枝 / 系統樹 / 体験の道筋の 3 つは
  既に書けるようになっていたのに、落ちたままだと書いてあった。

- **見本帳の全図を 2 度描いていた陰性対照を 1 つに束ねた** (#1704)

  `catalog-axis-text` と `catalog-bar-look` が「その種別を持たない図に印が出ない」 を
  別々に持っており、**どちらも見本帳の全図を描いていた**。 全件走査では 12 の worker が
  同時に走るので 1 件が 4 秒前後まで伸び、既定の上限 (5 秒) を越えて落ちる回があった。
  **図は壊れていないのに赤くなる** = 落ちたことが情報を持たない。

  1 度だけ描いて `catalog-kind-role-isolation` から両方を見る形にした。 描く回数が
  2 度から 1 度に減り、残った 1 度は `beforeAll` の中で上限を明示している
  (既定の `testTimeout` は変えていない)。

  ついでに **見る範囲が広がった**。 2 件は棒と折れ線の役割名を手で並べていたが、
  束ねた側は種別の一覧を見本帳から導き、役割名が種別の名前で始まるという engine の規則で
  照らす。 種別 9 種すべてが対象になり、engine が種別を足すと自動で追随する。

- **前の時点が消える円グラフの見せ方の切替を出さないようにした** (#1702)

  前の時点を書いた円グラフで見せ方を `積層の弧` か `銘板` に切り替えると、**内側の輪が
  黙って消えていた**。 描画エンジンが前の値を渡すのは `輪` だけで、これは意図した境界
  (`積層の弧` は系列の長さを比べる形、`銘板` は今の値を読む表)。

  誤っているのは見せられない形の切替を出している画面側なので、前の時点を持つ円グラフでは
  `円グラフの見せ方` の切替を出さないようにした。 見本帳の他の切替と同じく
  「効かない操作は出さない」 に揃う。

  判定は engine と同じ条件 (`chartData` のどれか 1 件でも `previous` を持つか) にした。
  「全件が書いてあるか」 で見ると、engine が輪を 2 つ描いている図を画面側が
  「持っていない」 と判定する。

- **見本の抜けを数える軸を、描画エンジンの型から出すようにした** (#1700)

  軸は 2 本 (件数 / 前の時点) を手で並べていた。 種別の一覧は engine から導いていたが、
  軸が手書きなので **導いた範囲が軸の数で頭打ち** になり、2 本目のずれ (#1698) は
  検査ではなく人が見つけていた。

  軸の候補を `chartData` の 1 件が持つ **任意の欄** から出す。 書くか書かないかで形が
  変わりうるのはこの欄だけで、`satisfies` で表を閉じているので engine が欄を 1 つ足すと
  `tsc` が落ちる。

  | 軸 | 出どころ | 形が変わる種別 |
  |---|---|---|
  | 件数 | `chartData` が配列であること (構造の軸) | 1 |
  | `previous` | 見本 1 件の任意の欄 | 3 |
  | `tone` | 同上 | 0 |

  **どの欄が軸として効くかは人が決めない**。 色しか変えない `tone` は engine を描いた結果
  0 種別と判定され、どの見本も要求しない。

  図表の節が持つ `chart*` の欄 6 つも型から出し、中身 (`chartData`) か見せ方の切替の
  どちらかに割り当たっていることを見る。 割り当て先は file の path で書き、その file が
  実際にその欄を触っていることまで確かめる。

- **前の時点を書いた図と書かない図を、両方見られるようにした** (#1698)

  `previous` (前の時点の値) を書くと形が変わる種別が 3 つある。 engine を実際に描いて数えた。

  | 種別 | 書くと増える役割 | 直す前に見えていた形 |
  |---|---|---|
  | 円グラフ | `chart-pie-slice-previous` (内側の輪) | 書かない形だけ |
  | 内訳の帯 | `chart-stacked-bar-period` (2 本目の帯) | 書いた形だけ |
  | 傾き図 | `chart-slope-line` / `chart-slope-period` | 書いた形だけ (書かないと線が引けない) |

  円グラフ と 内訳の帯 に `パターン` の切替を足し、`今だけ` と `前と今` を選べるようにした。
  傾き図は前の時点を書かないと線を引く相手がいないので、形が 2 通りあるのではなく図として
  成立していない = 変種を作らない。

  折れ線には足していない。 node から読む欄が `chartData` と 見せ方 3 つだけで、
  中身が違う形を作る軸が engine に無い。

  形のずれを数える検査を **軸ごとに回す形** へ広げた。 今までは件数の軸しか見ておらず、
  この ずれ も人が見つけた。 覆えない組 (傾き図 × 前の時点) は理由を書いて残し、
  engine が形の差を返さなくなった組が表に残っていると検査が落ちる。

- **図の上の切替を オプション と パターン の 2 群に分けた** (#1696)

  切替には性質の違う 2 種類がある。 `オプション` は 1 つの記法を変換する操作で、押しても
  図に載る項目と値は変わらない。 `パターン` は複数の記法から選ぶ操作で、押すと中身が
  入れ替わる。 混ぜて並べると、押す前にどちらが起きるか読めない。

  | 群 | 中身 | 出る図 |
  |---|---|---|
  | オプション | 再生速度 / 2 段目以降 / 図の色味 / 折れ線 / 円 / 傾き | 全部 (中身は今までどおり図ごと) |
  | パターン | `1 件` / `複数` | 変種を持つ図だけ (今は 大きな数字) |

  #1694 で足した `大きな数字 (複数)` は一覧の別行だったが、行を消して パターン の切替に
  一本化した。 一覧は「この記法でこう描ける」 の目録なので、同じ記法の中身違いが行を持つと
  項目の数 (16) と記法の型の数 (15) がずれる。

  見本の書き方を 1 つ足した。 `pattern__<元の見本>__<名前>` で export した図は一覧の行に
  ならず、元の見本の変種として束ねられる。 元の見本の名前は `patternBase__<元の見本>` で
  書く (既定値は置かない = 名前を書き忘れると読み込みが落ちる)。

  変種も画面に出るので、検査の母集団からは外していない。 一覧を走査する 4 つの検査
  (重なり / 図になる前 / 動き / 記法の一致) は変種を含めて数える。

- **大きな数字に複数件の見本を足し、効かない指定を外した** (#1694)

  `@cardenelabs/cdl` を `^0.40.8` から `^0.40.9` へ上げた。

  | 見たもの | 上げる前 | 上げた後 |
  |---|---|---|
  | 1 件の見本の割合 | `100%` (常に固定) | 出ない |
  | 複数件の見本 | 無し | `問い合わせの内訳` (`66.7%` / `24%` / `9.4%`) |
  | 1 件の見本の `previous` | 書いてあるが効かない | 削除 |

  割合は「その図に載っている全件の合計に対する取り分」 なので、件が 1 つだと必ず `100%` に
  なる (`cdl#759` で 1 件の図から外した)。 割合が意味を持つ形が見本帳のどこにも無かったので、
  複数件の見本を足した。

  `chart-stat` は `previous` を 1 度も読んでいない。 書いてあるのに効かない指定を記法の
  見本として見せていたので外した。

  種別が持つ形と見本帳に出ている形のずれを数える検査を足した。 種別の一覧は手で持たず、
  同じ種別を件 1 と件 3 で描いて役割名の差から導く。

- **同心の弧では 2 段目以降の描き方の切替を出さなくした** (#1692)

  残していた 動かすだけ が、弧では押しても読めない絵になる。
  既にある弧の先へ継ぎ足す動きになり、1 段目の「12 時から開く」 と揃わない。
  見本帳は「この記法でこう描ける」 を見せる場所で、読めない描き方を選べる状態にしておく
  理由がない。

  | 図 | 切替のボタン |
  |---|---|
  | 同心の弧 (`chart-radial`) | 出さない (常に 描き直す) |
  | それ以外 | 2 つとも出す (変わらず) |

  写せるかの判定 (`描き方を選べる`) は変えていない。 ここを false にすると弧に
  `描き直す` を写せなくなり、初期値ごと効かなくなる。

- **同心の弧を選んだ時、2 段目以降を描き直しから始めるようにした** (#1690)

  弧は 12 時からどこまで回ったかを読む図で、既にある弧の先へ継ぎ足すと起点を見失う。
  1 段目が 12 時から開く動きなのに、2 段目だけ継ぎ足しになって揃っていなかった。

  | 図 | 2 段目以降の初期値 |
  |---|---|
  | 同心の弧 (`chart-radial`) | 描き直す |
  | それ以外 | 動かすだけ (変わらず) |

  切替の 2 つのボタンは残るので、動かすだけ へ戻して見比べられる。
  止まっている絵はどちらの描き方でも同じで、変わるのは 2 段目の動き方だけ。

  初期値は図の節の種別から導く。 項目の id を並べた表は持たない = 見本が増えた時に
  表の更新を忘れると、新しく足した弧の図だけ古い初期値で出る。

- **折れ線グラフの字の大きさを棒グラフと揃え、目盛りを役割名で拾えるようにした** (#1688)

  `@cardenelabs/cdl` を `^0.40.7` から `^0.40.8` へ上げた。

  | 見たもの | 上げる前 | 上げた後 |
  |---|---|---|
  | 折れ線の値札 / 横軸の名前 | `11` / `12` | `12` / `11` |
  | 折れ線の目盛りの役割名 | なし | `chart-line-tick` |
  | 折れ線の横軸の名前の役割名 | なし | `chart-line-label` |

  折れ線だけ値札が横軸の名前より小さく、棒グラフ (`12` / `11`) と順が揃っていなかった
  (`cdl#753`)。 縦軸の目盛りと横軸の名前には役割名が無く、検査は字の中身で拾うしか
  なかったため、同じ数が 2 か所に出ると壊れる形だった (`cdl#755`)。

  目盛りの刻みの計算を 1 つに寄せた `cdl#751` も入るが、出る目盛りは変わらない。

  記法 (`*.cdl.ts`) は 1 file も変えていない。 棒グラフと円グラフの見た目も変わらない。

- **描画エンジンを `0.38.0` に揃えて描く速さを直した** (#1666)

  `@cardenelabs/cdl` を `^0.37.0` から `^0.38.0` へ上げた。 見本帳と公開 package の
  2 つを同時に上げている。

  図を描く動きが段の曲線 (`easeOutQuint`) を通っていたため、 前半に動きが寄っていた
  (cdl#711)。 曲げる前の進みで進める形に直り、 ペンが一定の速さで動くようになった。

  見本帳の「週ごとの応答時間」 で 5 点の値が出る時刻を測った。

  | | W1 | W2 | W3 | W4 | W5 |
  |---|---|---|---|---|---|
  | 上げる前 | 0 | 85 | 162 | 312 | 1205 |
  | 上げた後 | 0 | 313 | 573 | 922 | 1214 |

  4 点が 312 ms の間に固まって最後まで 893 ms 空いていたのが、 260-349 ms の間隔で
  1 つずつ出るようになった。 円グラフの扇も 180 度に達する時刻が 156 ms から
  595 ms へ動いている。

  描き終わった図の形は変わらない。 折れ線の座標を上げる前後で書き出して突き合わせ、
  完全一致することを見た。

- **描画エンジンを `0.37.0` に揃えた** (#1664)

  `@cardenelabs/cdl` を `^0.36.0` から `^0.37.0` へ上げた。 見本帳と公開 package の
  2 つを同時に上げている。

  傾き図の見せ方 (`chartSlopeForm`) に `rank` が足され、 書くと縦の位置が値ではなく
  順位の段 (等間隔) になる (cdl#707)。 この版を入れないと見本帳の切替に順位を足せない。

  欄を書かない図の絵は 1 文字も変わらない。 上げる前後で「経路別の申込み」 の線の形と
  値の字と名前の色を書き出して突き合わせ、 画像も含めて完全一致することを見た。

### Removed

- **箱の中の要素ごとの位置の欄 (`actors[].nodes`) を外し、部品が持たない状態の名前を書いた時に知らせるようにした** (#1976)

  `nodes: { header: { posX: 100, posY: 50 } }` は、1 人に `{名前}-{要素名}` の形で複数の箱を作る図種で要素ごとに位置を決める欄だった。
  #1466 で順序図が 1 枚の板になってから、その形の箱を作る図種は 1 つも無く、書いても置き場所は変わらず「当たる箱が無い」 と知らせるだけの欄になっていた。

  部品 (parts) の中の要素に当てる形は採らなかった。
  欄は絶対座標で、部品は位置と倍率ごとまとめて動くため、要素 1 つだけを絶対座標で止めると部品を動かした時にその要素だけが取り残される。

  | 書き方 | 外した後 |
  |---|---|
  | 記法の中括弧の形 / 縦に並べた形 | その行を指して `項目名が読めません: "nodes"` |
  | JSON | 知らない項目として `$.actors[0].nodes` を返す |
  | 部品 | 部品が持たない状態の名前として知らせる (下の知らせ) |

  型 (`DslActorNodeOverride` / `JsonActorNodeOverride`)、JSON の型定義の欄、当たる箱が無い時の知らせ (`sub-node-not-found`) も外した。

  部品に書いた未知の名前は、状態の上書き (`stateOverride`) に入るのに何も知らせず黙って捨てていた (`typo: 3` と書いても図は変わらず、知らせも出なかった)。
  部品が持たない状態の名前を書くと `part-state-missing` の知らせを出し、その部品が持つ状態の一覧を添える。
  段 (`phase`) は部品の状態でなくても効くので知らせない。

### Fixed

- **カタログで項目を選び直すと、最初の 1 コマだけ前の項目の見せ方で描かれていた** (#2022)

  見ている 1 件だけに効く 7 つの見せ方 (速さ / 2 段目以降の描き方 / 色味 / 折れ線 / 円 / 傾き / パターン) を、
  描き終えた後の効果で既定へ戻していた。 戻すのが描いた後なので、選び直した最初の 1 コマは
  **前の項目の見せ方で新しい図が描かれ**、その後に描き直されていた。

  2 段目以降の描き方は既定が図ごとに違う (弧は継ぎ足すと起点を見失うので描き直しから始める、#1690) ため、
  切替を 1 度も触っていない人にも出る。 弧の図から折れ線の図へ移ると、折れ線が 1 コマだけ
  2 段目以降も引き直す形で描かれていた。

  効果で戻すのをやめ、**どの項目の見せ方かを値と一緒に持って描く時に読み替える** 形にした。
  同じ画面の拡大の倍率 (#1745) と並びの倍率 (#1749) が既に使っている形に揃えている。
  選び直した 1 コマ目から新しい既定で描かれ、描き直しも 1 回減る。

  静的検査の `react-hooks/set-state-in-effect` の注意が 1 件から 0 件になり、`pnpm lint` の注意の上限を 1 から 0 に下げた。
  #2006 で上限を付けてから、注意は 12 → 6 → 3 → 1 → 0 と下がった。

- **画面が一度古い表示を出してから描き直されていた** (#2018)

  3 か所とも、最初の描画で古い値を出し、描き終えた後に正しい値を読んで描き直していた。

  | 場所 | 最初の描画で出ていたもの | 直した後 |
  |---|---|---|
  | 画面全体の言語 | 英語を選んだ人にも、開いた直後に 1 回日本語 | 最初の描画から URL の `?lang=` か保存した言語 |
  | 頁の上の配色の切替ボタン | 暗い配色の人にも、頁を移るたびに 1 回「暗い配色に切り替える」 と明るい側の印 | 最初の描画から保存した配色か端末の設定 |
  | カタログの部品の頁の一覧の欄 | 別の縦列から移った直後に「該当する項目がありません」、読み込みに失敗した後に戻ると前回の「読み込みに失敗しました」 | 移った描画から「読み込み中…」 |

  言語と配色は、描いた後に読むのをやめて最初の描画で読む形にした。
  画面はサーバーで作った HTML に後から繋ぐ形ではないので、最初の描画で保存を読んでも食い違わない。

  部品の頁は、読み込み中かどうかを状態で持つのをやめ、縦列と読み込みの結果から導く形にした。
  `/catalog/:slug` は縦列が変わっても頁を作り直さないため、状態で持つと前の縦列の値が最初の描画に残っていた。
  部品の頁を離れる時に読み込みの結果を空へ戻すので、戻った時に前回の失敗を出さない。

  静的検査の `react-hooks/set-state-in-effect` の注意が 4 件から 1 件になり、`pnpm lint` の注意の上限を 6 から 3 に下げた。

- **部品を置いた図で、既存の箱の下に大きな空きが入っていた** (#2002)

  空きは箱の数と一緒に増えた。 部品どうしの間が 120 なのに対し、箱 2 つの図で 496、箱 4 つの図で 720、
  高さ 600 の箱 1 つの図で 840 が空いていた。
  空いた分だけ図が縦に伸びるため、器に収めた時の倍率が落ちて題が読めなくなっていた。

  原因は確保する縦の場所を箱の **数** から概算していたこと。
  `flow` の箱は実際には高さ 68 で 168 おきに並ぶのに、箱 1 つを 1 段 (280) と数えるため
  1 段あたり 112 ずつ余分に見積もり、誤差が箱の数に比例して積み上がっていた。

  既存の図を組み立てて図枠の下端を測り、その 120 下から並べ始める形に変えた。
  測るのは箱ではなく図枠 = 格子は図枠どうしを 120 空けて並べるので、箱で測ると既存の図との間だけが
  部品どうしの段の間より狭くなる。

  | 図 | 直す前の空き | 直した後 |
  |---|---|---|
  | 箱 2 つ + 部品 1 つ | 496 | 240 |
  | 箱 4 つ + 部品 1 つ | 720 | 240 |
  | 高さ 600 の箱 1 つ + 部品 1 つ | 840 | 240 |

  カタログの `注文を受けてから出荷するまでの間に設備の稼働を置く` は viewBox が 525x1232 から
  525x976 に縮み、拡大の器 (1150x630) での題が 11.3px から 14.2px になった (下限 12px)。

  既存の箱が 1 つも無い図 (部品だけを並べる図) は今までどおり 240 から並べ始める。
  「箱が無い」 を下端 0 として扱うと、部品が図の上端へ貼り付く。

  組み立て側と画面側が同じ物差しで測るよう、受け渡す値を段数 (`partsBaseRows`) から
  図枠の下端 (`partsBaseBottom`) に変えた。

- **部品を 1 行の形で書くと、`位置` / `大きさ` / `色` が黙って消えていた** (#1996)

  `- 甲: { kind: edge-chain, 大きさ: 900,400 }` と書いても、書かない時と 1 ピクセルも変わらず
  (どちらも `viewBox 1951x552` / 部品の幅 896)、知らせも 1 件も出なかった。
  書いた人から見えるのは「書いたのに図が変わらない」 だけだった。

  部品の中括弧に書いた名前は状態の上書きになるため、読めない名前を知らせる側は部品では何もせずに戻っていた。
  一方この 3 つは予約の一覧に載っているので状態の上書きからも外れる。
  どの欄も読まず、状態にも入らず、誰も知らせない隙間に落ちていた。

  | 書き方 | 直す前 | 直した後 |
  |---|---|---|
  | 部品の 1 行の形 | 黙って消える | 「項目名が読めません」 と知らせ、段を分けた形を案内する |
  | 部品の段を分けた形 | 座標と幅高さと色に入る | 変えない |
  | 普通の箱の 1 行の形 | 「項目名が読めません」 | 変えない |
  | 部品の状態の名前 (`v` / `count` 等) | 状態の上書きになる | 変えない |

  知らせる名前は手で並べず、予約の一覧から 1 行で読める一覧と状態の明示欄 (`state`) を引いた差から導く。
  カタログ 502 図は 1 図も指摘が増えない。

- **別の段にある部品の縦列の境目に、矢印の説明文が掛かったと誤って知らせていた** (#1993)

  格子に並べた部品の 2 段目の矢印の説明文が、1 段目の部品の縦列の境目に掛かったと、描画側の検査
  (`lane-border-clearance`) に誤りとして数えられていた。
  1 段目の縦列は 2 段目まで伸びておらず、部品用の縦列の枠は絵に出ない (#1990) ので、絵の上では何にも掛かっていない。
  編集画面は描画側の検査の結果を位置関係の警告の帯に出すため、利用者にも誤りとして見えていた。

  検査は説明文の横の範囲と縦列の横の範囲だけを比べ、全ての縦列を「図の上下いっぱいの列」 として扱っていた。
  描画側 (`@cardenelabs/cdl`) で、縦の範囲を持つ縦列 (枠を描く縦列と、座標と高さを指定した縦列) は説明文と
  縦に重なる時だけ比べるようにして 0.64.1 を出し (cardene777/cdl#863)、dragon の依存を `^0.64.1` に上げた。

  | 組み立て | 誤りが出る並べ方 (1 段目の 2 つ目に部品 80 種、2 段目に `edge-chain`) |
  |---|---|
  | #1992 の前 | 12 通り |
  | #1992 の後 (描画側 0.64.0) | 51 通り |
  | 描画側 0.64.1 | 0 通り |

  カタログ 502 図の配置 (矢印の経路と説明文の位置) は 1 図も変わらない。

- **部品を図に置くと、部品の中の要素が部品の頁より詰まって並び、間隔の検査に掛かっていた** (#1992)

  組み込みは部品の要素を座標で置く。 横は部品が書いた縦列の中心、縦は段の番号に近似の送り幅 220 を掛けた位置で置いていた。
  部品の頁では描画側が縦列を広げて箱の間を 70 空け、段の間を箱の高さに合わせて取るため、頁で空いた分が置いた図で消えていた。
  部品 80 種のうち 14 種が、部品の頁では出ない間隔の検査 (`clearance`) の指摘を、図に置いた時だけ出していた。

  | 部品 | 部品の頁の要素の間 | 直す前に置いた図 | 直した後 |
  |---|---|---|---|
  | `wifi-signal` | 横 80 | 横 40 | 横 80 |
  | `bind-equalizer-5` | 横 80 | 横 20 | 横 80 |
  | `gauge-cluster` | 横 90 | 横 60 | 横 90 |
  | `bandwidth-meter` | 横 90 | 横 60 | 横 90 |
  | `bind-grid-4` | 横 80、縦 100 | 横 40、縦 40 | 横 80、縦 100 |
  | `traffic-light-stack` | 縦 100 | 縦 60 | 縦 100 |

  部品の頁を配置して要素の中心と縦列の左端と幅を読み、組み込み・大きさの見積り・`大きさ:` の横の基準の 3 つがその値を使うようにした (`部品の置き方を読む`)。
  頁の配置は部品ごとに 1 度だけ行い、部品の枠の計算と使い回す。
  部品 7 つを置いた図の組み立て時間は main と変わらない (実測の中央値 = 1 回目が main 0.6〜0.7ms・直した後 0.4〜0.7ms、2 回目以降が main 0.32〜0.54ms・直した後 0.32〜0.40ms)。
  頁の配置が使えない部品 (配置が失敗する、または縦列や要素の位置が有限の数でない) は、部品が書いた値で置き、頁の値と混ぜない。
  `大きさ:` の縦の基準は段の数 × 220 のまま変えない (#1018 の規則で既に本文が書かれているため)。
  カタログの「部品を箱に使う」 見本の「並べる」 切替に、縦列を 2 本持つ `bandwidth-meter` を足した。

- **部品の名前の名札が部品から離れ、部品を並べると別の部品の上に出ていた** (#1990)

  部品用の縦列は登場人物の名前を名札に持つが、部品の要素は座標で置かれ、縦列は描画側が左から詰めて並べ直していた。
  名札は図の上端に出て、流れ図の途中に置いた部品では先頭の箱の見出しに見えた。
  格子の 2 段目の部品の縦列は 1 段目と重なって右へ送られ、名札が別の部品の上に出て、送った分だけ図が横に伸びた。
  縦列を 2 本以上持つ部品は、縦列の数だけ同じ名前の名札を出していた。

  | 本文 | 直す前 | 直した後 |
  |---|---|---|
  | `flow`、部品 4 つだけ | `稼働2` の名札が `稼働3` の上、図の右に 875 の空き | 4 つとも自分の部品の上、空き 70 |
  | `sequence`、箱 2 つと部品 7 つ | 図の右に 4119 の空き | 空き 85 |
  | 縦列 5 本の `wifi-signal` | 名前の名札が 5 つ | 1 つ |

  全ての部品を組み込んだ後に 1 度配置して部品の要素の高さを測り、部品用の縦列を部品の位置に固定した。
  横は組み込みで決めた位置と幅のまま変えず、縦は部品全体の上に名札の分 (60) を取り、下端を部品の下端に合わせる (図の下の空きは箱だけの図と同じ 60 のまま)。
  名前の名札は左端が最も左の縦列だけに付け、部品が自分の縦列に書いた名札は残す。
  縦列に置く部品 (#1980) は図の縦列を使うので変わらない。
  カタログの「部品を箱に使う」 見本に「並べる」 の切替を足した。

- **流れ図の途中に部品を置くと、カタログの図だけ矢印が消えていた** (#1987)

  静止した `type: flow` は登場人物を書いた順に鎖で繋ぐ。 部品も鎖に入っていたため、`受付 / 印 (部品) / 出荷` は `受付 -> 印` と `印 -> 出荷` になり、書いていない並び順の矢印は部品へ繋がずに外す (#1979) ので 2 本とも消えていた。
  編集画面は部品を抜いてから鎖を作るので `受付 -> 出荷` を描き、絵が食い違っていた。

  | 本文 (登場人物の並び / 行) | 直す前のカタログ | 直した後 (カタログと編集画面) |
  |---|---|---|
  | 受付 / 印 / 出荷、行なし | 矢印 0 本 | `受付 -> 出荷` |
  | 受付 / 印 / 出荷、行 `受付 -> 出荷` | 矢印 0 本と `flow-endpoint-not-honored` 1 件 | `受付 -> 出荷`、知らせ 0 件 |

  どの行の端にも書かれていない部品を鎖に並べないようにした。
  部品の一覧に無い部品は仮の箱のまま描くので鎖に残し、大きすぎて取り込まない部品は鎖から外す。
  カタログの「部品を箱に使う」 見本に「流れの途中に置く」 の切替を足した。

- **向きを書いた流れ図で、矢印に書いた指定が別の矢印に載っていた** (#1986)

  `type: flow` に `direction:` を書くと、組み立ては書いた端のとおりに矢印を作る (#1494)。
  一方で矢印へ行の指定を書き写す処理と端の知らせは、向きを見ずに「登場人物を書いた順の鎖」 として行と対応させていた。

  | 本文 (`A / B / C`、行 `A -> C` と `C -> B`) | 直す前 | 直した後 |
  |---|---|---|
  | `A -> C` に書いた `head: none` | `C -> B` に載る | `A -> C` に載る |
  | 矢印の行番号 (`onEdgeSource`) | 2 本で入れ替わる | 書いた行を指す |
  | `flow-endpoint-not-honored` | 2 件 | 0 件 |

  鎖で繋ぐかの判定を 1 か所 (`鎖でつなぐ形か`) に集め、組み立ての振り分けもその判定を使うようにした。
  端の知らせの添え書きは、書いた端のとおりに繋ぐ回避策として `direction: 縦` を先に挙げる (並びは鎖の時と変わらない)。
  カタログの「流れ図の並ぶ向き」 見本に「書いた端のとおりに繋ぐ」 の切替を足した。

- **部品へ引いた矢印が、知らせも無く図から消えていた** (#1979)

  部品を置くと組み立ては仮の箱を消して部品の図の要素を足すが、仮の箱へ引いた矢印も一緒に消していた (実測 = 部品の一覧を渡すと矢印 1 本が 0 本になり、知らせは 0 件)。
  矢印を部品の中の要素へ繋ぎ、繋げない時は矢印ごとに知らせるようにした。

  | 部品 | 矢印に書くもの | 結果 |
  |---|---|---|
  | 要素 1 つ (80 件のうち 62 件) | 何も足さない | その要素に繋ぐ |
  | 要素 2 つ以上 (18 件) | 矢印の欄 `toPartNode` / `fromPartNode` に要素の id (`{ toPartNode: topL }`) | 名指しした要素に繋ぐ |
  | 要素 2 つ以上 | 名指しなし | 矢印を外し、`part-edge-dropped` で要素の id の一覧と書き方を知らせる |
  | 名指しした要素が部品に無い / 大きすぎて取り込まなかった部品 | — | 矢印を外し、`part-edge-dropped` で知らせる |

  要素が 2 つ以上ある部品は外枠を持たず同格の要素が並ぶ (信号の 3 灯、星 5 つなど) ため、1 つを自動で選ばない。
  名指しの欄を部品でない端や順序図 (`sequence` / `solidity`) に書いた時は `part-node-ignored` で知らせる。
  記法と JSON の矢印に `fromPartNode` / `toPartNode` を足した。
  編集画面の本文欄は、部品へ矢印を引いた本文を重ねずに組み立て側で部品ごと描く (重ねた部品は図の外の層で、矢印の端にできない)。
  カタログの「部品を箱に使う」 見本に「矢印を繋ぐ」 と「繋ぐ要素を名指しする」 の切替を足した。

- **縦列に属する部品が、別の縦列の上に描かれていた** (#1980)

  位置を書かない部品は格子に並ぶが、格子は縦列の位置を読まない。
  部品の要素だけが縦列に属し、描く位置は別の縦列の上になっていた (実測 = `lane: b` を書いた部品が縦列 b の 480〜890 ではなく 60〜420 に描かれた)。
  位置 (`posX` / `posY` / `位置:`) を書かない部品を、次の縦列の中心へ寄せるようにした。

  | 部品 | 置く縦列 |
  |---|---|
  | `lane:` を書いた部品 | 書いた縦列 |
  | 登場人物ごとに縦列を作る図種 (`swimlane` / `state` / `er` / `class`) の部品 | 部品の名前の縦列。 縦列は書いた順に並ぶ |
  | 全員が 1 本の縦列を共有する図種 (`flow` / `topology` / `c4`) の部品 | 今までどおり格子 |

  部品の形は保つ。
  縦に積んだ要素はそのまま積み、縦列を 2 本持つ部品は要素の横並びを保って、縦列を部品の幅と両側 25 まで広げる。
  縦は他の箱の下端から 120 空け、同じ縦列の部品は書いた順に積む。

  部品 80 件を 5 つの形で並べて、直す前と後を比べた。

  | 形 | 縦列の外に描いた部品 | 配置が止まった部品 |
  |---|---|---|
  | `swimlane` / `state` / `er` / `class` (それぞれ) | 62 → 0 | 0 → 0 |
  | `lane: b` を書いた `swimlane` | 68 → 0 | 12 → 0 |

  止まっていた 12 件は、縦列を 2 本持つ部品を 1 本の縦列に入れた時に同じ段へ要素が重なる形で、要素ごとに段を振り直した。
  他の箱の位置の基準になる部品 (`位置: 印 の右 200` の `印`) は、縦列へ動かすと書いた位置関係が崩れるため縦列に置かず、`part-lane-ignored` で知らせる。
  編集画面の本文欄は縦列に置く部品を図の上に重ねず、組み立て側で部品ごと描く。
  組み立て側で描いた部品にも「図の中に描く部品を持たない」 の知らせ (#1017) を出す。
  重ねる部品にしか出していなかったため、部品だけの本文 (#1973) にも知らせが出ていなかった。
  カタログの「部品を箱に使う」 見本に「縦列に置く」 変種を足した。

- **読み出しの札に書いた色の出どころ (`colorSource`) で札の色が変わらなかった** (#1974)

  記法と JSON は札の `colorSource` を受け付けるが、描画側 (`@cardenelabs/cdl` 0.63.0) の札を描く部品が 1 度も読まず、色は札の字でしか引いていなかった。
  描画側で直した版 (cdl#861) に依存を上げ (`@cardenelabs/cdl` を `^0.64.0` に)、カタログの「3 表示を並べる」 見本 (`readoutVariety`) に札の色の引き方を切り替える変種を足した。

  | 変種 | 札の書き方 | 状態を変えた時 |
  |---|---|---|
  | 札に色を付けない (元の見本) | `source: "temp"` だけ | 字は温度、色は付かない |
  | 札の字で色を引く | `source: "state"` と `map` | 字も色も状態に付いて変わる |
  | 別の状態で色を引く | `source: "temp"` と `colorSource: "state"` と `map` | 字は温度のまま、色だけ状態に付いて変わる |

  札の字は白で描かれるため、状態の点と札で使う色を白い字が読める濃さ (`#1f7a45` / `#7a6a5c` / `#c0392b`) に揃えた。
  元の見本の状態の点も同じ色に変わる。
  カタログの値の網羅の検査 (#1969) から `readouts[].colorSource` の「直してから見本」 を外した。

- **中括弧の形で部品に書いた色番号が効かず、編集画面では部品の状態の上書きと色番号が描かれていなかった** (#1973)

  カタログに部品を箱に使う見本を置くと、組み立てと編集画面に次の穴が見つかった。

  | 穴 | 実測 | 直した形 |
  |---|---|---|
  | 中括弧の形 (`- 印: { kind: state-indicator, color: "#d9534f" }`) だけが `color` を状態の名前として拾う | 縦に並べた形と JSON は赤、中括弧の形だけ既定の緑。 部品 80 件に `color` という名前の状態は無く、黙って捨てていた | 3 つの形とも色番号として読み、初期値が色番号の状態へ入れる |
  | 色番号の状態を持たない部品に色番号を書いても知らせない | 部品 80 件のうち 65 件が該当 (`arc-gauge` 等、塗りを図形に直接書く) | `part-color-ignored` を知らせ、塗りは変えない |
  | 部品に色の名前 (`color: 成功`) を書いても知らせない | 3 つの形とも塗りは既定のまま。 部品の色の状態は色番号しか受けない | 3 つの形とも `part-color-ignored` を知らせ、塗りは変えない |
  | 部品 1 つだけの図に中身の無い縦列が残る | 図種が自動で作った `flow` の縦列に仮の箱しか無く、仮の箱を消した後も残った | 仮の箱を消して空になった縦列を消す。 `lanes:` に書いた縦列は空でも残す |
  | 編集画面の本文欄が、重ねて描く部品に上書きを当てない | 箱と並べた部品は既定の値 (塗り 0 と緑) のまま。 部品だけの本文は図が空になり「読み込み中」 で止まった | 重ねる部品には組み立て側と同じ値を当てる (`部品に上書きを当てる`)。 部品だけの本文は抜かずに組み立て側で描く |

  カタログの部品の頁に「部品を箱に使う」 見本 (書かない / 状態を上書き / 倍率を変える / 色番号を変える) を足し、#1969 の覆い方の表から部品の状態と倍率の行を外した。
  段が状態を動かす部品は `phase: false` を書かないと上書きした値が最初の段の値に隠れるため、状態を上書きする切替は段を外して書く。

- **記法で縦列の組 (`groups:`) を書くと、枠が束ねた縦列を囲まず図の右端に離れて描かれていた** (#1972)

  組み立ては枠の縦列を 1 本足すだけで、束ねた縦列の位置を読まなかった。
  枠は全ての縦列の右に高さ 40 の見出しだけで描かれ、図の横幅も枠の分 (流れ図で 2819 → 1861) だけ広がっていた。
  縦列の位置が全て決まった後に、束ねた縦列と中の箱を全て含む位置と大きさで枠を置く。

  枠は作った時から座標を固定し、縦列の並びに加えない (加えると他の縦列の間隔が広がる)。
  縦列と箱と矢印の位置は組を書かない図と同じになり、位置のずらし (#1971) を書いた図でもずらした後の縦列を囲む。

  | 余白 | 値 | 理由 |
  |---|---|---|
  | 上 | 40 | 枠の名札を縦列の名札に重ねない |
  | 横 (囲いを持つ縦列) | 20 | 枠を縦列の囲いの線に重ねない |
  | 横 (囲いの無い縦列) | 0 | 縦列の間に置いた矢印の名前を枠の線が貫かない |

  束ねた縦列が書いたとおりでない時は知らせる。

  | 書いた形 | 扱い | 知らせ |
  |---|---|---|
  | 図に無い縦列を含む | 残りの縦列だけを囲む | `group-lane-missing` |
  | 図に在る縦列が 1 本も無い | 枠を描かない | `group-lane-missing` |
  | 束ねる縦列の間に束ねない縦列を挟む | 間の縦列ごと囲む | `group-lanes-apart` |

  カタログの部品に縦列の組の見本 (書かない / 縦列を束ねる / 2つの組) を足し、#1969 の覆い方の表から組の欄を外した。

- **JSON の箱・縦列・矢印に書いた位置のずらし (`pos`) を読まず、書いても図が変わらなかった** (#1971)

  型の説明は「自動で決まった位置からずらす」 と書いていたが、組み立てが 1 度も読まず、知らせも出なかった。
  配置が決まった後の図の上で、書いた量だけ置き直す。

  | 書いた場所 | 図での動き |
  |---|---|
  | 箱 (`actors[].pos`) | 箱の中心を書いた量だけ動かす。 座標 (`posX` / `posY`) も書いた箱は、座標で置いた位置からずらす。 見本 (parts) の箱は要素をまとめて動かす |
  | 縦列 (`lanes.*.pos`) | 縦列の左上と中の箱を書いた量だけ動かす。 縦列の幅と高さ、他の縦列とその箱は動かない |
  | 矢印 (`flow[].pos`) | 名前のずらし (`labelOffsetX` / `labelOffsetY`) に足し、名前だけを動かす |

  記法は箱と縦列に `offsetX` / `offsetY` を足した (片方だけ書くと残りは 0)。
  矢印は名前のずらしで書き、どれも JSON と同じ図になる。
  ずらした箱が同じ縦列の箱に近づくと、描画側が間隔を保つようそちらを押し下げる。

  動かせない所に書いた時は `position-offset-ignored` か既存の知らせで伝える。

  | 書いた所 | 知らせ |
  |---|---|
  | 名前の無い矢印 | 動かすものが無い |
  | 箱や縦列を描かない図種 (図表など) | 載せる箱や縦列が無い |
  | 順序図の板の箱と矢印 | 効かない欄に `位置` と `名前のずらし` を足した |

  カタログの部品に位置のずらしの見本 (ずらさない / 箱 / 縦列 / 矢印の名前) を足し、#1969 の覆い方の表から位置の欄を外した。
  箱の中の要素ごとの位置の欄 (`actors[].nodes`) はどの図種でも要素に当たらないため、#1976 で直してから見本を置く。

- **記法の中括弧で書いた箱に `color` を書くと、知らない項目名として図ごと読めなかった** (#1969)

  縦に並べた形 (`色:` / `color:`) と JSON の `color` は受け付けるのに、`- 受付: { color: 成功 }` の形だけが `項目名が読めません: "color"` を返した。
  中括弧の形も同じ振り分け (`splitColorValue`) を通し、色の名前は箱の色になる。
  `tone` と両方書いた時は JSON と同じく `tone` を採る。
  読めない値は行番号付きで `色の名前が読めません` を知らせる。
  見本 (parts) では従来どおり状態の上書きとして読む。

- **カタログで縦列の囲いの内側を押しても、縦列を相手にした出来事が届かなかった** (#1969)

  カタログの配色が縦列の囲い (`lane-container`) の塗りを消しており、塗った所だけを押せる既定のまま、内側の当たり判定が図の根に落ちていた。
  押せたのは縦列の名札の字だけだった。
  塗りを消したまま、囲いの内側を押せる形にした (`pointer-events: visibleFill`)。

- **カタログでパターンを切り替えても、入力の部品が前の図の既定値のまま残っていた** (#1969)

  描画側の入力の部品は既定値 (時間の速さの番号など) を作った時にしか読まない。
  カタログは図の部品を使い回していたので、既定の速さを 3 倍にした変種へ切り替えても速さの釦は `1倍` のままだった。
  項目とパターンの組が変わった時だけ図の部品を作り直す。
  速さや配色の切替では作り直さないので、動いている段は巻き戻らない。

- **記法の縦列・視点・矢印の中括弧・組・軸の名前に知らない項目名を書くと、知らせずに黙って捨てていた** (#1968)

  箱 (`actors:`) と段 (`animation:`) は綴り違いを行番号付きで知らせていたが、次の 5 か所は読む欄だけを拾って残りを捨てていた。
  縦列に `widht: 280` と書くと幅が既定のまま描かれ、知らせは 1 件も出なかった。

  | 書いた場所 | 綴り違いの例 | 出るようにした知らせ |
  |---|---|---|
  | 縦列 (`lanes:`) | `widht: 280` | `縦列 l1 の項目名が読めません: "widht"` |
  | 視点 (`viewport:`、1 行の形と縦に並べる形) | `widht: 800` | `viewport の項目名が読めません: "widht"` |
  | 矢印の中括弧 (`flow:` の `{ ... }`) | `haed: open` | `矢印の項目名が読めません: "haed"` |
  | 組 (`groups:`) | `lable: "x"` | `組 g1 の項目名が読めません: "lable"` |
  | 軸の名前 (`axes:` の `x` / `y`) | `lft: "安い"` | `軸 x の項目名が読めません: "lft"` |

  どの知らせにも使える項目の一覧を添える。
  一覧は各欄の表 (`LANE_VALUE_KINDS` / `VIEWPORT_VALUE_KINDS` / `FLOW_INLINE_KEYS`) と型から導き、JSON の入口の一覧 (`ACCEPTED_KEYS`) と一致することを検査で確かめる。
  縦列の `pos` だけは JSON だけが受ける。
  値を書かなかった項目 (`{ widht: }`) も拾う。

  視点を縦に並べる形では、`項目: 値` の形に合わない行 (`- x`) も黙って捨てていた。
  行番号付きで `viewport の行が読めません` と知らせる。
  先頭に `- ` を付けた `- scale: 2` は今までどおり読む。

  図形・部品・つまみの知らせは「図形の の項目名が読めません」 のように助詞が重なっていたので、「図形の項目名が読めません」 に直した。

  `dsl-unknown-keys-1968.test.ts` は、記法の最上位の項目 (`TOP_LEVEL_KEYS`) の全てを鍵に持つ表を回す。
  項目名を持つ 11 の欄は綴り違いの見本を持ち、行番号・使える項目・同じ綴り違いを JSON の入口も誤りにすることを確かめる。
  項目名を持たない 11 の欄 (`title` / `states` など) は理由を書き、最上位の項目を足して表に書き足さなければ落ちる。
  カタログの記法 477 件 (一覧 354 件と部品 80 件、切替の変種を含む) は、新しい知らせを 1 件も出さない。

  既存の検査を 2 件直した。
  `v05-branches.test.ts` の組の見本は、組が読まない `members: [A]` を書いて通っていたので、組が束ねる `lanes` を書く形にした。
  `diagram-scale-parity.test.ts` の視点の見本は、読めない行 `- x` を並べていたので、欄の形の `- gap: 20` にした (区切りの境目を確かめる意図は変えていない)。

- **強調した箱の枠が、書いた節の色 (`tone`) で描かれていなかった** (#1966)

  全ての箱の枠を 1 色に揃えるテーマの規則 (#1111) が、engine が強調した箱に当てる節の色まで消していた。
  カタログに足した「節の色 6 種」 は 6 枚とも同じ朱の枠になった。
  強調した箱に限って、線と同じ色の変数 (`--dragon-edge-tone`) で枠を描く。
  `accent` だけは強調の既定の色のまま残すので、`tone: accent` を書いた既存の図の見た目は変わらない。
  強調していない箱は、節の色を書いても揃えた枠の色のまま。

  `node-tone-frame.spec.ts` が、強調した 5 色の枠が線の同じ色と一致すること、`accent` が強調の既定の色のままなこと、
  強調していない箱の枠が 1 色に揃うことを実ブラウザで確かめる。

- **矢じりが線の太さに埋もれ、三角の向きも、白抜きや開いた矢の形も読めなかった** (#1966)

  線を 7 にした時 (#1598) に輪郭の太さは上げたが、矢じりの大きさ (engine の 14) は据え置いていた。
  三角は線とほぼ同じ幅になり、クラス図の継承の白抜き三角は塗りつぶしに、集約の菱形と依存の開いた矢は点に見えていた。
  矢じりを線の先端を中心に 2 倍に描き、枠の外へはみ出した分を見せる。
  輪郭は拡大で 2 倍になるので書く値を半分にし、画面上の太さは #1598 の 4.36 と 4.98 のまま保つ。
  ER の端 (`one` / `many` 等) は engine が既に 1.8 倍の枠で描いているので外した。

  `edge-stroke-width.spec.ts` は、三角 / 菱形 / 開いた矢 / 三又が 2 倍で枠の外まで見せること、ER の端が 1 倍のままなこと、
  輪郭の太さ × 倍率が 4.36 と 4.98 になることを確かめる。
  設計の記録は `docs/design/class/note.md` の `### 矢じりの大きさも 2 倍にした` に書いた。

- **描画の形の検査が、要素に付いた移動や伸縮で崩れた位置を見逃していた** (#1958)

  工程表の矢印の向きと矢じりの隙間、絞り込み図の幅、折れ線グラフの札の重なり、入れ子の状態遷移図の字の
  はみ出しの 5 軸は、要素の局所の値 (`getBBox` と `d` 属性) で比べていた。 局所の値は要素自身や祖先の
  `transform` を含まないので、状態遷移図の字に `translate(400 0)` を付けて画面の右端が 91 から 491 へ
  動いても、測る値は 66 のままだった。 今の見本では比べる要素どうしが同じ変換行列を持つので判定は
  正しかったが、`transform` で崩れる形は 1 件も捕まえられなかった。

  画面では要素ごとに枠 (`viewBox`) の座標への変換行列と局所の値だけを測り、枠の座標へ直す計算を Node 側の
  1 か所に置いて 5 軸と枝分かれ図の軸が使う形にした。 実証の壊し方は `transform` で崩す形に替え、反転を
  含む行列の外枠の求め方は値を直接渡す検査で固定した。 状態遷移図の字は、字を含む最も近い箱と比べる。

- **枝分かれ図の根の位置の検査が、根の字の局所の座標と始点を落とした枠の中央を比べていた** (#1954)

  描画の形の検査の枝分かれ図の軸は、根が図の中央から縦横とも 20% 以内にあることを見る。 根の字の位置を
  字の局所の座標 (`getBBox`) で測り、祖先の `g` の移動 (`translate(25 128)`) を入れていなかった。 中央も
  枠 (`viewBox`) の幅と高さの半分とし、始点 (`-35 68`) を入れていなかった。 2 つの抜けが打ち消し合って
  通っていただけで、根を持つ箱ごと図の中で動いても字の局所の座標は変わらず、判定は気付けなかった。

  根の字の中心を枠の座標に直して測り (`getScreenCTM` で変換)、始点を入れた中央と比べる形にした。 見本の
  根は枠の座標で (385.0, 367.7)、中央は (397.5, 368) で、描き方は直す必要が無かった。 実証の壊し方は字では
  なく根の親の `g` を動かす形にし、始点を入れた中央の計算は判定に値を直接渡す検査で固定した。

- **検知の 3 層の案内が、実物の命令と検査のファイルとずれていた** (#1955)

  監査の説明書 (`apps/playground-spa/audit-reports/README.md`) の検知システムの節は、層 1 を回帰検査 1 本が
  「全 route × 3 viewport … a11y」 を見ると書いていたが、`pnpm check:cdl` は画面の検査を全て走らせており、
  回帰検査は画面幅 1 つで読み上げの検査も持たない。 層 2 は「100+ diagram」「12 axis」、層 3 は見ていない型
  (四象限図・階層図・体験の道筋) と古い判定 (根は中央から ±10%) を挙げ、この repo に無い CI も案内していた。
  root の `README.md` は `pnpm fix:notation` を「自動適用」 と書き (実際は隣のファイルに書き出すだけ)、
  記法の検査の冒頭の注釈は無い項目 (`report.fixed`) と無い命令 (`pnpm dragon-lint`) を案内していた。

  3 か所を今の実物に合わせて日本語で書き直し、件数は字で書かないようにした。 説明書の層ごとの表には
  「検査のファイル」 と「命令」 を置き、命令を `package.json` で展開した先が表のファイルを走らせることを
  照らす検査 (`audit-readme-detect.test.ts`) を足した。 節に書いた命令とファイルが実在することと、
  `pnpm check:all` が型検査の後に表の層を順に走らせることも照らす。

- **図の型ごとの描画の形の検査の実証が、本番の判定ではなく自前の写しを試していた** (#1952)

  描画の形の検査 (`apps/playground-spa/tests/kind-geometry-check.spec.ts`) は、工程表の矢印の向きや
  絞り込み図の幅の減り方など 8 つの軸を画面から測って判定する。 実証の検査 (`kind-geometry-check.proof.spec.ts`)
  は画面をわざと壊して判定が違反を見つけることを確かめるが、判定を自前で書き写していた。 本番の工程表の
  矢じりの判定を「常に通る」 形に壊しても、実証は 5 件とも通った。 8 軸のうち 3 軸は実証を持たず、2 軸は
  測る対象が 0 件でも通り、折れ線の軸は名前と違って点が 2 つ以上あることしか見ていなかった。

  軸ごとの「見本を開く・待つ・測る・判定する・壊す」 を 1 つの表 (`tests/helpers/kind-geometry-axes.ts`) に
  置き、本番と実証の両方がその表を回す形にした。 表の 1 行は壊し方を持たないと組めない。 各軸は測った対象の
  数の下限を持ち、開いた図が軸の見本 (`presets.cdl.ts` の書き出しの名前から導く) であることを確かめてから
  測る。 見本の一覧の画面は開いた直後に折れ線グラフを出しており、見本を開き損ねると箱の字の軸は折れ線グラフの
  箱を測って通る (見本を開かない変異で実測)。 折れ線の点の数は見本の値の数と一致することを見る。

- **全体の回帰検査が、カタログの一部の分類とファイル名を付けた編集画面を開かず、端末の誤りを 1 画面 2 件まで通していた** (#1950、取り込みは #1951)

  全体の回帰検査 (`apps/playground-spa/tests/full-regression.spec.ts`) は開く画面を手で 15 件並べ、注釈に
  「全 10 route + 404」 と書いていた。 実際にはカタログの分類 11 件のうち 4 件 (`ethereum` / `charts` /
  `parts` / `interactive`) と、ファイル名を付けて開く編集画面 (`/editor/:filename`) を 1 度も開いていなかった。
  端末の誤りは 1 画面 2 件まで通しており、`React` の開発時の二重描画で 1 つの誤りが 2 回数えられるため、
  描画のたびに誤りを出す画面でも検査は通っていた。

  分類の画面はカタログの分類の一覧 (`CATEGORIES`) から導き、画面の行き先の型ごとに開く画面を 1 つ以上
  置いた。 行き先の定義 (`main.tsx` の `<Route path>`) を読み、開く画面も外す理由も持たない行き先を数える
  検査を足した。 開発用の画面にしか無い検査用の頁 (`/__render`) は理由を書いて外す。 端末の誤りは 0 件を求める。

- **監査の説明書が、記法の検査の古い自動修正の例と件数を案内していた** (#1948)

  監査の説明書 (`apps/playground-spa/audit-reports/README.md`) の記法の検査の節は、自動修正の例を #1934 より
  前の出力 (`統計チャート を示す図` など) のまま、実証用の台本を「8 rule 全 detect」、自動修正が見る型を
  「20+ pattern」 と手書きの一覧で案内していた。 説明書を読む検査が無く、記法の検査を変えた PR のどれも
  この節を直していなかった。

  節を今の挙動に合わせて日本語で書き直し、件数と型の一覧を字で書かないようにした。 規則の表の規則・重さ・
  自動修正の欄は、実証用の台本の図に記法の検査を当てて出た指摘と照らし、自動修正の例は書いた説明に
  自動修正を当てた値と照らす検査 (`audit-readme-lint.test.ts`) を足した。 重さの呼び名 (`注意` / `参考`) は
  記法の検査の道具の出力から読む。

- **記法の検査の実証用の台本が、四象限図の偏りの規則を踏まないまま全ての規則を検知したと出していた** (#1944)

  実証用の台本 (`pnpm lint:notation:proof`) は、わざと問題を入れた図に記法の検査を当て、全ての規則が
  指摘を出すことを示す。 期待する規則を台本の中に手で 8 つ並べていたため、規則が 9 つある今も、
  四象限図の項目が 1 つの区画に偏る規則 (`quadrant-single-quadrant`) を踏まないまま
  「期待 rule 全 8 件 検知成功」 と出して終了値 0 で終わっていた。

  期待する規則を記法の検査の実物 (`notation-lint.ts` の `rule: "…"`) から読む形に直し、項目を 4 つ同じ区画に
  置いた四象限図を台本の図に足した。 規則を足して台本の図に踏ませ忘れると、台本は踏まなかった規則を挙げて
  非 0 で終わる。 注釈に書いていた件数 (指摘 8 件、自動修正できる指摘 3 件) は実物とずれていたので消した。
  台本を素の `node` で起動する検査 (`lint-proof-fixture.test.ts`) を足し、台本が読んだ規則を図に実際に
  当てて出た規則と突き合わせる。 別名の dir を通して起動した時に、台本が何もしないまま終了値 0 で
  終わらないことも確かめる。

- **記法の検査の指摘の文に英単語と実装の言葉が残り、図の説明の修正案が自動修正と食い違っていた** (#1940)

  記法の検査 (`lintDiagram`) が書き手に出す指摘の文と修正案を、日本語に開いた。 書き手が打ち込む識別子
  (`dependsOn` / `parent` など) と書き手の値だけを `` ` `` で囲む。 直す前は `chart node "c0" が datum 0 件` や
  `参照先 id を修正 or dependsOn を除去` のような文で、今は「図表 `c0` に値 (`datum`) が 1 件も無く、 図表が描かれない」
  と出る。 全規則の文をカタカナ語と英単語の判定に通す検査 (`lint-message-words.test.ts`) を足した。

  図の説明の規則は、修正案に `「〜 を示す図」 のように` と例を字で書いていたため、#1934 で自動修正の出力を
  変えた後も例だけが古い文型で残っていた。 修正案を自動修正が実際に書く値にし、例を字で持たないようにした。
  括弧の外に実装の言葉がある説明 (`SVG polyline を使う`) は自動修正を当てても字が変わらないのに
  「自動修正できる」 と数えていたので、直した後の説明にもう 1 度当てて消える時だけ数えるようにした。
  消えない説明には直し方の文を返し、例は書き換え先の表から作る。

  修正案が自動修正の値と一致することは、当初は説明 4 通りでしか確かめていなかった。 自動修正が書き換える
  21 型と実装の言葉 4 種を掛けた 84 組すべてで、修正案が自動修正の書く説明そのものになり、直した説明に
  指摘が残らないことを画面側の検査 (`lint-topic-names.test.ts`) が確かめる (#1942)。 実装の言葉の字は
  検出の形の理由 (`hint`) の数と突き合わせるので、形を足して字を足し忘れると検査が落ちる。

- **記法の検査の自動修正が、見本帳と違う型の名前で図の説明を書き換えていた** (#1934)

  記法の検査 (`lintDiagram`) の自動修正 (`autoFix`) は、図の説明が型の名前 (`gantt` など) で始まると
  日本語の説明へ書き換える。 書き換え先が `ガントチャート を示す図` / `マインドマップ を示す図` のように、
  見本帳が #1930 で開いた名前 (`工程表` / `枝分かれ図`) と違う呼び名を使っていた。 自動修正を当てた図の
  説明の名前で見本帳を探しても、同じ型の見本に辿り着けなかった。

  書き換え先を「{何を示すか}を示す{見本帳の名前}」 の形に組み直した (`作業の期間と前後の関係を示す工程表`)。
  名前を差し替えるだけだと `工程表 を示す図` / `状態遷移図 を示す図` と図が重なるため、見本帳の見本の説明
  (`全体に対する内訳の割合を示す円グラフ`) と同じく名前を末尾に置いた。 21 型のうち見本帳に同じ型の見本が
  ある 19 型で名前が見本帳と一致することと、どの型でも図が重ならず残す語の一覧の外のカタカナ語が出ないことを、
  画面側の検査 (`lint-topic-names.test.ts`) が照らす。 自動修正を当てる条件と英語の説明は変えていない。

- **見本帳の分類の表に画面が読まない欄が残り、検査がその欄を画面に出る前提で確かめていた** (#1932)

  分類の表 (`CATEGORIES`) から、画面が 1 度も読んでいない前置き (`eyebrow`) と群れ (`cluster`) の欄を外した。
  前置きは #188 で画面から消えていたが、呼び名と同じ字でないことを「呼び名のすぐ上に出るため」 と確かめる
  検査が残っていた。 この検査を、表の欄 (`slug` / `label` / `desc`) がどれも見本帳の一覧と分類の画面に
  出ることを確かめる検査に置き換え、読まれない欄を足し直すと落ちるようにした。

  意匠 file の見本帳の一覧の札は、見出し 5 件が古い呼び名 (`レシピ集` / `パターン` / `テキスト DSL` /
  `スタイル` / `インタラクティブ`) のままで、実装が番号の横に出す件数を描いていなかった。 明暗 22 枚の札の
  見出しを分類の表の呼び名に、説明を仕様書の表に揃え、件数を足した。 仕様書の札の要素も
  「番号と件数 / 見出し + 説明 / `開く →`」 に直した。

- **見本の名前に、同じ見本の説明や図が日本語で呼んでいる語がカタカナで残っていた** (#1930)

  見本帳と編集画面の一覧に並ぶ見本の名前 432 件のうち、カタカナ語を含む 280 件を 1 語ずつ判定し、192 件の
  名前を変えた。 開く先は、同じ見本の説明と図の題、隣の分類、変更履歴が既に使っている日本語に揃えた
  (`ガントチャート` → `工程表`、`マインドマップ` → `枝分かれ図`、`ステータスドット` → `状態を示す点`、
  `ウォレット` → `財布` など)。 名前を変えた見本は、図の題と、コードのタブに出す記法 2 種の題も同じ名前に
  直した。 `ファネル図` のように語の置き換えだけでは不自然になる名前は、見本が何を見せるかで言い直した
  (`セールスファネル5段` は図の段が 4 つだったため `購入までの4段の絞り込み`)。

  残した語は理由を付けて `残してよいカタカナ語` に 39 語足した。 定訳の無い図の型の名前 (`スイムレーン` /
  `フロー` / `レーダー`)、技術・製品の名前 (`トークン` / `ノード` / `マイクロサービス`)、操作と画面部品の
  名前 (`タブ` / `ボタン` / `アップロード`)、日常語として定着して見本の図も同じ語で描く名前 (`メール` /
  `パスワード` / `クーポン`) の 4 つに分けた。 カタカナ語を含む名前は 280 → 121 件、語は 216 → 59 種に減り、
  残る語はすべて一覧に載っている。

  見本の名前を画面の字として判定に入れ、判定を待つ材料 (`カタカナの判定を待つ材料`) を空にした。 待つ宣言が
  古くなった時 (file が無い / 理由に Issue 番号が無い / 外しても残る語が無い) に落ちる検査と、名前の file を
  待たせると 400 件以上の名前が判定から外れることを確かめる検査を足した。 呼び名の照合に `レーン → 縦列` を
  足し、`スイムレーン` の一部としての `レーン` は数えないようにした。 カタカナ語の天井は字 354 → 195 /
  語 232 → 73 / 延べ 411 → 228 に下がった。

  見本を名前で開く画面検査 8 ファイルと、検査・図の定義・描画エンジンの注釈に残っていた旧い名前も新しい
  名前に揃えた。 図の中の箱の題 (`アークゲージ` など) は Issue の対象外として変えていない。

- **見本帳の分類の説明と見本集の説明と札に、同じものを日本語で呼べる語がカタカナで残っていた** (#1929)

  分類の説明の `コピペ` / `ループ` / `メモ` / `バッジ` / `リッチ` と、部品の分類が並べる
  `バケット / ゲージ / インジケーター / タイムライン` を開いた。 部品の 4 語は仕様書の分類の表が既に
  `入れ物 / 計器 / 指標 / 時系列` と呼んでおり、その呼び名に揃えた。 見本集の泳ぎ線と申請の流れ図は
  `3 レーン` / `2 レーン` を `3 つの縦列` / `2 つの縦列` にし、札の `レーン` も `縦列` にした。

  図の型の名前 (`クラス` / `コンポジション`) と、定訳の無い技術・製品の名前 (`データ` / `イーサリアム` /
  `サーバー` / `ネットワーク` など 15 語) は、見本の図が同じ名前で描いているため理由を付けて残した。
  分類の説明と見本集の札も画面の字として判定に入れ、残す語の一覧の外が 0 件であることを検査が見る。
  カタカナ語の天井は字 390 → 354 / 語 236 → 232 / 延べ 453 → 411 に下がった。

  分類の表の `items` (代表の中身) は画面のどこからも読まれていなかったので消した。 見本集の説明に書いた
  縦列の数を図と照らす検査は `N レーン` の形で数を読んでいたため、書き換えると照合に 1 件も当たらなくなる。
  読む形を `N つの縦列` に変え、箱と線の数と縦列の数のそれぞれに 1 件以上当たることを確かめるようにした。

- **画面の行き先と見出しと釦に、実装や仕様書が日本語で呼ぶものをカタカナで出していた** (#1854)

  行き先の帯は `トップ / カタログ / エディタ / ドキュメント` と出していたが、変更履歴は同じ画面を主に
  見本帳 / 編集画面 と呼び、仕様書は道筋の根を 概要、編集画面の右の領域を 実況表示 と呼んでいた。 帯を
  `概要 / 見本帳 / 編集画面 / 使い方` にし、見出し・道筋・呼びかけの釦・編集画面の操作の名前 (`書き出す` /
  `枠に合わせる` / `表示を戻す`)・参加方法の札 (`不具合の報告`) を同じ呼び名に揃えた。 仕様書の画面一覧と
  意匠 file の画面の枠の名前 (`01 概要` / `02 見本帳の一覧` / `03 見本帳の分類` / `04 編集画面` / `05 使い方`)
  も同じ名前に変えた。

  意匠 file にだけ残っていた古い分類名 `プリセット` は、分類の表と画面が呼ぶ `ひな形` に直した。 呼び名の
  照合に `カタログ → 見本帳` / `エディタ → 編集画面` / `ライブプレビュー → 実況表示` / `バグ → 不具合` /
  `プリセット → ひな形` の 5 対を足し、画面・仕様書・意匠のどこかに古い呼び名が戻ると落ちるようにした。

  画面の枠に残してよいカタカナ語を理由付きで 17 語に決めた (図の型の名前 `シーケンス` / `グラフ` など、
  定訳の無い技術の名前 `データベース` / `ブラウザ` など、定着した操作の名前 `コピー` / `ファイル` など)。
  画面の枠の file 58 件・字 507 件を走査して、一覧の外のカタカナ語が 0 件であることを検査が見る。 見本帳の
  分類の表と見本の名前は判定をまだ当てていない材料として外し、#1929 と #1930 が片付ける。 カタカナ語の天井は
  字 430 → 390 / 語 251 → 236 / 延べ 503 → 453 に下がった。

  あわせて、見本の字を日本語にした取り込みに画面検査の期待値が追随していなかった 3 件を揃えた。 基本パーツの
  見本の小見出し (`wallet` → `財布`、#1894)、動きの見本の状態の値 (`idle` → `待機`、#1881)、C4 の図の枠の
  札 (`System Context` / `Container` → `全体の見取り図` / `動かす単位`、#1886) の 3 つで、いずれも検査の件数は
  変えていない。

- **画面の字の検査が見本の名前を 2 度数え、カタカナ語の数が実数より多く出ていた** (#1927)

  画面の字を集める時に `lib/i18n.ts` を走査して見本の名前を拾い、その後で見本の名前 432 件を全件足していた。
  日本語を含む名前 430 件が 2 回ずつ入り、カタカナ語の天井は延べ 820 回のうち 317 回が重なりだった。
  英語の判定は語の種類で数えるので、重なっても数が変わらず気付かれなかった。

  `lib/i18n.ts` を走査して拾った字から、見本の名前を名前の件数分だけ差し引くようにした。 同じ字が別の画面にも
  あればそちらは残す。 カタカナ語の天井は字 710 → 430 / 延べ 820 → 503 に下がった (語の種類は 251 のまま)。

- **見本帳の一覧に出る図の説明が、記法の技術メモのまま英語で書かれていた** (#1829)

  一覧と拡大の窓に出る説明 129 件のうち 122 件が `7-day week calendar を 7-lane 個別 day 分散 + calendarWeek
  readout 併存` のような記法の語彙の羅列で、読む人には図に何が描かれているかが伝わらなかった。 1 件ずつ図の
  中身を見て、読む人に見える物で書き直した (例 `1 週間を曜日ごとの 7 つの縦列に分け、今週の暦で見せる
  (calendar-week)`)。 呼び名は画面が既に使う 縦列 / 箱 / 矢印 / 段 / つまみ / 部品 に揃え、見せる機能の記法の
  綴りだけを括弧で添えた。 配置の数え上げ (`2 列 2 段`) は図を見れば分かるので書かない。

  説明の英語の検査は天井 (説明 122 件 / 語 423 種 / 延べ 589 回) から 0 件を求める形に変え、日本語を含まない
  説明も数えるようにした。 天井を置いていた間は日本語を含む説明しか数えておらず、英語だけで書いた説明を足すと
  素通りした (書き直す前の時点で該当は無かった)。 カタカナ語の天井は説明 16 件 / 語 23 種 / 延べ 29 回から
  10 件 / 9 種 / 12 回に下がった。 残る `サーバー` / `テスト` などは同じ図の箱の題が使う語で、説明だけを開くと
  図と呼び名が食い違う。

- **見本帳の絵だけを描く箱の題に英語が残り、英語の検査がそれを見ていなかった** (#1882)

  絵だけを描く種別 (`dyn-arc` / `dyn-rect` / `dyn-wave` など) は箱の題と副題を図に描かないので、英語の天井の
  検査はその字を 1 度も読んでいなかった。 描いた字が 0 件の図は 447 図のうち 33 図ある。 図には出ないが、
  詳細画面の「コード」 タブには記法の文字列として出るので、読む人は目にする。

  検査が読む字に箱の題と副題を足した。 差し込み (`{cpu1}`) は値に置き換わって描かれるので、空白に置き換えて
  から語に割る。 広げた判定で `parts.cdl.ts` の 24 図 / 32 語と `animation.cdl.ts` の 3 図 / 8 語が見えたので、
  日本語に開いた (`active` → `稼働中` / `srv-1` → `サーバー 1` / `max cap` → `上限手数料` / `SALE` → `セール`
  など)。 箱の名前は `YAML` の役名と `focus:` の参照、`JSON` の `name` と `focus` も同じ名前に揃えた。 単位の
  `Mbps` と記憶装置の略語の `SSD` は理由を付けて残してよい語に載せた。 英語の天井は 12 ファイルとも 0 のまま。

- **画面と見本帳の英語の検査が、`constructor` のような名前を英語として数えていなかった** (#1922)

  残してよい語の一覧を `in` で照らしていたため、`constructor` / `toString` のような object が元から持つ名前が
  一覧に載っている扱いになり、画面や図に出ても咎めなかった。 一覧の自前の鍵だけで照らすようにした。

- **記法の検査 (`pnpm lint:notation`) が、隣のファイルを読み込む見本で止まらず全ファイルを検査するようにした** (#1918)

  検査の段は見本を素の `node` で読み込んでおり、拡張子を書かない相対の読み込み (`presets.cdl.ts` の
  `./relation-focus`) を解決できなかった。 `presets.cdl.ts` が並びの先頭にあるため、#1758 から 12 ファイル
  445 図のどれも検査されていなかった。 見本を `tsx` の読み込み口で読むようにし、445 図を検査して指摘 0 件で
  終わる。 読み込めない見本は黙って飛ばさず、これまでどおり非 0 で終わる。

- **読み出しに長い和文を書いた時、枠からはみ出さずに省かれるようにした** (#1914)

  かんばんの札の題やログの文など 12 種の読み出しは、長い字を字数で省いていた。 上限は英字の幅で決めてあり、
  和文は上限の字数に届く前に枠からはみ出していた (かんばんの札は使える幅 114 に対し、和文 17 字で 187)。

  描画エンジン (`@cardenelabs/cdl`) を `^0.59.0` から `^0.59.1` へ上げた (`cdl#851` = 上限を半角の桁数と
  読み替えて省く)。 英字だけの字は変更前と同じ所で省く。

  見本帳の図は、どの読み出しの字も上限の桁数に収まっているので描き方が変わらない。 エディタで図を書いた時に効く。

- **認可の流れの図で、やり取りの時刻の最初の札が欠けないようにした** (#1908)

  `interactive-oauth-flow` の時刻の読み出しで、時刻 0 の札 `認可要求` の左半分が図の外へ出て `可要求` と
  見えていた。 札は図の左端から 8 の点の真上に中央揃えで置かれ、札の幅の半分が余白を超えていた。

  描画エンジン (`@cardenelabs/cdl`) を `^0.57.0` から `^0.57.1` へ上げた (`cdl#845` = 端に来る札をはみ出す側で
  揃える)。 見本の字は変えていない。

- **操作できる図の見本 97〜129 図の仮置きの英語を日本語にした** (#1906)

  `interactive.cdl.ts` の 129 図を 4 回に分けて直す最後の回。 記法の 3 か所 (組み立て API /
  YAML / JSON) で、書き手の字と、配列と状態で渡す値を日本語に開いた。

  | 直した所 | 例 |
  |---|---|
  | 箱の題と副題 | `Week 4-5` → `第 4〜5 週`、`Alice CEO` → `佐藤 社長`、`level ≥ 1 で pass` → `段階 1 以上で満たす` |
  | 矢印の札 | `reports` → `率いる`、`POST /pay` → `購入を送る`、`retry 要求` → `再送を求める` |
  | 読み出しの名札 | `Month view (7 column grid)` → `1 か月 (7 列の升目)`、`決済 status` → `決済の状態` |
  | 段の札 | `hover` → `かざす`、`Intro` → `はじめに`、`retry` → `再送` |
  | 配列と状態の値 | `["todo", "Design API schema", "high"]` → `["todo", "API の型を設計する", "high"]`、`worker crash: OOM` → `ワーカーが落ちた: メモリ不足` |
  | 段の説明 | 状態の名前と英語が混じった構成の説明を、その段で何が見えるかの説明に |

  見本の中身を、日本語の画面で成り立つ形に組み直した図がある。

  | 図 | 前 | 後 |
  |---|---|---|
  | 端末 (`cliTerminalSession`) | `git status` / `pnpm test` / `docker ps` と英語の出力 | `ls -1` / `cd 作業場` / `git diff` / `pnpm -v` / `wc -l メモ`。 出力が英語の見出しを持たない命令にした |
  | 決済の流れ (`exemplarPaymentFlow`) | 実在の銀行名 (`MUFG`) と架空の英字の店名 | `発行銀行` と `ひだまり雑貨店` |
  | ログインと通知の流れ (`exemplarLoginFlow` / `exemplarNotificationFlow`) | 判定の一覧に無い製品名 (`Pixel` / `Galaxy` / `Chrome` / `FCM` / `APNs`) | `スマートフォン` / `Android` / `タブレット`、`iOS の通知網` など |
  | 組織図 (`startupOrgChart`) | 10 字を超える名前で省略を見せる | 名前を 7 字までにした。 描画側は字数で省くので、日本語は省いても箱を超える |
  | 記事の札 (`blogArticlePreview`) | 26 / 28 字を超える題と抜粋で省略と折り返しを見せる | 札に収まる長さにした。 日本語は題 15 字・抜粋 18 字ほどで札を超える |

  描画側がこの綴りで読む値は訳していない (かんばんの列 `todo` / `inprogress` / `done` と重さ
  `high` / `med` / `low`、状態の帯の `active` / `idle` / `error`、在席の `online` / `away` / `offline`、
  共有ボタンの鍵 `tw` / `fb` / `li` / `rd`)。 共有ボタンは順位の説明が箱の題と行の値を結ぶので、
  題に鍵を括弧で残して呼び名を添えた (`X (tw)` など)。 知らない鍵で灰の印になる 5 つ目の `hn` は、
  描画側が知る `ig` にした。

  日本語にして描画側が字を縮めた箱は、`main` と同じ測り方で比べて差が 2 以上の 2 か所 (駒の奥の列の副題) を
  直した。 画面ではみ出した週の暦の副題 2 つと記事の札の字も直し、97〜129 図ではみ出す字は 0 件。
  題の見積もり (`interactive-text-readability`) が箱の幅を超えると見た題 2 つは縮めた。

  命令 5 語 (`ls` / `cd` / `git` / `diff` / `wc`) と略語 2 語 (`SMS` / `NPS`)、画像の書式の `png` を
  判定の一覧に載せた。 `interactive.cdl.ts` の天井は `図=53 語=286 延べ=817` から `図=40 語=84 延べ=480` に
  下がった。 97〜129 図に残る 18 図は描画側が書く字と読む値、形が自分で描く字 (`PAY` / `API GATEWAY` など)、
  鍵を題に残した共有ボタンの図だけ (#1877)。

  判定の「経路の専有」 の検査は、図の字を相手にした時の `記法が配る一覧` の専有を 0 件で固定した。
  最後の 1 語だった名札の `Day events` を開いたためで、経路の生存は説明文を相手にした検査が見ている。

- **操作できる図の見本 69〜96 図の仮置きの英語を日本語にした** (#1904)

  `interactive.cdl.ts` の 129 図を 4 回に分けて直す 3 回目。 記法の 3 か所 (組み立て API /
  YAML / JSON) で、書き手の字と、配列と状態で渡す値を日本語に開いた。

  | 直した所 | 例 |
  |---|---|
  | 箱の題と副題 | `Low band` → `低い帯`、`80-89 (blue, default here)` → `80〜89 (青、初期値はここ)` |
  | 矢印の札 | `advance/pause` → `進める / 止める`、`in transit` → `輸送中` |
  | 入力欄と読み出しの名札 | `Elapsed sec` → `経過した秒数`、`Queue (current highlight)` → `再生待ち (いまの曲を強調)` |
  | 段の札 | `ML conf` → `確信度`、`tracking` → `追跡` |
  | 配列と状態の値 | `Tokyo` → `東京`、`["Dark mode", 42]` → `["暗い配色", 42]`、`Server started on port 3000` → `3000 番で待ち受けを始めた` |
  | 段の説明 | 英語混じりの構成の説明を、その段で何が見えるか、何を動かすと何が変わるかの説明に |

  見本の中身として意味を持たない実在のものは、架空の見本に置き換えた。

  | 図 | 前 | 後 |
  |---|---|---|
  | 再生待ちの一覧 (`playlistSongQueue`) | 実在の曲名と歌手名 5 組 | `紙飛行機の午後` / `灯台守` など架空の曲と歌い手 |
  | 検索結果 (`searchResults`) | 実在の場所の URL 5 件 | `手引き › TypeScript` のような架空の所在 |
  | 登録の控え (`signupFormSummary`) | メールの宛先 (`alice@example.com`) | 電話番号 (`090-0000-1234`)。 宛先は英字の綴りしか持てないため |
  | 変更の履歴 (`gitCommitList`) | 英字を含む短い識別子 (`e5f6g7h`) | 数字だけの識別子 (`5830617`) |

  描画側がこの綴りで読む値 (進捗の `running` / `done` / `error`、優先度の `high` / `med` / `low`、
  ログの重さ `info` / `debug` / `warn` / `error`) は訳していない。 入れ替えの状態 (`true` / `false`) を
  そのまま差し込んでいた副題 2 つは、切り替えで何が変わるかの説明にした。

  日本語にして描画側が字を縮めた箱は、`main` と同じ測り方で比べて差が 2 以上の 5 か所を直した
  (副題と値を短くし、`ニューヨーク` の箱は幅を 180 から 200 に広げた)。 縮みが残るのは 7 か所で、
  どれも字の大きさ 1 の差。 題の見積もり (`interactive-text-readability`) が箱の幅を超えると見た
  `TypeScript` を含む題 2 つは、題を言語の名前だけにした。

  製品名 2 語 (`Vite` / `Bun`) と通貨の略号 2 語 (`SOL` / `DOGE`)、時差の表示に描画側が書く
  `UTC` を判定の一覧に載せた。 `interactive.cdl.ts` の天井は `図=75 語=556 延べ=1378` から
  `図=53 語=286 延べ=817` に下がった。 69〜96 図に残る 6 図は描画側が書く字と読む値だけ
  (進捗の値、式の名前と中身、確信度の帯、優先度の値、再生数の後ろの `views`、出席表の角の `day`、#1877)。

- **操作できる図の見本 46〜68 図の仮置きの英語を日本語にした** (#1902)

  `interactive.cdl.ts` の 129 図を 4 回に分けて直す 2 回目。 記法の 3 か所 (組み立て API /
  YAML / JSON) で、書き手の字と、配列と状態で渡す値を日本語に開いた。

  | 直した所 | 例 |
  |---|---|
  | 箱の題と副題 | `Day 1 ▲` → `1 日目 ▲`、`day 3-8 (5 day, largest)` → `3〜8 日目 (5 日、最長)` |
  | 矢印の札 | `handover` → `引き継ぐ`、`gap = target - actual` → `差 = 目標 - 実績` |
  | 入力欄と読み出しの名札 | `Current step` → `いまの段`、`Price (composite tag)` → `値札 (3 つの値)` |
  | 段の札 | `scoreboard` → `大きな数字`、`tachometer` → `回転計` |
  | 配列と状態の値 | `Alice` / `Bob` → `佐藤` / `鈴木`、`["Mon", …]` → `["月曜", …]`、`2 min ago` → `2 分前` |
  | 段の説明 | 英語混じりの構成の説明 (`3-lane (Low 0-1.5 / …) で rating range を分散`) を、その段で何が見えるかの説明に |

  人名は 6 図で同じ対応 (佐藤 / 鈴木 / 高橋 / 田中 / 伊藤 / 渡辺) にそろえた。 配列の値は最初の値と
  段の書き換えの両方を直し、画面で段が巡る間の字を拾って、最後の段の値 (`v2.0 を公開した` /
  `返金しました。 3〜5 日で反映されます`) まで日本語で出ることを確かめている。 名前の入力欄に
  `鈴木 一郎` と書くと、頭文字の読み出しは `鈴一` を出す。

  日本語にして描画側が字を縮めた箱は、`main` と同じ測り方で比べて差が 2 以上の 19 か所を短くした。
  縮みが残るのは 2 か所で、どちらも字の大きさ 1 の差。

  工程表 (`projectGantt`) の副題は、段で入れ替わる期間を固定の数 (`day 0-3 (3 day)`) で書いており、
  段 3 で図の帯 (実装 2〜8 日目) と食い違っていた。 `main` では `0-3` の `-3` が負の数と読まれて
  「固定値を書いた箱」 の検査を素通りしており、`0〜3 日目` に開いたことで捕まった。 描画側の差し込みは
  入れ子の添字を読めないので、副題は工程の役割 (`最初の工程` / `最も長い工程` など) に直した。
  1 週間の図 (`activityPolar`) は、配列の値を箱の題と同じ `月曜` 〜 `日曜` にそろえた。

  製品名 7 語 (`React` / `Svelte` / `Vue` / `Python` / `Rust` / `Go` / `Deno`) と回転数の単位 `rpm`
  を判定の一覧に載せた。 `interactive.cdl.ts` の天井は `図=94 語=716 延べ=1793` から
  `図=75 語=556 延べ=1378` に下がった (延べ 10 は一覧に載せた製品名を持つ 69〜96 図の分)。
  46〜68 図に残る 4 図は描画側が書く字だけ (傾きの目盛 `before` / `after`、星の見出し、
  値札の `You save`、信号の値、#1877)。

- **操作できる図の見本 1〜45 図の仮置きの英語を日本語にした** (#1900)

  `interactive.cdl.ts` の 129 図を 4 回に分けて直す 1 回目。 記法の 3 か所 (組み立て API /
  YAML / JSON) で、書き手の字と画面に出る値を日本語に開いた。

  | 直した所 | 例 |
  |---|---|
  | 箱の題と副題 | `Signal state` → `値の状態`、`Block N+1` → `ブロック N+1` |
  | 矢印の札 | `1. redirect (with client_id)` → `1. 認可画面へ移す (識別子付き)` |
  | 入力欄と読み出しの名札と単位 | `Revenue (k)` → `月の売上 (千ドル)`、` items` → ` 個` |
  | 選択肢と状態の値 | `low` / `mid` / `high` → `低` / `中` / `高`、`online` → `稼働` |
  | 配列で渡す名前と段の書き換え | `Stocks` / `Bonds` → `株式` / `債券` |
  | 雛形で作る箱 (`gridNodes` / `repeatNodes`) | `r0 c0` → `行 0 · 列 0`、`Block {i+1}` → `ブロック {i+1}` |

  選択肢の値は、初期値 / 状態 / 色の対応 / 段の書き換えを同じ値にそろえた。 画面で選ぶと図の字が
  追いかけることを 4 図 (ラジオ / 選択欄 2 つ / タブ) で確かめている。

  日本語にすると箱の枠に収まらず、描画側が字を縮める箱が 19 か所出たので、目で分かる差の出た
  13 か所の字を短くした。 `main` と比べて縮みが残るのは 7 か所で、どれも字の大きさ 1〜2 の差。
  決定木 (`decisionTree`) の矢印の札は「はい / いいえ」にすると札の幅で縦列の間が開き、一覧の枠
  (幅 874px) に収めた時に箱の題が読める下限 (12px) を割ったので、「○ / ×」にした
  (`viewBox` の幅は `main` の 1557 に対して 1521)。

  段の説明の「束ねる」「結び付ける」は、何が何を決めるかを書く形に直した。 `clickToggle` の
  状態 (`initial: "off"`) は入力欄の初期値に負けて画面に出ない値だったので外した
  (`eventVariety` と同じく入力欄だけが値を持つ)。

  単位の `px` を判定の一覧に載せた。 `interactive.cdl.ts` の天井は `図=127 語=909 延べ=2233`
  から `図=94 語=716 延べ=1793` に下がった。 1〜45 図に残る 12 図は描画側が書く字だけ
  (式の名前と中身 / 巻き取りの名前 / 升目の見出し / 真偽の字、#1877)。

- **図の判定が、画面の操作部と入力欄の初期値を読んでいなかったのを直した** (#1898)

  判定は図を `layout()` に通してから描いていたが、画面はどれも図をそのまま渡している
  (見本帳 / 詳細 / 表紙の 3 経路)。 `layout()` の戻り値は入力欄と読み出しを持ち越さないので、
  描画側は **操作を持たない図** と見なし、画面の操作部 (入力欄の名札 / 選択肢 / 読み出しの
  名札と単位) を描かず、入力欄の初期値も差し込まなかった。 実測で `eventVariety` の判定は
  `{lastEvent} · 累計 {received} 回` を読み、画面は `まだ無し · 累計 0 回` を出していた。

  判定も図をそのまま描く形にした。 差し込まれずに残る `{名前}` は見本帳全体で 1 図から 0 図に
  なった。

  天井 0 の file にも、画面では英語が出ていた。 新たに見えた書き手の字を記法の 3 か所
  (組み立て API / YAML / JSON) で日本語に開き、天井 0 のまま保っている。

  | file | 直した所 | 例 |
  |---|---|---|
  | `parts.cdl.ts` | 読み出しの名札 15 図 | `percent ring` → `達成率`、`CPU trend` → `CPU の推移` |
  | `parts.cdl.ts` | 読み出しの単位 | ` bpm` → ` 回/分` |
  | `parts.cdl.ts` | 状態の値 (通知の題と本文、状態帯の状態名) | `active` / `warning` / `error` → `稼働` / `警告` / `異常` |
  | `animation.cdl.ts` | 読み出しの名札と単位 | `累計 kill` → `累計の撃破数` |
  | `ethereum.cdl.ts` | 単位の仮置き | `TKN` → `トークン` |

  状態帯 (`status-timeline`) は状態名から色を引く。 描画側の既定の対応は `active` / `idle` /
  `error` の英語しか知らないので、日本語の状態名に色の対応 (`colorMap`) を渡している。
  `warning` は既定の対応に無く灰色で出ていたので、`警告` には黄を当てた。

  `interactive.cdl.ts` の天井は `図=114 語=674 延べ=1283` から `図=127 語=909 延べ=2233` に
  上がった (操作部の英語を数え始めたため)。 この file は別に 4 回に分けて日本語にする。

- **図の判定が、色の値を英語の語として数えていたのを直した** (#1896)

  語を拾う形は英字始まりの並びを取るので、`#8a5a2a` が `a5a2a` に、`#2563eb` が `eb` に
  割れていた。 #1874 の `0x` の 16 進と同じく、**色の値は開く先が無く、開くと図が別の色を指す**。
  副題の差し込み `hex: {color}` は色を選ぶ部品の値をそのまま出すので、書き手の字を直しても
  消えない。

  実測で `interactive.cdl.ts` の 4 図に 4 種 (`#2563eb` / `#38bdf8` / `#8a5a2a` / `#f97316`)。
  `#` と 6 桁の 16 進を語に割る前に外す形にした。 天井は `語=677 延べ=1288` から
  `語=674 延べ=1283` に下がった。

  **外すのは 6 桁だけ** = 3 桁 (`#fff`) と 8 桁は見本に無い。 `#` の付かない並び (`8a5a2a`) と、
  後ろに 16 進が続く並び (`#8a5a2a0`) は外さない。 色の値の後ろに続く英語 (`#2563eb の Alice`)
  も従来どおり数える。

- **場面の見本の英語を日本語にした** (#1894)

  基本の部品の見本 (`primitives.cdl.ts`) のうち、形を 3 つ縦に並べて 1 つの流れを見せる
  場面の見本 30 図で、見本が書いた字 (箱の題 / 小見出し / 副題 / 段の題と説明と札) を日本語に
  直した。 1 図につき 3 か所 (組み立て API / YAML / JSON) を揃えている。

  | 直した所 | 例 |
  |---|---|
  | 箱の題 | `DEX` → `分散型取引所` / `OracleContract` → `外部データの受け口` / `shop.example.com` → `山田商店の通販` |
  | 小見出しと札 | `wallet` → `財布` / `marketplace` → `売買の場` / `src chain` → `送る側の台帳` |
  | 副題 | `clearing` → `清算` / `yield-bearing` → `利息が付く` / `canary` → `一部へ先に配る` |
  | 段の題 | `crypto 送金` → `暗号資産の送金` / `DevOps` → `開発と運用` |
  | 段の説明 | `code push → CI build/test → cloud deploy。 ...` → `コードを送ると、自動で組み立てて検査し、クラウドへ配備する。 ...` |

  1 段目と 2 段目の題は `1. <箱の題>` の形なので、箱の題と一緒に直した。 札は光る箱の
  小見出しと同じ字にしている。

  製品名と言語名と通貨の略号 (`Infura` / `OpenSea` / `GraphQL` / `TypeScript` / `USDC` /
  `BTC` など 22 語) は原形のまま残し、`残してよい語` に理由付きで載せた。 製品名に含まれる
  普通の英単語 (`GitHub Actions` の `Actions` / `Bitcoin Core` の `Core`) は載せず、見本の名前を
  変えた。 天井は `図=44 語=254 延べ=510` から `図=33 語=34 延べ=84` に下がった
  (`interactive` も下がった)。

  残る 19 図の英語は、描画側 (`@cardenelabs/cdl`) が箱の中に書く固定の字で、#1877 で扱う。
  これで `primitives.cdl.ts` の 89 図すべてで、残る英語が描画側の字だけになった。

- **部品と形の見本の英語を日本語にした** (#1892)

  基本の部品の見本 (`primitives.cdl.ts`) のうち、種別 5 / 縦列 3 / 段 2 / 形 49 の 59 図で、
  見本が書いた字 (箱の題 / 小見出し / 副題 / 値 / 行 / 段の題と説明と札) を日本語に直した。
  1 図につき 3 か所 (組み立て API / YAML / JSON) を揃えている。

  | 直した所 | 例 |
  |---|---|
  | 箱の題 | `report.pdf` → `月次報告書` / `AuthService` → `認証の役務` / `example.com` → `会社案内` |
  | 小見出し | `wallet` → `財布` / `exchange` → `取引所` / `in progress` → `作業中` |
  | 副題 | `{v} req/s` → `毎秒 {v} 件` / `{v} PV/日` → `閲覧 {v} 回/日` |
  | 段の題 | `file` → `shape-file` (記法に打ち込む綴りそのものにした) |
  | 段の説明 | `右上がドッグイアで折り返された rect。 ...` → `右上の角を折り返した四角。 ファイルや文書、報告書を表す。` |

  製品名と規格名と単位 (`MetaMask` / `Stripe` / `ERC` / `ETH` / `NFT` / `ATM` / `km` など
  22 語) は原形のまま残し、`残してよい語` に理由付きで載せた。 天井は
  `図=89 語=438 延べ=953` から `図=44 語=254 延べ=510` に下がった (`interactive` も下がった)。

  残る 14 図の英語は、描画側 (`@cardenelabs/cdl`) が箱の中に書く固定の字
  (`EXCHANGE` / `TRUST` / `function execute()` / `Jul 10` など) で、この repo からは直せない。
  #1877 で扱う。 場面の見本 30 図は別に直す。

- **よく使う形の見本の英語を日本語にして、26 図を厳しい判定に回した** (#1890)

  よく使う形の見本 (`cookbook.cdl.ts`) は Web 開発で頻出する 26 例を順序図で見せる。 天井が
  `図=26 語=149 延べ=272` で、厳しい判定 (#1859) から外れていた。 図は記法から導かれるので、
  直した所は記法の文字列 2 つ (YAML と JSON) になる。

  | 直した所 | 例 |
  |---|---|
  | 仮置きの役名 | `Client` → `利用者側` / `AuthServer` → `認可窓口` / `ObjectStorage` → `保管庫` |
  | 矢印の字 | `click login` → `認証を始める` / `enqueue job` → `仕事を積む` / `201 Created` → `201 作成済み` |
  | 段の題と札 | `respond` → `返す` / `consent` → `同意の確認` / `page 1` → `最初の 20 件` |

  HTTP の要求の種類 (`PUT`) と SQL の命令と句 (`INSERT` / `WHERE` / `ORDER BY` / `DESC` /
  `LIMIT`) と書式や規格の名前 (`JSON` / `CSV` / `JWT` / `transfer`) は綴りが決まっているので
  残し、`残してよい語` に載せた。 一覧は 12 file すべてに効くので `interactive` と
  `primitives` の天井も下がった。

  長い英語の役名が短い日本語になって縦列の幅が縮み、4 図 (`oauth-flow` / `export-data` /
  `email-notification` / `erc20-transfer-solidity`) が一覧の器で読める範囲に収まったので、
  読みにくさを受け入れた図の一覧から外した。

- **組み合わせの見本の英語を日本語にして、12 図を厳しい判定に回した** (#1888)

  組み合わせの見本 (`patterns.cdl.ts`) は処理の形 12 種を 1 図ずつ見せる。 天井が
  `図=12 語=74 延べ=139` で、厳しい判定 (#1859) から外れていた。 この見本は 1 図につき
  3 か所 (YAML / JSON / 組み立て API) を持つので、3 か所を揃えて直した。

  | 直した所 | 例 |
  |---|---|
  | 仮置きの役名 | `Client` → `利用者側` / `Dispatcher` → `配り手` / `Worker 1` → `働き手 1` |
  | 見本のコードの名前 | `decrement(...)` → `残数を減らす(...)` / `if (valid?)` → `もし (正しい?)` / `ValidationError` → `検証の失敗` |
  | 矢印と段の札 | `call` → `呼ぶ` / `submit` → `送る` / `on error` → `失敗した時` |
  | 段の説明 | `edge path が Gateway の上を通る` → `矢印の道筋が入口の上を通る` |

  組み立て API の id (`read` / `hook` / `check` など) は英数字のまま残した。 画面に出ない
  内部の名前で、検査が id で引くため。 SQL の命令 (`BEGIN` / `COMMIT` / `ROLLBACK` / `tx`) と
  入力の形を確かめる枠組みの製品名 (`zod` / `yup`) は `残してよい語` に載せた。

- **見本帳の英語の判定が、最初の段で光らない矢印の札を読んでいなかった** (#1888)

  判定は描いた図の字に、段の題・説明・札を data から足して読む。 描いた図は最初の段で
  光る矢印の札しか出さないため、2 段目から光る矢印の札は 1 度も読まれていなかった
  (`patternLoop` の札を `each item` に戻しても判定が通った)。 矢印の `label` と `sub` も
  data から足し、新たに見えた `TLS` / `SET` (`presets.cdl.ts`) は日本語に開く先が無いので
  一覧に載せた。

- **書き方の見本の英語を日本語にして、16 図を厳しい判定に回した** (#1886)

  書き方の見本 (`text-dsl.cdl.ts`) は箇条書きの記法から図を組み立てて見せる。 天井が
  `図=13 語=58 延べ=82` で、厳しい判定 (#1859) から外れていた。

  **この見本だけ図が記法から導かれる** (`textDslToDiagram(sourceYaml__*)`)。 他の見本の
  ように組み立て API と記法を別々に持たないので、直した所は記法の文字列 2 つ (YAML と
  JSON) になる。

  | 直した所 | 例 |
  |---|---|
  | 仮置きの役名 | `Client` → `利用者側` / `ServiceA` → `役務 A` / `Browser` → `閲覧ソフト` |
  | 状態の名前 | `Idle` → `待機` / `Loading` → `読込中` / `Error` → `異常` |
  | 矢印の字 | `submit` → `送信` / `dispatch` → `振り分け` / `calls` → `呼ぶ` |
  | 段の題と札 | `request` → `要求` / `fetch` → `取得` / `DB update` → `DB を更新` |

  表と型とコードの名前を見せる 3 図 (`textDslEr` / `textDslErMesh` / `textDslClass`) は
  箱の字が SQL やコードに書く識別子そのものなので、`識別子を見せる図` に理由付きで足した。
  SQL と HTTP の命令と道筋の 5 語 (`SELECT` / `UPDATE` / `GET` / `orders` /
  `OrderCreated`) は日本語に開く先が無いため `残してよい語` に載せた。 一覧は 12 file
  すべてに効くので `cookbook` / `interactive` / `patterns` / `primitives` の天井も下がった。

  **記法の状態と値の名前は英数字と `_` しか受け付けない** ため、`count` / `task1_progress`
  のような状態の鍵は英字のまま残した。 これは記法の決まりで、図には描かれない。

  **名前に空白を含むと `focus:` が空白で割れる**。 `役務 A` / `作業 1` を光らせる指定は
  引用符で囲んだ (`focus: ["役務 A", "役務 B"]`)。

- **種別の見本の英語 52 語を日本語にして、21 図を厳しい判定に回した** (#1884)

  種別の見本 (`primitives-extra.cdl.ts`) は箱の種別 21 種を 1 図ずつ見せる。 天井が
  `図=21 語=52 延べ=92` で、21 図すべてが厳しい判定 (#1859) から外れていた。

  **延べ 92 のうち 42 は組み立ての helper の 2 行**から来ていた (`badge("active")` /
  `badge("running")` を全 21 図に付ける)。 `動作中` / `値が動く` に直して 42 が消えた。

  残りは箱の題と小見出しと副題の例示 (`User` / `AuthService` / `in-memory store`)。
  箱の題は記法では節の名前なので、記法の鍵と `focus:` の参照も揃えた。

  製品名と規格名と単位の 11 語 (`Kafka` / `Cloudflare` / `Express` / `Bull` / `SQS` /
  `HMAC` / `REST` / `CDN` / `GB` / `CPU` / `users`) は日本語に開く先が無いため、理由付きで
  `残してよい語` に載せた。 一覧は 12 file すべてに効くので `cookbook` / `interactive` /
  `primitives` / `text-dsl` の天井も下がっている。

  **`Next.js App` だけは製品名を外した**。 綴りに点を含むため語が `Next` と `js` に割れ、
  どちらも普通の英単語なので一覧に載せると他の file の仮置きを見逃す。

- **動きの見本の英語 19 語を日本語にして、10 図を厳しい判定に回した** (#1881)

  動きの見本 (`animation.cdl.ts`) は箱の題 (`Counter` / `Sum` / `Process` / `Step` /
  `Operation`) と状態値 (`idle` / `running` / `done` など) と段の説明の地の文が英語だった。

  箱の題は記法では **節の名前そのもの**なので、`sourceYaml__*` の鍵と `focus:` の参照、
  `sourceJson__*` の `name` も揃えた。 状態値と札は記法が決めた値ではなく **見本の作者が
  選んだ文字列**なので、図に出るぶんを日本語にした。

  | 直した所 | 例 |
  |---|---|
  | 箱の題 | `Counter` → `数え上げ` / `Process` → `処理` |
  | 状態値と札 | `idle` → `待機` / `running` → `実行中` / `preparing` → `準備中` |
  | 段の説明 | `header に badge='preparing' を表示。` → `見出しの札に「準備中」 を出す。` |
  | 段の題 | `Round 2 (山田様 lead)` → `第 2 戦 (山田様が先行)` |

  `gwei` はイーサリアムの手数料の単位で日本語に開く先が無いため、`ms` (#1861) と同じく
  理由付きで `残してよい語` に載せた。 **一覧は 12 file すべてに効く**ので
  `interactive.cdl.ts` の天井も 693 → 692 語に下がっている。

- **部品の見本の段の題を日本語にして、80 図を厳しい判定に回した** (#1879)

  部品の見本 (`parts.cdl.ts`) は 80 図を持つが、天井が `図=14 語=17 延べ=18` で
  厳しい判定 (1 語でも英語が戻れば落ちる、#1859) から外れていた。

  残っていた 17 語は **すべて段の題**だった (`sweep 進行` / `trend 描画` / `→ dark` など)。
  箱の題にも説明文にも 1 語も無い。 段の題 26 件を日本語にして天井を 0 にし、80 図が
  厳しい判定に回るようにした。

  直したのは 26 件で、天井が数えていたのは 18 件。 残り 8 件 (`step 進行` / `state 表示`
  など) は記法が知る綴りなので数に入らないが、**同じ図の中で日本語と英語が並ぶ**ため
  一緒に直した。

  **判定が読む字と、空振りを見る字がずれていた**。 判定は描いた字と段の題の両方を読むのに、
  「1 つも拾えていない」 の下限は描いた字だけを数えていた。 実測で `parts.cdl.ts` の
  50 図が描いた字を 1 つも持たない = 絵だけを描く種別 (`dyn-arc` など) は箱の題を
  `data-cdl-title` の控えにしか置かず、表に出る字が無い。 下限を判定と同じ所から数える形にした。

- **分散台帳の見本に残っていた人名と出来事の名前を片付けた** (#1876)

  送金の図が登場人物を `Alice` / `Bob` と書いていた。 暗号の説明で使う慣例の仮名で、
  日本語の見本に英語のまま置く理由が無い。 `太郎` / `花子` にした。
  縦列の題 (`送り手` / `受け手`) とは別の語にして、箱と列が重ならないようにしている。

  `Transfer` は送金を知らせる Solidity の出来事の名前で、コードに書く識別子そのもの。
  訳すと実物と対応しなくなるので、理由付きで `残してよい語` に載せた。

  `ethereum.cdl.ts` の天井は 9 → 6 語。 残る 6 語 (`execute` / `hash` / `prev` /
  `merkle` / `nonce` / `tx`) は箱の種類を指定すると描画側が中に書く固定の字で、
  見本の宣言のどこにも現れない。 別 Issue (#1877) が扱う。

  **`残してよい語` は 12 file すべてに効く** = `Transfer` を載せたことで
  `cookbook.cdl.ts` の天井も 153 → 152 語に下がった。 一覧に 1 語足すと、その語を持つ
  file がすべて下がる。

- **図の判定が、16 進の値を英語の語として数えていたのを直した** (#1874)

  語を拾う形は英字始まりの並びを取るので、`0x7b3f92c1` が `x7b3f92c1` に割れていた。
  **16 進は値であって英語ではない** = 日本語に開く先が無く、開くと図が別の値を指す。

  実測で 2 file に 3 種 (`0x7b3f92c1` / `0xaf31c9d2` / `0xc1892f4a`)。 語に割る前に外す形にした。
  `ethereum.cdl.ts` は 11 → 9 種になり、0 にする作業の母数が正確になる。

  **`0x` の付かない並び (`af31c9d2`) は外さない** = 普通の英字の並びと見分けが付かない。
  16 進の後ろに続く英語 (`0x1234 の Alice`) も従来どおり数える。

- **図の判定が、段の札の字を 1 度も見ていなかったのを直した** (#1870)

  図の字の判定は描いた結果の字と段の題と説明を見る。 描いた図は最初の段しか説明を出さないので
  段の題と説明を data から足していたが、**段の札 (`badge`) を足し忘れていた**。

  #1869 で `styles.cdl.ts` の英語を片付けた時、札だけを英語に戻す変異が図の判定を素通りした
  ことで見つかった。 実測で札は 649 件あり、**天井 0 の `presets.cdl.ts` にも 5 語残っていた**
  = 「天井 0 = 全部日本語」 が成立していなかった。

  母集団に札を足した結果、12 file のうち 7 file で天井が上がる (語の合計 1509 → 1530、
  延べの合計 2850 → 2974)。 **上がるのが正しい向き**で、いままで見ていなかった分が見える
  ようになっただけ。

  `presets.cdl.ts` を 0 に保つため、残った 5 語を 3 通りに分けた。

  | 語 | 扱い |
  |---|---|
  | `journey` | 記法が配る図の型の名前。 型の一覧を判定から引く形にして通した |
  | `preset` | 手書きの札。 他の見本と揃えて `swimlane` にした (図の型の名前になる) |
  | `fsm` / `mindmap` / `statemachine2` | 記法が自動で付ける札で、型の一覧と綴りがずれている。 理由付きの一覧に載せ、揃える作業を #1872 に切り出した |

  段に足す欄を一覧で持つ形にして、足し忘れた欄を後から確かめられるようにした。

- **線と色の見本に残っていた英語 6 語を片付けた** (#1869)

  #1867 の後、`styles.cdl.ts` (線の種類と色の見本 10 図) に残る英語は 6 種 / 延べ 22 回で、
  `charts.cdl.ts` を 0 にした回の次に 0 へ近かった。

  | 直す前 | 直した後 | どこ |
  |---|---|---|
  | `From` / `To` | `始点` / `終点` | 矢印の左右の箱 (8 図) |
  | `active` | `動作中` | 矢印の札 |
  | `active 状態` | `動いている状態` | 段の題 |
  | `visible` | `見える` | 段の説明 |
  | `inactive` | `停止中` | 矢印の札 |
  | `inactive 状態` | `止まっている状態` | 段の題 |
  | `edge は inactive` | `矢印は停止中` | 札 |
  | `dash` | `破線` | 段の説明 |

  **記法の綴りは残す** = `activate` / `edge` / `style` / `tone` / `dotted-flow` は記法に
  打ち込む字で、照合が既に通している。 同じ file の記法の文字列 (`sourceYaml__*` /
  `sourceJson__*`) も揃えたので、画面の「コード」 タブと図が食い違わない。

  天井を 0 にして、**1 語でも戻れば落ちる厳しい判定**に 10 図を移した。
  12 file の合計は語 1515 → 1509、延べ 2872 → 2850。

- **図の判定が、記法の値を横棒で割って別の英語として数えていたのを直した** (#1867)

  #1865 で図の字の判定が記法の綴りを照合で引くようにしたが、**横棒 (`-`) で繋いだ記法の値が
  割れて照合を外れていた**。 語を拾う側が横棒を区切りとして扱う一方、記法の綴りを集める側は
  横棒を値の一部として拾うため、`dotted-flow` は記法が知っているのに `dotted` と `flow` に
  割れ、`flow` だけが照合を通って `dotted` が「開くべき英語」 に残っていた。

  実測で割れていたのは 6 種 (`dotted-flow` / `external-user` / `merkle-tree` / `message-bus` /
  `traffic-light` / `user-group`)。 記法が横棒繋ぎの値として知っている塊を、語に割る前に
  外す形にした。

  **塊の一部が記法の値でも、全体を知らなければ外さない** = `user-alice` を外すと `alice` まで
  消える。 記法と無関係な `Alice-Bob` は従来どおり断片ごとに数える。

  3 file の天井が下がる (語の合計 1522 → 1515、延べの合計 2903 → 2872)。 #1865 と同じく
  数え方が変わっただけで、図の字は 1 文字も直していない。

- **見本の図の判定が、記法に打ち込む字まで「開くべき英語」 に数えていたのを直した** (#1865)

  見本帳の図に残る英語を数える判定は、許す語を**手書きの一覧 21 語**だけで持っていた。
  ところが**見本帳の図は記法そのものを見せる場所**なので、箱や段の字に記法の綴りが
  そのまま出る (`fill 進行` / `flow 通過` / `dotted`)。 訳した瞬間に図が別の意味になる字を、
  判定は開く対象として数えていた。

  画面の字 (#1825 / #1827) と図の説明 (#1830) は記法が知る名前を 4 経路から集めて照合する
  仕組みを既に引いており、**図の中の字だけが引いていなかった**。 同じ 2 つの関数
  (`記法が知る名前()` + `綴りの照合()`) に通す形にした。

  12 file すべてで天井が下がる (語の合計 1842 → 1522、延べの合計 3897 → 2903)。 図の字は 1 文字も
  直していないので、**数え方が変わって減っただけ**。 実物を直して減らした回 (#1861) と
  混ぜて読まないよう、天井の説明に理由を書いた。

  手書きの一覧は製品名 / 規格名 / 単位なので残す。 ただし**経路と重なる語は持たない** =
  重ねて持つと、経路がその語を落とした日に手書きの側が黙って覆って気付けなくなる。
  死蔵を見る検査と、4 経路それぞれが図の字で守りを足していることを見る検査を足した。

- **記法の綴りの照合が、型の説明文の作り話の名前まで通していたのを直した** (#1863)

  画面の英語が「記法に実在する綴りか」 を照合する相手のうち、`cdl の型の値` の経路は配られた
  型宣言 (`dist/*.d.ts`) から引用符の中身を拾う。 doc には「値の union を集める」 と
  書いてあったが、**実際は書き手の覚書 (JSDoc) の中の例示まで拾っていた**。

  `.step({ id: "user", title: "User" })` のような説明の中の作り話の名前が 151 語あり、
  `User` / `Browser` / `Admin` / `CEO` などが「記法の綴りだから開けない」 として通っていた。

  覚書を外してから拾う形にした。 図の説明の英語の天井は実物に直って上がる (語 395 → 423、
  延べ 556 → 589)。 **上がるのが正しい向き** で、いままで隠れていた分が見えるようになった
  だけ。 開く作業 (#1829) の母数が正確になる。

  併せて `true` を「記法が一覧を配らない綴り」 へ足した。 真偽は文字列ではないので型の宣言の
  引用符には現れず、以前は覚書の中の `"true"` を拾って通っていた。

- **グラフの見本に残っていた英語 3 語を片付けた** (#1861)

  #1859 で file ごとに天井を置いた結果、`charts.cdl.ts` は 26 図で残り 3 種 (延べ 11 回) と
  0 に最も近かった。 残っていたのは `SNS` / `ms` / `dragon` の 3 語で、どれも日本語に
  置き換える語を持たない (交流サイトの定着した略語 / 時間の単位の記号 / この製品の名前)。

  3 語を理由つきで `残してよい語` へ足し、天井を 0 にした。 **天井 0 の file は 1 語でも
  英語が戻れば落ちる厳しい判定へ自動で移る** ので、`charts.cdl.ts` の 26 図がその相手に
  加わった。

  一覧は 12 file すべてに効くので、足した分だけ `interactive.cdl.ts` と
  `primitives-extra.cdl.ts` の実数も下がる。 天井が実数ちょうどなので、下げ忘れると落ちる
  (実際に落ちたので下げた)。

- **図の中の英語を見る検査が見本帳 12 file 中 1 file しか見ていなかったのを直した** (#1859)

  図の中の字 (箱の名前 / 縦列の名前 / 矢印の名前 / 段の題と説明) に英語が残っていないかを
  見る検査は、描いた結果から字を取り出す唯一の言語の検査 (#1778)。 その母集団が
  `presets.cdl.ts` の import 1 本で決まっており、**見本帳の図 444 件のうち 21 件しか
  見ていなかった**。 外した理由はどこにも書かれていなかった。

  置き場を走査して 12 file 444 件すべてを母集団に入れ、file ごとに英語が残る図 / 語の種類 /
  延べ の天井を置いた。 `presets.cdl.ts` は天井 0 で、いまの厳しい判定 (1 語でも戻れば
  落ちる) がそのまま残る。 厳しい判定の相手は天井 0 の file から導くので、file 名を 2 箇所に
  書かない。

  外す / 入れるの 2 値ではなく数にしたのは、`charts.cdl.ts` のように 26 図で 3 種まで
  減っている file を、手付かずの file と同じ扱いにしないため。 減った分がそのまま見える。

  検査 file の名前を `preset-diagram-words.test.tsx` から `diagram-words.test.tsx` へ
  変えた (母集団が見本帳全体になったので、元の名前が実物と食い違う)。

- **図の説明のカタカナ語が増えても誰も気付かなかったのを直した** (#1857)

  図の説明 (`topics/catalog/*.cdl.ts` の `subtitle__*`) は英語にだけ天井が置かれていた
  (#1830)。 画面の字の側 (#1855) は `topics/` を丸ごと母集団から外しているので、
  **カタカナはどちらの検査から見ても外側に落ちていた**。

  英語の天井と同じ母集団 (`図の説明()`) と、画面の字と同じ拾い方 (`カタカナの連なり()`) を
  引いて、説明 16 件 / 語 23 種 / 延べ 29 回 を天井として置いた。 開く判断は英語と同じ
  #1829 が持つ。

  拾い方そのものの境界は持ち主の検査が見る。 ここの 3 つの数は件数しか見ないので、
  語の中身が入れ替わっても件数が釣り合う形は捕まえられない (変異で実測)。 そのため
  変異試験は 2 つの検査 file をまとめて回す形にした。

- **画面のカタカナ語が増えても誰も気付かなかったのを直した** (#1855)

  画面に出る字のカタカナ語を見ている検査は、見本帳の分類の呼び名だけに閉じていた (#1805)。
  **画面の字の全体は誰も見ていない** ので、新しい画面を足した日にカタカナ語が増えても
  落ちる検査が無かった。

  開く先は実測で決まらない (記法の型の名前 / 実装が既に日本語を使う語 / 候補が複数ある語 の
  3 区分に割れる) ので、開く判断そのものは #1854 が持つ。 待っている間の歯止めとして
  **いまの数を天井として置いた** (図の説明の英語に #1830 で当てたのと同じ形)。

  天井は 字 / 語の種類 / 延べ の 3 つ。 延べが無いと、*既にカタカナを含む字に既出の語を
  足す* 形が 3 つの数を 1 つも動かさずに通る。 実数ちょうどに置いてあるので、
  #1854 で開いた分だけ下げることになる。

  母集団は英語の判定と同じ `画面に出る字たち()` を引く (外す置き場 / 外す材料 の宣言も
  同じもの)。 何をカタカナ語と読むかも `カタカナの連なり()` に寄せ、呼び名の側の検査
  (#1805) がこれを引く形にした。 許す語の一覧だけは見る相手が違うのでそれぞれが持つ。

- **字の検査から外した材料の宣言が古くなっても落ちなかったのを直した** (#1852)

  画面の字の検査は、母集団から外す相手を 2 通りの宣言 (置き場ごと / file ごと) で持つ。
  **置き場の側だけが「宣言が古くなったら落ちる」 検査を持っていた** (#1842)。 file の側は
  実在と理由の長さしか見ておらず、外した file の英語が全部片付いた日に宣言だけが残ると、
  その file は永久に一般の判定の外になる。

  外した file だけを母集団へ戻して残る英語が 1 種以上あることを見る形にした。
  走査の形は置き場の検査と共通の 1 つに寄せている。

  外す理由は「この file だけを見る専用の検査が全語を照合する」 こと。 その対応も誰も
  確かめていなかったので、宣言した file 名が検査 file に現れることを見る検査を足した
  (中身までは見ない = 静的に判定できない)。

  併せて、宣言の理由文から走査で導ける件数を落とした。 #1846 で見本の説明文を母集団へ
  入れた時、たまたま同じ数だったので気付けなかった形。

- **記法一覧に図の向きが載らず、別名が説明文に手書きだったのを直した** (#1850)

  記法一覧 (編集画面の左に出る一覧) は、受け付ける値を記法の実装から引く。 図種 / 箱の種類 /
  起点から描ける図種 / 色の 4 つがそうなっている一方、**図の並ぶ向き (`direction:`) だけが
  載っていなかった**。 受け付ける値の表 (`DIRECTION_ALIAS`) を package から配っていない
  ためで、書式の例が説明文で `(vertical / horizontal)` と手書きしていた。

  字の検査にも同じずれが出ていた。 「記法が一覧を配らない綴り」 の宣言が
  `vertical` / `horizontal` について「記法は値の説明文の中にしか綴りを持たない」 と
  書いていたが、記法は一覧を持っていて配っていないだけだった。

  表を配り、記法一覧に向きの区画を足した。 別名のまとめ方は色と共通の 1 つにしている。
  宣言からは 2 件が消え、照合で引けるようになった (残る 4 件は記法が分岐で持っており、
  一覧にするには記法側の設計が変わる)。

- **色の名前を英字のまま残す根拠が記法とずれても気付けなかったのを直した** (#1848)

  `teal` を残す理由に「6 色のうちこれだけ記法が日本語の別名を持たない」 と書いていたが、
  **この主張を誰も確かめていなかった**。 理由を見る検査は字数しか見ないので、記法が
  `青緑: "teal"` を足した日に理由は事実と食い違ったまま残り、`teal` は永久に画面の字として
  通る。 数 (「6 色のうち」) も実物から導ける値を手で書いた形だった。

  記法の別名表 (`TONE_ALIAS`) から「日本語の別名を持たない色」 を導いて一覧へ畳み込む形に
  した。 隣の「記法でそう打ち込む名前」 の群が #1825 で採った形と同じ。 記法が別名を足すと
  一覧から色が消え、その色が画面の字として咎められる。

- **編集画面の見本の登場人物の説明文が日本語の歯止めで母集団から落ちていたのを直した** (#1846 / #1847)

  #1844 で登場人物の **名前** は母集団へ入れたが、名前に続く **説明文** は入れていなかった。
  説明文は引用符の中の字を集める粗い経路に載るものの、そこには「日本語を含む字だけを残す」
  歯止めが掛かっている。 歯止めはコードの切れ端を画面の字と読まないためのもので、
  説明文には当てはまらない = `"Postgres"` は箱の小見出しとして画面に出るのに、
  日本語を 1 文字も持たないという理由だけで落ちていた。

  実測で説明文 85 件のうち **64 件が日本語を持たず**、残してよい語を引いた後に
  **28 種** の英字が残った。 起票時の実測 (67 件 / 15 種) は 1 行に複数並ぶ書き方
  (`["id: PK", "email: string"]`) を 1 件で数えていたため少なく出ていた。

  記法の `actors:` から説明文を取り出して母集団へ入れた。 `Postgres` / `Redis` は
  製品の名前なので残す語へ理由つきで足し、`Q1`〜`Q4` は `1期`〜`4期` へ開いた。
  `er` と `class` の識別子 (`id: PK` / `+name: string` ほか 20 種) は
  **見本ごとの宣言** (`識別子を見せる見本`) で通す = 語ごとに許すと `name` / `id` /
  `string` が画面のどこでも通ってしまう。 宣言が「何でも通す」 口にならないよう、
  通るのは `名前: 型` の形をした説明文だけで、宣言した見本でも裸の英語は咎める。

- **編集画面の見本の登場人物の名前をどの検査も見ておらず、英語が残っていたのを直した** (#1844)

  #1842 で見本 (`src/data/editor-samples.ts`) に専用の検査を入れたが、**引用符の中の字しか
  見ていなかった**。 記法の登場人物は `- LB: cloud "ロードバランサー"` のように引用符なしで
  書くので、説明文は拾っても名前そのものは 1 文字も読まれない。 画面には箱の名札として出る。

  一般の判定も同じ字を見ていない (行をまたぐ記法を見本とみなして捨てるため)。
  実測で登場人物 96 件のうち **11 種の英字** が、どの検査にも見られないまま画面に出ていた。

  検査が記法の `actors:` から名前を取り出して母集団へ入れる形にした。
  `DB` / `Python` / `Rust` / `Go` は残す語へ理由つきで足し、`LB` → `負荷分散` /
  `W1`〜`W5` → `1週`〜`5週` / `Ballot` → `投票箱` は日本語に開いた。

  「その語を使っているか」 を見る検査 (死蔵) は、外した置き場と材料も含めて数える形に直した。
  外したのは「英語が残っていないか」 の判定からで、その file の字も画面には出ているため。

- **画面の字を集める範囲が手書きの一覧で、16 file が黙って外れていたのを直した** (#1842)

  画面の字を集める所は置き場を `pages/` / `components/` / `lib/` と手で 3 つ並べていた。
  並べる形は **書き手が思い付いた置き場が上限** になり、`src/` の 76 file のうち
  16 file が母集団の外にあった。 同じ file の別の関数は #1830 で同じ誤りを直しており、
  直した側の隣で直していない側が残っていた。

  走査を `src/` 全域にして、外す方を宣言で書く形にした。 外した置き場 (`topics/`) は
  理由と一緒に書き、**宣言が古くなったら知らせる** = その置き場を母集団へ戻して判定が
  赤くならなくなったら、外す理由をもう持っていないので落ちる。

  走査を広げて 3 つの食い違いが出た。 記法の説明文に `パーツ` が 3 箇所残っていた
  (`部品` へ改名済)。 編集画面の見本の名前 `認証状態遷移 (state-machine)` は括弧の中が
  図の型 (`state`) と食い違っていた。 見本の登場人物が `Client` / `Cart` / `Payment` /
  `Push` のまま画面に出ていた。

  `L1` / `L2` は開けない = C4 の図で段を決める印で、記法が説明文の先頭の綴りを見て枠へ
  振り分ける。 開くと枠が 1 つに潰れる (実測で検査 2 件が赤くなった)。 開けない理由を
  宣言して残した。

  編集画面の見本 (`data/editor-samples.ts`) は記法の型名と印しか英語を持たないので、
  `syntax-forms.ts` と同じく **その file だけを見る検査** が全語を図の型と照合する。
  照合の相手は記法が配る図の型の一覧で、4 経路の照合では引けない
  (集合で配られた一覧を 1 件も読まないため、`flow` が当たっていたのは同名の関数を
  配っているという偶然だった)。

- **呼び名の照合が意匠 file を見ておらず、設計だけ古い語が残っていたのを直した** (#1840)

  呼び名を揃える表 (7 対) を見る検査は、画面 (2 通り) と設計の仕様書の 3 つから字を集めていた。
  **意匠 file (`docs/design/app.pen`) が入っていない**ため、意匠だけが古い呼び名のまま残っても
  検査は緑のままだった。

  実測で `シーン` が 56 箇所 / 14 種、`パーツ` が 4 箇所 / 2 種。 画面と仕様書はどちらも 0 件で、
  意匠だけが 3 つの改名 (#1819 / #1805 / #1821) に追い付いていなかった。

  意匠と実装の照合は **節の見出し (24px 以上) しか見ない**。 残っていたのは説明文と札で
  13〜15.5px なので閾値の下にあり、閾値の下を見る名指しの一覧では改名を追えない
  (改名するたびに 60 行足すことになる)。 呼び名の表で見るのが正しい経路だった。

  意匠の字は 2 箇所に入る。 node 自身の字と、共有部品を使い回した時の差し替えの側。
  実測で前者が 2127 件 / 後者が 39 件なので、**出どころを分けて数える** =
  合計だけだと差し替えの側を丸ごと落としても数がほとんど動かない。

- **図形の検査が古い英語の字で図の中身を探していたのを直した** (#1838)

  折れ線の札を `Jan` 〜 `Dec` で、見本の根を `Project` で探していた。
  図に実際に出るのは `1月` 〜 `4月` と `新しい企画` で、どちらも 1 件も当たらない。

  当たらないと「重なり 0 件」 を期待する側は **空振りのまま緑になる**。
  「1 件以上」 を期待する植え込み対照だけが落ちるので、
  赤い 1 件が緑の 1 件の空振りを教えている形になっていた。

  折れ線の札は役割の印 (`chart-line-value` / `chart-line-label`) で選ぶ形にした。
  印は呼び名を日本語に開いても別の言語に替えても動かない。
  印を持たない見本の根だけは `presetMindMap` の定義から導く
  (根の名前が `{theme}` なので、図が持つ状態の初期値へ解く)。

  **印で選んでも、印の名前が変われば同じことが起きる**。
  0 件を期待する検査は母集団が空でも通るので、集めた件数の下限を別に置いた。
  下限を外すと、印を壊した図で本番の検査がふたたび空振りの緑に戻ることを実測した。

- **仕様書と実装の照合が 2 件赤いまま残っていたのを直した** (#1836)

  どちらも仕様書の側が実装に追い付いていない形だった。

  1 つ目は、数の主張を見る一覧が **仕様書から消えた文を要求し続けていた** こと。
  カタログ一覧の節は「枚数は `CATEGORIES` が決める」 と書く形に直っており、
  導ける値を人手で書かない側へ寄せた跡だったが、直した側が検査の一覧を直していなかった。

  一覧から黙って落とすと「数の主張が消えた」 と「一覧に足し忘れた」 を分けて読めないので、
  **数を書かない節を理由つきで宣言する** 形にした。 宣言した節に数を書き戻したら落ちる。

  2 つ目は、参加方法の手順の名前が仕様書だけ英語 (`branch` を切る / `test` を先に書く)
  で残っていたこと。 画面は日本語に開いているので、仕様書を揃えた。

- **画面の呼び名を開いた時に古い字を持つ検査が赤いまま残っていたのを直した** (#1834)

  画面を動かす検査 (Playwright) を全件回すと 635 件中 9 件が落ちる。 うち 5 件が
  **呼び名を変えた日から赤いまま気付かれていない** 形だった。

  | 検査 | 期待していた字 | 画面の今の字 |
  |---|---|---|
  | 見本の詳細の見出し | プリセット | ひな形 (#1805 / #1807) |
  | 操作で動く分類 | スクロール駆動のフェーズ進行 | スクロール駆動の段進行 (#1821) |
  | 編集画面の自動修正 | 対応可なし | 直せるものなし |

  `pnpm verify` は型検査とテストだけで、画面を動かす検査 (全件 11 分) を回さない。
  明示的に叩いた時にしか走らないので、赤くなってから気付くまでの間隔に上限が無かった。

  出どころが 1 つに決まるものは、検査が実物を引く形に直した (見本の詳細の添えの語は
  分類の一覧から、操作で動く分類の 4 つの名前は見本の名前の一覧から)。

  あわせて、**検査が画面の字として名指しする日本語が実物に出ているか** を見る検査を
  vitest 側に置いた。 名指しの形は 3 つ (呼び出しの引数 / 一覧に置いてから回す /
  名前に置いてから渡す) で、走査は 89 件。 実物に出ない字は 8 件を理由つきで宣言している
  (検査が打ち込む本文と、検査が自分で作って自分で読む字)。

- **画面の名前が設計の表と 2 通りに割れていたのを揃えた** (#1832)

  設計の仕様書 (`docs/design/specs/screens.md`) は画面の一覧を表で持ち、7 番を `更新履歴`、
  8 番を `参加方法` と呼ぶ。 ところが **同じ file の本文と実装だけがカタカナで残っていた** =
  画面は `リリースノート` と `コントリビュート` と書き、表だけが日本語だった。

  設計の表を出どころとして、画面 6 箇所と設計の本文 5 箇所を揃えた。

  | 直す前 | 直した後 | 出どころ |
  |---|---|---|
  | リリースノート | 更新履歴 | 設計の画面一覧の表の 7 行目 |
  | コントリビュート | 参加 | 同じ表の 8 行目 (`参加方法`) |

  `コントリビュート` の揃える先を語幹 (`参加`) に置いたのは、画面に
  `dragon にコントリビュートする` と `コントリビュート方法` の 2 つの形で出るため。
  語幹で揃えると両方が 1 行で直る。

  割れたその日に落ちるよう、#1819 の `揃える呼び名` に 2 行足した。

  読み上げ用の名前 (`aria-label`) も直した。 目に見える字だけを直すと、画面を読み上げて
  使う人には古い呼び名が残る。

  あわせて、**札で 2 つに割れた語を検査が見ていなかった** のを直した。

  ```tsx
  リリース<span className="nm-gradient-accent">ノート</span>
  ```

  この形は `>` と `<` の間を 1 件として数えると `リリース` と `ノート` に割れ、
  `リリースノート` という語がどこにも現れない。 起票時の一覧が 6 箇所だったのに実物が
  7 箇所だったのはこれが理由で、検査に戻す変異を当てても 0 件で通っていた。

  札を外してつないだ行も母集団に足した。 母集団は画面 1532 件 + 札をまたぐ 1229 件 +
  設計 198 件の計 2959 件で、直した後の残りは 0 件。

- **図の説明に残る英語が増えても誰も気付かなかったのを直した** (#1830)

  見本帳の一覧と拡大の窓に出る図の説明 (`subtitle__*`) 129 件が、記法の語彙をそのまま
  書いた技術メモのまま画面に出ている。 日本語に開く作業は呼び名の決め方に判断が要るので
  #1829 で待ちにしたが、**待っている間に増えても気付けない** 状態だった。

  いまの数を天井として置き、超えたら落ちる形にした。

  | 数 | いま | 何を数えるか |
  |---|---|---|
  | 説明 | 122 | 開くべき英語を含む説明の件数 (129 件中) |
  | 語 | 396 | 開くべき英単語の種類 |
  | 延べ | 557 | 開くべき英語の延べ回数 |

  **延べを数えるのが要る**。 説明の件数と語の種類だけでは、*既に英語を含む説明に、
  既に出ている語を足す* 形が素通りする (変異試験で実測 = `pipeline` を 1 語足しても
  2 つの数が 1 つも動かなかった)。

  減った時も落として天井を下げるよう促す。 下回ったまま通すと、一度直した分がまた増えても
  気付けない。

  判定は自前で持たず、画面の字の判定 (`残る英単語()`) と記法が知る名前 (#1827 の 4 経路)
  をそのまま引く。 記法が知る名前は 2 つ目の引き手ができたので
  `lib/notation-names.ts` に切り出した。

  あわせて、判定を引く検査を探す走査を **置き場の列挙から全域走査に変えた**。
  以前は `pages/` / `components/` / `lib/` と並べており、`topics/` に検査を足した日に
  その検査だけが母集団から外れていた = 並べる形は書き手が思い付いた置き場が上限になる。

- **記法の説明文だけが字の検査から外れたままだったのを直した** (#1827)

  画面へ字を渡す材料のうち `lib/syntax-forms.ts` だけが母集団から外れており、
  外した理由は「記法の項目の名前を綴りのまま置くので、残してよい語の仕分けが別に要る」
  だった。 **実測すると仕分け先は 1 つしかなかった** = 説明文 130 件に出る英単語 66 種を
  1 種ずつ読むと、すべて記法で打ち込む綴りで、開ける語は 1 つも無い。

  要るのは英語を開くか残すかの仕分けではなく、**綴りが記法に実在するかの照合** だった。

  この file だけを見る検査を置き、説明文の英語を 1 語ずつ記法と照合する。
  **`残してよい語` には足さない** = `start` / `text` のような一般語が混ざっており、
  一覧に足すと画面のどこでもその語が通る抜け道になる。

  記法が知る名前の経路を #1825 の 2 つから 4 つに増やした。

  | 経路 | 集めた件数 | 記法の名前 16 語での専有 | 説明文 66 種での専有 |
  |---|---|---|---|
  | cdl が配る名前 | 144 | 1 | 0 |
  | cdl の型の項目 | 605 | 12 | 9 |
  | cdl の型の値 | 631 | 0 | 10 |
  | 記法が配る一覧 (`TOP_LEVEL_KEYS` 等) | 64 | 0 | 7 |

  **経路の専有は照合する相手で変わる**。 片方だけを見て 0 件の経路を落とすと、
  もう片方の守りが消える。 4 経路とも、どちらかの相手で専有を持つ。

  記法が配る一覧を集める深さは 1 段に留める。 際限なく潜ると図の仕様の内部まで集まり、
  照合が「何でも通る」 方へ緩む (実測で 64 件 → 587 件に膨らみ、記法が一覧を配っていない
  `vertical` / `horizontal` まで通った)。

  記法が機械で読める一覧を配らない 6 種 (`abs` / `ceil` / `floor` / `round` /
  `vertical` / `horizontal`) は、理由付きの別の表に分けた。
  記法が一覧を配れば経路が覆うので、この表は空に近づくのが正しい向き。

- **画面に残した記法の綴り 16 語が実在するかを誰も見ていなかったのを直した** (#1825)

  画面に英語を残してよい語の一覧には、記法でそう打ち込む語が 16 語ある
  (項目の名前 6 語 = `type` / `posX` …、機能の名前 10 語 = `signal` / `readout` …)。
  訳すと読み手が記法を探せなくなるので綴りのまま残している。

  **この 16 語が記法に実在するかを、誰も見ていなかった**。 一覧は理由の字が
  「記法の…の名前」 で始まるだけの表で、記法との結び付きが 1 つも無い =
  記法が機能を消しても名前を変えても、一覧は古いまま残り検査は通り続ける。

  記法が知っている名前を 2 経路から集めて照合する。

  | 経路 | 実測 | そこでしか覆えない綴り |
  |---|---|---|
  | 記法が配る名前 | 144 件 | `signal` |
  | 記法の型 (`dist/*.d.ts`) | 605 件 (3 file) | `renderOffsetX` / `renderOffsetY` と項目の名前 6 語 |

  組み立て口 (`diagram()` が返す物の名前) は経路に入れない。 実測で 150 件を集めたが、
  そこでしか覆えない綴りが **0 件** だった = 経路を増やしても守りが増えない。

  画面の字と記法の実物がずれる語があるため、一覧を語だけの表から組にした
  (`renderOffset` は記法では `renderOffsetX` / `renderOffsetY` の 2 つ)。
  **`残してよい語` はこの表から組み立てる** = 16 語を 2 箇所に書くと片方だけ直って食い違う。

  照合は **完全一致**。 前置き一致に緩めると、2 文字の語を足した日に記法の適当な名前に
  当たって素通りする (`si` が `signal` に当たる形)。

- **色の名前を記法から導かず手で写していた所を検査で結んだ** (#1823)

  #1821 で色調 5 件の名前を記法自身の別名 (`TONE_ALIAS`) に合わせたが、**導く検査を
  置いていなかった**。 手で写した字なので、記法側が別名を変えた日に片方だけ古くなる。

  効いていた検査は「英語が残っていない」 だけで、それは `accent` が消えたことしか言わない =
  `中立の主張色` を `目立つ色` に書き換えても通る状態だった (英語が 1 文字も混ざらないため)。

  鍵 (`toneAccent`) から記法の色 (`accent`) を導き、`TONE_ALIAS` を逆に引いて別名 (`中立`) を
  取り、値がその別名で始まることを照合する。

  **値そのものは生成しない**。 名前は「別名 + 何の色か」 の組 (`中立の主張色`) で、
  後半を `TONE_ALIAS` は持たない。 生成すると後半を別の表に持つことになり、
  二重管理が場所を移るだけになる。

  逆に引く時は **日本語を含む別名だけを採る**。 1 つの色に別名が複数付くため
  (`accent` は `中立` / `neutral` / `accent` の 3 つ)、英字の別名を採ると
  `accent主張色` が「別名で始まる」 と判定され、#1821 で直した英語がそのまま通る。

  `teal` は記法が日本語の別名を持たないので対象外にし、理由を出力に載せた。

  0 件を期待する検査に母数を足した。 英語の側 (432 件) を見る検査が件数を出していなかったため、
  0 件が「混ざっていない」 か「1 件も見ていない」 かを読み手が分けられなかった。

  ```
  [英語の側] 走査=432 日本語が混ざる=0
  [色調] 対象=5 対象外=1 (teal=記法が日本語の別名を持たない色) 外れる=0
  [出どころ] 色調=5 件 (導く形にした) / 記法の機能=13 件 (10 語、まだ導いていない)
  ```

- **見本帳に並ぶ 432 件の名前を字の検査が 1 件も見ていなかったのを直した** (#1821)

  見本帳の分類の頁は、並ぶ見本 1 件ごとに名前を出す。 その 432 件を持つ `lib/i18n.ts` を
  **母集団から file ごと外していた** (#1815)。 外した理由は「英語の側も同じ file が持つ」
  だったが、字を拾う側は日本語を含む字しか残さないので **英語の側はもともと入らない**。
  道連れになった日本語の側だけが、以後 1 件も読まれないまま残っていた。

  実測で 432 件のうち 70 件に英語。 分類の呼び名 (#1805) と説明文 (#1815) は開いたのに、
  その下に並ぶ名前だけが手付かずで、画面に出る字としては ここが最も多い。

  | 種類 | 件数 | 扱い |
  |---|---|---|
  | 定着した略語と固有名詞 (`JWT` / `IoT` / `ATM` / `OAuth` / `Bitcoin`) | 32 語 | 1 語ずつ理由を書いて残す |
  | 記法の機能の名前 (`signal` / `input` / `readout` / `waterfall`) | 10 語 | 同上。 読み手はこの綴りで記法を探す |
  | 色調の名前 (`accent` / `success` / `error` / `warning` / `info`) | 5 語 | **開く** |
  | 末尾を追う操作 (`tail`) | 1 語 | **開く** |

  色調は作文せず記法自身の別名 (`TONE_ALIAS`) から取った (`success緑` → `成功の緑`)。
  #1815 が同じ 6 色を分類の説明文で開いた時、すぐ下に並ぶ名前は `accent主張色` のまま
  残っていた = 片側だけ開いた状態を閉じた。

  記法の説明が既に日本語で呼んでいる語はそちらへ寄せた (`edge` → `矢印`、`node` → `箱`)。
  機能の名前を途中で切っていた 1 件も実物に合わせた (`repeatとderive` → `repeatNodesとderiveChain`)。

  呼び名の食い違いも 25 件見つかった。 #1819 で `シーン` / `局面` を `段` に揃えた時、
  この file は外れていたので見えていなかった。

  - `フェーズ` 4 件 → `段` (#1819 で揃えた呼び名の 4 つ目)
  - `シーン` 21 件 → `実例`。 **こちらは `段` ではない** = 「図が切り替わる単位」 ではなく
    「複数の部品を組み合わせた実例」 を指す別の意味で、1 件ずつが中に段を 3 つ持つ。
    `暗号資産送金の段` に直すと「3 つの段を持つ 1 つの段」 になって意味が壊れる

  語そのものを開いたので、呼び名の表は `シーン → 段` の 1 対 1 のまま保てる。
  表に「この置き場では咎めない」 の例外を足す案は退けた = 一覧に 1 行足すだけで
  検査を黙らせられる形になる。

  名前を見る口は 2 つに分けた。 材料の字を拾う側は `i18n.ts` を母集団に入れ直し、
  それとは別に表を直に引いて **432 件全部** に判定を当てる検査を足した。
  前者は日本語を含む字しか見ないので、`NFT` / `ATM` のように日本語を 1 文字も
  含まない名前が落ちる (実測で `ATM` が死蔵と判定された)。

- **見本帳の分類の説明文に英語が残っていたのを開いた** (#1815)

  画面の字の検査は `pages/` の画面 file だけを母集団にしており、**画面へ字を渡す材料 file を
  1 つも見ていなかった**。 見本帳の分類の説明文は画面 file の側が差し込み (`{c.desc}`) しか
  持たないので、実物の字が 1 文字も読まれないまま通っていた。

  実測で `lib/catalog.ts` に 29 件、`lib/presets.ts` に 2 件の英語が残っていた。
  #1809 で同じ画面の上の行を `dragon DSL` から `dragon のテキスト記法` に直した時、
  その 16 行下の説明文が `dragon DSL の最小構成要素` のまま残っている状態だった。

  | 分類 | 直す前 | 直した後 |
  |---|---|---|
  | 基本要素 | `lane / node / edge / phase / state` | `縦列 / 箱 / 矢印 / 段 / 値` |
  | 線と色 | `accent / success / error / warning / info` | `中立 / 成功 / 失敗 / 警告 / 情報` |
  | 操作で動く | `user が slider を動かすと…scroll に応じて phase が進む` | `読む人がつまみを動かすと…巻き上げに応じて段が進む` |
  | 動く図 | `phase で状態が推移…badge でシーン表示` | `段で状態が推移…バッジでシーン表示` |
  | 実用例 | `認証 flow / CRUD` | `認証の流れ / データ操作` |

  言い換えは作文せず、記法の説明 (`lib/syntax-forms.ts`) と記法自身の別名 (`TONE_ALIAS`) が
  既に使っている語に合わせた。 `UML` / `TypeScript` / `ERC` / `EIP` / `teal` は
  訳す先が無いので、理由を書いて残す語に足した。

  母集団は「画面と画面の枠が `@/lib/` の形で引いていて、日本語の字を持つ file」 から導く。
  記法の綴りそのものを大量に持つ 2 件 (`syntax-forms.ts` / `i18n.ts`) は理由付きで外した。

- **編集画面が見本と部品を見本帳と別の呼び名で出していたのを揃えた** (#1811)

  編集画面の左の一覧は 3 つの見出しで切り替える。 3 つ目だけが「記法」 で日本語、
  前の 2 つは「サンプル」「パーツ」 とカタカナのまま残っていた。
  どちらも同じものを別の場所では「見本」「部品」 と呼んでいる。

  | 直す前 | 直した後 | 出どころ |
  |---|---|---|
  | サンプル | 見本 | 同じ画面の知らせが既に「見本を読み込めませんでした」 と呼んでいる |
  | パーツ | 部品 | 見本帳の分類の呼び名 (#1805)。 `CATEGORIES[].label` から引く |

  部品の呼び名は字で持たず `CATEGORIES` から引く。 #1805 で見本帳の側だけ呼び名が変わった時、
  この画面が古い呼び名のまま取り残されたのと同じことを繰り返さないため。

  実ブラウザの検査 2 件が一覧の見出しを字で掴んでいたので、目印で掴む形に直した。

- **画面の枠の検査が、改行をまたぐ字を見ていなかったのを直した** (#1812)

  #1809 で画面 (`pages/`) 側の穴を直したが、隣の画面の枠 (`components/`) 側に同じ穴が残っていた。
  地の字の拾い方が中括弧に加えて改行も除いており、JSX が要素の中身を改行して書くため、
  複数行に渡る字が丸ごと候補から外れる。

  実測で 9 file の日本語の地の字を 8 件しか見ておらず、実際は 20 件あった。
  見えていなかった字に、記法の一覧を押した時の案内が含まれる。

  ```
  クリックで actors: に 1 行追加します。 位置は自動で決まるので、 変えたい時は DSL に posX / posY を書きます。
  ```

  `DSL` を「記法」 に直し、`posX` / `posY` / `ms` を理由付きで残す語に足した。

  改行を許すとコードも入るので、打ち終わりと等値の比較と関数の宣言を外す印にした。
  等号だけでは外さない (#1809 で、等号を含むというだけで画面の字が落ちることを実測した)。

- **画面の字の検査が、変数を差し込んだ後ろに続く文を見ていなかったのを直した** (#1809、取り込みは #1810)

  「画面に出る字に英語が残っていない」 ことを見る検査が、字を `>` と `<` に挟まれ、かつ
  中括弧を 1 つも含まない区間からしか拾っていなかった。 JSX は変数を `{…}` で差し込むので、
  差し込みを含む文が丸ごと候補から外れる。

  実測で 11 件の文がこの形で外れており、その 1 つが英語 (`DSL`) を残したまま通っていた。
  差し込みを **切れ目ではなく文の一部** として読むよう直し、見つかった英語を
  「dragon のテキスト記法の各要素を…」 に書き直した。

  外す条件からも等号を落とした。 等号を含むというだけで、画面に出る字が 2 件
  (`1 PR = 1 つの主題` ほか) 黙って候補から消えていた。 等号を条件から外しても
  コードは 1 件も漏れない。

  開発時だけ登録される画面 (公開ビルドに出ない) は母集団から外す。 file 名を手で並べず、
  経路の並び (`main.tsx` の `import.meta.env.DEV`) から導くので、足しても消しても追従する。

- **クラス図の短い縦の線で、両端の多重度が詰まって 1 つの札に見えるのを直した** (#1773)

  描画エンジン (`@cardenelabs/cdl`) を `^0.52.0` から `^0.53.0` へ上げた。
  見本帳のクラス図で `Order` → `Line` の線は長さが 100 しかなく、両端の多重度 `1` と `1..*` が線の同じ側に
  間 10 で縦に並び、2 行の札のように読めた。 engine が、両端に字がある縦の線は 2 つの字の間が字と自分の端の間 (22) の
  2 倍以上空く長さまで縦の間隔を広げるようにした (`cdl#829`)。 図の縦の間隔は揃ったまま全て広がる。

- **クラス図の多重度 `1..*` を、線の中ほどの札から行き先の端へ移した** (#1769)

  描画エンジン (`@cardenelabs/cdl`) を `^0.50.0` から `^0.51.0` へ上げた。
  クラス図の多重度は札の語の下の行に入っており、語と続けて読まれ (英字の頃は「HAS1」)、
  どちらの端の数かも読めなかった。 engine が多重度を行き先の端の近くへ、札と反対の側に置くようにした (`cdl#821`)。

  記法の `sub` は、クラス図では多重度として engine に渡している。 変換がそれとは別に同じ値を
  札の下の行へも写していたので、クラス図では写さないようにした。 写したままだと記法と JSON の書き方だけ
  `1..*` が札と端の 2 か所に出る (組み立て器の書き方と図が食い違う)。

- **クラス図の型の `string[]` が `string□` と四角 1 つに見えるのを直した** (#1767)

  描画エンジン (`@cardenelabs/cdl`) を `^0.49.0` から `^0.50.0` へ上げた。
  等幅の書体 (`JetBrains Mono`) の `[` と `]` は横棒が字の枠いっぱいまで伸びており、図を画面の幅に合わせて
  縮めると (画面上 14px 前後) 2 本の横棒がつながっていた。 engine が等幅の文字の `[]` の間にだけ隙間を足した
  (`cdl#817`)。 文字の中身は `string[]` のまま変わらない。

- **クラス図の英大文字の札と、ER 図の読めない関係名を中身どおりの語に直した** (#1765)

  クラス図 (`class-demo`) の札が `EXTENDS` / `IMPLEMENTS` / `HAS` / `USES` / `OWNS` / `LINKS` の英大文字で、
  `HAS` と `OWNS` の下に多重度 `1..*` が小さく付くため「HAS1」 「OWNS1」 と読めた。
  もう 1 つのクラス図 (`class-complex-demo`) と同じ語にそろえた。

  | 種類 | 直す前 | 直した後 |
  |---|---|---|
  | `extends` | EXTENDS | 継ぐ |
  | `implements` | IMPLEMENTS | 満たす |
  | `aggregates` | HAS | 持つ |
  | `uses` | USES | 使う |
  | `composes` | OWNS | 抱える |
  | `associates` | LINKS | 結ぶ |

  ER 図 (`er-complex-demo`) は、左の表を主語にして読むと意味の通らない 3 つを直した。
  `orders` → `order_items` を「並べる」 から「含む」、`products` → `order_items` を「売る」 から「明細に載る」、
  `products` → `inventory` を「数える」 から「在庫を持つ」 にし、段の題も「3. 在庫を持つ」 にした。

  どちらも組み立て / 文章 / JSON の 3 つの形をそろえて直した。

- **ER 図の利用者と住所の関係名を「住む」 から「登録する」 に直した** (#1763)

  複雑な ER 図 (`er-complex-demo`) の `users` → `addresses` は 利用者 1 人 : 住所 0 件以上 の関係で、
  `addresses` は利用者が登録する住所録を表す。 同じ図で住所は「届け先」 と「請求先」 に使われており、
  住んでいる場所とは限らないため、「住む」 は関係の中身と合っていなかった。
  組み立て / 文章 / JSON の 3 つの形をそろえて直した。

- **札の列を線が横切る所の隙間が埋まった** (#1759)

  描画エンジン (`@cardenelabs/cdl`) を `^0.47.0` から `^0.48.0` へ上げた。

  `interactive-oauth-flow` は、名前が 4 枚縦に積まれた列を下を回る線が横切る形で、
  列の 3 枚目が線の 10 の所に残っていた (要求 14)。 engine が札の列の積み方を直し
  (`cdl#805`)、4 枚の分かれ方が 上 1 枚 / 下 3 枚 から 上 2 枚 / 下 2 枚 に変わった。

  | | 上げる前 | 上げた後 |
  |---|---|---|
  | 札と横切る線の隙間 | 10 | 14 |
  | 札と自分の弧の距離 (最遠) | 158 | 130 |
  | 見本帳 444 枚の error | 2 件 | 1 件 |

  残る 1 件は `pattern-passthrough` の `edge-node-cross` で、意図どおりの形。

  受け入れ宣言を 2 箇所から外した (`visual-validate-sweep` の見逃す表と
  `interactive-error-gate` の受け入れ欄)。 どちらも消えても増えても落ちる形にしてあるので、
  engine が直した時点で落ちて教えてくれた。

- **線の曲がり角から出ていた突起が消えた** (#1755)

  `@cardenelabs/cdl` を `^0.45.0` から `^0.46.0` へ上げた。

  角は「手前 14 world で直線を止め、弧で 14 world 先へ抜ける」 形で作るが、上下に折れる
  辺の分岐だけが手前と先を上下に固定していた。 上向きに出る辺では直線が角を 14 通り過ぎ、
  弧が戻る形の突起が見えていた。 原因の特定は cdl #801、修正は cdl #802 にある。

  見本帳で実際に出ていたのは 4 箇所 (辺 3 本)。

  | 図 | 辺 |
  |---|---|
  | `class-complex-demo` | `Transaction` → `CardPayment` (2 箇所) |
  | 流れの図 2 枚 | `api-client` / `revCard-npsCard` (各 1 箇所) |

  同じ誤りが engine に戻った時に見本帳側で落ちるよう、全図の辺を走査して角の通り過ぎを
  数える検査を足した。 判定は内積の符号だけで行い、長さは見ない = 角の半径が変わっても
  同じ検査が使える。

  走査の母数は図 444 枚 / 辺 371 本 / 角 210 個。 辺のうち 4 本は `C` と `A` を含み
  点の並びの読み方が違うため測れない側に数え、0 件側に混ぜていない。

  **`interactive-oauth-flow` の隙間 1 件は受け入れた**。 名前が 4 枚 72 world 間隔で縦に
  積まれ、下を回る線がその 3 枚目の 9 world 上を通る (要求 14)。 名前を下げると隣との
  隙間 (要求 36) を割り、上げると線に近づくため engine が動かせない。 隣ごと下げる試作では
  巻き込んだ 4 枚目が自分の弧から 166px 離れ、別の破綻 (上限 160px) に変わった。

  隙間 9 自体は前からある = 角の突起が名前に重なっていたため、この軸ではなく別の扱いに
  なっていて表に出ていなかった。 直すのは engine 側で cdl#805 が持つ。 受け入れは
  `visual-validate-sweep` の見逃す表と `interactive-error-gate` の受け入れ欄の 2 箇所に
  理由付きで置き、どちらも **消えても増えても落ちる** 形にした。

- **縦が長い図の読みにくさが見回りに出るようになった** (#1743)

  `@cardenelabs/cdl` を `^0.43.0` から `^0.44.0` へ上げた。

  図は `preserveAspectRatio="xMidYMid meet"` で器に収まるよう中で縮む。 効く倍率は
  `min(器の幅 ÷ viewBox 幅, 器の高さ ÷ viewBox 高さ)` で、横が余っていても縦が足りなければ
  縦が決める。 engine は幅しか見ておらず、**縦が長い図を丸ごと見逃していた**
  (`cdl#793` で倍率の下限に置き換えた)。

  上げた後の実測 (見本帳 444 枚)。

  | 何を | 前 | 後 |
  |---|---|---|
  | 軸 `responsive-viewport` | 12 件 | 20 件 |
  | うち幅だけでは拾えない図 | 0 枚 | 10 枚 |
  | それ以外の軸 | — | 1 件も増減なし |

  増える 10 枚はいずれも縦が長い図で、画面では 8.2-11.9px しかない。
  最も小さいのは `多対多が-2-組-8-表-8-関係` (viewBox 1255×1700) で、
  幅から出す見込みは 19.3px だが画面での実測は 8.2px だった。

  減る 2 枚 (`interactive-kpi-bullet` / `interactive-exam-grade`) は横に長く縦が短い形で、
  画面では 12px を超えている。

  注意の文面も変わる。 いままでの 上限 / 親幅 / 下限 に代わり、viewBox の縦横の寸法 /
  器の寸法 / 倍率 / **どちらの辺が効いたか** / その辺で下限に届く寸法 が出る。
  効いた辺を出さないと、幅を削っても直らない図で書き手が幅を削り続けることになる。

  10 枚の図そのものの手直しは別 Issue で行う。

- **見本帳の 76 枚から余分な広がりが消えた** (#1741)

  `@cardenelabs/cdl` を `^0.42.0` から `^0.43.0` へ上げた。

  帯 (lane) の間隔を決める式の各項 (箱と名札の余白 32 × 2、経路の突き出し 40 × 2) は
  どれも箱の縁が起点なのに、engine が合計を帯の縁から測っていた
  (`cdl#789` で箱の縁へ移した)。 `(帯の幅 − 箱の幅) / 2` を 2 枚分 余計に空けていた。

  上げた後の実測 (444 枚、親の幅 1100px)。

  | 何を | 前 | 後 |
  |---|---|---|
  | 幅が変わった図 | — | 76 枚 (広がった図は 0 枚) |
  | その 76 枚の幅の合計 | 123,861 | 116,528 |
  | 軸 `responsive-viewport` | 16 件 | 12 件 |
  | それ以外の軸 | — | 1 件も増減なし |

  最も縮んだのは `swim-demo` の 2148 → 1848 で、箱の題が 11.3px から 13.1px になる。
  読める大きさ (12px) に戻ったのは `er-demo` / `eth-erc4337-flow` / `pattern-rollback` /
  `swim-demo` の 4 枚。

  **2 枚は帯の間隔を記法に書いた**。 `interactive-traffic-sankey` と
  `animation-rich-order-status-flow` は 1 つの隙間に名札を 2 枚以上置く図で、
  engine が確保するのは最も広い名札 1 枚分だけ = 元から足りておらず、余分な広がりが
  それを隠していた。 `0.42.0` まで engine が実際に取っていた間隔
  (それぞれ 582 と 422) を `lanes:` の `x` に書いたので、この 2 枚の幅は上げる前と同じ。

- **見回りが「読みにくい図」 を実際に読みにくい図で拾うようになった** (#1738)

  `@cardenelabs/cdl` を `^0.41.1` から `^0.42.0` へ上げた。

  2 つの注意が実物と順位が逆だった。

  `subpixel-precision` (4 件) はにじみを図の world 座標の小数で測っていた。 にじみは画面の
  pixel で起きるので、器の幅次第の倍率が掛かる world 座標では判定できない (`cdl#783` で軸ごと廃止)。

  `responsive-viewport` (16 件) は縦横比 6:1 を境にしていた。 画面上の字の大きさは
  `字の world 寸法 × (親の幅 ÷ viewBox 幅)` で決まり、この式に縦横比は出てこない
  (`cdl#785` で viewBox 幅の上限へ差し替え)。

  上げた後の実測 (親の幅 1100px)。

  | 何を | 前 | 後 |
  |---|---|---|
  | `subpixel-precision` | 4 件 | 0 件 |
  | `responsive-viewport` | 16 枚 | 16 枚 |
  | うち画面上の字が 12px 未満の図 | 4 枚 | 16 枚 |

  枚数は同じだが中身が入れ替わった。 12 枚が外れ、12 枚が入っている。
  外れたのは `lane-multi` (比 8.0 / 字 15.8px) のように読める図で、入ったのは
  `class-complex-demo` (比 1.4 / 字 7.2px) のように読みにくい図。

  この repo では軸 33 の名前を 3 file から外し、軸 33 の発火を見ていた検査を軸 23 を見る
  検査へ差し替えた。 差し替えた検査は **縦横比が旧判定の境の内側** に収まる入力で書いてある
  = 縦横比で測る形に戻したら落ちる。

  編集画面が軸の単位で注意を隠す仕組みも外した。 隠していた唯一の軸が `subpixel-precision`
  で、一覧の要素が 0 個になったため。 画面に出る注意の集合は変わらない。

- **見回りの速さの注意が、同じ図でも毎回出たり出なかったりしなくなった** (#1736)

  `@cardenelabs/cdl` を `^0.41.0` から `^0.41.1` へ上げた。

  軸 `validate-performance-budget` が **1 枚ごとの壁時計** を境 (100ms) と比べており、
  壁時計は図の性質ではなく機械の都合で動いていた。 見本帳 444 枚を同じ process で
  3 周した実測で、`interactive-social-share-buttons` が 53 / 0 / 1ms、
  `interactive-traffic-sankey` が 50 / 159 / 115ms。 超過した枚数は 1 / 0 / 0 件と割れた。

  1 つ目は最初の 1 回だけ掛かる下準備、2 つ目は機械の混み具合で、どちらも図の性質ではない。

  描画エンジン側で 1 枚ごとの判定を廃し、全図の合計 (5000ms) だけを見る形にした
  (`cdl#776`)。 落ちる情報 (どの図が重いか) は、超過した時の文面に重い順 5 枚として出る。

  この repo では 3 周の件数が揃うことを見る検査を足した。 上げた後の実測は 3 周とも 0 件で、
  合計は 383 / 221 / 179ms だった。

  軸の名前は消えていないので、この repo の軸の一覧に手は要らない。

- **箱 1 つの縦長の図が「大きすぎる」 と言われなくなった** (#1734)

  `@cardenelabs/cdl` を `^0.40.12` から `^0.41.0` へ上げた。

  見回りが図の大きさを **箱の面積の合計** で測っており、順位が実物と逆だった。
  `oauth-flow` は箱 1 つ・描く物 5 個で超過し、線の折れが 44 ある
  `interactive-traffic-sankey` は 5 分の 1 の値で通っていた。

  描画エンジン側で軸 40 `memory-budget` を廃し、描く物の数は軸 34
  `dom-complexity-budget` に一本化した (`cdl#775`)。

  この repo では軸への参照を 3 file から外し、軸 40 の発火を見ていた検査を
  軸 34 を見る検査へ差し替えた。 差し替えた検査は **面積が旧軸の境の内側** に収まる入力で
  書いてある = 面積で測る形に戻したら落ちる。

  見回りの出力から `memory-budget` の 4 件が消えるだけで、他の軸の件数は動かない。
  記法も図も 1 文字も変わらない。

- **表の列に書いた型の名前が、見回りでデータと読み違えられなくなった** (#1732)

  `@cardenelabs/cdl` を `^0.40.11` から `^0.40.12` へ上げた。

  見回り (`pnpm test:sweep`) が `total: numeric` を「合計は数字のはずなのに `numeric`」 と
  読んでいた。 `numeric` は列の **型の名前** で、データではない。

  描画エンジン側の逃げ道が型名を並べた手書きの一覧で、`BIGINT` は載っている一方 `numeric`
  が抜けていた (`cdl#771`)。 行頭の印から列の宣言を見分ける形に変わり、方言ごとに増える
  型名を一覧に足さなくても通るようになった。

  この repo の記法も図も 1 文字も変わらない。 見回りの出力から `row-content-typing` の
  2 件 (`er-demo` / `er-complex-demo`) が消えるだけで、他の軸の件数は動かない。

- **棒 / 弧 / 半円 に書いた前の時点の値が、絵に出るようにした** (#1722)

  `@cardenelabs/cdl` を `^0.40.10` から `^0.40.11` へ上げた。

  #1718 で書き出した「描画エンジンが読み落としている」 3 件が直った側の取り込み
  (`cdl#767`)。 3 つの見本に `パターン` の切替を足し、書いた図と書かない図を押して
  見比べられるようにしている。

  | 見本 | 前の時点の出かた |
  |---|---|
  | 棒グラフ (`先月と比べた経路別の流入`) | 棒を横切る破線 1 本。 棒より少し左右へはみ出す |
  | 弧 (`先月と比べた機能ごとの利用率`) | 帯を横切る短い線 1 本。 前が小さくても外から読める |
  | 半円 (`前期と比べた売上進捗`) | 内側に前の内訳の輪 1 本。 外周は動かない |

  棒は縦軸の天井を前と今の大きいほうで取る。 検索は前 520 で今 420 と前のほうが高く、
  今だけで天井を決めると破線が枠の外へ出て「下がった」 が読めない。

  `この種別だけ描かない組` の宣言は 7 件から 4 件になった。 残るのは折れ線と 100 個の印の
  `previous:` と、棒と折れ線の `tone:` で、いずれも描かないと決めた側。

- **書けるのに絵が変わらない欄を、種別ごとに数えられるようにした** (#1718)

  `previous:` を読まない図が 5 種、件ごとの色 (`tone:`) を読まない図が 2 種あり、
  **どの検査からも見えていなかった**。 同じ家族の別の種別 (円 / 内訳の帯 / 2 時点の線 /
  大きな数字) が描いているため、家族単位では「描いている」 に数えられていた。

  実際に 1 件が漏れている。 大きな数字が `previous:` を読まない状態が #1711 まで残り、
  見つけたのはこの検査ではなく画面の文字数の記録が落ちたことだった。

  | 欄 | 絵が変わらない図 |
  |---|---|
  | `previous:` | 棒 / 折れ線 / 半円 / 弧 / 100 個の印 |
  | `tone:` | 棒 / 折れ線 |

  7 件に 1 つずつ理由を書いた。 その図の読み方では置けないのか、描画エンジンが読み落として
  いるのかを書き分け、後者には行き先を添えている (`cdl#767`)。 描き始めたら宣言が残るので
  検査が落ちる。

- **数値ひとつの図に書いた前の時点の値が、絵に出るようにした** (#1711)

  `@cardenelabs/cdl` を `^0.40.9` から `^0.40.10` へ上げた。

  記法は `previous:` を受け取り、組み立ても描画側へ渡していたのに、**この種別だけが
  受け取った値を 1 度も読んでいなかった** (`cdl#763`)。 見本に書いた `previous: "38"` は
  画面のどこにも出ず、書いた人からは黙って捨てられたように見える。

  | 何を | 上げる前 | 上げた後 |
  |---|---|---|
  | 数値の下の行 | 無し | `前 38` (数値より小さい字) |
  | 書かない図 | 無し | 無し (行そのものが出ない) |

  見本帳には `パターン` の切替として `前の値つき` を足した。 書いた側と書かない側を
  同じ見本の上で押し比べられる = 「書くと何が増えるか」 が 1 画面で読める。

- **編集画面が起動時に落ちていたのを直し、画面の検査の赤 36 件を消した** (#1709)

  編集画面の土台 (`@codemirror/state`) が 2 つの版で入っており、拡張の見分け
  (`instanceof`) が成立せず **画面が 1 つも描かれていなかった**。 組み上げた画面でも
  開発用の画面でも同じで、そこを見る検査が 21 件落ちたまま積み上がっていた。

  依存を 1 版に寄せた。 同じ壊れ方を早い側で捕まえるため、依存の一覧を読んで 2 版
  入っていないかを見る検査を足した (画面の検査は全件 12 分かかり、気付くのが遅れる)。

  残る 15 件は検査の側だった。

  | 何がずれたか | 件数 | 直し方 |
  |---|---|---|
  | 描画エンジンが矢頭の飾りを **使う分だけ** 出すようになった | 8 | 1 本を複製して色味を差し替える読み方をやめ、6 色を実際に描いた図を開いて本物を読む |
  | 手で並べた一覧が実物とずれた | 4 | 見本の登録簿から導く / 配色の口を実物から導く。 名前に `(` を含む見本が 1 件も当たらなかったのも直した |
  | 箱が低くて直せる警告が出なくなった | 2 | 見本と同じく箱の大きさを書く |
  | 描画エンジンが変わって記録が古い | 1 | 実測に合わせる (書いた値が絵に出ない 1 件は [#1711](https://github.com/cardene777/dragon/issues/1711) に切り出した) |

  赤が積み上がらないように、画面 1 つにつき 1 件の軽い検査を足した。 経路は `main.tsx` の
  `<Route>` から導くので、画面が増えても対象から外れない。

- **棒グラフの見た目を直した** (#1686)

  `@cardenelabs/cdl` を `^0.40.6` から `^0.40.7` へ上げた。

  見本帳の棒グラフを測ると、普通の棒グラフから外れた値が 4 つ出ていた (`cdl#747`)。

  | 見たもの | 上げる前 | 上げた後 |
  |---|---|---|
  | 縦軸の目盛り | `0 / 170 / 340 / 510 / 680` | `0 / 200 / 400 / 600 / 800` |
  | 棒幅 対 隙間 | `126.5px 対 10px` = 12.7 : 1 | `88.6px 対 40.3px` = 2.2 : 1 |
  | 棒の色 | 1 種類 | いちばん大きい棒だけ濃い |
  | 角丸 | `2` (幅 126.5px に対して) | 棒幅の 6% |

  `170 / 340 / 510` は最大値を 4 等分した数で、機械が割った数字がそのまま目盛りに出ていた。
  棒は隙間の 12.7 倍の太さがあり、4 本が 1 枚の塀のように見えていた。

  記法 (`*.cdl.ts`) は 1 file も変えていない。 棒の高さの比は値の比のままなので、
  高さから値を読む使い方も変わらない。

- **図表を描くたびに「使われていない」 と警告が出るのを直した** (#1684)

  `@cardenelabs/cdl` を `^0.40.5` から `^0.40.6` へ上げた。

  図表は 1 つの節が図の全体を表す形で、記法にその節を activate する書き方が無い。
  それでも描画側が「どの phase でも activate されていない (unused、 typo の可能性)」 を
  出しており、見本帳の図表の記法 15 件すべてで鳴っていた (`cdl#743`)。 図表の見本を 1 つ
  開くたびに、画面の記録に 2 回ずつ出る。

  **書き手に直せない警告は、読む価値のある警告まで一緒に無視させる**。 同じ検査は
  「未定義の state を参照している」 のような本当の誤りも出す。

  記法を `validate` に通して警告を数えた = 上げる前は 15 件中 15 件で出ており、
  上げた後は 0 件。 記法は 1 file も変えていない。

- **内訳が多い半円ゲージで合計が主役でなくなるのを直した** (#1682)

  `@cardenelabs/cdl` を `^0.40.4` から `^0.40.5` へ上げた。

  段を折り返すようにした後 (`0.40.3`)、件数が増えると弧に残る高さが尽き、
  **合計の字が内訳の名前より小さくなっていた** (`cdl#735`)。 この種別は合計を大きく
  見せるために作ったので、主役が入れ替わると図の意味が変わる。

  描画側が弧に残す高さを先に確保して置ける段数を逆算し、入り切らない件を末尾 1 件に
  「その他」 として畳むようになった。 畳んだ件の値は足して持たせるので合計は変わらない。

  記法から件数を増やして測った = 上げる前は件数 22 で合計の級 13 に対し内訳の名前 14、
  件数 30 と 40 では合計が弧の内側の穴からはみ出していた。 上げた後は 3 つとも合計が
  名前より大きく、穴に収まる。 内訳 3 件の見本は今までと同じ 1 段のまま。

- **名札の重なりを見落とす字幅の見積りを直した** (#1678)

  見本の円グラフで名札の重なりを数える検査は、字幅を半角 0.55 の見積りで出していた。
  描画側の実測は 0.86 で **3 割狭い**。 0 件を期待する検査で見積りが狭いと、
  重なっていても幅を狭く見て重なりとして数えず、そのまま偽の合格になる。

  見積りを描画側と同じ階級 (広い 1.04 / 細い 0.37 / その他の ASCII 0.86 / ASCII の外 1.04)
  に揃え、`lib/text-width-estimate.ts` へ切り出して円グラフと半円ゲージの両方から読む。

  係数は書体の実測値なので描画側と同じ値でよい (書体が変われば両方が同時に誤りになる)。
  箱幅や余白のような描画側が決めた値は今までどおり写さない。

- **値の動きが始まった瞬間に終わって見えるのを直した** (#1679)

  `@cardenelabs/cdl` を `^0.40.3` から `^0.40.4` へ上げた。

  段の値を動かす動きは描画側の既定の曲線 `easeOutQuint` を通っており、動きが前半に
  寄りすぎていた (`cdl#736`)。 見本の折れ線 (段の長さ 1.2 秒) で測ると、動きの 9 割が
  最初の 0.449 秒で終わり、残りの 0.75 秒はほとんど止まって見える。

  既定が `easeOut` になり、半分に達するのが段の 13.1% から 34.2% の時点になった。
  段の終わりまで動き続ける。

  見本の折れ線を実際に動かして測った = 上げる前は段の 13% の時刻で既に 49.6% 進み、
  80% の時刻で 100% に達していた。 上げた後はそれぞれ 20.0% と 93.6% になる。
  段の始まりと終わりの値は変わらないので、図が主張する量は今までと同じ。

- **半円ゲージの内訳の数字が隣とくっつくのを直した** (#1676)

  `@cardenelabs/cdl` を `^0.40.2` から `^0.40.3` へ上げた。

  描画側で下段の内訳を件数で横に等分し、名前だけを区画に収まるまで切り詰めて数値は
  そのまま置いていたので、件数が増えると数字が隣の区画へはみ出した (`cdl#731`)。
  `10  5.4%11  5.9%` のようにくっついて読めない。

  区画の幅を数値の行から決め、入り切らない件は次の段へ送るようになった。
  数値も名前と同じ幅で切り、色の印も区画より広ければ区画に合わせて縮む。

  記法から件数を増やした半円ゲージで実測した = 上げる前は 12 件で 10 組、20 件で 19 組が
  重なり、上げた後はどちらも 0 組になった。 見本の内訳 3 件は 1 段のままで変わらない。

- **円グラフで小さい取り分が続くと名札が重なるのを直した** (#1674)

  `@cardenelabs/cdl` を `^0.40.1` から `^0.40.2` へ上げた。

  描画側で名札 (割合と名前) を扇の中央角の向きに置いていたので、角度の近い扇が続くと
  同じ場所に印字されて読めなかった (`cdl#727`)。
  小さい取り分が 2 個続くだけで崩れ、5 個では重なりが 20 組になる。

  名札の縦は重なりをほどき、横は左右の列に揃うようになった。
  扇と縁の点は動かないので、図が主張する量は今までと同じ。

  記法から小片の続く円を組んで実測した = 上げる前は 2 / 6 / 12 / 20 組が重なり、
  上げた後は 2 個から 5 個まで全て 0 組になった。

- **傾き図で同じ値の系列の名札が重なるのを直した** (#1672)

  `@cardenelabs/cdl` を `^0.40.0` から `^0.40.1` へ上げた。

  描画側で名札 (値と名前) を点と同じ縦の位置に置いていたので、値が同じ / 近い系列が
  2 件以上あると同じ場所に印字されて読めなかった (`cdl#723`)。

  見本帳の「経路別の申込み」 も踏んでいた。 2 段目で `sns` を 400 へ動かすと今期の値が
  420 と 400 に並び、名札の縦の差は 15.8 になる = 1 行に要る 18 を下回って重なる。
  版を上げた後の実測では 18 に開いた。

  点と線の位置は変わらないので、図が主張する量は今までと同じ。

## [0.27.0] - 2026-09-07

### Added

- **傾き図の右の列に何を出すかを見本帳の画面から選べるようにした** (#1659)

  「今の値」 と「増減」 の 2 つを切り替えられる。 傾き図を持つ図でだけ切替が出る。

  傾き図は増減を読ませる図なのに、差 (`420 - 380 = +40`) はどこにも字になっていなかった。
  engine は増減を出す形を持っている (cdl#702) が既定ではないため、記法を手で書き換えないと
  見比べられず「使えるが誰も見ない」 まま残っていた。

  既定は「今の値」 で、engine が欄を書かない図をどう描くかに揃えてある。 触っていない図が
  「触った後の形」 で出ないようにするため。 項目を選び直すと既定に戻る。

  円グラフの見せ方 (#1645) と同じ形。 2 つは互いに排他なので `radiogroup` で出す。

### Changed

- **見本帳と公開 package の描画エンジンを `0.36.0` に揃えた** (#1657)

  `apps/playground-spa` と `packages/dragon` の `@cardenelabs/cdl` を `^0.35.0` から
  `^0.36.0` にした。

  傾き図に見せ方の欄 `chartSlopeForm` が足され、`delta` を書くと右の列が今の値ではなく
  増減 (`+40` / `-90`) になる。

  この取り込みでは欄を 1 つも書かないので、見本帳の絵は 1 文字も変わらない。
  画面から切り替えられるようにするのは別の取り込みで行う。

- **見本帳と公開 package の描画エンジンを `0.35.0` に揃えた** (#1655)

  `apps/playground-spa` と `packages/dragon` の `@cardenelabs/cdl` を `^0.34.0` から
  `^0.35.0` にした。

  傾き図の系列の名前が、その線と同じ色になる。 名前と線を突き合わせるのに線を目で辿る
  必要がなくなり、交差した先をそのまま追える。 値は太字の濃い色のままなので、数の読み比べは
  変わらない。

  併せて時点の軸の縦線が太さ 1 から 2 になり、「前」「今」 の 2 本が柱として立つ。

  傾き図を持たない図の絵は変わらない。

### Fixed

- **`.context/` を lint から外す指定が workspace まで届いていなかった** (#1659)

  `eslint.config.mjs` の `.context/**` は repo 直下の 1 つにしか当たらず、
  `apps/playground-spa/.context/` の手元 script が lint の対象に残っていた。
  `**/` を付けて、意図どおり全ての `.context/` を外す。

  外れる側と残る側の両方を `ESLint#isPathIgnored` で見る検査を足した。
  外れることだけを見ると、全部を外す指定 (`**`) でも通ってしまうため。

### Security

- **依存の既知の脆弱性 6 件を更新と受容記録で閉じた** (#1642)

  `fast-uri` の 4 件は束ねの奥まで上げて消えた。 `qs` の 2 件は上流が版を固定していて
  上げられないため、理由と再確認期限をつけて [osv-scanner.toml](osv-scanner.toml) に記録した。

  併せて `scripts/check-dependency-vulnerabilities.sh` を足した。 週に 1 度の監査だけでは
  依存を上げた直後の検出に追いつかないため、手元で 1 command で見られるようにする。
  終了状態は「検出なし」「検出あり」「走らせられない」 の 3 つに分かれる。

## [0.26.0] - 2026-09-07

### Added

- **傾き図の寸法を意匠帳の実測表に出せるようにした** (#1651)

  線の太さ / 点の半径 / 軸の太さ / 時点の字 / 軸の脇の値の字 / 系列の名前の字 の 6 行を足した。
  行が無い役割は測る対象にすら入らないため、 それまでは傾き図を吸い出しても寸法が 1 行も
  残らなかった。

  行の名前は既存のどの行とも重ならない語にした。 折れ線が「値の字」、 進捗の輪が「名前の字」 を
  既に持ち、 同じ語を当てると表のどの行がどの図の値か引けない。

- **記法から傾き図を書けるようにして見本帳に載せた** (#1647)

  `type: slope` を足した。 2 時点を直線でつなぎ、線の向きで増減を、線の交差で順位の
  入れ替わりを読む図。 前の時点は `type: stacked` と同じ `- 名前: { value: "420",
  previous: "380" }` の形で書く。

  併せて描画エンジン `@cardenelabs/cdl` を `^0.32.0` から `^0.33.0` にした。
  傾き図の描画はこの版が持つ。

  負の値を受けるのは折れ線だけだったが、傾き図も受けるようにした。 増減を追う図なので
  損益のように 0 を跨ぐ値が来る。


- **見本帳の円グラフで見せ方を選べるようにした** (#1645)

  輪 / 積層の弧 / 銘板 の 3 つから選べる。 円グラフを持つ図でだけ切替を出す = 他の図では
  欄が効かず「押しても何も起きない操作」 になる。

  積層の弧は系列ごとに 1 本の弧を置き、割合を角度ではなく弧の長さで読む。 銘板は輪を左に
  寄せ、右に 名前 / 割合 / 実数 の 3 列の表を組む。

  既定は輪で、描画エンジンが欄を書かない図をどう描くかに揃えてある。 既定のままの絵は
  記法に欄を書かない元の絵と 1 文字も違わない。

  記法 (`*.cdl.ts`) には値を書かない。 切替の初期値と二重管理になるため。

- **図表の実測表に折れ線以外の行を足した** (#1638)

  意匠帳の実測表を組む `SPEC_ROWS` は、 図表について折れ線の 2 行しか持っていなかった。
  円グラフを吸い出しても箱の行しか出ず、 扇の値が 1 つも記録できなかった。

  描画エンジンが既に出している役割のうち、 実際に属性を持つ組み合わせだけを 27 行足した。
  円の扇の間隔、 棒の幅と角、 ゲージの弧、 大きな数字の輪と字、 印の大きさ、 内訳の帯の区画、
  絞り込みの段、 工程表の帯と目盛、 道筋の帯と札と線、 発想の枝が対象。

  `d` しか持たない役割 (`chart-radial-arc` / `chart-radial-track`) は足していない。
  `getComputedStyle` が `stroke-width` に既定の `1px` を返すため、 行を足すと決めていない値が
  表に出る。 足さない判断は検査で固定した。

### Changed

- **見本帳と公開 package の描画エンジンを `0.34.0` に揃えた** (#1649)

  `apps/playground-spa` と `packages/dragon` の `@cardenelabs/cdl` を `^0.33.0` から
  `^0.34.0` にした。

  傾き図の名札が変わる。 軸の脇に置く値と、その外に置く系列の名前が別の役割になり、
  値だけに字体を当てられる。 1 つの役割にまとめていた形をやめた。

  傾き図を持たない図の絵は 1 byte も変わらない。

- **見本帳と公開 package の描画エンジンを `0.32.0` に揃えた** (#1643)

  `apps/playground-spa` と `packages/dragon` の `@cardenelabs/cdl` を `^0.31.1` から
  `^0.32.0` にした。

  円グラフの名札が変わる。 割合が 1 行目に級 19 の太字で出て、 名前は 2 行目に級 12 で下がる。
  実数は割合と違う数になる図でだけ 2 行目に添う。 名前と割合を 1 行に並べていた形をやめた。

  併せて 2 つの欄が書けるようになった。 datum の `previous` を書くと円グラフに内側の輪が出て
  2 時点を 1 枚で読める。 `chartPieForm` を書くと見せ方を輪以外にできる。 どちらも欄を書かない
  図の絵は変わらない。

### Fixed

- **記法から組む図を意匠帳に吸い出せるようにした** (#1640)

  吸い出しが catalog の source から `id: "<図の id>"` を探す作りで、 記法から組む図は
  その行を持たないため 1 件も扱えなかった。 図表 12 件がまるごと意匠帳に納められなかった。

  群は画面から引く形にした。 source で見つからない時だけ、 `textDslToDiagram` を含む file の
  群を順に開いて一覧に `id` があるかを見る。 `slugify` の規則を吸い出し側へ写さずに済む。

  記法の抜き出しも同じ行を起点にしていた。 記法から組む図では **記法そのもの** を納める形にし、
  file 名を `source.cdl` にした。 包む宣言 (`textDslToDiagram(...)`) を納めても、 記法本体が
  別の宣言にあるので写して動かないため。 一覧は `source.cdl.ts` と `source.cdl` の
  どちらかがあれば揃っているとみなす。

- **束ねの鮮度判定が中身ではなく時刻を見て誤検知するのを直した** (#1636)

  検査の前に走る判定が、 `pnpm-lock.yaml` の `mtime` と束ねの `mtime` を比べていた。
  `mtime` は中身の代理でしかなく、 lockfile の中身が 1 行も変わらなくても動く。
  実測で時刻だけが動いて偽陽性になり、 `vite` は内容 hash で判定するので開発 server を
  起動し直しても束ねを作り直さず、 逃し口を使うまで検査が止まり続けた。

  `vite` が束ねに記録した package の実体 path と、 app がいま解決する実体 path を
  突き合わせる形にした。 判定できない依存は「古い」 に倒さず、 見られなかった件数として数える。

  「記録された path が実在するか」 では足りない。 `pnpm` は古い版を `node_modules/.pnpm/` に
  残すため (実測で `@cardenelabs/cdl` が 9 版同居)、 版を上げた後も古い path は実在し続ける。

## [0.25.0] - 2026-09-06

### Changed

- **公開する package の描画エンジンを `0.31.1` に揃えた** (#1632)

  `packages/dragon` の `@cardenelabs/cdl` を `^0.30.3` から `^0.31.1` にした。
  `^0.30.3` は `>=0.30.3 <0.31.0` に閉じるため、 見本帳の `^0.31.1` と同じ monorepo の中で
  2 つの版が同居していた。

  `@cardenelabs/dragon` を入れた人には `0.30.x` が渡っており、 折れ線図の目盛の直しも枠も
  届いていなかった。 揃えたことで `pnpm-lock.yaml` の解決先も `0.31.1` の 1 つになった。

  記法の書き方は変わらない。 検査は 322 file / 30,529 件がそのまま通る。

## [0.24.0] - 2026-09-06

### Added

- **折れ線図の見せ方を見本帳から切り替えられるようにした** (#1624)

  折れ線図を選んでいる時だけ、 図の上に `塗り` / `せり上げ` / `なぞり` の 3 つの入り切りが出る。
  `塗り` は線の下を薄く塗り、 `せり上げ` は値の字を点への到着直後だけ下からせり上げ、
  `なぞり` は線を描き切った後に粒で経路をなぞる。

  engine は `0.31.0` からこの 3 つを持っていたが、 見本帳の記法が書いていないため
  **在ることが判らなかった**。 記法には書かず、 画面の切替から図に流し込む形にしている
  (書くと切替の初期値と記法の二重管理になる)。

  初期値は 3 つとも切。 engine が欄を書かない図を偽として描くのに揃えた。
  切り替えた状態は拡大表示にも同じく効く。

### Changed

- **折れ線グラフの意匠帳を戻った枠に合わせた** (#1628)

  `docs/design/notation/presets/chart-line-demo/` の 3 file を実物に揃えた。 記録は
  「図表を囲む枠を持たない」 と書いており、 焼き付けた `look.svg` にも枠が無かった。

  併せて、 画面に出る枠と engine が書く枠で値が違うことを記録に残した。 見本帳は
  `cdl-theme.css` が `!important` で上書きするため、 engine の 1.5 に対して画面は 2.5 になる。
  同じ形の追随漏れは `0.31.0` の時 (#1621) に続いて 2 度目。

- **図の描画エンジンを `0.31.1` に上げた** (#1625)

  `@cardenelabs/cdl` を `^0.31.0` から `^0.31.1` にした。 折れ線図に図表を囲む枠が戻る。

  `0.31.0` で枠を外した時は「図表の枠は何も表していない」 という判断だったが、 実物では枠が
  図表の範囲そのものを示していた。 外した結果、 目盛線の外側と台の区別が付かなくなった。

  前と同じ枠には戻っていない。 engine が書くのは太さ 1.5 と目盛線と同じ `--cdl-divider` で、
  折れ線と同じ `--cdl-tone-accent` は使わない。

  見本帳の画面に出る値はこれとは別。 画面側の `cdl-theme.css` が `!important` で上書きするため、
  光っている枠は太さ 2.5 で色は `--d-accent` になる。 それでも折れ線 (太さ 3) より細いので、
  読ませたい線のほうが目立つ。

  折れ線図以外の図の見た目は変わらない。

- **図の描画エンジンを `0.31.0` に上げた** (#1619)

  `@cardenelabs/cdl` を `^0.30.3` から `^0.31.0` にした。 見本帳では折れ線図の見た目だけが変わる。

  目盛を全段で固定した。 前は段ごとに測り直していたため、 値が変わっても最小と最大の点が
  段をまたいで動かず、 計画 1,000 から実績 900 に変わっても高さが同じだった。
  目盛の刻みは読み手が使う数に丸め、 目盛と点の数は 3 桁区切りに揃えた。

  軸に見えていた下端と左端の実線と、 図表を囲む枠を外した。 端の点は内側に入れ、 線の先が
  点に着いた瞬間に輪が広がって消えるようにした。

  全図では、 図に出ない矢印の定義を書き出さなくなった。 辺を 1 本も持たない図は
  53,432 byte から 1,592 byte になった。 折れ線図以外の図の見た目は変わらない。

### Fixed

- **書き出した図が画像に変換できない属性の重複を直した** (#1616)

  書き出し時に `preserveAspectRatio` を無条件に前置していたため、 描画側が同じ属性を出す今は
  根の要素に 2 回書かれていた。 ブラウザは黙って許すので画面では気付けず、 `rsvg-convert` が
  `Attribute preserveAspectRatio redefined` で止まって初めて分かった。

  属性を足すのをやめ、 個数が 1 でなければ止める形にした。 0 個でも止めるので、 描画側が
  出さなくなった時に黙って足し直すことはない。

## [0.23.0] - 2026-09-05

### Added

- **見本帳に複雑な ER 図とクラス図を足した** (#1595)

  ER 図は 12 表 14 関係で、中継表 2 つが多対多を作り、分類が自己参照する。
  クラス図は 12 クラス 14 関係で、抽象 ・ 実装 ・ 組み立ての 3 層に 6 種の関係が全て出る。

  どちらも単純版の直後に並べてある。 いままでの見本は ER 図が 3 表、クラス図が 7 箱で、
  記法が大きい図に耐えるかを見せられていなかった。

### Changed

- **複雑な見本の段を小刻みにした** (#1599, #1602)

  組み直す前は箱を全部出してから線を全部出す形で、最初の線が出るのはクラス図で 4 段目
  (約 8.7 秒)、ER 図で 3 段目 (約 5.8 秒) だった。
  1 段の増分もクラス図で最大 7 件、ER 図で最大 10 件あり、単純な見本の最大 4 件かつ
  2 段目から線が出る形と違っていた。

  組み直した後はどちらも 1 段の増分が 4 件以下になり、クラス図は 8 段、ER 図は 9 段になった。
  段の切替は組み直す前後とも、段の長さ 900ms と `@cardenelabs/cdl` が持つ静止 2000ms を合わせて、
  1 段あたり約 2.9 秒になる。
  静止は `@cardenelabs/cdl` 側に固定されていて見本帳から変えられないため、段の組み方だけで直した。

  段ごとの増分を数える単体テストも足し、5 件以上増える段があれば落ちるようにした。

- **関係の線を全ての図で 7 にした** (#1597)

  線が 2.25 (クラス図は 3.5) では細く、線の多い図で追いにくかった。
  印の輪郭も線との比 (白抜き 0.622 / 開いた矢 0.711) を保ち、1.4 から 4.36、
  1.6 から 4.98 へ一緒に上げている。

  **図の種類では分けない**。 #1587 でクラス図だけを 3.5 にしていたが、
  太い方が読みやすいのは図の種類に依らないので、`svg[data-cdl-type="class"]` に絞った
  3 規則を落として全体を 7 にした。

  engine が出す `data-cdl-type` は残る。 種類ごとに描き分けられる状態は作ってあり、
  いま使っていないだけ。

- **図の描画エンジンを `0.30.3` に上げた** (#1607)

  `@cardenelabs/cdl` を `^0.29.0` から `^0.30.3` にした。 取り込んだのは線の引き方の直し 4 件。

  `0.30.0` = 根元の重なりを分ける (`#649`) と、 同じ辺から出る線の共有区間を短くする (`#651`)。
  `0.30.1` = 折れを持たない線が起点で折れる形を戻す (`#655`)。
  `0.30.2` = 経路を動かした後に関係の説明を箱や線から離す (`#659`)。
  `0.30.3` = 線の終わりに潰れた角ができる形を直す (`#663`)。

  `0.30.0` だけを取り込むと、 見本帳の視覚検査が 15 件中 4 件落ちる。
  `0.30.1` で起点の折れ 6 件が消え、 `0.30.2` で説明の重なり 9 件が消えて 15 件すべて通る。
  `0.30.3` で ER 図に残っていた 30px 未満の直線 2 件も消えた。

### Fixed

- **クラス図で関係の札が別の線に乗らなくなった** (#1608)

  「継ぐ」 と「満たす」 の 2 つの札が互いの線の上に乗り、字が線に潰されて読めなかった。
  #1600 で入れた 5 列 5 段の配置では、カード払いが親を継ぐ線と財布払いが約束を満たす線が
  同じ区画で交差する。 縦に走る部分の間は 26px しかなく、両方の札がその区画の真ん中に載る。

  **配置を探した時の費用に、この重なりが入っていなかった**。 線と線の重なりだけを 0 に
  寄せた結果、札と線の重なりが 2 件残った。 取り込み前に回した検査も線と線しか見ておらず、
  `packages/dragon` の視覚検査だけが落ちる状態になっていた。

  カード払いと財布払いを入れ替えて、それぞれが自分の親の真下に来るようにした。
  取引 ・ 台帳 ・ 証明書は 1 列だけ右へ寄せた。 5 列 5 段と折れ 28 は変わらない。

  段を崩さずに列を 6 本まで広げた 2592 通りを数え上げると、上 2 段だけでは 5 つの条件を
  同時に満たす置き方が 1 つも無かった。 下段も動かして初めて両立する。

  札と線の重なりを見る検査を見本帳側に足した。 直す前の配置に当てると落ちること、
  その配置でも線と線の重なりは 0 件だったことも、同じ file で固定した。

- **クラス図で色の違う線が重ならなくなった** (#1600)

  色の違う線は 5 対が 73px から 137px にわたって完全に重なっていたが、0 対になった。
  後から描いた色が前の色を隠すため、関係の群を色で読み分けられなかった。
  `@cardenelabs/cdl` は全ての線を箱の辺の中点に寄せるので、同じ辺に 2 本以上付くと
  出だしが必ず重なり、見本帳から変えられるのは箱の配置だけになる。

  12 クラスを 4 列 4 段から 5 列 5 段へ置き直した。
  色の違う線の重なりは 5 対から 0 対、折れは 32 から 28、線の総長は 7892 から 7662 に減った。

  上 2 段の 7200 通りと下 2 段の 3024 通りを数え上げた。
  4 列では色の違う線の重なり 0 と `@cardenelabs/cdl` の視覚検査の `error` 0 が両立せず、
  5 列 5 段でだけ両立した。
  縦列の左右順は記法 (`YAML` / `JSON`) 側では書いた順で決まるが、組み立て `API` は
  並べ直しを通すため、両方を列の順に揃えた。

- **ER 図でほぼまっすぐな線に角が付かなくなった** (#1601)

  28px と 1px の段差のために線が 2 度折れ、1px の側は角丸の四半円 2 つに挟まれて
  潰れた角になっていた。
  `orders` は 5 本の関係を持つので 4 辺に割り振れず、どこか 1 辺が 2 本を受ける。
  同じ辺に 2 本入ると `@cardenelabs/cdl` が取り付け位置を上下に散らし、その分だけ段差ができていた。

  列の割り当て (利用者系 / 注文系 / 商品系) と列の中の上下の順は保ったまま、
  間の空け方 3375 通りを数え上げて 12 表の段を組み直した。
  30px 未満の直線区間は 3 本から 0 本、折れは 26 から 20、折れを 1 つも持たない線は
  9 本から 10 本になった。
  5 段以内に収めた形では `addresses` → `orders` が `payments` の箱を貫くため、6 段にした。

- **図に重ねる段の表示が見本帳の図を覆わなくなった** (#1593)

  札と進み具合の帯を舞台に貼り付ける作りは保ったまま、実際の字の高さから求めた
  上下の空きを一覧と詳細の舞台に持たせた。左右の余白と、舞台を掴んで動かす操作は変わらない。

## [0.22.0] - 2026-09-04

### Changed

- **クラス図の関係の線と印を太くした** (#1587)

  依存を `^0.29.0` に上げた。 `cdl` `0.29.0` が絵の根に出す `data-cdl-type` により、
  クラス図だけに線幅を指定できるようになった。

  クラス図の関係の線を 2.25 から 3.5 にし、線との比を保つため白抜きの印の輪郭も
  1.4 から 2.18、開いた矢などの輪郭も 1.6 から 2.49 にした。
  ER 図、状態遷移図、流れ図など、クラス図以外の線幅は 2.25 のまま変わらない。

- **記法から作った図にも種類が乗るようにした**

  記法に書いた図種を組み立て経路へそのまま渡すようにした。段の札は表示用なので、
  札の値や段の有無にかかわらず同じ `data-cdl-type` になる。

  記法に図種が無い `flowchart` / `network` / `infrastructure` は、記法ではそれぞれ
  `swimlane` / `flow` / `flow` になる。組み立て器経路との種類の差は見本帳の検査で宣言する。

## [0.21.0] - 2026-09-04

### Changed

- **クラス図の関係を 3 群の色で分けるようにした** (#1583)

  依存を `^0.28.0` に上げた。 cdl 0.28.0 が関係の種類から色みを決めるので、その落ち先を配色に流す。

  直す前は 6 種の関係が全て同じ色で引かれていた。
  見分けを担うのは 線 (実線 / 破線) と 印の形 (三角 / 菱 / 開いた矢) と 印が付く側 の 3 軸だけで、
  6 種を 1 枚に並べると印の形まで見ないとどれがどれか判らない。

  配色の口を 7 から 9 に増やし、3 群に色を当てた。

  | 群 | 種類 | 群の中の見分け |
  |---|---|---|
  | 縦の関係 | 継ぐ / 満たす | 線 (実線 / 破線) |
  | 所有 | 持つ / 抱える | 印の塗り (白抜き / 塗る) |
  | 向きだけ | 結ぶ / 使う | 線 (実線 / 破線) |

  **群の中は色を分けない**。 群の中の 2 種は既に線か印の塗りで分かれているので、
  色まで足すと同じことを 2 回言うことになる。

  縦の関係は既にある「線」 の色を使うので、増える口は 2 つ。
  行の面と台に対する対比は最小 4.86 (下限 4.61)、群どうしの見分けは最小 ΔE 34.5 (下限 15)。

  ER 図と状態遷移図は関係が 1 種類なので「線」 だけを使い、増やした 2 口は読まない。

  見本帳の記法 3 形から関係ごとの色みの指定を外した = 書くと種類の既定より勝って
  6 本とも同じ色になる。

## [0.20.0] - 2026-09-04

### Added

- **見本帳のクラス図に種類の札を出した** (#1577)

  見本帳の `class-demo` で、`Auditable` に `interface`、`User` に `abstract` の札を出した。
  描き手は札を出す口を元から持っていたが、見本帳では 1 度も使っていなかった。
  見本にあって図に無い記法は、読み手が確かめられない決まりになる。

  **2 つ違う値を出すのは、札が「クラス」 と書くための飾りではなく種類を分ける口だと読めるようにするため**。
  1 種類だけでは、その語が箱の説明なのか種類なのかを図から決められない。

  入口によって名前が違い、記法と JSON は `eyebrow:`、組み立て器は `stereotype:` で書く。
  見本帳は記法 3 形を揃えて持つため、3 形すべてに札を書いた。

  札が 3 形すべてに入っているかを確かめる検査
  (`apps/playground-spa/src/lib/catalog-class-stereotype.test.ts`) を足した。
  既存の記法 3 形の一致検査が比べるのは、縦列 / 箱 / 矢印の数と、箱の題、矢印の説明、
  段の題だけだった。
  そのため、札を 1 形だけ書き忘れても落ちなかった。

- **クラス図に既定の配色を持たせた** (#1575)

  `type: class` に既定の配色 `kinari` (生成りに茶) を持たせた。
  `type: er` と同じ扱いで、書き手が `palette:` を書いた時はそちらが勝つ。

  縞の色は配色からしか来ない。 配色を持たない舞台では `--cdl-row-stripe` が未定義になり、
  描画側の落ち先 (箱の面) が出ていた。 色の値で測ると、明るい側は縞も面も `rgb(255,253,247)`、
  暗い側はどちらも `rgb(36,31,24)` で完全に同じ値だった。
  縞の矩形は描かれているので、消えているのは色だけ。

  **箱の作りが同じなら既定も同じにする**。 クラス図の箱は ER 図と同じ作り
  (行頭の印 + 左に名前 + 右に型) で、名前と型が離れて並ぶ。 行を横に追う目印が要る理由も
  同じ。 別の色みを既定にすると、2 つの図種が並んだ時に理由の無い差が出る。

  「既定を持つのは ER だけ」 と述べていた記述が 7 箇所あり、実装だけ直すと全部が誤りになる。
  全件走査で導出して同じ変更で揃えた (型 / 記法の読み手 / JSON の読み手 / 語の一覧 /
  公開 schema / README / 画面に出る記法の説明)。

  意匠帳 `docs/design/class/note.md` を新設した。 クラス図として何を見せるかを 3 案の絵で
  見比べて決めた記録で、採った案と却下した 2 案が入っている。

  **既定が効くのは記法と JSON の入口だけ**。 組み立て器 (`classDiagram({ ... })`) は既定を
  持たず、`palette:` を書いた時だけ載る (`er({ ... })` も同じ)。
  見本帳の図は組み立て器で組んでいるので見た目は変わらない。

- **見本帳に 8 表 8 関係の ER 図を足した** (#1573)

  見本帳の ER 図は箱 3 つ / 関係 2 本で、中継表も多対多も無かった。 意匠帳が線の手当てを
  決めた図は 8 表 8 関係 / 多対多 2 組で、そこで見えていた問題 (同じ通り道を並走する線 /
  交わる線 / 札の位置) が見本帳では 1 つも見えなかった。

  Text DSL の頁に `textDslErMesh` を足し、記法 3 形を揃えた。

  置き場所は 2 列 × 4 段。 `type: er` は組み立て器に任せると 1 箱 1 縦列で横一列になるので、
  全ての箱に縦列を書く (#1571 で開いた口)。

  **行を揃えると斜めの線が減る**。 3 列で組んだ最初の形は、`割り当てる` の札が `受け持つ` の
  線に重なり、`抱える` が `user_roles` を貫通した (`visual-validate-sweep` が両方を検出)。

  **線も段に載せる**。 描き手は段が名指ししていない線を「光っていない合図」 として細かく刻むので、
  載せないと `style: solid` の関係が破線に見える。

- **ER 図でも書いた縦列に表を置けるようにした** (#1571)

  `type: er` は表を横 1 列にしか並べられなかった。 組み立て器が実体 1 つにつき帯を 1 本作り、
  必ず段 0 に置くため、関係を 4 本持つ実体があるとどう並べ替えても 2 本は隣を飛び越す。

  意匠帳 (`docs/design/er/note.md` § 記法の制約) が 4 通りを実測しており、1 列は縦横比 7.0 /
  最長の線 4068、格子は 3.5 / 836。 書き手が段を指定する口が無いので、後者は書けなかった。

  `縦列を選べる図種` に `er` を足した。 この集合は既に 5 図種を持ち、**全ての箱が縦列を
  書いた時だけ** 効く = 書かない図はいままでどおり組み立て器が並べる。

- **見本帳に色味の切替を足した** (#1569)

  配色は 2 組ある (生成りに茶 / 青磁に墨) が、見本帳では図に書いた方しか見られなかった。
  もう一方を見るには開発者の道具で `data-cdl-palette` を手で書き換えるしかなく、
  意匠を決める時に 2 つを見比べられない。

  描き手は色を持たない = 図に載るのは名前だけなので、切替も **名前を差し替えるだけ** で済む。
  速さ / 描き方と同じ形にしてある。

  **配色を持つ図でだけ出す**。 持たない図で名前を足すと、site の色で描かれていた図が急に
  別の色みになり「見比べる」 ではなく「着せ替える」 道具になる。

  記法のタブには掛けない。 速さは記法にも掛かるが、あれは記法に書いた秒数が画面と食い違う
  ため。 配色は記法に `palette:` として書いてあり、切替は「別の色で見る」 だけ。

### Changed

- **行頭の印を持つ箱の下の余白が詰まった** (#1579)

  依存を `^0.27.0` に上げた。 cdl 0.27.0 で箱の下の余白の起点が変わる。

  直す前は最後の行の帯の下に 48 空いていた。 行送りが 56 なので、48 が空の行 1 つに見える。
  最後の行に縞が乗る箱では余白が別の帯として読め、乗らない箱では最後の行が倍の高さに見えた。

  余白の起点が最後の行の中線から縞の帯の下端に変わり、24 になった。
  3 行の箱で高さが 340 から 316 になる。

  **効くのは行頭の印を持つ箱すべて**。 見本帳では ER 図 / 状態遷移図 / クラス図が対象になる。
  `pnpm run test:sweep` は 15 件とも通り、新しい指摘は出ていない。

  併せて意匠帳 `docs/design/class/note.md` の「箱の下の余白」 節を、engine 待ちの記述から
  直した後の記述に書き換え、絵を実際の描画に差し替えた。

- **cdl 0.26.0 を取り込んでクラス図に縞と配色を出した** (#1567)

  依存を `^0.26.0` に上げた。 cdl 0.26.0 で `classDiagram` に `rowStripe` と `palette` が
  入り、クラス図の箱も ER 図と同じ縞を持てるようになった。

  クラス図の箱は ER 図と同じ作り (行頭の印 + 左に名前 + 右に型) で、名前と型が離れて並ぶ。
  行を横に追う目印が要るのは同じだった。

  見本帳の `class-demo` に `palette: kinari` を書いた。 **縞の色は配色から取る** ので、
  書かないと縞が箱の面と同じ色になって 1 本も出ない。

  **行が 1 つの箱には縞が出ない** (cdl 側の変更)。 縞の役目は「隣り合う行を見分ける」 ことで、
  1 行しか無い箱には見分ける相手が居ない。 見本帳では `Auditable` の 1 本が消えて 8 本になった。

  記法 3 形すべてに配色を書いた。 組み立て API だけに書くと `catalog-source-parity` が落ちる。

## [0.19.0] - 2026-09-03

### Added

- **ER 図に行の縞と既定の配色を入れ、別の色みを指定できるようにした** (#1553)

  依存を `^0.25.0` に上げた。 ER 図は小さい字が密に並ぶので、行を横に追う目印が無かった。
  色を増やさず、表の箱の中を 1 行おきに薄く敷いて面で分ける。 縞は行の位置から導くので、
  行が動いても縞だけ取り残されることがない。

  **配色は名前だけを図に載せる**。 cdl は色を持たない (形だけを描く) ので、`palette:` に
  書いた名前が `data-cdl-palette` として舞台に出る。 色の値は `cdl-theme.css` が名前を見て
  決める = 色を変えても engine を出し直さずに済む。

  色を置く口は 7 つ (台 / 行の面 / 縞 / 枠 / 字 / 型名 / 線)。 決めた 2 組と根拠は
  意匠帳 (`docs/design/er/note.md`) が SSOT。

  | 名前 | 色み | いつ当たるか |
  |---|---|---|
  | `kinari` | 生成りに茶 | ER 図は書かなくてもこれになる |
  | `celadon` | 青磁に墨 | `palette: celadon` と書いた図だけ |

  台と行の面は必ず離す = 同じにすると箱の輪郭だけが浮いて面が消えて見える (明るい側で
  CIE L* を 8.2、暗い側で 7.9 離した)。 字 / 枠 / 線は面に対して対比 4.61 以上を保つ。

  **記法 3 形すべてに欄を通した**。 組み立て API だけに足すと text-dsl と JSON が読まない。
  併せて README の欄の一覧、JSON Schema、記法一覧の例文、検査の試す値も足した。

  縞と配色は **組み立ての出口で 1 度だけ当てる**。 図種ごとの組み立ては 20 か所以上あり、
  そのどれかに足すと残りが取り残される。 動きを持つ ER 図は `er()` 組み立て器を通らないので、
  出口で揃えないと同じ図が動きの有無で縞を持ったり持たなかったりする。

- **ER 図の意匠帳を納めた** (#1553)

  記法の制約 / 読み違いを生む形 / 線の引き方 / 手当ての効き / 見るところ / 配色 /
  行の組み方 / 関係の名前 / まだ決めていないこと の 9 節を
  `docs/design/er/note.md` に置いた。 図を 19 枚添えてある。

- **線の手当てを決めた ER 図を意匠帳に納めた** (#1549)

  09-01 に ER 図の線の手当てを 7 つ決め、6 つを cdl に入れた。 判断の材料にした図
  (`er-mesh`、8 表 8 関係、多対多が 2 組) が意匠帳に無く、下見の絵は残っていない。

  `/dragon-design` は「納めたら artifact を捨てる」 と決めているが、09-01 は engine への
  実装だけ進めて納めていない。 捨てる約束だけが働いた。

  `docs/design/notation/er/er-mesh/` に 3 file を置いた。 群を `er` にしたのは、
  この図が catalog に無く、線の手当てを測るために組んだものだから。

  「決めたこと」 には 09-01 に詰めた 6 手当てを、「見送ったこと」 には同日に却下した
  3 案 (弓の幅を札に合わせる / 手当てを 2 本に分ける / 1 本にまとめる) を書いた。
  いずれも判断の記録に理由が残っている。

- **意匠帳に時系列 / 構成 / 図表の 3 図を足した** (#1541)

  `presets` 群 18 図のうち意匠帳に入っていたのは `er-demo` (関係の型) 1 図だけだった。
  型の代表が無いと、次に足す図がどの型に属するかを判断する基準を持てない。

  | 型 | 図 | 選んだ理由 |
  |---|---|---|
  | 時系列 | `seq-demo` | 縦の時間軸でやり取りを並べる基本形 |
  | 構成 | `infra-demo` | 列と行で階層を作る基本形 |
  | 図表 | `chart-line-demo` | 軸・目盛・凡例・線の色と決める点が多い |

  `seq-demo` は他の型と違い箱を 1 件も持たない。 役者は縦線とその頭の名前で表し、
  囲まない = 囲むと箱の図に見えて、時間が下へ流れる軸が弱まる。

  `infra-demo` では同じ節から出る 2 本を重ねたまま残した。 太い幹から枝が出る形として読め、
  分けると図幅が広がるだけで得るものがない (cdl `#211` の判断がこの図でも成り立つ)。

  `chart-line-demo` は目盛の刻み方・値の位置・下端の線の 3 点を見送りとして残した。
  いずれも engine が決めており、意匠帳の側から指定する口を持たない。

  併せて `er-demo` の `source.cdl.ts` を取り直した (`#1545` の直しで 90 行 -> 127 行)。
  `look.svg` は決めた日の記録なので据え置く。

### Changed

- **cdl 0.24.0 を取り込んで箱の見せ方を画面に出した** (#1551)

  依存を `^0.24.0` に上げた。 cdl 0.24.0 で箱の見せ方 (`look` 5 通り) と
  辺の役目 (`role` / `labelPlate`) が入ったが、
  `cdl-theme.css` が `node-body > rect` の `fill` を `!important` で固定しており画面に出なかった。

  0.24.0 は見せ方を持つ箱に `data-cdl-look` を出す。 上書きを
  `:not([data-cdl-look])` で絞り、見せ方を持つ箱だけ engine の決めた面を通す。
  名前の色も同じ形で絞った = 墨のベタの上では紙の色で抜くので、上書きすると黒地に黒で消える。

  `infra-demo` に順路の役目と下地外しを書いた。 要求が通る 1 本道 (`User` から `App`) を
  朱で引き、預け先の 2 本は墨のまま = どちらも同じ色だとどこから読むかが決まらない。

  **記法 3 形すべてに欄を通した**。 組み立て API に書くだけでは text-dsl と JSON が
  読まず、`catalog-source-parity` が落ちる。 値は 4 層 (記法の読み手 / `DslStep` の型 /
  組み立て / JSON の写し) を通るので、1 層でも抜けると黙って落ちる。

  併せて README の欄の一覧、JSON Schema、検査の試す値も足した。
  いずれも「一覧が実装と drift しない」 ために実物から導く作りになっており、
  欄を足すと追随を要求される。

  線の太さの上書き (2.25) は据え置いた。 `er-demo` の意匠帳が「繋がりの線は箱の枠より太く、
  光る箱の枠より細い」 と決めており、engine の既定 (1.25 / 2.5) に落とすと食い違う。

- **意匠帳を決めた日の記録として読める形にした** (#1538)

  `#1531` で配色を茶墨に変えた時、意匠帳が 8/30 のまま取り残された。
  実測すると、いま画面に出ている色が意匠帳に 1 件も入っていない (`#fffdf7` / `#2a231d` /
  `#221811` / `#1a1611` が全て 0 件)。 代わりに藍系が 53 箇所残っていた。

  `note.md` にも古い色が literal で書かれており、4 値とも今の値と違っていた。

  **`look.svg` を追随させない**。 追随させると色を変えるたびに過去の決定が消える =
  「何を決めたか」 を残す場所ではなくなる。 代わりに、決めた日の記録だと読める形にした。

  | 直したもの | 何を書くか |
  |---|---|
  | `index.html` の冒頭 | 今の見た目は catalog で見る |
  | 各図の card | `note.md` の `decided:` から読んだ決めた日 |
  | `README.md` | catalog との役割の違い、色は変数名で書く |
  | `note.md` | 色を `--d-surface` 等の変数名へ |

  決めた日は `note.md` の frontmatter から読む。 file の更新日は使わない = 綴りの直しでも
  動くので「いつ決めたか」 を表さない。

- **図の色を紙と茶墨の 1 組に揃えた** (#1531)

  箱の枠が藍、繋がりの線が葡萄、図表がさらに別の帯と 3 系統に割れており、型が変わると
  図の印象まで変わっていた。 10 案から絞って紙と墨の方向を選び、墨に茶を混ぜた組に
  差し替えた。

  色の変数を明暗あわせて 63 件、設計 (`docs/design/app.pen`) の色を 33 件そろえている。

  承認した値のままでは線の 6 色のうち 3 組が判別できなかったため (枠 と 繋がり が
  明 6.0 / 暗 6.7、下限 15)、色相と色みは残したまま明るさだけを広げて離した。

  注意色が記述欄の地の上で 5.92 を満たすようになったので、`syntax.css` の派生をやめた。

- **線の見た目を 3 つ直した** (#1533)

  `@cardenelabs/cdl` を `^0.22.0` から `^0.23.0` へ上げた。

  交わる所を弧で跨ぎ (cdl #608)、同じ通り道を並んで走る線をずらし (cdl #610)、
  同じ辺に入る線の終点を辺に沿って離す (cdl #611)。

  同じ変更で、記法側の鍵と値を分ける空行もやめた (cdl #606)。 群の区切りは行頭の印が
  持っている = ER は鍵の名前に下線が付く。 空行はその 2 つ目の手掛かりで、代わりに行の
  間隔を不揃いにしていた。 組み立て器が空行をやめたので、記法を読む側も同時にやめる
  = 片方だけ残ると記法と図の照合が落ちる。

  線が動いたことで `interactive-traffic-sankey` の札 1 つが別の線に重なったので、
  左へ 40 寄せた。

### Fixed

- **段を書かない図で全ての箱が「いま」 の見た目になっていた** (#1557)

  `animation:` を書かなくても段は 1 つ作られ、そこに全ての箱が載っていた。 描き手は段に載った箱を
  「いま」 として描く (枠を主役色にして太くする) ので、静止した図は全ての箱が「いま」 になり、
  止まっている箱の枠が 1 度も出なかった (実測 = ER 図 3 箱 / 流れ図 2 箱とも主役色)。

  全部を強調するのと何も強調しないのは、どちらも「区別が無い」 状態。 後者の方が落ち着いて読める。
  段を書かない図では、組み立ての出口で箱を段から外す。

  **線は段に載せたまま置く**。 描き手の既定 (`edgeReveal: "phase"`) は「段が名指しする矢印は、
  その段が来るまで描かない」 ので、外すと静止した図から線が 1 本も出なくなる。

  判定は `animation:` を書いたかどうかで行う。 「段に全ての箱が載っている図」 を目印にすると、
  書き手が 1 段だけ書いて全部を焦点にした図と見分けが付かない。

  段を書いた図は変わらない = 段に当たる箱だけが「いま」 の色を持つ。

- **記法の抜き出しを宣言の塊で切るようにした** (#1545)

  `extractSource` は「id を含む行から前後へ `const` を探す」 形で切っていた。
  宣言が複数に分かれる図では途中だけを拾い、次の塊に前置きが付く図では他人のコメントまで
  含めていた。 4 図とも外していた。

  | 図 | 直す前 | 何が起きていたか |
  |---|---|---|
  | `er-demo` | 90 行 | 末尾が次の塊のコメント |
  | `seq-demo` | 72 行 | 同上 |
  | `infra-demo` | 35 行 | 末尾が無関係なコメント |
  | `chart-line-demo` | 6 行 | `LINE_POINTS` も段の宣言も落ちる |

  `chart-line-demo` の 6 行は定義されていない `LINE_POINTS` を参照しており、写しても動かない。

  深さを数えて宣言の塊で切り、直前に続く注釈だけをその塊のものとして連れて行く。
  id を含む塊が `export` でなければ組み立てて使う側まで辿り、参照する塊を前から集める。

  文字列と注釈の中の括弧は数えない = 数えると深さが狂い、以降の塊を 1 つも切れなくなる。
  参照は語の境界で見る = 文字の一致で辿ると `STEP_LONG` が `STEP` を持ち込む。

  落ちた名前が出た時は件数と名前を 1 行出す。 黙って落とすと、意匠帳が持つ 3 file のうち
  1 つが役割を失う。

- **段が落ち着いてから markup を控えるようにした** (#1543)

  `design-export.mjs` は段が変わったことを検知した その場で markup を控えていた。
  線を引く動きが終わるのを待たないため、最後に引かれる線が引きかけの状態で焼き付く。

  線本体は進行に応じて切り詰められる一方、その下に敷く光は経路を丸ごと使う。
  引きかけの瞬間は線が短く光だけが全長なので、**矢の先に光だけの棒が伸びた絵**になる。

  実測 (`infra-demo` の最後の段)。 同じ辺で 45.5 の差が出ていた。

  | 役割 | path |
  |---|---|
  | `edge-line` | `M 1714 458 L 2050.5 458.0` |
  | `edge-glow` | `M 1714 458 L 2096 458` |

  **待つ長さを決め打ちにしない**。 段の長さは engine が DOM に出しておらず
  (`data-cdl-phase-id` と `data-cdl-phase-index` だけ)、記法から辿っても段の id と
  対応が付かない。 「まだ引いている」 ことは markup 自身に出るので、光と線の経路が
  一致するまで読み直す。

  待つ間に次の段へ進んだ markup は捨て、次の周で拾い直す = 捨てないと前の段の名前で
  次の段の絵を控える。 諦めた時は理由を 1 行出す = 段が欠けた時に、待ちが足りなかったのか
  段が無いのかを分けるため。

- **吸い出しの実測を図の作りに追随させた** (#1540)

  寸法を測る対象が `node-body` の `rect` に決め打ちで、図の作りが違うと値が取れず
  行だけが `—` で残っていた。 `—` は「該当なし」 と読めるが実体は「測れなかった」 で、
  2 つが混ざると意匠帳の記録が「決めなかった」 のか「測れなかった」 のか後から引けない。

  | 直した点 | 効いた図 |
  |---|---|
  | 表の行を図に在る役割から組む。対象 0 件の役割は行ごと出さない | `seq-demo` (箱を 1 件も持たない) |
  | `g` で組む箱は中で最も大きい図形まで降りて測る | `infra-demo` (6 件中 1 件で絵記号を拾っていた) |
  | 段ごとに測り、役割ごとに最も多く見つかった段を採る | `er-demo` (繋がり 6 本が 1 本も測れていなかった) |
  | 一部しか取れない時は母数を併記する | `infra-demo` (角を持たない箱が 1 件) |

  `seq-demo` は直す前は寸法 4 行が全て `—` だった。 いま縦線の間隔 224 / 帯の幅 52 等
  9 行が出る。

### Security

- **依存の既知脆弱性 41 件を解消した** (#1528)

  `osv-scanner scan source -r` で `pnpm-lock.yaml` を見たところ 41 件が出ていた
  (CVSS 7.0 以上が 15 件)。 いずれも推移依存で、0 件にした。

  **大半は lockfile が古かっただけ**。 各 `overrides` を 1 つずつ外して測ると、
  外した時に脆弱性が戻るのは 4 package のみで、残り 9 package は入れ直すだけで
  安全な版に解決された (`nanoid@3.3.18` / `postcss@8.5.23` / `undici@7.29.0` 等)。

  semver が修正版を止めている 3 件だけを `package.json` の `pnpm.overrides` に書く。

  | package | 上げ先 | 外した時 |
  |---|---|---|
  | `dompurify` | 3.4.13 | 18 件 |
  | `brace-expansion@1` | 1.1.18 | 3 件 |
  | `qs` | 6.15.2 | 1 件 |

  効いていない `overrides` は残さない = 下限であると同時に上限でもあるので、
  上流が新しい patch を出しても古い版に固定され続ける。

  **`esbuild` だけは上げられなかった** (`#1535` で解消)。 修正版 0.28.1 に上げると開発 server が
  起動しなくなる = 分割代入を es2020 へ変換できないという誤りが 1055 件出て、依存の束ね作りが
  落ちる。 残る 1 件は CVSS 2.5 で「HIGH 0 件」 の範囲外だったため切り出した。

- **開発 server の依存の変換先を本番と揃えた** (#1535)

  `build.target` は `es2022` を指していたが、**開発 server には届いていなかった**。
  build は rollup が束ね、開発 server は esbuild が別に束ね直すので、変換先も別に決まる。
  書かないと vite の既定 (`chrome87` / `es2020` 等) が効く。

  `optimizeDeps.esbuildOptions.target` に `es2022` を置いて揃えた。
  これで `esbuild` を 0.28.1 に上げられ、脆弱性が 0 件になった。

  **配信する成果物の範囲は変わらない**。 `optimizeDeps` は手元の開発 server にしか出ない。
  揃えたのは、開発で通った構文が本番で落ちる形を作らないため。

  直接依存の major は上げていない。 確かめ方は `README.md` の「依存の脆弱性を確かめる」 に書いた。

## [0.18.3] - 2026-08-30

### Changed

- **伸びた箱の字が中央に来るようになった** (#1512)

  `@cardenelabs/cdl` を `^0.21.2` から `^0.22.0` へ上げた。 段の高さは行内の最大値で決まる
  ので、行に高い箱が 1 つあると同じ行の箱も伸びる。 字は上から置かれるため、伸びたぶんが
  下に空いていた (実測 = 240 の箱に 67 ぶんの字が上端に貼り付いていた)。

  **この不具合は画面を 1 枚ずつ見て見つけた**。 検査は cdl / dragon / 画面のすべてが
  通っていた。

## [0.18.2] - 2026-08-30

### Changed

- **雲の字が絵の芯に載るようになった** (#1508)

  `@cardenelabs/cdl` を `^0.21.1` から `^0.21.2` へ上げた。 雲は箱の上へはみ出し下は手前で
  終わるので絵の芯が箱の芯より上にあり、字が絵に対して下へ寄っていた (上 71.5 / 下 27.9)。
  直して 49 / 51.5 になる。

  動くのは雲 (`cloud`) を使う図だけ。 見本には雲を使う図が無いため、記録は 1 件も動かない。

## [0.18.1] - 2026-08-30

### Changed

- **最後の回避を外した** (#1504)

  `@cardenelabs/cdl` を `^0.21.0` から `^0.21.1` へ上げた。 説明が別の線の縦区間の真下に
  張り付く形を engine が解消するようになり、`interactive-kpi-dashboard` に残していた
  高さの指定が要らなくなった。

  #1498 で 8 件に書いた回避は、これで `infra-demo` の中身 (小見出しと説明) だけが残る。
  そちらは中身として妥当なので外さない。

- **見本 6 件の回避を外した** (#1502)

  `@cardenelabs/cdl` を `^0.20.1` から `^0.21.0` へ上げた。 説明どうしの近接を engine が
  解消するようになり、#1498 で見本に書いた高さの指定が要らなくなった。

  `interactive-kpi-dashboard` の指定は残す。 engine は world unit で間隔を確保するが検査は
  px に投影して測るため、この図 (viewBox 2511) だけ engine 側で解消できない。

  `infra-demo` に書いた小見出しと説明も残す = 中身として妥当で、外す理由が無い。

## [0.18.0] - 2026-08-29

### Added

- **図の並ぶ向きを記法から選べるようにした** (#1494)

  最上位に `direction:` を書く。 `縦` は 1 つの縦列に積み、`横` は 1 人ずつ縦列を作る。
  英語でも書ける (`vertical` / `horizontal`)。

  これまで向きは 2 つのことに紛れていた = どの `type` を選ぶかと、縦列を書き分けるかどうか。
  その結果、流れ図は縦に固定され、横にしたければ泳法図を選ぶしかなく、すると縦列の見出しが
  付いてきた。

  効くのは `flow` と `swimlane` だけ。 他の図種は並び方そのものが読み方を担う (表の図は
  「1 縦列 1 表」、クラス図と状態の図は格子、順序図は 1 枚の板) ため、書くと知らせが出る。
  全ての箱が縦列を書いた形では、書いた縦列が勝つ (同じく知らせが出る)。

  **書かない図の並びは変わらない**。 描画側の変更も無い = 縦列 (`lane`) が横、段 (`stack`) が
  縦という置き方は元からで、足したのは名指しする言葉だけ。

### Changed

- **箱の高さを書いた段の数から決めるようにした** (#1498)

  `@cardenelabs/cdl` を `^0.19.0` から `^0.20.1` へ上げた。 名前 1 語だけの箱が
  `actor` 165 → 73、`function` 151 → 67、`service` 等 21 種別が 116 → 54 になる。
  3 段 (小見出し + 名前 + 説明) を書いた箱の高さは変わらない。

  段の芯が動くので golden を取り直した。

  **見本 8 件に中身か高さを書き足した**。 箱が低くなって段が近づき、斜めの線の説明とその行の
  横線の説明が 33 しか離れなくなった (要る間隔は 36)。 `infra-demo` は小見出しと説明を書いて
  中身ぶんの高さに戻し、`interactive-*` の 7 件は高さを書いた。

  余白の検査を 4 つの書き方 (名前だけ / 小見出し + 名前 / 名前 + 説明 / 3 段) へ広げた。

- **字だけの箱の高さを詰めた** (#1496)

  `@cardenelabs/cdl` を `^0.18.1` から `^0.19.0` へ上げた。
  字しか持たない箱が字の下に余白を大きく残す形が直り、23 種別が 19 から 104 縮む
  (`person` / `service` / `api` 等 18 種別は 116 に揃う)。

  この repo に高さの値は無い = 高さを決めるのは cdl 側で、こちらは版を上げるだけ。
  段の芯 (`cy`) が動くので golden 51 件を取り直した。

  **上下の余白が揃っているかを画面で測る検査を足した**。 golden は「その値である」 ことしか
  言わないので、次に cdl が高さを変えた時に揃っているかを誰も見ていない状態だった。

## [0.17.0] - 2026-08-29

### Added

- **矢印をいつ出すかを記法から書けるようにした** (#1470)

  最上位に `reveal:` を書く。 `phase` (既定) は「段が名指しする矢印は、その段が来るまで
  描かない」、 `all` は段に関わらず最初から全部描く。

  `cdl#582` で描き手が既定を `phase` にした。 段を進めるごとに関係が 1 本ずつ増える図で、
  全ての矢印が 1 段目から見えていたため (実測 = 段が矢印を引き直しても「増えた」 とは読めず、
  無関係な線がちらついたようにしか見えない)。

  **段が矢印を 1 度も名指ししない図は既定でも隠れない**。 そういう図では矢印が骨格そのもの
  なので、既定を変えても既存の図は動かない。

  依存を `^0.17.1` から `^0.18.0` に上げた。

- **4 図の見た目 3 つ (端の形 / 自己参照 / 始点終点) を記法から書けるようにした** (#1462)

  4 図 (クラス / ER / 状態 / シーケンス) の設計に対し、描画側に無かった 3 つを `cdl#560` で
  足して 0.15.0 で公開した。 ところが **記法にも組み立てにも配線していなかった** ので、
  画面は 1 つも変わっていなかった。

  **端の形** を `flow` の矢印に書けるようにした。

  ```
  flow:
    - Dog -> Animal: "継承" { head: triangle }
    - Dog -> Leg: "合成" { head: diamond }
  ```

  端の形で関係の種類を示す = `triangle` (継ぐ) / `diamond` (持つ) / `open` (使う) /
  `crow` (多)。 書けないと 4 種とも同じ塗った三角になり、線の種類だけで 6 種の関係を
  区別することになる。 受ける語は描画側の一覧から導くので、描画側が形を増やせば書ける。

  **自己参照** を落とさなくなった。 #1227 の時点では描画側が両端の同じ矢印を受けず、
  組み立てから外して知らせていた。 描画側が輪として描けるようになったので外す理由が消えた。
  設計は 3 図で使う = 状態の自己遷移 (催促する) / シーケンスの自分宛て (控えを書く) /
  ER の自己関係 (REPORTS TO)。

  **`type: flow` の静止図だけは残らない**。 登場人物を書いた順に鎖状に繋ぐ形で、
  書いた矢印そのものを使わないため。 段を書くと別経路になり残る。

  **始点終点の印** は #1450 で書けるようになっている (`kind: mark-start` / `mark-end`)。

### Changed

- **図を 1 つの箱で描く種別で、パーツが図に重なるのを直した** (#1481)

  パーツの置き場所は「既存の図の下」 に決まる。 その下端を **箱の数** から概算していたため、
  図を丸ごと 1 つの箱で描く種別で足りなくなっていた。

  #1466 で順序図が 1 枚の板になり、高さ 432 の箱 1 つになる。 1 箱 = 1 段 (280) と数えられ、
  その下に置いたパーツが板に 31px 重なっていた (実測)。

  大きさを自分で持つ箱は、その高さから段数を出すようにした。 高さを書かない箱は今までどおり
  1 段と数えるので、既存の図の並び方は変わらない。

- **「局面」 を「シーン」 に言い換えた** (#1472)

  図に重ねる札と、その周りの説明で使う語を揃えた。 段の呼び名として「局面」 は硬く、
  何が起きているかが読み取りにくい。

  依存を `^0.18.0` から `^0.18.1` に上げた。 `cdl#584` で **矢印が伸びるのは初めて出る
  シーンだけ** になり、既に見えている線がシーンの変わるたびに引き直されなくなる。

- **箱の色と字を画面で見て直した** (#1468)

  #1466 の見本を実機で見ると、箱が濁って字が読みづらかった。

  | 直したもの | いままで | これから |
  |---|---|---|
  | 図の紙 | 白 `#ffffff` | 暖かい地 `#faf9f6` |
  | 箱の面 | 砂 `#e8e6df` | 白 `#ffffff` |
  | 関係の札 | 砂 `#e8e6df` | 砂 `#f2f1ec` (箱と分ける) |
  | いまの言づて | 意味の青 | 主役の朱 |

  **紙と箱を入れ替えた**。 白い紙に砂の箱を置くと、箱の方が濁って見えて図全体が沈む。
  地の上に白い紙の箱が乗る形にすると濁りが消える。

  「いまどこか」 の色は `--cdl-now` で渡す。 意味の 6 色 (`--cdl-tone-*`) は図の中で種類を
  描き分けるもので、この repo は青を当てている。 板が指すのは意味ではなく「いま」 で、
  役割は強調した箱の枠と同じ = 主役色に揃える。

  依存を `^0.16.0` から `^0.17.1` に上げた。 `cdl#578` で **行頭の印と両端の形 / 塗り、
  それに 1 枚の板 (`sequence-board`)**、`cdl#580` で **題を行より大きくする字の 3 段と
  段の芯での揃え** が入る。

- **順序図を 1 枚の板で描くようにした** (#1466)

  面ごとに名札 / 余白 / 足の 3 箱と縦線を組んでいた形をやめ、図を丸ごと 1 つの箱
  (`sequence-board`) が描く。 言づては矢印ではなく板の中の行になり、段は「どこまで描くか」 を
  状態に書く。

  記法に 6 つの項目を足した。

  | 項目 | 場所 | 内容 |
  |---|---|---|
  | `bands:` | 最上位 | 面が動いている間の帯。 途中で手が空く面は区間を分けて書く |
  | `marks:` | 面 | 行頭の印。 語の意味は図の種類が決める (`pk` / `+` / `entry` 等) |
  | `kind:` | 矢印 | 言づての種類 (`call` / `return` / `fire`) |
  | `relation:` | 矢印 | クラス図の関係の語。 端の形 / 塗り / 線種がまとめて決まる |
  | `tailHead:` | 矢印 | 出どころ側の端の形 |
  | `headFill:` / `tailHeadFill:` | 矢印 | 両端の塗り (`solid` / `hollow`) |

  板は名前と呼び名だけを描くため、面に書いた種類 / 大きさ / 位置 / 行 / 色と、言づてに書いた
  色味 / 添え字 / 寄せは効かない。 黙って落とさず知らせを出す
  (`actor-kind-not-honored` / `message-option-not-honored`)。

  依存を `^0.16.0` から `^0.17.0` に上げた。 `cdl#578` で **行頭の印と両端の形 / 塗り、
  それに 1 枚の板 (`sequence-board`)** が描けるようになり、設計の 2 軸が図に出る。

- **4 図の見本を設計どおりの見た目に直した** (#1464)

  #1462 で 3 つを記法から書けるようにしたが、**見本が 1 つも使っていなかった**ので
  画面は変わっていなかった。 画面に出るのは見本なので、そこを直さないと届かない。

  依存を `^0.15.0` から `^0.16.0` に上げた。 `cdl#574` で **クラス図の関係の種類から
  端の形が決まる** ようになり、組み立て API 経由の図でも端の形が出る。

  | 図 | 直したところ |
  |---|---|
  | クラス | `extends` が三角、`aggregates` が菱 (記法にも `head:` を書いた) |
  | 状態 | 再試行を自分へ戻る輪にした (従来は別の状態へ戻していた) |

  **記法 / JSON / 組み立て API の 3 つを揃えた**。 片方だけ直すと、画面と「コード」 のタブで
  別の図になる。 揃っていることは既存の検査 (`catalog-source-parity`) が見る。

  **状態図に始点終点の印は入れていない**。 `stateMachine` preset が印の状態を作れず、
  記法だけに入れると 3 つの書き方がずれる。 印は `text-dsl` の見本が持つ。

### Fixed

- **押せない button の字が読めなくなっていたのを直した** (#1488)

  エディタの知らせにある「一括反映」 は、対応できる警告が無いと押せなくなる。 その状態に
  `opacity: 0.65` が掛かっており、知らせの地の上で対比 2.51 (明) / 2.95 (暗) まで落ちていた。
  下限は 4.5 で、字の色 (`--d-text-muted`) だけなら 4.78 / 4.82 と足りている = 不透明度が
  原因だった。

  押せないことは枠の色と `not-allowed` が伝えるので、不透明度を外した。

- **図表の色が暗い画面で入れ替わるようにした** (#1480)

  描画側は図表の系列色を `var(--cdl-chart-N, <既定>)` の形で読むが、この repo は
  `--cdl-chart-1` から `--cdl-chart-6` を 1 つも定義していなかった。 既定色がそのまま出るので
  明暗で色が変わらず、暗い箱の上で 6 色中 3 色が対比の下限 3.0 を割っていた
  (実測 = 藍 1.76 / 紅 2.31 / 若草 2.79)。

  色相はそのままに、暗い側だけ 1 段明るくした。 いちばん低い対比が 1.76 から 4.67 に上がる。
  明るい側は描画側の既定と同じ値を書き写したので、見た目は変わらない。

- **エディタの舞台と図の箱が同じ色になるのを直した** (#1480)

  舞台 (`.v4-editor-stage`) と箱の面がどちらも `--d-surface` を指していた。 箱の面が
  この変数に移ったのは #1468 で、舞台は前から使っていたため暗い画面で完全に同じ色になっていた。

  舞台をカタログと同じ地 (`--d-bg`) に揃えた。 図の後ろの紙は元からこの色を塗っているので
  見た目は変わらず、舞台の地だけが図の紙と揃う。

- **画面の依存の束ねが古いことを検知するようにした** (#1460)

  依存を上げても、動いていた開発 server は前に作った束ね
  (`node_modules/.vite/deps`) を使い続ける。 Vite が束ねを作り直すのは **server の起動時だけ**
  なので、動かしたまま `pnpm install` しても気付かない。

  実測すると、束ねは 8/27 08:11 のままで **`drawRatio` も `chart-stat` も 1 件も入って
  いなかった** (実物は 8 件 / 14 件)。 画面は 0.15.0 を 1 つも反映しておらず、
  記法に足した項目を書いても何も出ない状態だった。

  検査の前段で、束ねが `pnpm-lock.yaml` より古くないかを見る。 落ちた時は
  「server を止めて `pnpm dev` で起動し直す」 と案内する。

  **server の起動時に見る形は採らない**。 動かしっぱなしの server には効かない
  (起動時にしか走らない)。 検査は依存を上げた後に必ず回すので、そちらに置く。

  束ねが無い形は落とさない = server を 1 度も起動していない人を止めない。

## [0.16.0] - 2026-08-28

### Added

- **入れた依存が公開されている中身と一致するかを確かめられるようにした** (#1456)

  `pnpm verify:deps` で、公開されている tarball を取り寄せて `node_modules` の中身と
  **file 一覧と中身の hash** で突き合わせる。 差は 3 種に分けて出す。

  | 差 | 意味 |
  |---|---|
  | 余分 | 手元で書き加わった (#1448 の形) |
  | 欠け | 手元で消えた |
  | 中身違い | 手元で書き換わった |

  #1448 で未公開の build が `node_modules` に入り、公開版に無い種別を使う記法を merge した。
  #1454 で発生源を 7 経路にわたって調べたが特定できなかったので、**入ったことを検知する側**
  を持つ。

  実測で 23 依存すべてが一致した (`@cardenelabs/cdl@0.15.0` は 123 file が完全一致)。
  差が出たら手元で変わっていることを意味する。

  **日常の検査には入れない**。 network が要るので、手で回すか依存を上げた後に回す。
  比べる処理は 2 つの dir を受けるだけにしてあり、検査は fixture で回す。

  対象は `dependencies` から導く = 依存を足せば自動で入る。 `devDependencies` は
  出荷物に入らないので見ない。

  **測れなかったことを「一致」 と数えない**。 場所を引けない / 取り寄せに失敗した依存は
  「飛ばした」 として数え、1 件でもあれば非 0 で終わる。 共通の file が 0 件の時も
  「一致」 とは言わない。


- **状態の図で始点終点の印を書けるようにした** (#1450)

  `- 始: { kind: mark-start }` の形で、「ここから」「ここまで」 を状態そのものと分けて
  示せるようにした。 `mark-start` は塗りつぶした丸、`mark-end` は輪の中に丸。

  静止した状態の図は `stateMachine` 経由で組み立てており、**書いた `kind:` が黙って消えて
  いた**。 動く図 (`animation:` を書いた図) だけは別経路を通るため効いており、
  同じ記法が動きの有無で違う結果になっていた。

  **既定と違う種類を書いた図だけ経路を切り替える**。 「書いたか」 だけで判定すると、
  記法だけの種類 (`kind: state` 等) も対象になる = 読み替えた後は既定と同じ形なので、
  切り替える理由が無いのに既存の図の id と枠の作りが変わる。

  #1263 が縦列 (`lane:`) について同じ形を直しており、これはその 2 例目。

  これで #1446 で依存を 0.15.0 に上げた時に増えた 7 種すべてが記法から書けるようになり、
  種別に見本があることを見る検査の対象外が空に戻った。

- **値で描く図表に 3 種を足した** (#1450)

  `type: stat` (値 1 つを大きく示す) / `type: waffle` (割合を 100 個の印で示す) /
  `type: stacked` (内訳と時点間の変化を帯で示す) の 3 種。
  #1446 で依存を 0.15.0 に上げた時に増えた 5 種のうちの 3 種で、種別に見本があることを見る
  検査の対象外から外した (残る 2 種は登場人物として書く形が未定なので #1450 に残す)。

  既存の 5 種 (`pie` / `bar` / `line` / `gauge` / `radial`) と同じ経路に載せた。
  3 種とも中身の形 (`label` + `value`) が同じなので、新しく要るのは **前の時点の値** の
  1 欄だけだった。

  `- 新規: { value: "320", previous: "280" }` の形で書く (`前の値` とも書ける)。
  `type: stacked` は 2 本目の帯として描き、`type: stat` は差として出す。
  書かない図は 1 本のまま = 内訳だけを示す図としても使える。

  **3 つの書き方すべてで受ける**。 中括弧の形 / 縦に並べた形 / 日本語の項目名 のどれでも
  書ける。 1 つだけに配線すると、書き方を変えた時だけ「書いたのに 2 本目の帯が出ない」 が起きる。

  読めない値は黙って捨てず知らせる。 その時も値そのものは残すので、図は消えない
  (前の値は見え方の足しなので、読めないことを理由に節ごと落とさない)。

  高さは帯だけ 368 で、値 1 つと印は 320。 描画側の既定と揃えないと、同じ値を同じ図種で
  描いても見本と記法で高さが変わる。

- **描く速さを段の長さと別に書けるようにした** (#1441)

  段の `draw` に載せた図は起点から現れるが、伸び具合は **段の進みそのもの** で決まっていた。
  そのため「線はゆっくり引きたいが値の移りは短くしたい」 が書けず、描く速さのために段の長さを
  動かすことになる。 段の長さを動かすと同じ段の `tween` まで遅くなる。

  `draw: line 0.4` のように **語の後ろに割合を書ける** ようにした。
  「描き終えるまでに段の何割を使うか」 で、0.4 なら進みが 0.4 に達した時点で描き終わり、
  残りの 0.6 は描き終わった姿のまま値だけが動く。

  **語と同じ行に書かせる**。 別の項目にすると「割合だけ書いて `draw` が無い段」 が書けてしまい、
  何も起きない指定になる。 同じ行なら書けない。 JSON は `drawRatio` を別の欄で持つので
  その形を作れるが、写す時に外して記法側と同じ状態に揃える。

  描画側は `@cardenelabs/cdl@0.15.0` の `drawRatio` (cdl#570) が受ける。
  掛かる相手は描く対象の箱だけで、同じ段の他の箱と矢印と `tween` は元の速さのまま。

  範囲外 (0 以下 / 1 超 / 数でない) と、項目が 3 つ以上ある形は解析が知らせる。
  黙って捨てると「書いたのに速さが変わらない」 が手掛かりなしで起きる。

- **描画側を 0.15.0 に上げ、半円と弧の図を記法から書けるようにして見本を置いた** (#1446)

  依存を `^0.14.0` から `^0.15.0` に上げた。 `chart-gauge` (合計を半円で示す) と
  `chart-radial` (弧の長さで比べる) が入ったので、記法 (`type:`) から書けるようにする。
  依存の種別は 89 個から 96 個に増えた。

  **一度は公開前に着手して戻している** (#1448)。 手元の `node_modules` が未公開の build で
  上書きされており、0.14.0 に 2 種があるように見えていた。 実際の 0.14.0 には型にも実行時にも
  無く、入れ直すと build が落ちた。 公開されたのでここで入れ直す。

  既存の 3 種 (`pie` / `bar` / `line`) と同じ経路に載せる。 どれも「名前と値を並べる」 形で、
  1 つの関数が受けているため、型と分岐を足すだけで通る。

  高さは円と同じ 320 にした。 半円と弧は縦を使わないので、棒と折れ線の 368 は要らない。

  **段の `draw` は受けない**。 描画側が半円と弧に「起点から描く」 動きを持たないため
  (`cdl#549` / `cdl#550` で対象外にした)、書いても効かない。 書いたのに効かない語を
  案内に載せない。

  見本は記法で書いた (`charts` の 9 種と同じ形)。 組み立て API で書くと「エディタで開く」 が
  効かず、コードを見る経路も無くなる。

### Fixed

- **型定義が 1 つに決まらない時の案内を件数で書き分けた** (#1454)

  表を作る 2 つの段は、依存の `dist` から目当ての型を持つ file を 1 つに絞れないと止まる。
  止まる形は正しいが、**message が誤った原因を指していた** = 何件でも
  「描画側の作り方が変わった可能性があります」 と出ていた。

  #1448 でこの message を読み、「手元の環境の問題」 として脇に置いた。 同じ汚れは
  「種別が 2 つ多い」 という形でも現れており、そちらを信じて公開版に無い種別を使う記法を
  merge してしまった。

  **件数で原因が分かれる**。 公開されている全 9 版 (0.9.0 から 0.15.0) が型定義を
  ちょうど 1 つしか持たないことを実測したので、2 件以上は配布物がそうなっている形では
  ありえない = 手元で書き加わったことを意味する。

  | 件数 | 案内 |
  |---|---|
  | 0 件 | 描画側の束ね方が変わった可能性がある (探している目印を添える) |
  | 2 件以上 | 手元の install が汚れている。 `pnpm install --force` で入れ直す |

  絞り込みと文面は共通の場所に置いた。 2 つの段が同じ形を持っていたので、片方だけ直すと
  同じ状況で別のことを言う。

  **判定は緩めていない**。 1 つに決まらない時に止めるのは正しい (どの定義から出したのかが
  後から辿れなくなる)。 変えたのは止まった後に渡す情報だけ。


- **公開されていない種別を使っていたのを戻し、同じ形を止める検査を足した** (#1448)

  #1446 の最初の版は「描画側は半円と弧を描けるのに記法が受け付けない」 という見立てで
  2 種を足したが、**当時公開されていた 0.14.0 に 2 種は無かった** (型にも実行時にも無い)。
  手元の `node_modules` に未公開の build が上書きされており、種別の一覧に 2 種が入って
  見えていた。

  同じ汚れは型定義の file が 1 つから 3 つに増える形でも現れ、それを見る 2 つの検査
  (`input-table-generated` / `readout-table-generated`) が落ち続けていた。
  **その 2 件を「手元の環境の問題」 として脇に置いたのが分かれ目**。 同じ汚れが
  「種別が 2 つ多い」 という形でも嘘を返していることに気付けなくなった。

  依存が知らない種別を使っていないかを見る検査を足した。 記法が出しうる種別を組み立てを
  実際に走らせて集め、`NODE_KINDS` に無いものが 1 つでもあれば落ちる。 母集団は実物から
  導くので、型を足せば自動で入る。

- **描き直す時だけ段の長さを揃え、動かすだけの段を短いままにした** (#1444)

  #1440 は描き直しが 1 段目より 2.7 倍速いのを、見本の 2 段目に長さを書いて塞いだ。
  **書いた長さは既定の「動かすだけ」 でも効く** ため、値が移るだけの段まで 0.9 秒から
  2.4 秒に伸びていた。 既定は「動かすだけ」 なので、見本の頁を普通に開いた人が見るのは
  長くなった側だった。

  揃える処理を見本側から写す側 (`redraw-mode.ts`) へ移した。 `描き直す` は 1 段目の `draw` を
  2 段目以降へ写すので、同じ場所で長さも写せば、切替が入の時だけ揃い、切の時は見本に書いた
  ままの長さで動く。

  記法 (YAML / JSON) の表示も同じ扱いにした。 図だけ揃えて記法が古いままだと、画面に出ている
  記法を写しても同じ絵にならない。

  見本の 2 段目からは長さの指定を外し、元の既定に戻した (組み立て API 7 件 / YAML 7 件 /
  JSON 7 件)。

- **部品が図の倍率に追随することを見る検査を足した** (#1436)

  図の倍率を上げた時に canvas へ重ねた部品も一緒に拡大することを、実ブラウザで見る検査が
  どこにも無かった。 主張そのものは説明文として残っていたが、それを使う検査は #923 で消えており、
  説明文だけが置き去りになっていた。

  図の SVG と部品の **両方** を測る。 片方だけを見ると、両方が動かない形と両方が動く形を
  区別できない。 大きさだけでなく置き場所も見る (取り残される形は位置にも出るため)。

- **描き直しが 1 段目より 2.7 倍速かったのを揃えた** (#1440)

  `描き直す` を選ぶと 1 段目の `draw` が 2 段目以降へ写され、その段でも起点から描き直される。
  ところが伸び具合は **段の進みそのもの** で決まる = 描画側は段の進み 0→1 を
  `strokeDashoffset` や角度に直接使い、段の長さと別の「描く時間」 を持たない。

  1 段目 2.4 秒に対して 2 段目は既定の 0.9 秒のままだったため、同じ絵が 2.4 秒かけて
  描かれた後 0.9 秒で描き直されていた。 1 段目が作った期待を裏切る形になっていた。

  `draw` を持つ 7 見本 (`tree` / `user-journey` / `mind-map` / `funnel` / `pie` / `line` /
  `gantt`) すべての 2 段目に同じ長さを置いた。 1 つだけ直すと同じページの中で間がばらつく。

  代償として、既定の「動かすだけ」 側でも 2 段目が 0.9 秒から 2.4 秒に伸びる。 描く速さを
  段の長さと別に指定できれば代償なしで直せるため、#1441 に切り出した。

- **画面の検査を build 済へ移し、実行中の編集で壊れないようにした** (#1439)

  検査は開発 server を見ており、実行中に file を編集すると Vite が繋いでいる画面を
  全部読み込み直して走行中の検査を巻き添えにしていた。 落ち方が「要素が現れず 30 秒で
  時間切れ」 に見えるため、2 度とも flake として扱われていた。

  見に行く先の既定を build 済の画面 (`/dragon/`) にした。 build 済は編集で作り直されない。
  副産物として、大半の検査が subpath 配信の検証も同時に得るようになった。

  開発時のみの頁 (`/__render`) を使う 1 件だけは build 済に route が無いため開発 server に
  残し、待ちに上限を付けて「壊れた」 と読める形で落ちるようにした。

  公開物 (`packages/dragon/src/`) は触っていない。

- **呼ばれていない宣言 5 件を消し、`lint` の errors を 0 にした** (#1435)

  `no-unused-vars` の 5 件。 helper 2 件 (`dropPart` / `getAllPartsRects`) は #923 が
  部品の drag drop ごと機能を外した際の置き去りで、使う側の検査はあの PR で既に消えていた。

  `dropPart` に付いていた説明文は #1436 に原文で切り出した。 「図全体を拡大した時、
  canvas に置いた部品も一緒に拡大する」 という主張で、部品を DSL から置く今も成り立つが、
  倍率の検査は **図の SVG しか測っていない** ためどの検査も見ていない。 消すと主張ごと消える。

  公開物 (`packages/dragon/src/`) は触っていない。

予定 ... feedback を反映した patch / minor。

## [0.15.0] - 2026-08-26

### Added

- **`CdlDiagram` の型を公開 API から引けるようにした** (#1415)

  `textDslToDiagram` が返す型そのものだが、再 export が無かった。 使う側が戻り値に型を
  付けるには `@cardenelabs/cdl` を直接の依存に足す必要があり、足さずに書くと
  **型が暗黙の `any` に落ちてその先の `.map((n) => ...)` まで検査が効かなくなる**。

  実測でこの 1 件が **41 件** の型の誤りを生んでいた。

- **検査 code の型検査を回せるようにし、天井を固定した** (#1413)

  `packages/dragon/test/` は **型検査を 1 度も通っていなかった**。 `tsconfig.test.json` は
  `include` に `test/**` を持つが、読むのは eslint だけで `tsc` からは呼ばれない。
  eslint は型情報を parse に使うものの、TypeScript の compile error は報告しない。

  #1411 で `satisfies Readonly<Record<PresetType, ...>>` が 13 型しか並べていないのに
  通っていたのは、この穴の現れ方だった。

  設定側の `rootDir` を直して回せるようにすると **211 件** の誤りが出た
  (`rootDir` は出力の配置を決める設定で `noEmit` では意味を持たないが、親から `src` を
  継承して読む file すべてを「範囲外」 にしていた)。

  211 件は 1 PR で直さず、**今の件数を天井として固定する検査** を置いた。
  増えれば落ち、減らしても落ちる (天井を下げろと言う)。 内訳は #1414 / #1415 / #1416 /
  #1417 に分割した。

- **`type: solidity` の見本を足した** (#1411)

  記法が受ける図種は 18 あるが、**`solidity` だけ見本が 0 件**だった。

  `solidity` は `sequence` の別名ではなく、箱の種別で縦列を並べ替える固有の機能を持つ。

  | 種別                                               | 並び   |
  | -------------------------------------------------- | ------ |
  | `eoa` / `actor` / `multisig` / `signer` / `wallet` | 0 (左) |
  | `contract` / `proxy` / `library` / `interface`     | 1      |
  | `storage`                                          | 2      |
  | `event`                                            | 3 (右) |

  書いた順に関わらず「人 → 契約 → 保存 → 出来事」 で並ぶため、契約のやり取りを読む時に
  左から右へ流れが揃う。 **見本が無かったため、この機能を知る手段が無かった**。

  見本は `cookbook` に置き、わざと逆順 (出来事 → 保存 → 契約 → 人) で書いてある。
  順に書くと `sequence` との違いが見えない。

### Changed

- **添字の型の厳しさを `packages/dragon` と揃え終えた** (#1426)

  #1418 で固定した天井のうち、画面の検査 11 file が持っていた **86 件** を直した。
  これで 3 群すべてが片付き、天井が **0** になった。

  0 に達したので設定を本体の `tsconfig.json` へ移し、`tsconfig.strict.json` と天井を
  固定する検査を消した。 `packages/dragon` と `apps/playground-spa` の型の厳しさが揃った。

  **天井を消すと守りも消える**。 設定を外しても誤りは 0 件のままで `tsc -b` も
  `pnpm build` も通る (実測)。 件数ではなく **実効値** を `tsc --showConfig` で見る検査を
  代わりに残した。

  `!` は 1 件も足していない。 文字の大きさを測る検査では、既存の `!` 12 個を確かめてから
  返す helper に置き換えて減らした。 色を重ねる計算では 3 つ組を 1 つずつ書く形にして、
  `as` の潰しも 2 箇所外れた。

  副次的に `no-unnecessary-type-assertion` の警告が **437 → 85** に落ちた。 設定が無い間
  eslint が「不要」 と報告していた `!` の 352 件は、実際には必要だった。

### Fixed

- **実装 28 件の添字の型の誤りを直した** (#1428)

  #1418 で固定した天井のうち、実装 7 file が持っていた分。 3 群で唯一 画面の動きに
  直結しうる群だったが、**実際に `undefined` になる形は 1 件も無かった**。
  28 件すべてが型の届かない局所の不変条件で、画面の動きは変えていない。

  引けない時の扱いは、その場の役割で分けた。 値を変換する箇所は元の値をそのまま返し、
  **描く箇所と本文へ書き戻す箇所は落とす** = 黙って捨てると、書いた行が図からも本文からも
  消える。 見本の一覧は「必ず 1 件以上持つ」 型にした。

  型を変えたら **束ねた後の並び** が落ちた。 そちらは空から始まるのが正常なので別の型で
  持ち直した = 1 つの型が「一覧」 と「束ねた結果」 の 2 つの性質を混ぜていたことが表に出た。

  天井が 114 → **86** に下がり、併せて検査 code 側の天井も 2 → **0** になった
  (`presets.cdl.ts` はそちらの設定からも読まれるため)。

- **見本帳の検査 23 件の添字の型の誤りを直した** (#1427)

  #1418 で固定した天井 137 件のうち、見本帳の検査 12 file が持っていた分。

  23 件すべてが 2 種のどちらかだった。 **必須の正規表現の群** (一致した以上その群は必ず
  取れる) と、**直前の assert で存在が保証された添字**。 実際に `undefined` になる形は
  1 件も無かった。

  `!` は使わず、群は取れない形を捨てるか落とし、添字は取り出してから存在を確かめる形にした。
  場面を表す語の表は `ja` / `en` を **必ず 1 語以上持つ** 型に変え、不変条件を型で表した。

  天井が 137 → **114** に下がった。

- **位置と JSON の検査群 20 件の型の誤りを直した** (#1417)

  #1413 で型検査を回せるようにした 22 件のうち、位置と JSON を見る検査 11 file が持っていた分。

  6 件は **見本が図として成立していなかった**。 位置を見る 4 file の見本は `CdlDiagram` を
  名乗りながら `topic` を持たず、線の見本は `tone` を持っていなかった。 `tone` の無い線は
  cdl 側が `TONE_COLORS` を引けず、矢印の marker が dead ref になる判定を毎回踏んでいたが、
  各 file の assert が自軸だけを絞り込むため誰にも見えていなかった。

  `jsdom` は自前の型定義を持たないため `@types/jsdom` を devDependency に足した。

  天井が 22 → **2** に下がり、`packages/dragon` 側は 0 件になった。

- **添字の型の厳しさを `packages/dragon` と揃えた** (#1418)

  `apps/playground-spa` は `noUncheckedIndexedAccess` を持たず、「配列の外を引いた」 形が
  型検査を素通りしていた。 素通りしていた間に **137 件** 溜まっていた。

  設定は本体の `tsconfig.json` ではなく、それを継承する `tsconfig.strict.json` に置いた。
  root の `tsconfig.json` が `references` で本 project を含むため、本体に足すと `tsc -b` と
  `pnpm build` がその瞬間に 137 件で落ちる。 天井が 0 に達した時点で本体へ移す。

  天井の検査には空振りの見張りを 2 つ置いた。 `tsc` を起動できていない形と、設定 file は
  在るのに `noUncheckedIndexedAccess` が外れている形。

- **見本を走査する検査群 37 件の型の誤りを直した** (#1416)

  #1413 で型検査を回せるようにした 59 件のうち、見本を走査する検査 16 file が持っていた分。

  6 件は **`@/` の別名が型検査側に無かった** ことが原因。 `vitest.config.ts` は alias を
  持つので実行は通るが、型検査側に無いと **その import から先が丸ごと `any` に落ちる**。
  同 config のコメントが「揃えていないと型だけの import は通るのに実体を呼ぶ test が落ちる」
  と書いていた状態が、逆向きに残っていた。

  併せて 2 つの実害を直した。 図の違反に id を添える箇所が
  `{ diagramId: r.diagramId, ...v }` の順で、**添えたはずの id が `undefined` で上書きされて
  いた** (件数だけを見る assert なので消えても通る)。 `find` に絞り込みを持たせていた箇所は
  callback が `boolean` を返すため型が絞られず、`filter` で先に絞る形にした。

  cdl が公開していない矩形の型 (`BBox`) は、検査側で同じ形を宣言して実物と噛み合うことを
  検査で固定した。

  天井が 59 → **22** に下がった。

- **組み立ての検査群 70 件の型の誤りを直した** (#1415)

  #1413 で型検査を回せるようにした 129 件のうち、組み立ての検査 11 file が持っていた分。

  大半 (41 件) は上記の再 export で消えた。 残りは添字の `undefined` と、手で組む fixture が
  必須項目を欠いていた形。

  併せて 2 つの型注釈を実物に合わせた。 `parseTextDsl` の戻りは `ok` で分かれる共用体で
  `?.` では引けない形だったのを、失敗を落として何が読めなかったかを残す形にした。
  色の知らせの型が `hint` を落としていたのを `DslError` に揃えた。

  天井が 129 → **59** に下がった。

- **変異試験の検査群 79 件の型の誤りを直した** (#1414)

  #1413 で型検査を回せるようにした 208 件のうち、変異試験の検査 9 file が持っていた分。

  大半は添字の `undefined` (`noUncheckedIndexedAccess` が有効)。 `!` で潰さず、
  **何が無かったかを残して落ちる** 道具 (`test/support/at.ts`) を通した。 `!` だと
  `Cannot read properties of undefined` で落ち、何番目が無かったのかも並びの長さも残らない。

  残りは手で組む fixture が必須項目 (`pos` / `title` / `body` / `activate`) を欠いていた形で、
  既定を埋める helper を置いた。 併せて `EdgeStyle` に無い `"dashed"` を実在する値へ、
  `CdlState` に無い `value` を `initial` へ直した。

  天井が 208 → **129** に下がった。

- **記法だけが持つ種別を書くと図が出なかったのを直した** (#1420)

  記法は `contract` / `eoa` のような **描画側に無い種別** を意図的に受け付ける
  (`DSL_ONLY_KINDS` の 8 種)。 図種ごとの役割分け (`solidity` の縦列の並べ替えなど) に使う。

  実装のコメントは「組み立ての段階で描画できる種類に置き換わる」 としていたが、
  **置き換わるのは一部の図種だけ** だった。 `flow` / `swimlane` / `state` / `topology` は
  書いた種別をそのまま描画側へ渡しており、図の組み立てが落ちていた。

  ```
  type: flow
  actors:
    - A: { kind: contract }
  → Cannot read properties of undefined (reading 'h')
  ```

  記法の解析は通り、画面では図が出ない。 見本帳が無事だったのは **この組み合わせを書いた
  見本が 1 つも無かった** だけで、守られていたわけではない。

  描画側へ渡す手前で必ず読み替えるようにした。 箱を作る経路は **8 箇所** あり、その全てを
  通す (変異試験で 1 箇所ずつ外して確かめた)。

  併せて `DslActor.kind` の型を実装に合わせた。 `NodeKind` に縛っていた間、記法が受ける値を
  型が表せていなかった。

- **記法が受ける 6 型が「図になる前」 の検査を 1 度も通っていなかった** (#1411)

  型と種類の表が `satisfies Readonly<Record<PresetType, ...>>` を持ちながら 13 型しか
  並べておらず、`bar` / `line` / `funnel` / `journey` / `quadrant` / `tree` が抜けていた。

  原因は **型検査の対象に `test/` が入っていないこと**。
  `packages/dragon/tsconfig.json` の `include` は `src/**` に限るため、この `satisfies` は
  1 度も検証されていなかった。

  6 型を表に足し、覆いの判定を実行時の集合 (`PRESET_TYPES`) で行う形に変えた。
  型注釈と違って走らせれば必ず評価される。

- **一覧との突き合わせを共有の仕組みに括り、4 度目の漏れを塞いだ** (#1409)

  「図になる前」 を見る検査 (`pre-render-axes.test.ts`) が `charts` を対象に入れていなかった。
  同 file は「全件を通している」 と書きながら 11 module しか並べていない。

  **同じ形が 4 度目**。 #1403 (図の一致) / #1405 (重なり) / #1407 (動き) といずれも
  「対象を手で並べ、一覧の SSOT と繋げていない」 ことが原因で、足し忘れても検査の件数が
  減るだけで何も落ちなかった。

  3 度は各 file に同じ突き合わせを書いて直した。 4 度目で共有の道具
  (`apps/playground-spa/src/lib/catalog-scope.ts`) に括り、4 つの検査がそれを呼ぶ形にした。
  道具そのものの性質 (重複数を落とさない / `parts` を漏らさない) は道具側の検査が固定する。

- **動きの検査の分類が `ethereum` の 4 図を覆っていなかったのを直した** (#1407)

  動きの検査は図を 2 系統に分ける。 動きが意味を持つ分類 (静止を残さない) と、形を見比べる
  見本帳 (3 一覧に振り分ける) の 2 つ。 `ethereum` はどちらにも現れず、静止の判定を 1 度も
  通っていなかった。

  見本帳側は「3 一覧が互いに重ならず全件を覆う」 ことを機械で見ていたが、**2 系統を合わせて
  一覧の全 module を覆うか** は誰も見ていなかった。

  実測すると `ethereum` の 4 図はすべて値も注目先も動くため、動きが意味を持つ側に入れた。

  #1403 / #1405 と同じ形が 3 度目になる。 同じ直し方 (一覧から導いて漏れを落とす検査を置く)
  を適用し、突き合わせは図の id で重複数を保って行う。

- **重なりの検査が `charts` の 9 図を見ていなかったのを直した** (#1405)

  図の重なりを見る sweep (`visual-validate-sweep.test.ts`) が `charts` を対象に入れて
  いなかった。 重なりの 4 軸 (`edge-label-overlap` / clearance 違反 / `node-visibility` /
  alignment) が 9 図ぶん無検査だった。

  同 file は「catalog topic module 全 11 件」 と宣言していたが、**その 11 は一覧の 11 ページ
  とは別の集合** で、`primitives-extra` を含み `charts` を欠いていた。

  #1403 と同じ形が別 file に残っていたことになる。 どちらも「対象を手で並べ、一覧の SSOT と
  繋げていない」 ことが原因。 同じ直し方 (一覧から導いて漏れを落とす検査を置く) を適用した。

  **突き合わせは図の id で行う**。 `primitives-extra` は一覧では `primitives` に畳まれるため、
  ページ名で比べると実在する module が「一覧に無い」 と誤って落ちる。

- **図の一致検査が一覧の 2 ページを見ていなかったのを直した** (#1403)

  記法から作った図が組み立ての図と一致するかを見る検査が、一覧に載る 11 ページのうち
  `charts` (9 件) と `text-dsl` (13 件) を対象に入れていなかった。
  合わせて 22 件が「記法を書いたが図と一致するか誰も確かめていない」 状態だった。

  どちらも「図は記法から組み立てる」 形で、既に対象だった `cookbook` / `ethereum` /
  `parts` と同じ作りをしている。 同じ性質のページで扱いが割れていた。

  対象は手で並べる形のまま (module を動的に集めると記法を持たない module まで拾う) だが、
  **一覧 (`CATALOG_ITEMS`) から導いて漏れを落とす検査を足した**。 一覧にあって対象に無い
  形と、対象にあって一覧に無い形の両方を見るため、次にページを足した時も漏れが落ちる。

## [0.14.0] - 2026-08-25

### Added

- **記法の作りに由来する 2 つの穴を塞ぎ、見本 129 件すべてが記法を持つようになった** (#1394)

  縦列の並びを `lanes:` に書いた順で決めるようにした。 箱が最初に使った順で並べていた間、
  左から順に宣言しても箱の書き順で入れ替わっていた (実測 = 中心を先に書いた放射の図で、
  左端の縦列が中心の右へ回った)。 `lanes:` に無い縦列は従来どおり後ろに続く。

  段の番号を書いたまま持つようにした。 書き順で 0 から詰め直していた間、`stack: 1` と
  書いた箱が 0 へ落ちていた。 決定木のように「同じ高さに並ばない」 ことが図の意味そのものに
  なる形では、詰めた瞬間に別の図になる。 書かなかった箱は、その縦列で空いている一番小さい
  段に置く (単に数え上げると書いた番号と重なる)。

  これで「動く見本」 129 件すべてが記法を持つ。 画面の「コード」 タブから全件の記法を読める。

- **記法に押下 (`events:`) と巻き上げ (`scrolls:`) を足した** (#1393)

  記法は図を書くためのもので、読む人の操作に応じて値が動く仕掛けを持たなかった。
  「動く見本」 3 件がこれだけを理由に記法を持てなかった。

  ```yaml
  events:
    - { on: click, box: Button, handler: toggle-active }
    - { on: keydown, diagram: true, handler: on-key }

  scrolls:
    intro: { start: 0.9, end: 0.1, scrub: 1, label: "Intro reveal" }
  ```

  相手は名前で指す (`box` / `lane` / `arrow` / `diagram` のどれか 1 つ)。
  識別子は記法で書けないため、名前から識別子への読み替えは組み立てが行う。
  指す先が見つからない形は図に載せずに知らせる = 描画側は知らない識別子を黙って無視する
  ため、残すと「書いたのに押しても何も起きない」 が手掛かりなしで起きる。

  「動く見本」 は 126 件が記法を持つ。

- **記法の矢印に値へ追随する 3 欄を足した** (#1396)

  描画側の矢印は太さ / 色 / 破線の位置を値に追随させられるが、記法にも JSON にも書く
  項目が無かった。 「動く見本」 1 件がこれだけを理由に記法を持てなかった。

  ```yaml
  flow:
    - Source -> Pipe: "produce" (accent) { widthBind: "{flow}", dashOffsetBind: "{dash}" }
  ```

  併せて **矢印の中括弧の切り出しを深さで数える形に直した**。 これまでは `{[^}]*}` で
  切っていたため、欄の値に中括弧が入る形 (`{ widthBind: "{flow}" }`) で内側の `}` に
  引っかかり、塊ごと読み落として説明文の一部として図に載っていた。
  箱の側は #1381 で同じ理由から直してあり、矢印だけが古い形で残っていた。

  「動く見本」 は 123 件が記法を持つ。

- **記法につまみの値から決まる値 (`formulas:`) を足した** (#1391)

  記法には `values:` があるが、これは段が動かす状態を読む経路 (`derived`) で、
  つまみが握る値を読む経路 (`formulas`) とは描画側で別の欄になる。
  そのため「動く見本」 11 件が式だけを理由に記法を持てなかった。

  ```yaml
  inputs:
    input: { kind: slider, min: 0, max: 100, defaultValue: 50 }

  formulas:
    doubled: "input * 2"
    clamped: "Math.min(doubled, 80)"
  ```

  書き方は `values:` と同じで、違うのは解かれる仕組み。 名前を中括弧で囲うかは自由
  (描画側の parser は `"{a} + 1"` と `"a + 1"` を同じ名前として読む)。

  **式は描画側の parser に通す**。 自前で書き方を決めると、通ったのに描画側が解けない
  式を受けてしまう。 どこにも書かれていない名前を読む式は知らせる (止めはしない)。

  「動く見本」 は 122 件が記法を持つ。

- **記法の箱に値へ追随する 5 欄を足した** (#1392)

  描画側の箱は幅 / 高さ / 濃さ / 描く時のずらしを値に追随させられるが、記法にも JSON にも
  書く項目が無かった。 「動く見本」 2 件がこれだけを理由に記法を持てなかった。

  ```yaml
  actors:
    - bar: { kind: card, lane: l, stack: 0, wBind: "{barW}", opacity: "{fade}" }
    - dot: { kind: card, lane: l, stack: 1, renderOffsetX: 30, renderOffsetY: "{dy}" }
  ```

  大きさ (`wBind` / `hBind`) は文字列だけを受け、濃さとずらし (`opacity` /
  `renderOffsetX` / `renderOffsetY`) は数も受ける。 空の値は捨てずに知らせる = 描画側は
  空文字を 0 として読むため、書き忘れが「箱が消えた」 形で出る。

  配置計算と矢印は追随前の値を使う。 見た目だけが動くので、矢印の通り道は変わらない。

  「動く見本」 は 112 件が記法を持つ。

- **記法につまみ (`inputs:`) を足し、見本 37 件に記法を持たせた** (#1389)

  記法は図を書くためのもので、読む人が動かすつまみを持たなかった。 そのため見本帳の
  「動く見本」 129 件のうち 56 件が記法を持てず、その最大の塊がつまみ (52 件) だった。

  部品の表 (#1385) と同じく、**つまみの表は人手で書かない**。 描画側の型定義から 14 種を
  生成し、生成し直して突き合わせる検査でずれを止める。

  ```yaml
  inputs:
    value: { kind: slider, min: 0, max: 100, defaultValue: 50, label: "Value" }
    mode: { kind: dropdown, options: [日, 週, 月], defaultValue: 日 }
    clock: { kind: timeline, duration: 3000, speeds: [0.5, 1, 2, 4] }
  ```

  併せて欄の形に「数の並び」 を足した。 `timeline` の再生速度の候補がこの形で、
  文字列の並びとして読むと数が文字列のまま図に載る。

  「動く見本」 は 110 件が記法を持つ。 残り 19 件は式 (`values:` では書けない `formulas`) /
  押下 / 巻き上げ / 箱の欄を使っており、別の穴として残る。

## [0.13.0] - 2026-08-24

### Added

- **記法が受ける部品を 107 種に広げ、表を描画側の型定義から生成するようにした** (#1385)

  記法が受ける部品は 16 種で、描画側が持つ 107 種の一部しかなかった。 そのため見本帳の
  「動く見本」 129 件のうち、記法で書けるのは 6 件だけだった。

  **表は人手で書かない**。 描画側は 107 種 / 510 欄あり、写すと写し間違いと描画側の変更への
  ずれが残る。 型定義から生成し、生成し直して突き合わせる検査でずれを止める。

  ```yaml
  readouts:
    lc: { kind: line-chart, source: hist, min: 0, max: 100 }
    kb: { kind: kanban-board, source: cards }
  ```

  併せて 3 つ足した。 欄の形に「真偽」、矢印に `side:` (どの辺から出るか)、そして
  引用符で明示した空文字を落とさないようにした (`unit: ""` は「単位を出さない」 という指定で、
  落とすと書かなかった場合と同じになる)。

  「動く見本」 は 73 件が記法を持つ。 残り 56 件はつまみや押下で値が変わる仕掛けを使っており、
  記法は図を書くためのものなので対象外になる。

- **記法に部品 7 種と `visibleIf:` / `title:` を足した** (#1381)

  見本帳の「基本パーツ」 80 件に記法を持たせようとして、3 つ足りないことが実測で出た。
  部品の種類が 9 種しか無く 10 件が書けず、場所だけを空ける見えない箱を書けず 7 件が
  書けず、同じ題の箱を並べる図と題を持たない箱が名前と切り離せなかった。

  ```yaml
  readouts:
    dot: { kind: status-dot, source: st, map: [{ value: "online", color: "#22c55e" }] }
    dn: { kind: donut, source: parts, colors: ["#4e9dc4", "#22c55e"], innerRatio: 0.6 }

  actors:
    - 星1: { kind: card, title: "★" }
    - 場所取り: { kind: card, visibleIf: "0", title: "" }
  ```

  足した部品は `donut` / `radar` / `step-progress` / `status-dot` / `notification` /
  `kpi-card` / `status-timeline` の 7 種で、部品の表が持つ種類は 16 種になった。
  `map` のように組の並びを取る欄のために、欄の形に「組の並び」 を足している。

  `title:` は名前と題を切り離す。 名前は図の中で 1 つに決まる必要がある (`focus:` と
  `flow:` が名前で指すため) 一方、題は重なってよい。 書かなければ従来どおり名前が題になる。

  `visibleIf:` はその箱を出すかどうかの条件で、`"0"` / `"false"` / 空文字が偽になる。

  **題は図種を問わず出る** (#1382)。 順序図の上下の名札、swimlane、C4、値の図表、工程表、
  枝分かれ図のいずれでも、名前ではなく書いた題が出る。 併せて、題や組の値に中括弧を書いた
  時に入力が途中で切れる形を直し、公開している JSON の形にも部品 7 種を載せた。

- **記法に `shape:` (箱の中に描く図形) と `readouts:` (値を見せる部品) を足した** (#1374)

  この 2 つが書けなかったため、見本帳で最も動きが豊かな 9 件 (`animation` の rich 5 件 /
  `ethereum` 4 件) が記法を持てなかった。 コードのタブが押せず、見た図を自分で書けない
  状態だった。

  ```yaml
  readouts:
    ring: { kind: percent-ring, source: total, max: 500, label: "全体進捗" }

  actors:
    - 検証:
        {
          kind: dyn-wave,
          posW: 140,
          posH: 200,
          shape: { kind: wave, level: "{s}", amplitude: 100 },
        }
  ```

  描ける図形 5 種 (`rect` / `circle` / `arc` / `wave` / `polygon`) と、1 行で書ける部品
  9 種 (`bar` / `gauge` / `stat` / `sparkline` / `countup` / `typewriter` / `delta` /
  `percent-ring` / `heat-cell`) を受ける。 値と色の対応表が要る 3 種 (`badge` /
  `status-dot` / `stacked-bar`) は 1 行に収まらないため載せていない。

  **知らない種類 / 知らない欄 / 足りない必須の欄は黙って捨てず知らせる**。 捨てると
  「書いたのに図に出ない」 が手掛かりなしで起きる。 YAML と JSON が **同じ表** を見るので、
  片方だけ通る形は作れない。

  これで **見本帳の 9 ページが全て記法を揃えた**。 残るのは `parts` と `interactive` の
  2 ページで、どちらも「まだ書いていない」 だけになる。

- **手引きの見本 25 件で記法がコードタブに出るようになった** (#1378)

  `cookbook` の 25 件は **もともと記法から組み立てていた**。 その記法が `sourceYaml__<key>` として
  export されておらず一覧が拾えないだけだったので、取り出して名前を付けた。 中身は変えていない。

  `sourceJson__<key>` は記法を読んで JSON の欄へ並べ替えて作った。 組み立て済みの図から出すと
  `type: sequence` が失われて `flow` + 縦列の明示に変わり、2 つのタブが別物に見えてしまう。

  これで catalog の見本 203 件が全て `sourceYaml__` の形に揃った。 呼出しに記法を直接書く形が
  残っていないことを検査で固定してある (残っているとその見本はコードのタブに出ない)。

  記法を 1 件も持たないページを陰性対照に使っていた検査 2 件は `ethereum` / `parts` へ移した。

- **基本パーツの見本 80 件で記法がコードタブに出るようになった** (#1376)

  `primitives` は 110 件のうち 30 件しか記法を持たず、残り 80 件はコードのタブが押せなかった。
  これで記法を持つページは 7 のうち 6 が全件揃い、残るのは `animation` の 5 件だけになる
  (その 5 件は #1374)。

  **手で書かず、組み立て済みの図から機械で出した**。 出す前に 2 記法が同じ図になることと、
  組み立て済みの図と骨格が一致することを確かめている。 この確認が上の `topology` → `flow`
  の切替を見つけた。

  残骸を見る検査 (`catalog-source-pairs.test.ts`) も `scene*` 限定をやめ、図の有無で
  判定するようにした。 限定したままだと、足した記法が残骸として落ちる。

- **見た目の見本 10 件と動きの見本 5 件で記法がコードタブに出るようになった** (#1373)

  `styles` は全 10 件、`animation` は 10 件のうち 5 件に `sourceYaml__` / `sourceJson__` を
  足した。 これで記法を持つページは 5 から 7 になる。

  **同じページの中で押せる件と押せない件が混ざるのは `animation` が初めて**。 残る 5 件は
  `dyn-wave` / `dyn-arc` の箱に `shape` を渡すため記法で書けない (うち 4 件は割合の輪や
  数え上げを出す `readouts` も使う)。 記法側に欄を足す話は #1374 に分けた。

  台帳 (`catalog-notation-coverage.test.ts`) の「一部」 の区分に **なぜ全件でないか** の欄を
  足した。 書かないと「まだ書いていない」 と「書けない」 が区別できず、残りを埋める時に
  どちらを相手にしているか分からなくなる。

  `ethereum` の 4 件も同じ理由で書けないため、台帳では「まだ」 に据え置いた。

- **構成の見本 12 件で記法がコードタブに出るようになった** (#1371)

  見本帳は 11 ページあるのに記法を持つのは 4 ページだけで、構成の見本 (`patterns`) は
  組み立て API だけで書かれていた。 コードのタブが押せず「エディタで開く」 も出なかった。

  12 件すべてに `sourceYaml__<key>` と `sourceJson__<key>` を足した。 図は組み立て API の
  ままにしてある = 記法から組み立て直すと図の識別子が題から導かれ、`pattern-direct` が
  `pattern-direct-隣接-node-直結` に変わる (識別子は一覧と検索に出る)。

  併記は写し違いが起きるため、**同じ図になることを検査で固定した**。 検査は
  `preset-source-parity` を `catalog-source-parity` に広げて 2 ページ分を同じ battery に通す。

  ついでに 2 つ直した。

  - 粒子が載る矢印の参照 (`data-cdl-particle` / `mpath` の `href`) を、比較で落とさず
    読む人に見える名前へ読み替えるようにした。 落とすと **粒子が別の矢印に載っている図** まで
    一致になる
  - `patternHook` で箱と矢印が同じ識別子 (`hook`) を持っていたのを分けた。 段が
    `activate("fn", "hook", "hook")` と書いていて、1 つの名前で箱と矢印の両方を指していた

  記法で表せない穴も 1 つ残る。 同じ 2 者を結ぶ矢印が 2 本ある図 (`patternCallReadWrite` の
  read と write) は `focus:` が `A -> B` の形しか受けないため段ごとに光らせ分けられない。
  検査に宣言を置いて、解消したら落ちるようにしてある。

  どのページが記法を持つかは台帳 (`catalog-notation-coverage.test.ts`) が固定する。
  ページを足した時にどの区分へ入るかの判断を必ず通る = 新しいページが黙って埋もれない。

### Fixed

- **基本パーツの見本 30 件が、図と違う記法をコードタブに出していたのを直した** (#1376)

  `primitives` の `scene*` 30 件は一致検査の対象に入っておらず、誰も気付いていなかった。
  実測すると 30 件とも同じずれ方をしていて、写して使うと違う図が出る状態だった。

  | 項目             | 記法が言っていた形             | 実際の図                                  |
  | ---------------- | ------------------------------ | ----------------------------------------- |
  | 縦列             | 幅 460 / 内包あり / 見出しあり | 幅 440 / 内包なし / 見出しなし            |
  | 箱の上の小見出し | なし                           | `wallet` / `exchange` / `chain`           |
  | 段の札           | なし                           | `wallet` / `exchange` / `chain`           |
  | 図種             | `topology`                     | `topology` では再現しない (`flow` で一致) |

  110 件すべてを組み立て済みの図から出し直し、一致検査の対象に入れた。

## [0.12.0] - 2026-08-23

### Added

- **Text DSL のページで記法がコードタブに出るようになった** (#1365)

  「記法で書くと図になる」 ことを見せるページなのに、記法が画面に出ていなかった。
  コードのタブは無効で、当てると「この図に記法は登録されていません」 と出る状態だった。

  図は記法の文字列から組み立てているので **文字列は元からある**。 それを `sourceYaml__<key>` /
  `sourceJson__<key>` として export していないだけだった (コードタブはその名前で拾う)。

  13 件の記法を定数に出し、組み立てはその定数を渡す形にした。 **文字列は 1 byte も変えていない**
  = 組み上がる図が変更前と完全一致することを実測で確かめた。

  JSON の記法は **記法を解いた結果から機械生成した**。 手で 13 件書くと写し間違いが混ざるので、
  元を 1 つに保つ。 生成後に「YAML と JSON が同じ図に解決される」 ことを 1 件ずつ確かめている。

  この repo は「YAML を持つ見本は JSON も持つ」 を不変条件にしており (#1292)、片側だけ足すと
  その検査が落ちる。 当初は JSON を対象外にしていたが、実際に落ちたので対応した。

- **Text DSL のページの 3 件も起点から描かれるようになった** (#1363)

  全ページを実測したところ、起点から描ける図 18 件のうち 3 件が動かないままだった。
  いずれも `text-dsl` のページ (ガント / 円グラフ / マインドマップ) で、`charts` (#1318) と
  `presets` (#1358) を埋めた時に対象へ入っていなかった。

  記法に `draw:` を 1 行足し、描く段を 2.4 秒に伸ばした (`presets` と同じ理由 = 1 秒では
  扇や枝が動く様子を追う前に描き終わる)。

  **ページを 1 つずつ直す形をやめた**。 次にページが増えた時にまた漏れるため、全ページを走査して
  「起点から描ける図は描く指定を持つ」 を件数ごと固定する検査を足した。

  対象の種別一覧は cdl が持っている (公開されていない) ため、**cdl の検査そのものを通して**
  判定する = 一覧を写さずに済み、cdl 側で対象が変わっても追随する。

- **2 段目以降を「動かすだけ」 と「描き直す」 で切替えられるようになった** (#1359)

  図は 1 段目で起点から描かれ、2 段目以降は値が動くだけだった。 折れ線なら 1 段目で左から線が
  引かれ、2 段目は点の高さが移るだけで線は引き直さない。

  どちらが見やすいかは図と見る人による。 値の移り方を見たい時は動かすだけがよく、
  「別のデータで描き直した」 ことを示したい時は引き直した方が伝わる。

  速度の切替 (#1356) と同じ形で画面に置き、**選んだ側を図とコードタブの両方に反映する**。

  仕組みは新しく作っていない。 段に `draw` があればその段は起点から描かれる (描画側が段ごとに
  判定する) ため、**1 段目の `draw` を 2 段目以降へ写す** だけで引き直しになる。 描く相手も
  記法の語も 1 段目から取るので、図の種別が増えても対応表を持たずに済む。

  **起点から描けない図には切替を出さない**。 2 段目に指定を足しても何も変わらないため、置くと
  「押しても何も起きない操作」 になる。

  効く範囲は見ている 1 件だけで、項目を選び直すと「動かすだけ」 に戻る (速度と同じ)。

- **見本帳の「プリセット」 で、起点から描ける見本がすべて動くようになった** (#1357)

  折れ線だけが左から伸び、他は開いた瞬間に全部描かれた状態で出ていた。 速度の切替 (#1356) を
  置いても、動かない図では見え方が変わらない。

  起点から描ける 6 件 (円グラフ / ガントチャート / ファネル図 / マインドマップ / ツリー図 /
  ユーザージャーニー) の 1 段目に描く指定と長さを足した。 記法の見本 (YAML / JSON) も揃えている。

  描く段の長さは 1 つの定数 (`DRAW_DURATION`) に集めた。 見本ごとに違う値も書けるが、同じページの
  中で速さがばらつくと比べにくい。

  **消える陰性対照を 3 つ取り直した**。 円グラフは「描く指定を書いていない図」 として画面の検査の
  対照に使われており、指定を入れた時点でその対照が消える。 折れ線と同じく、1 段目 (書いた) と
  2 段目 (書いていない) を同じ図で見る形にした。

  単体の対照 2 つも、「折れ線以外は持たない」 から **「起点から描けない種別は持たない」** へ
  作り直した。 対象の種別一覧は cdl が持っている (公開されていない) ため、cdl の検査そのものを
  通して判定する = 一覧を写さずに済み、cdl 側で対象が変わっても追随する。

  ガントチャートの図形を見る検査の待ち方も直した。 帯を起点から描く段では **依存の矢印は帯が
  出揃ってから出る** ため、固定の待ち時間だと描いている途中を読んで 0 件になっていた。

- **見本帳の図の再生速度を画面で切替えられるようになった** (#1355)

  図の上に 0.5x / 1x / 2x を置いた。 押すと **図の段の長さとコードタブの秒数の両方** が変わる。

  見本帳は記法を示す場所なので、見ている図とコードが一致していることに意味がある。
  片方だけ変えると、写したコードが画面と違う速さで動く。 倍率の適用は 1 箇所
  (`lib/playback-speed.ts`) に集め、図と記法の両方をそこから出す。

  効く範囲は **見ている 1 件だけ**。 項目を選び直すと 1x に戻る = 「なぜこの図だけ遅いのか」 を
  後から見失わないため。 保存先 (URL / localStorage) を決める判断も要らなくなる。

  **1x では図の object も記法の文字列も元のまま返す**。 新しい object を返すと描画側が別の図と
  みなして描き直し、速さを触っていない図が切替えのたびに最初へ戻る。

  描画側 (`@cardenelabs/cdl`) は変えていない。 dragon は図を自分で組み立てて渡しているので、
  段の長さを掛けた図を渡せば速さが変わる。

- **見本ごとに段の長さを書けるようになった** (#1353)

  段を組み直す `withSteps` は段の長さを 900ms で直書きしており、見本ごとに変えられなかった。

  起点から描く段はこれでは速すぎる。 #1352 で折れ線が左から伸びるようになったが、線が引かれる
  様子を追う前に引き終わっていた。

  記法 (text DSL) は元から段ごとに秒数を書ける (`- step: "計画" 2.4s`)。 組み立て API の側だけが
  1 つの値に固定されており、**その表現力を落としていた**。

  段の指定 (`Step`) に `duration` の欄 (ミリ秒) を足し、折れ線の描く段を 0.9 秒から 2.4 秒に
  伸ばした。 書かない段は従来どおり 900ms のままで、他の見本の速さは動かない。

  伸ばしたのは **描く段だけ**。 値が動くだけの段まで伸ばすと、見る人が待たされる時間が増える。
  検査で「既定より長い段」 と「描く段」 が一致することを固定した。

### Changed

- **`@cardenelabs/cdl` の依存を `^0.13.1` から `^0.14.0` に上げた** (#1349)

  | 版     | 入った変更                                                   | cdl 側            |
  | ------ | ------------------------------------------------------------ | ----------------- |
  | 0.14.0 | 箱に入らず落ちた補足を検査が知らせる (軸 `box-line-dropped`) | cdl#538 / cdl#539 |

  木と放射は箱の中に名前と補足を 2 行で積むが、箱の高さは段の間隔から出るため、段や枝が
  増えると 2 行が入らない。 入らない行は後ろから落ち、**落ちたことはどこにも出ていなかった**。

  幅の側は切った印 (`…`) が図に出る (cdl#526) が、高さの側は箱に描く余地が無いから落ちて
  いるので印を出せない。 検査で伝えるのが唯一の出口になる。

  `0.x` の caret はきざみを固定するので、`^0.13.1` のままでは `0.14.0` が入らない。
  `packages/dragon` と `apps/playground-spa` の 2 つを同時に上げた = 片方だけ上げると同じ
  repo の中で 2 つの版が同居し、描画と検査が別の engine を通る。

  **本 repo の見本では 1 件も出ない**。 実測すると木を持つ節 1 / 放射を持つ節 2 に対し、
  補足を書いた箱が **0 件** だった = 落ちる対象そのものが無い。 軸は境界を越えた見本を書いた
  時に初めて働く。

- **配信の script を `deploy` から `deploy:pages` に改名した** (#1345)

  pnpm 組込みの `deploy` command が同名の script を食うため、`pnpm deploy` は script に
  届かず **一度も動いたことがなかった** (実測 = `ERR_PNPM_NOTHING_TO_DEPLOY`)。
  組込みの command 名に `:` は含まれないため、`:` を持つ名前は構造的に衝突しない。

  打つ時は `pnpm run deploy:pages`。

### Fixed

- **中括弧の中で、引用符の中のカンマが区切りになるのを直した** (#1367)

  `- X: { kind: card, subtitle: "a, b" }` と書くと補足が `"a` に切れて図に出ていた。
  書いた文字と出る文字が違う。

  中括弧の中身を項目ごとに割る処理が **入れ子 (`[` / `{`) しか見ていなかった**。 配列
  (`{ initial: '["入力", "確認"]' }`) が壊れていなかったのは角括弧の深さで守られていたためで、
  引用符だけで守られる値は守られていなかった。

  割る時に引用符の中かを見るようにした。 **開いた記号と同じものだけが閉じる** 扱いで、
  `"Guns N' Roses, live"` の `'` は文字として残る。

  **閉じない引用符が残った時はこれまでどおりの割り方に落とす**。 壊れた入力で挙動が変わると、
  今まで通っていた書き方が黙って別の結果になる。 実測では見本の中括弧 9832 件のうち閉じない形は
  0 件で、この経路は保険。

  見本 421 件の図が読み方ごと変わらないことを、変更前後で突き合わせて確かめた。

  `#1366` はこの欠陥を見本の書き方を変えて回避したもの。 記法側を直したので、どちらの書き方でも
  正しく読める。

- **段の切替わりを 1 sample だけ拾って誤検知する検査を直した** (#1359 / #1365)

  #1360 で足した画面の検査が、全件を並列で回した時に落ちた。

  段の札と図は別々に更新されるため、段が切り替わる瞬間に「札は 2 段目だが図はまだ 1 段目の
  dash を持っている」 状態が 1 sample だけ現れる。 負荷が上がると再現し、「動かすだけ」 側が
  1 を数えていた。

  2 回続けて 2 段目だった時だけ数える形にした。 2 段目は 0.9 秒あって 9 sample 取れるので、
  引き直している側は依然として何度も数えられる (繋ぎを外すと落ちることを実測で確認済)。

- **変更履歴から辿れない commit があったのを直した** (#1361 / #1363)

  #1362 は検査だけを足す PR で変更履歴を書かなかったが、この repo は範囲内の全 commit が
  変更履歴か宣言のどちらかから辿れることを求める。 書かなかった時点で main の検査が 1 件
  落ちる状態になっていた。

  「利用者から見えない」 の宣言に理由付きで足した。 **書かない判断も残す** = 宣言が無いと
  「書き忘れ」 と「書かないと決めた」 が区別できない。

- **見本帳の「プリセット」 の折れ線が、左から伸びるようになった** (#1351)

  開いた瞬間に線が全長で引かれた状態で出ており、静止画と区別が付かなかった。

  描画側 (`@cardenelabs/cdl`) は段に `draw` を書くと起点から伸ばす仕組みを持っている。
  同じ見本帳でも「図表」 のページは 8 種すべてでこれを使っていたが、「プリセット」 の
  ページは **1 件も使っていなかった**。

  原因は段を組み直す関数にあった。 `presets.cdl.ts` の `withSteps` は段を 8 つの欄から
  組み立てており、`draw` を運ぶ経路が無い。 段の指定 (`Step`) にも欄が無かったので、
  書こうとしても書けない状態だった。

  `Step` に欄を足し、`withSteps` が段に載せるようにした。 折れ線の 1 段目で使っている。

  **書いていない段には欄ごと付けない**。 付けると `draw` を使わない見本の JSON の形が
  変わる (描画側の `builder.ts` も同じ扱いをしている)。

  陰性対照を **同じ図の中** に取り直した。 それまでこの見本は「`draw` を書いていない図」
  として画面の検査の対照に使われており、図に `draw` を入れた時点でその対照が消える。
  1 段目 (書いた) と 2 段目 (書いていない) を同じ図で見る形にした = 棒グラフの対照と同じ形で、
  図の違いによる差も混ざらない。

  記法との一致検査に `段が描く先が一致する` を足した。 記法は箱の識別子を題から導き、
  組み立て API は明示に書くため、識別子のままでは比べられない = 読む人に見える名前へ
  直してから比べる (光らせる先と同じ扱い)。

- **配信対象に repo の dotfile が混ざるのを直した** (#1347)

  1 度配信して数えたところ、48 file のうち 12 件が repo の dotfile だった
  (`.claude` / `.mcp.json` / `.npmrc` / `.github` 等)。 `dist` 自体には dotfile が 1 件も
  無いので、`dist` の外から入っていた。

  `gh-pages` は公開用の clone を掃除してから `dist` を写すが、掃除は `globby` の既定
  (`dot: false`) で走るため **dotfile に届かない**。 clone に残った repo の file がそのまま
  commit されていた。

  掃除の対象を dotfile まで広げた。 `.git` は clone の管理情報なので除外する
  (`.*/**` は `.git/**` にも一致するため、外さないと配信そのものが壊れる)。

  CLI の `--remove` は pattern を 1 つしか受けず `.git` の除外を書けないため、API を呼ぶ
  script に差し替えた。

  **公開はされていない**。 GitHub Pages は現在の plan で有効化できず、branch も削除済。

- **script から script を呼ぶ 20 箇所に `run` を明示した** (#1345)

  `run` を挟めば組込みかどうかに関わらず script に届く。 `pnpm --help` は `deploy` を
  列挙せず、help に出ない組込みがあるため、名前の一覧と突き合わせる形は作れない。

  今壊れていたのは `deploy` の 1 件だけだが、組込みは後から増える。 `pnpm` が将来 `build` の
  組込みを足せば、何も変えていないのに `pnpm build` が別のものを指す。

  併せて、`run` の省略を見つける検査を足した。

- **監査 README が消えた検査を層 1 の実装として案内していたのを直した** (#1343)

  `apps/playground-spa/tests/audit-3vp.spec.ts` は `#1095` で削除された (33 件が `expect` を
  1 つも持たず判定していなかった) が、README は 2 箇所でこの file を指したままだった。

  `#1339` は同じ形を `package.json` と dashboard script について直したが、docs は対象外に
  していた。 その残りを閉じる。

  **md の参照 path を機械で見る検査は置かない**。 実測すると repo 内 path を参照する 45 件の
  うち実在しないのは 6 件で、そのうち 5 件は誤検知だった (別 repo の file / 過去の記述 /
  「新 file を作るなら」 の仮定 / 実装予定を書いた spec)。 除外条件を機械で見分ける形は
  収束しない。

- **手で書いた値が実物とずれていたのを直し、ずれたら落ちる検査を足した** (#1341)

  実物から導ける値を手で書いた記述が 2 件ずれていた。 画面に出す版の例が `0.10.0` のまま
  (実物は `0.11.0`)、parts の個数の注記が 60 個のまま (実物は 80 個) だった。

  数を持つ意味がない記述からは数を消した。 `PARTS_COUNT_ESTIMATE` は画面の件数表示に要る
  ため残し、実物と突き合わせる検査を足した。 以前は 2 つの file に「同期更新する」 と
  書いてあるだけで、揃っているかを確かめる経路が無かった。

- **script が実在しない検査を名指ししていたのを直し、同じ形が戻らない検査を足した** (#1339)

  `#923` (図の直接操作を外す) と `#1095` (判定を持たない 91 件を外す) で検査 file が消えたが、
  それを呼ぶ script 側が残っていた。 実測で `package.json` の 20 件中 10 件、dashboard の
  25 件中 14 件が実在しない file を名指ししていた。

  **Playwright は名指しが外れても exit 0 を返す**。 `pnpm test:audit` は
  `Total: 0 tests in 0 files` を出しながら成功扱いで、打った人には「通った」 と読めていた。
  vitest 側は exit 1 で落ちるため気付ける。

  `test:audit` を外し、`test:editor:l2` / `l3` と dashboard の一覧を実在する file だけに揃えた。
  併せて、script が名指しする path と pattern が実在することを見る検査を足した。

  `#1328` は「`CONTRIBUTING.md` に書いた cmd が `package.json` に実在するか」 を見る。
  本検査はその 1 つ先を見る = どちらが欠けても「打ったが 1 件も走らない」 が起きる。

## [0.11.0] - 2026-08-22

### Added

- **見本の文字が図の上で切り詰められていないことを全図で見る検査を足した** (#1334)

  記法に長い名前を書いても組み立ては通り、描画側が箱に収める時に末尾を `…` で切る。
  誰も見ていない図なら気付かない = `#1332` で見つかった `新プロジェ…` は 8 週間残っていた。

  見本帳と記法の見本を合わせて 440 件を描き、`…` を含む文字の集合が **描画側が自分で書く
  literal だけ** であることを見る。 切り詰めが起きると集合に新しい文字が増えるので落ちる。

  箱幅も字の大きさも余白も検査に書き写さない。 木の箱幅は図ごとに変わり、縮める下限も
  描画側の版で動くため、書き写すと版を上げた時に片方だけ古くなる。

### Changed

- **`@cardenelabs/cdl` の依存を `^0.12.0` から `^0.13.1` に上げた** (#1332)

  3 つの修正がそれぞれ別の版で入っている。

  | 版     | 入った修正                                     | cdl 側            |
  | ------ | ---------------------------------------------- | ----------------- |
  | 0.12.1 | 箱に文字を収める (どう渡されても枠を超えない)  | cdl#526 / cdl#527 |
  | 0.13.0 | 放射図の中心に補足を渡す欄 (`rootSubtitle`)    | cdl#530 / cdl#531 |
  | 0.13.1 | 入りきらない名前を切る前に字を小さくして収める | cdl#534 / cdl#535 |

  0.13.1 が要るのは、補足を書いていない箱では 2 行に分けられないため。 中心の箱は幅 120 の
  固定で全角 6 文字までしか入らず、`新プロジェクト` (7 文字) が `新プロジェ…` と切られていた
  (`editor-samples.ts` の見本で実測)。 字を 1 段小さくすれば収まる。

### Fixed

- **放射図と木で、名前と補足を分けて渡すようにした** (#1332)

  名前と補足を 1 つの文字列に連結して渡していたため、描画側が 1 行で描いて箱幅を超え、
  末尾が切られていた (実測 = 箱 120px に対し文字 163px)。 分けて渡すと 1 行あたりが短く
  なり、同じ箱幅でも 2 行に収まる。

  木は補足を **渡していなかった** = 書いた文字が図に出ない状態だった。 併せて直した。

  `subtitle` と `value` の両方を書いた場合は空白で繋いで 1 つの補足にする (描画側の補足は
  1 行なので、2 つを別々の行にはできない)。

- **枝分かれ図の見本で `draw` が 2 つの意味を持つのを直した** (#1330)

  値の名前と段の項目に同じ `draw` を使っていた。 階層が違うので組み立ては正しく解釈するが、
  見本を読む人が階層から意味を判断することになる。 値の名前を `paint` に変えた。

  同じ形が戻らないよう、**見本の値の名前が段の項目の語と重ならない**ことを検査で見る。
  記法としては禁止しない (`focus` などを値の名前に使えなくするのは制約が強すぎる)。

- **画面の検査を回す手順を書き、書いた cmd が実在することを検査で見るようにした** (#1328)

  `CONTRIBUTING.md` に書かれていた `pnpm --filter dragon-playground-spa test` は、その
  script が存在せず **何も実行せずに exit 0** を返していた。 pnpm は filter 付きで
  存在しない script を求められると黙って skip する。 手順どおりに打った人は「通った」 と
  読むが、1 件も走っていない。

  実際に走る `test:e2e` を足し、2 つの server (開発 4323 / 本番 build の preview 4324) を
  立てる手順を書いた。 書いた cmd が `package.json` に実在することは検査が見る。

- **本番 build を配る port を検査と揃えた** (#1326)

  画面の検査 22 件が常に落ちていた。 検査は `http://localhost:4324/dragon` を見に行くのに、
  配る側 (`vite preview`) は開発 server と同じ 4323 を使っていた。 **同じ port なので開発
  しながら preview を立てられず**、4324 は誰も listen していなかった。

  配る port を 4324 に変え、開発 server と同時に立てられるようにした。 これで
  `pnpm build` の後に `pnpm preview` を立てれば通しの検査が失敗 0 件で終わる。

  port の数字は `apps/playground-spa/ports.ts` に集めた。 設定と検査に散っていると、
  片方だけ直したずれに気付けない (今回がそれ)。 設定を実際に読んで、配る port が宣言と
  一致することも検査で見る。

- **描画側の指摘が書き手の書いた名前で読めるようにした** (#1324)

  editor に出る描画側 (`@cardenelabs/cdl`) の指摘は、箱を **組み立てが作った id** で指していた
  (`node "a-header"`)。 書き手が書いたのは `A` で、`a-header` は source のどこにも現れず、
  id の作り方も見えていない。

  `node "<id>"` を、その id を持つ箱の名前に読み替える (`名札 "A"`)。 名前を持たない箱
  (組み立てが作る詰め物) は id のまま残す。

  同じ文面に出る種別名 (`shape-smart-contract`) は **読み替えない**。 記法にそのまま書ける語で、
  記法一覧の「箱の種類」 にも並んでいる。 置き換えてよいのは書き手が絶対に書かない語に限る。

- **トップの版の札が `package.json` から導かれるようにした** (#1320)

  手で書いた `v0.7` が 3 版分古いまま残っていた。 `vite.config.ts` が
  `packages/dragon/package.json` を読んで差し込む形にしたので、版を上げれば札も追随する。

- **画面の検査 5 件のずれを直した** (#1320)

  通しで回すと落ちる状態が続き、「自分の変更で壊した」 と読める形になっていた。 内訳は
  版の札 / カタログの分類 / 行数の期待値 / 文字の下限 / 名札の説明の 5 件で、いずれも
  期待値と実装が別々の `PR` で動いたことによる。

  仕様書 (`docs/design/specs/screens.md`) にカタログの分類「図表」 を足した。 実装は
  11 分類あるのに仕様書が 10 のままだった。

## [0.10.0] - 2026-08-22

### Added

- **`draw:` が残り 5 種も受けるようにした** (#1318)

  `journey` / `mind` / `tree` / `gantt` / `funnel` を足し、受ける語が 8 つになった。
  体験の道筋は気持ちの線が左から、放射と木は枝が中心 / 根から、工程表は帯が各自の始端から、
  漏斗は段が上から順に現れる。

  `quadrant` は入れない。 4 つの区画に項目を置く図で **項目に順序が無く**、起点を決められない。
  書いた順で決めると、動きが図の意味を持たなくなる。

  語と図種の対応表 (`DRAW_TARGETS`) に 5 行足すだけで、受ける語の一覧も組み立て側が見る図種の
  一覧もそこから追従する。 JSON Schema の `enum` だけは file なので実装から生成できないため、
  **表と `enum` の一致を見る検査**を足した。

- **記法一覧に「起点から描ける図種」 の一覧を出すようにした** (#1318)

  受ける語は `DRAW_WORDS` から引く。 図種 / 箱の種類 / 色と同じ扱いで、記法に語が増えたら
  一覧も追従する。 語を手で並べると、増やした時に一覧だけが古いまま残る。

### Changed

- **`@cardenelabs/cdl` の依存を `^0.11.0` から `^0.12.0` に上げた** (#1318)

  図表 8 種が段の進みで描けるようになったのは cdl 0.12.0 から (cdl#522 / cdl#523)。

### Fixed

- **画面の検査が見に行く server を `SPA_URL` で差し替えられるようにした** (#1318)

  依存の版を上げた直後は、動いている dev server が古い版を配り続ける (Vite は起動時に依存を
  抱え込む)。 別 port に立て直した server へ向けないと、新しい描画を見ているつもりで古い描画を
  見ることになる。

  `chart-line-draw.spec.ts` だけが自前で `SPA_URL` を読んでいたため、**同じ回の検査が 2 つの
  server に分かれていた**。 設定側に移し、spec からは相対 path で書くようにした。

  URL を自分で持っていた 12 spec も相対 path に直した。 内訳は固定値が 8 件、
  `AI_VERIFY_BASE_URL` で `baseURL` を上書きしていたものが 3 件、`PROD_BASE_URL` の名前で
  dev server を指していたものが 1 件。 `scripts/test-editor-dashboard.sh` は
  `AI_VERIFY_BASE_URL` を受け取る口を保ったまま、spec へは `SPA_URL` として渡す。

  同じ形が戻らないよう、**dev server を見る spec が URL を自分で持たない**ことを見る検査を
  足した。 本番 build の preview を見る 3 件だけを名指しで除き、その 3 件が実在することも
  併せて確かめる (名指しが実物からずれると対象が静かに減るため)。

## [0.9.0] - 2026-08-21

`v0.8.0` の tag を打った後も `[0.8.0]` の節に書き足していたため、**公開済の `0.8.0` に
入っていない項目がその節に混ざっていた**。 本節を切るにあたり、tag 後の commit を説明する
12 項目を `[0.8.0]` からここへ移した。 `0.8.0` の節は公開したものだけを説明する。

### Added

- **記法と JSON を並べて見せる見本を 30 件から 58 件に増やした** (#1292)

  `presets` 19 件と `charts` 9 件は `yaml` タブに中身があるのに `json` タブが空だった。
  同じ図に解決される JSON を足し、**片側だけ増やせない検査**を入れた。 検査は「両方ある」
  だけでなく、両方の入口が同じ図に解決されることまで見る (空の JSON や別の図の JSON では通らない)。

- **折れ線を左の起点から伸ばす動きを段に書けるようにした** (`draw:`、#1312)

  段に `draw: line` と書くと、その段の間だけ折れ線が左端から右へ伸びる。 点と値の数字は
  線の先が自分の位置に届いた時に現れ、軸と格子と横軸の名前は最初から出る。 書かない段では
  今までどおり全長で描かれる。

  最上位ではなく段に置いたのは、記法が既に持つ分け方に従うため。 段に書くのは動き
  (`tween:` / `set:` / `focus:` / `badge:`)、最上位に書くのは静的な性質 (`eyebrow:` / `axes:`)。
  線が伸びるのは動きに当たる。 副産物として、どの段で描くかを書き手が選べる。

  受ける語は `line` の 1 つだけで、他の語は行番号付きの誤りになる。 折れ線以外の図種に
  書いた場合は「効きません」 と知らせる (黙って捨てない)。

- **`draw:` が棒と円も受けるようにした** (#1314)

  `draw: bar` と `draw: pie` を足した。 棒は横軸から上へ伸び、扇は 12 時から時計回りに開く。
  数字は棒の頭に付いて動き、凡例はその扇が開き始めた時に出る。

  語が 3 つになったため、`type: bar` の段に `draw: pie` と書く形が生まれる。 **図は描けるので
  誤りにはせず、知らせに出す** (`eyebrow:` や `lane:` と同じ「書いたのに効かない」 の扱い)。

  語と図種の対応は 1 つの表 (`DRAW_TARGETS`) が持ち、受ける語の一覧も組み立て側が見る図種の
  一覧もそこから導く。 3 つを別々に並べると、語を足した時にどれかが古いまま残る。

### Changed

- **`@cardenelabs/cdl` の依存を `^0.9.0` から `^0.11.0` に上げた** (#1312 / #1314)

  段が `draw` を持てるようになったのは cdl 0.10.0 から (cdl#513)、棒と扇が対応したのは
  cdl 0.11.0 から (cdl#517)。

  伸ばし方は種別ごとに違うが、**形を表す属性を触らない** 点は共通する。 折れ線は `points` を、
  棒は `rect` の `height` / `y` を、円は `path` の `d` を進みに関わらず実値のまま保ち、
  見える範囲だけを変える (dash / 倍率 / 切り抜き)。 図形の寸法から値を測る検査に影響しない。

### Removed (破壊的変更)

- **JSON Schema の線種から `dashed` と `dotted` を落とした** (#1304)

  描画側 (`@cardenelabs/cdl`) が分岐を持つのは `solid` と `dotted-flow` の 2 種だけで、
  残る 2 種は schema が宣言しているだけだった。 書いても図の線は変わらず、知らせも出ない。
  宣言を実装に合わせ、書いた時は誤りとして返す。

  | 消えたもの                 | 代わりに使うもの                       |
  | -------------------------- | -------------------------------------- |
  | `flow[].style` の `dashed` | `dotted-flow` (点線) か `solid` (実線) |
  | `flow[].style` の `dotted` | 同上                                   |

- **JSON の `layout` を受けるのをやめた** (#1295)

  記法 (`TOP_LEVEL_KEYS`) に無く、JSON にだけあった。 `doc.layout` を読む場所が実装にも
  画面にも 1 つも無く、検査を通っても何も起きない項目だった。 drag で位置を保存する mode
  (Phase 4) が入る時に、記法と JSON の両方へ同時に足す。

### Fixed

#### 書いたのに効かない / 黙って消える形

- **記法と JSON に色を付け、4 箇所の割り当てを 1 つに揃えた** (#1310)

  記述を見せる場所が 4 つあり、色の割り当てが 3 通りに割れていた (見本帳と記法一覧は色ゼロ /
  エディタは汎用 YAML 文法で 3 色 / ホームは手書きの `<span>` 5 役)。

  本文を受けて `[種類, 開始, 終わり]` の並びを返す分解器を 1 つ置き、4 箇所が共有する。
  分解器は描画色を知らず、色名の別名と矢印の書き方は `keywords.ts`、正規の色名は
  `@cardenelabs/cdl` から引く。

  **色名は図で引く色そのもので描く**。 `(成功)` と書いた語が、その図で実際に引かれる枝と
  同じ緑になる。 色が飾りではなく予告になり、本文と図が目で対応する。

  併せて、ホームに出ていた記法が現行記法として通っていなかったのを直した (`animate:` は
  最上位に無い項目、箱の書き方が違う、`(1)` が色名として読まれる の 6 件)。 手書きで
  parser を通っていなかったため誰も気付けなかった。 通ることを検査で固定した。

  記述の文字の対比を測る検査も足した (それまで図の名札しか測っていなかった)。

- **見本 (parts) の箱に書いた 5 欄が黙って捨てられるのを直した** (#1308)

  JSON で見本の箱に `tone` / `owner` / `end` / `touchpoint` / `opportunity` を書くと、
  検査を通ったうえで値が丸ごと捨てられていた。 誤りも警告も出ないため、書いた人には
  「書いたのに図が変わらない」 としか見えない。

  誤りとして返し、`state` を使う道を hint で案内する。 #1294 が決めた向き (見本にしか
  効かない `state` / `scale` を普通の箱に書いたら誤り) の鏡になる。

  記法では同じ本文が状態の上書きに入るが、これは `tone` を特別扱いしているのではなく、
  見本の中括弧に書いた名前をすべて状態の上書きとして読むため。 JSON は欄ごとに型を宣言
  する形なので同じ設計を持ち込めず、効く書き方 (`state`) が既にあるため誤りに倒した。

  併せて、どちらの箱にしか効かないかを 2 つの表で宣言し、検査と組み立ての両方が同じ表を
  見るようにした。 落ちる欄を実際に組み立てて数える検査を置き、表と食い違ったら落ちる。

- **記法が数と真偽の読めない値を誤りとして知らせるようにした** (#1306)

  数を書く欄に `posX: q` と書いても、真偽を書く欄に `overlay: yes` と書いても、値が黙って
  捨てられて欄ごと無かったことになっていた。 誤りも警告も出ないため、書いた人には
  「書いたのに図が変わらない」 としか見えない。 対象は 21 欄 (数 16 / 真偽 5)。

  同じ記法の中でも扱いが割れていた。 日本語で書く `位置:` と `大きさ:` は読めない値を
  行番号付きで知らせる (#1028) のに、英語で書く `posX:` / `posW:` は黙って捨てていた。

  欄と値の形を並べた表を実装に置き、読めない値を行番号付きで知らせる。 表は
  `DslViewport` / `DslActorNodeOverride` から欄を導くため、欄を足して表に書き忘れると
  型検査が落ちる。 知らせは書いた行を指し、同じ欄名が別の場所に出る形
  (`width` は図全体と縦列、`posX` は箱と箱の中の要素) ではどこの欄かを添える。

  値を書かなかった形 (`posX:` の右が空) は「書かなかった」 と同じ扱いのままにした。
  書いていないものが効かないのは当たり前で、黙って消えているわけではない。

- **型違いと列挙外の値を誤りとして返すようにした** (#1304)

  項目名の側は #1295 で閉じたが、値の側が残っていた。 JSON 入口は 20 欄が型違いの値を
  そのまま通し、`type: sequence` では 17 欄の値が図まで届いていた
  (`labelOffsetX: "q"` が矢印の中に文字のまま入る、`tone: "bogus"` がそのまま色として載る)。
  縦列 (`lanes`) と群 (`groups`) は外側の形すら見ておらず、`lanes: 5` が素通りしていた。

  欄ごとの型を並べた表を `ACCEPTED_KEYS` の隣に置き、両方が同じ欄を持つことを型検査で
  固定した。 欄を足して型を書き忘れると型検査が落ちる。 誤りの `path` は欄を指す
  (`$.flow[0].labelOffsetX`)。 色と線種の一覧は engine (`TONES` / `STYLE_VALID`) から取る。

  記法側は読めない色名と線種を行番号付きで知らせる (`色の名前が読めません: "むらさき"`)。
  黙って既定色に落としていた 3 か所 (矢印の丸括弧 / 箱の `色:` を縦に書く形 / 箱の中括弧の
  `tone`) を揃えた。 併せて、矢印の色も箱と同じ読み替えを通すようにした
  (`tone: "成功"` が色名に直らないまま図に届いていた)。

- **記法 v0.5 が受けない日本語を誤りとして知らせるようにした** (#1301)

  段の項目名 (`強調` / `説明` / `バッジ` / `遷移` / `切替`) と箱の種類の値
  (`人` / `関数` / `ストレージ` / `イベント`) は v0.5 の受理集合に無いのに、誤りを返さず
  黙って消えていた (種類の値は `actor` の箱に潰れていた)。 行番号付きで知らせ、同じ意味の
  英語を勧める。 受ける範囲は今のまま (箱の欄 8 語 + 矢印の色名 5 語) で広げていない。

  併せて、中括弧の形でも `種類` / `補足` / `値` / `行` を読めるようにした。 英語
  (`kind` / `subtitle` / `value` / `rows`) は読めるのに日本語だけ落ちる状態を無くす。
  `色` / `位置` / `大きさ` は英語側も中括弧では読めないため据え置き。

- **`種類:` を縦に書いた本文が旧記法として読まれるのを直した** (#1300)

  版の目印を探す正規表現が字下げにも当たっていたため、`種類:` を箱の欄として縦に書いた
  正しい本文と、見本の状態名が `状態` / `動作` 等である本文が、旧記法として読まれていた。
  出る誤りは別の行を指し、真因 (版の誤判定) はどこにも出ない。 目印を行頭に限った。

- **JSON が知らない項目を誤りとして返すようにした** (#1295)

  綴り違い (`animation` → `animations`) や打ち間違いが検査を通り、書いたのに効かない図が
  出来ていた。 公開している JSON Schema は元から全階層で `additionalProperties: false` を
  宣言しており、parser が自分の契約に追いついていなかった。 近い項目名があれば hint で勧める。

  併せて、受ける項目の一覧を実装の表 (`ACCEPTED_KEYS`) 1 箇所に集め、schema との一致を
  両方向で検査するようにした。 `viewport` は検査そのものが無かったため型を見るようにし、
  schema に無かった 4 項目 (`actors[].pos` / `flow[].pos` / `lanes.*.pos` / `viewport.scale`)
  を載せ、色の一覧を engine と揃えた。

- **記法では効く箱の項目 12 個と `axes` を JSON でも書けるようにした** (#1294)

  `tone` / `owner` / `end` / `touchpoint` / `opportunity` / `posX` / `posY` / `posW` / `posH` /
  `nodes` / `scale` / `color` と、2 軸で仕分ける図の `axes` が JSON では読まれず、書いても
  図が変わらず知らせも出なかった。 記法と同じ意味で読むようにし、両入口が同じ箱に解決する
  ことを検査で固定した。 併せて、見本 (parts) にしか効かない `state` と `scale` を見本でない
  箱に書いた時は誤りとして返す (記法側も読めない項目名として知らせる)。

- **JSON の `kind` が記法と同じ 108 種を受けるようにした** (#1293)

  JSON 側だけ 31 種の一覧を写し持っていたため、後から増えた 77 種は見本の名前として扱われ、
  `shape-wallet` などが `actor` の箱に変わっていた。種類の一覧と読み替えを記法側と共有し、
  JSON と記法が同じ種類を同じ図へ解決するようにした。

## [0.8.0] - 2026-08-20

**破壊的変更を含む**。 記法から型を 1 つ落としている。

### Added

#### 記法で書ける欄が増えた

- **矢印の説明文を線の上に重ねられるようにした** (`overlay`、#1267)

  分岐図の条件ラベル (`true` / `false`) 用。 書いていない矢印には付かない。

#### 記法に書ける欄を README に載せた

- **箱 / 矢印 / 最上位に書ける欄の一覧を載せ、実装との一致を検査で固定した** (#1276)

  一覧は実装 (`TOP_LEVEL_KEYS` / `INLINE_ACTOR_KEYS` / `FLOW_INLINE_KEYS`) と両方向で
  突き合わせる。 実装に欄が増えて README を直さないと検査が落ちる。

  併せて `lane:` の効く条件と、静止した `type: flow` が書いた矢印の端を使わない癖を書いた。

#### 見本帳

- **見本 19 件すべてに記法を付けた** (#1242 / #1243 / #1252 / #1264 / #1265 / #1267)

  「コード」 タブで記法を読め、そのままエディタで開ける。

- **図がいまどの局面かをカタログと見本の詳細にも出した** (#1240)

- **カタログの分類を設計と同じ 2 面の姿に揃えた** (#1238)

#### 記法で書ける欄が増えた (詳しい説明)

- **箱を書いた縦列へ入れられるようにした** (`lane:`、 #1263 / #1266)

  縦列が 2-4 本あり箱がそこに散る図 (構成図 / 処理の流れ / 役割ごとの流れ) を記法で書けなかった。
  書いても全部が 1 列に入っていた (実測 = 見本は `Client` に 1 個 / `AWS` に 3 個)。

  ```
  type: topology

  lanes:
    front: { label: "利用者側" }
    back:  { label: "サーバ側" }

  actors:
    - 画面: { lane: front }
    - 受付: { lane: back }
  ```

  効くのは縦列を **箱を並べるための入れ物** として使う 3 図種 (`flow` / `topology` /
  `swimlane`)。 順序図は縦列がそのまま生命線として描かれる骨格なので、 従来どおり選べない
  (#1246 の知らせもそちらでは残る)。

  **全ての箱に書くか 1 つも書かないか** のどちらかにする。 一部だけ書いた形は、 書かなかった
  箱の行き先を決められないため知らせを出して従来の並びにする。

- **箱の大きさを書けるようにした** (`posW:` / `posH:`、 #1259 / #1262)

  組立て API 側は箱ごとに幅を指定することがある。 記法から書けないと描画側の既定 (640) に
  なり、 **既定より狭い幅を指定した図では縦列ごと広がる** (実測 = 拡張ステート図は箱 280 /
  縦列 330 で、 既定 640 が縦列を押し広げて描いた図の幅が `2439` 対 `2279` になった)。

  ```
  actors:
    - 待機: { kind: card, posW: 280 }
  ```

  書かなければ従来どおり描画側の既定になる。 縦列に収まる幅なら図は変わらない
  (実測 = 320 は縦列 370 に収まるため見た目が変わらない)。

- **工程の並びに担当と終わる時期を書けるようにした** (`owner:` / `end:`、 #1251 / #1256)

  組み立て API の帯は担当を持ち、 終わりを状態から取って段で伸び縮みさせられるが、 記法には
  書く場所が無かった。 記法で書き直すと担当が消え、 帯は常に 1 コマになる。

  ```
  type: gantt

  actors:
    - 設計: { value: "Q1", owner: "デザイナー" }
    - 実装: { value: "Q2", end: "{done}" }
  ```

  `end:` に時期の名前を書くとそこまで帯が伸び、 `{名前}` を書くと状態から取る。 書かなければ
  従来どおり始まりと同じ時期に終わる。 効くのは `type: gantt` だけで、 他の図種では知らせる。

- **2 つの軸で仕分ける図に軸の名前を書けるようにした** (`axes:`、 #1251 / #1255)

  組み立て側が「小さい / 大きい」 を直接書き込んでおり、 記法から変える経路が無かった。
  軸の名前が書けないと **何を判断する図か読めない**。

  ```
  type: quadrant

  axes:
    x: { left: "手間 小", right: "手間 大" }
    y: { bottom: "効き 小", top: "効き 大" }
  ```

  区画の名前 (`右上` 等) は軸の名前から `{上} × {右}` の形で決まる。 組立て API 側が同じ規則で
  導いているため、 同じ内容を書けば同じ図になる。 書かなければ従来どおり位置の名前を出す。

- **体験の道筋に場所と改善の余地を書けるようにした** (`touchpoint:` / `opportunity:`、 #1251 / #1254)

  組み立て API の段は「どこで起きたか」 と「何を直せるか」 を持つが、 記法には書く場所が無く、
  記法で書き直すと 2 つとも落ちていた。

  ```
  type: journey

  actors:
    - 登録: { value: "不満", touchpoint: "申込み画面", opportunity: "入力を減らす" }
  ```

  効くのは `type: journey` だけ。 他の図種には相手が無いため、 書かれていたら組み立て側が
  知らせる。 `type: mind` は描けない欄をまとめて 1 件で伝えるため、 そちらには二重に出さない。

- **放射の図で枝の親を書けるようにした** (`type: mind` の矢印、 #1251 / #1253)

  それまで枝は全て中心の直下に並び、 階層を書く手段が無かった。 矢印は「描けません」 として
  落としており、 `type: tree` を使うよう案内していた。

  ```
  type: mind

  actors:
    - Project
    - Features
    - Auth

  flow:
    - Features -> Auth: ""
  ```

  矢印は線にならず「どの枝の下に置くか」 の指定として読む。 親を決める規則は `type: tree` と
  共有するため、 同じ本文で図種を変えても親子の解釈が割れない。 書かなければ従来どおり
  全ての枝が中心の直下に並ぶ。

- **図表の箱に上の小見出しを書けるようにした** (`eyebrow:`、 #1247 / #1250)

  図全体を 1 箱にする図種 (`pie` / `bar` / `line` / `funnel` / `tree` / `journey` /
  `quadrant` / `mind` / `gantt`) は箱を 1 つしか作らないため、 上の小見出しの相手が決まる。
  そこへ渡す経路が記法に無く、 組立て API で作った同じ図と見比べると小見出しだけが空だった。

  ```
  title: "経路別の流入"
  eyebrow: "棒グラフ"
  type: bar
  ```

  箱ごとに分かれる図種 (`sequence` / `flow` 等) では「どの箱の小見出しか」 が決まらないため、
  書かれていたら組み立て側が知らせる。 そちらは従来どおり箱ごとに書く
  (`- A: { eyebrow: "..." }`)。

- **値が「きっかけ」 で動き出す形を書けるようにした** (`trigger:` / `to:` / `dur:`、 #1161 段 2 / #1229)

  それまで `values:` に書けるのは「常に成り立つ関係」 だけで、 いつ動き出すかは段に手で書く
  しかなかった。 工程が 4 つあれば段も 4 つ並べる必要があり、 「フロー 1 が終わったら
  フロー 2」 を書く場所が無かった。

  ```
  states:
    vDone: 0
    cDone: 0

  values:
    vDone: { trigger: step "取込み", to: 100, dur: 2s }
    cDone: { trigger: vDone >= 100, to: 100, dur: 2s }

  animation:
    - step: "取込み" 4s
  ```

  `trigger: step "名前"` はその段が始まった時、 `trigger: <値> >= <数>` は別の値が境目を通った
  時に動き出す。 `to:` が動いた先、 `dur:` が動く長さで、 動き始めの値は `states:` から取る。

  上の例は段を 1 つ書くだけで 2 本が順に動く。 `cDone` が動き出す時刻は `vDone` の傾斜から
  逆算されるため、 段を人が並べ直す必要が無い。

  組み立ての時点で段の中の傾斜へ畳むため、 描画側は変わらない。 畳めない形 (段が無い /
  相手が境目を通らない / 段に収まらない / きっかけが一周する) は図に載せず、 書いた行で伝える。

- **記法で「他の値から決まる値」 を書けるようにした** (`values:` / `states:`、 #1163 / #1183)

  それまで図の中の値は段ごとに手で書くしかなく、値どうしの関係を書く場所が無かった。
  要素が増えるほど段に書く量が増え、連動する図を書けない状態だった。

  ```
  states:
    inflow: 8200
    done: 130

  values:
    waiting: "{inflow} - {done}"
  ```

  `states:` が初期値を、`values:` が他の値から決まる関係を持つ。 段で `states:` の値を動かすと、
  `values:` の式がその都度解かれる。 箱の欄に `{waiting}` と書くとその値が出る。

  `#1163` が記法として読めるところまで、`#1183` が図に載せて描画側で解ける経路までを入れた。

- **JSON の入口でも値と状態を書けるようにした** (#1187 / #1215 段も動かせる)

  記法が `values:` / `states:` を受けるようになった一方、JSON の入口には受ける場所が無く、
  書いても黙って消えていた。 `DragonJson` に `states` と `values` を足して同じ機能に揃えた。

- **図表の欄が値を読めるようにした** (#1199 / #1202)

  円 / 棒 / 折れ線 / 絞り込み / 進捗の数の欄と、気持ち / 区画の語の欄が `{名前}` を受ける。
  それまで図表は配列を書いて一度描くだけで、状態を読む経路が無く永久に静止していた。

- **値の書き方を記法一覧と編集画面の見本に載せた** (#1191)

  `values:` は解けて図に載る状態まで来ていたが、記法一覧に 0 件 / catalog の図に 0 件 /
  編集画面の見本に 0 件で、**書ける人が居なかった**。 一覧と実例を足して書けるようにした。
  一覧の検査対象は実装から導き、`title:` / `type:` / `flow:` / `states:` / `values:` /
  `animation:` / `viewport:` の追加漏れを検知する。

### Changed

- **`@cardenelabs/cdl` の依存を `^0.7.0` から `^0.9.0` に上げた** (#1257)

  cdl 0.8.0 は tag だけ打たれて公開されておらず、その後 17 commit (機能追加 3 件 /
  不具合修正 12 件) が載っていた。 cdl 側で `0.9.0` を切って公開し、そこへ上げた。

  工程の帯で終わりが始まりより前に落ちた時、始まりへ丸める処理が届くようになった
  (cdl#484)。 これで `end:` に状態を書いた工程表の帯が消えなくなる。

  上げたことで挙動が変わった箇所が 2 つあり、本 repo 側も揃えた。

  - **状態の表を継承なしの入れ物で作るようにした**。 engine が継承なしに揃えた
    (cdl#456 / cdl#490) のに組み立て側が通常の object のままで、`__proto__` のような
    名前で **組み立てだけが「値が無い」 と知らせる** 状態になっていた
    (実測 = 描画は `a = 5` を出すのに知らせは `value-unresolved`)

  - **控えの欄の検査で、題を材料に使えなくなった**。 cdl 0.9.0 で全ての種別が題を
    描くようになったため (実測で 110 種すべて)、「控えだけが変わる図」 を題では
    作れない。 副題と上の小見出しは実際の図で見て、題は絞り込みそのものを直接見る形にした

- **`type: mind` の絵が変わった (破壊的変更、 #1177 / #1223)**

  記法の `type: mind` は `card` を 3 列 (`mind-left` / `mind-center` / `mind-right`) に並べる
  別実装で、 engine の `mind-map` 種別を使っていなかった。 中心から放射する絵を出す種別が
  engine 側にあるのに、 記法からは辿り着けない状態だった。

  ```
  title: "アイデア"
  type: mind

  actors:
    - Core        # 1 つ目が中心
    - Idea1       # 残りが枝
    - Idea2
  ```

  **既存の図の見た目が変わる**。 3 列に並んだ箱と箱の間の矢印から、 中心と放射の 1 枚絵になる。

  | 変わったこと   | 前                                         | 後                              |
  | -------------- | ------------------------------------------ | ------------------------------- |
  | 箱の数         | 登場人物の数だけ                           | 1 つ (図全体を 1 箱で描く)      |
  | 枠             | `mind-left` / `mind-center` / `mind-right` | `chart` の 1 つ                 |
  | 矢印           | 中心から枝へ自動で引く                     | 引かない (繋がりは箱の中が持つ) |
  | 位置の相対指定 | 効く                                       | 効かない (箱が 1 つのため)      |

  併せて 2 つが直った。 枝の親を見る規則 (`ruleMindMapParentReference`) が記法から作った図にも
  当たるようになり、 `SINGLE_BOX_KINDS` の `mind-map` が記法から到達する状態になった。

  **矢印は書いても描けない**。 書かれていたら伝える (`type: journey` / `type: quadrant` と同じ)。
  枝の親を矢印で書く形は `type: tree` が持っており、 `mind` は「1 つ目が中心、 残りが枝」 の
  簡便形として別に残す。

  **箱ごとに描いていた頃の欄の一部は描けなくなる**。 1 箱で描く種別が持てるのは名前、
  名前の後ろに続けて出す副題 / 値、 枝の色だけで、 以下の表で「出ない」「効かない」欄は
  書かれていたら伝える。 副題 / 値に `{状態名}` を書いた場合も、中心と枝の表示文字として
  段の状態を読む。

  | 欄                                         | 中心             | 枝               |
  | ------------------------------------------ | ---------------- | ---------------- |
  | 名前                                       | 出る             | 出る             |
  | 色 (`tone`)                                | **出ない**       | 出る             |
  | 副題 / 値                                  | 名前の後ろに出る | 名前の後ろに出る |
  | 上の小見出し / 行                          | **出ない**       | **出ない**       |
  | 種類 / 枠の指定 / 積む順 / 色番号          | **効かない**     | **効かない**     |
  | 位置 (座標 / 相対) / 大きさ / 配置のずらし | **効かない**     | **効かない**     |

  これらを描くなら `type: tree` か `type: flow` を使う。 位置と大きさは「登場人物ごとの箱を
  どこに置くか」 の指定で、 箱が 1 つの図では置く先が無い。

  描ける欄と描けない欄の割り当ては型で固定してある。 `DslActor` に欄が増えた時、 どちらにも
  入れ忘れると型検査が落ちる。 伝える判定も同じ表から導くので、 欄を足して判定を書き忘れる
  形も型検査で止まる。

  見本 (`parts`) を重ねた登場人物は **中心にも枝にもせず**、 見本の中身だけを描く。 載せると同じ
  登場人物が 2 箇所に描かれるため。 見本しか書かなかった記法では放射の箱そのものを作らない。

- **`@cardenelabs/cdl` の依存を `^0.6.1` から `^0.7.0` に上げた** (#1205)

  cdl 0.7.0 で木 (`tree`) と放射 (`mind`) の名前が状態を読むようになったため、
  依存を上げて見本 2 件を動かした。

- **見本帳 127 件に段を付けた** (#1185 / #1193 / #1195 / #1197)

  看板が「動く図の記法」 なのに見本帳が 1 件も動いていなかった。 群ごとに段を足した。

  | 群                   | 件数 | 動かし方                                                 |
  | -------------------- | ---- | -------------------------------------------------------- |
  | primitives-extra     | 21   | 指標を 1 つずつ割り当てて段で動かす                      |
  | `scene-*`            | 30   | 箱を 1 つずつ光らせて流れを読ませる                      |
  | presets              | 19   | 箱を 1 つずつ、図全体が箱 1 つのものは図表の中身を動かす |
  | `shape-*` / `kind-*` | 50   | 形を保ったまま副題 (値の欄 / 行) の数を動かす            |
  | charts               | 7    | 数の欄を段ごとに動かす                                   |

  **検査を描画結果ベースにした**。 宣言の層で見る検査だと、値を足しても絵が変わらない種別が
  「動いている」 と判定される (値を描く経路は 2 種別しか持たない)。

- **`@cardenelabs/cdl` の依存を `^0.5.0` から `^0.6.1` に上げた** (#1166)

  同じ workspace の中で cdl の解決先が 2 つに割れており (`apps/playground-spa` が隣の
  checkout を `link:` で、 `packages/dragon` が公開版を掴む)、 同名だが別物の型として
  扱われて型検査が 103 件落ちていた。 両方を公開版に揃えて 0 件になった。

  cdl 0.6.0 は `mindMapRadial` と `mind-radial` を消しているため、 これらを使う図は
  cdl 側の移行が要る (cdl の `CHANGELOG` に手順がある)。

### Removed (破壊的変更)

- **記法から `type: radial` を外した** (#1170)

  放射状に枝を配る記法で、 繋がる先の `mind-radial` 種別を engine 側で落としたため
  (`@cardenelabs/cdl` 0.6.0)、 記法の入口も同じ段で閉じた。 閉じないと「型は通るのに
  描けない記法」 が一覧に残る。

  | 消えたもの                              | 代わりに使うもの |
  | --------------------------------------- | ---------------- |
  | 記法の `type: radial`                   | 無し。 下記      |
  | `PresetType` の `"radial"`              | 同上             |
  | `diagramJsonSchema` の型一覧の `radial` | 同上             |

  **そのまま置き換えられる記法は無い**。 `type: mind` は枝を書き並べる用途では足りるが、
  `card` を 3 列に並べる別実装なので出てくる絵が変わる (放射状にはならない)。

  `type: radial` を書いた記法は `unknown type: "radial"` で弾かれる。 黙って別の絵に
  なることは無い。

### Fixed

#### 書いたのに効かない / 黙って消える形

- **矢印が `actors` に無い名前を指した時に知らせ、壊れた図を作らないようにした** (#1221)
- **解決できない矢印を組み立てから外すようにした** (#1225)
- **自分へ戻る矢印を組み立てから外して知らせるようにした** (#1232)
- **静止した `type: flow` が書いた矢印の端を無視することを知らせるようにした** (#1271)
- **`values:` の 1 行形が黙って消えるのを塞いだ** (#1216)
- **英数字でない登場人物の名前でも見本の値が届くようにした** (#1233)
- **順序図の生命線が箱より 100 長く伸びるのを直した** (#1274)

  組立て API は `y2=848`、記法は `y2=948` だった。 原因は footer の箱に `role` が
  無かったこと。 枠の大きさは同じなので、図の大きさの比較では捕まらない。

#### 描いた図が組立て API と違う形

- **図表の記法が図の題を 2 度描くのを直した** (#1252)
- **放射の枝と中心に副題と値を描くようにした** (#1231)
- **名前から作る id の重なりを解いた** (#1226)
- **縦列の幅が合わない記法を外し、幅を検査に入れた** (#1244)

#### その他

- **JSON の入口で 1 度だけ読んで写しを作るようにした** (#1222)
- **放射状の図の削除で残った記録漏れと死んだ項目を片付けた** (#1176)

- **記法で書いた図の大きさを組立て API に揃えた** (#1260 / #1261)

  一致検査は箱 / 矢印 / 縦列 / 段の中身を突き合わせていたが、 **描いた図の大きさを見ていな
  かった**。 そのため中身が同じでも大きさの違う図が通っていた (実測 = 記法を持つ 13 件のうち
  **7 件** で viewBox が違い、 `785x488` 対 `712x600` 等)。

  原因は 2 系統。 図表の箱の大きさが組立て API と違っていたこと (`funnel` は `640x368` 対
  `560x480` 等) と、 後ろへ戻る矢印を箱の上に回していなかったこと
  (`routing: back-detour` が付かず、 図が 82px 低くなっていた)。

  どちらも組立て API 側の値と規則に揃えた。 併せて一致検査に「描いた図の大きさ」 の軸を足した。

- **表と状態の図で名前を 2 度描かないようにした** (#1241 / #1258)

  `er` と `state` は箱を 1 つずつ持ち、 その箱が既に名前を描く。 縦列にも同じ名前を渡していた
  ため、 **同じ字が縦に 2 つ並んで** いた (実測 = 描いた絵に同じ名前が 2 度出て、 図が 92px 高い)。

  `swimlane` は縦列そのものが「誰の担当か」 を読ませる図なので、 従来どおり見出しを付ける。

- **`lanes:` / `groups:` の id が hyphen と日本語を受けるようにした** (#1241)

  組み立て側は登場人物の名前から縦列 id を作るため、 hyphen と日本語が入る
  (`lane-idle` / `lane-待機` / `lane-sign-up`)。 英数字と下線だけを受けていた間、
  **自動で作られた縦列の幅や見出しを書き直す手段が無かった**。

  読み取りを壊す 4 種 (空白 / `:` / 中括弧) だけを受けない形にした。

- **箱に書いた縦列が効かないことを伝えるようにした** (`lane:`、 #1246 / #1248)

  記法は箱の中括弧で `lane` を受け取るが、 組み立て側は 1 度も読んでいなかった。 縦列は
  図種が決めるため、 書いた縦列は落ち、 `lanes:` で宣言した縦列だけが中身のないまま残る。
  知らせも出ないため、 書いた人は出来上がった図を見るまで気付けなかった。

  見本 (parts) の `lane` は張替え先として実際に効くため、 従来どおり知らせない。
  `type: mind` は描けない欄をまとめて 1 件で伝えており、 そこに `枠の指定` が既に入っている
  ため二重には出さない。

- **英数字でない登場人物の名前でも、重ねた見本の値が届くようにした** (#1189)

  値と状態の名前に使う前置きだけを英数字に直す。 それまでは登場人物の名前をそのまま前置きに
  していたため、日本語の名前で見本を重ねると値が 1 つも届かなかった。

  ```
  actors:
    - 受付 1: { kind: <見本> }    # 値は p1__... の名前空間に載る
  ```

  `{名前}` に書ける字種は描画側が 1 箇所で決めており、英数字と `_` に限る。 読む側 (置き換え)
  と書ける側 (式) の両方がその定義を使うため、記法の側だけ広げることはできない。

  **箱 / 縦列 / 矢印の id は変えない**。 これらは `{名前}` の対象ではなく、画面側が id から
  登場人物の名前を取り出す経路があるため。

  同じ形に潰れる名前 (`受付 1` と `受付-1`) は書いた順に番号で分ける。 英数字の名前しか無い図では
  1 つも番号が付かないため、既存の図の名前は変わらない。

- **自分へ戻る矢印を書いても図が組み立つようにした** (#1227)

  `A -> A` の形は組み立てから外し、 書いた行で理由を伝える。 それまでは記法の側が通してしまい、
  描画の直前で図ごと落ちていた。 本文のどの行が原因かも出なかった。

  ```
  flow:
    - Wait -> Wait: "tick"    # 外して知らせる
    - Wait -> Done: "exit"    # そのまま描く
  ```

  描画側は図種を問わず自己ループを受けない (`validate` の判定に分岐が無い)。 実測すると、
  golden 検査が持つ自己ループの見本 4 件 (順序図の自分宛て / 帯図の自己ループ / ER の自己参照 /
  状態図の自己遷移) は **どれも描けない状態だった**。 外して残りを描く形にしたことで、
  4 件とも図が出るようになる。

  自己ループそのものは描けないままなので、 知らせが直し方を 2 通り案内する = 途中の箱を
  1 つ足して 2 本に分けるか、 段 (`animation`) で状態が変わる様子として見せる。

  図全体を 1 箱で描く種別は矢印を作らないため対象にしない (本数を数えて別に伝えている)。

- **見本が持つ値を重ねた先でも解けるようにした** (#1188)

  見本 (parts) は図の定義そのものなので値を持てるが、取り込む側が箱 / 矢印 / 縦列 / 状態を
  写すだけで値を写していなかった。 重ねた先で `{名前}` の生の形が出ていた。

- **図表の欄が読む値の検査を 1 か所に集めた** (#1203)

  数の欄と語の欄で同じ土台を 3 度書いており、3 度とも review で同じ形の穴を指摘されていた
  (同じ名前を 2 回宣言した時に後ろが効くこと / 段で状態に入る値も見ること)。

## [0.7.0] - 2026-07-15

### Added

- **龍鱗ゴールド palette** = site 全 chrome を parchment (light) + obsidian (dark) + copper / amber / ember の 3-color system に redesign。 dragon = 「財宝を守る西洋龍」 metaphor、 dev tool の boring UI からの離脱。 light = illuminated manuscript vibe、 dark = dragon's hoard vibe (2 光源 radial glow: amber 75% 30% + ember 15% 90%) (#367-#375 の 5 PR で段階実装、 #373 が palette 全面差替 SSOT)
- **parts catalog** (20 parts、 rich exemplar 合成用 reusable atoms) を lazy dynamic import で分離、 non-parts route の TTI 改善 (#371)
- **breadcrumb aria-label locale 対応** (SiteHeader / ThemePicker / Toast + 6 page breadcrumb を useLocale で JA/EN 切替) (#370)
- **editor URL hash `#preset=<slug>`** で catalog / preset detail から直接 sample load、 SPA navigation でも SAMPLES へ即遷移 + slug 未登録時 toast 通知 (#367)
- **editor 「一括反映」 button** = fixable warning count で disabled 制御 + count 表示 (#367)
- **GitHub Pages deploy setup** = 手動 build + gh-pages branch push 経路 (`pnpm run deploy` = `pnpm build:pages && gh-pages -d dist`)、 CI 未使用 (#376)

### Changed

- **UI 全面日本語化** = Compare / Contribute / Release Notes / Preset Detail / Docs / Editor の 6 page 全 h1 / breadcrumb / button / aria-label (#367)
- **dark mode invisible text hotfix** = globals.css の html.dark base override 追加、 blueprint SVG viewer stage は cream paper (#fcf8ee) 維持で navy 描画明瞭 (#372)
- _*preset detail nm-* class CSS 補完_* + hideHeader で 01 badge / phase title overlap 解消 (#367)

### Removed

- **`/compare` page (テーマ比較)** = site palette を龍鱗ゴールド 単一に committed した文脈で 6 テーマ選択 UI は decision fatigue、 route + nav link + preset detail button + component + test entry 全削除 (cdl の 6 テーマ機能自体は `data-cdl-theme` 属性経路継続) (#374)
- **dead component 4 削除** = NmPresetCard / ThemePicker / PresetCard / shot-events.mjs = 0 参照 verified、 src/components/ 8 → 5 file (37.5% dead code 削減) (#375)

### Fixed

- CategoryPage 検索 filter の日本語 label 対応 (#367)
- editor `handleAutoFix` axis whitelist 統一で count drift 防止 (#367)
- Preset Detail の `preset.slug` URL 統一で 20 preset 全 hash 経由到達可 (#367)

## [0.6.0] - 2026-06-30

### Changed

- catalog 全 page (cookbook / patterns / text-dsl / presets / animation / styles / primitives) + docs (ja/en) を WebApp 軸の汎用 doc tool 表現に書直し、 blockchain 専用文脈から脱却
- cookbook を WebApp 軸 25 例 (API/Auth 5 + データ操作 5 + UI 5 + 非同期 5 + 運用 5) に再構築、 card 囲い 16:9 統一
- patterns を 12 種の汎用 pattern (Direct / Passthrough / Call-RW / Emit / Hook / Branch / Loop / Fan-out / Fan-in / Rollback / Schedule / Validate→Process) に再構築

### Removed

- 旧 patterns docs (bridge / permit / dex-swap / multicall / approve-pull + solidity preset 解説) を全削除
- primitives-extra の 8 NodeKind (wallet / validator / miner / blockchain-node / mempool / block / bridge-node / relayer) 参照を削除
- 旧 cookbook 25 例 (DeFi 10 / NFT 5 / DAO 5 / Bridge 5) を全削除

## [0.5.0] - 2026-06-28

### Added

Initial OSS release.

- **6 preset** ... sequence / flow / swimlane / er / state / topology
- **多数の NodeKind** ... actor / function / storage / event / service / database / cache / queue / 他
- **8 EdgeStyle** ... solid / dotted / dashed / dotted-flow / 他
- **6 Tone** ... accent / teal / success / error / warning / info
- **phase / state / tween / set / badge** ... 時系列 animation を宣言的に
- **Text DSL v0.5** ... mermaid 感覚の 5 ブロック箇条書き DSL (parser + compiler、 JA / EN i18n)
- **Visual Editor** (`apps/playground`) ... GUI 編集 + live preview + SVG export
- **Catalog** ... 12 preset × animation の visual 一覧
- **Docs site** ... 全 16 概念 × JA / EN、 humans tab + LLM tab、 Diátaxis 準拠
- **Pagefind 全文検索** ... Cmd+K で全 docs 高速 lookup
- **React + Astro 両対応** ... static / SSR / hybrid どこでも動く
- **CdlDiagramThumbnail** ... thumbnail + viewport いっぱいのモーダル拡大
- **CdlDiagramView** ... phase autoplay / focusPhaseId / hideHeader / debug props
- **Performance optimization** ... spatial hash 採用、 1000 node でも 60ms 以下の layout
- **TypeScript strict 全面 ON** + zod runtime validation
- **OSS 公開準備** ... README / CONTRIBUTING / SECURITY / ISSUE_TEMPLATE / PR_TEMPLATE 整備
- **380 件のテスト** ... unit + integration + visual screenshot diff + e2e

### Changed

- chainome monorepo から独立 OSS lib として分離 (旧 `chainome/packages/cdl` + `chainome/packages/anim` + `chainome/apps/web` の cdl 関連を移植)
- repo URL を `git@github.com:cardene777/cdl.git` に変更
- playground (旧 `apps/web`) を `apps/playground` に rename

## [0.4.0] - 2026-06-27

### Added

- **Text DSL v0.4** ... 残 5 preset (flow / swimlane / er / state / topology) animation 拡張
- catalog の Text DSL demo page (`/catalog/text-dsl`) ... 6 preset × animation の visual 確認
- 全 docs code block への preview 強制 (48 件、 lint script による品質保証)
- integration test 21 件追加 (Permit / Topology / edge case / regression / 6 preset × animation)

## [0.3.0] - 2026-06-27

### Added

- **Text DSL v0.3** ... sequence preset で `アニメーション:` ブロック full compile
- DSL の `状態` / `ステップ` / `強調` / `遷移` / `切替` / `バッジ` / `説明` を実 phase / state / tween / set / activate / badge / body に注入
- highlight 解決 ... actor 名 → `header` + `footer` + `step box` を active 化、 `A→B` 矢印 → edge id を active 化
- アーキテクチャ ... animation あり時 builder 直接経路、 なし時 preset 経由 (v0.2 互換)
- test 6 件 (v0.3 機能)

## [0.2.0] - 2026-06-27

### Added

- **Text DSL v0.2** ... 6 preset 全対応 (sequence + flow + swimlane + er + state + topology)
- cardinality 自動解析 (1:1 / 1:N / N:M / 0..1 / 1..*)
- ER preset で entity 自動生成 + relation 自動 cardinality
- state preset で最初 actor = initial、 最後 = final 自動付与
- topology preset で 1 group 内 container 配置
- test 8 件 (5 preset 動作確認)

## [0.1.0] - 2026-06-27

### Added

- **Text DSL v0.1** ... 「タイトル / 種類 / 登場人物 / 流れ / アニメーション」 の 5 ブロック箇条書き DSL
- parser + compiler (sequence preset only) ... 行ベース parser + AST + LaidDiagram 変換
- 日本語 + 英語両対応 (キーワード i18n)
- 矢印正規化 (→ / -> / => / >>)
- duration 解析 (1.5 秒 / 1500ms / 2s)
- エラー親切 (行番号 + hint + 修正提案)
- LLM 生成ガイド + 5 few-shot 例
- ChatGPT/Claude/Cursor 即用 system prompt
- preview 強制 lint script (`pnpm lint:docs-preview`)
- text-dsl-spec.md + text-dsl-llm-guide.md (docs site 統合)
- test 11 件 (parse + compile + アニメーション parse)
